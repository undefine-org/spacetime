GH-27 E0923 false ambiguity — VERDICT: STILL-BROKEN

shape: canonical `@data inline $events : [];` and `@data inline $filterCategory : "all";`
(both literal-aware — parse selects data-inline and stores it in FormMatch.matched_macro).

Command: check --verbose index.st
  warning[E0923]: ambiguous dispatch: @data resolves to `data-inline` and
                  `data-derive` (both score 13)   --> index.st:6:1
  warning[E0923]: ambiguous dispatch: @data resolves to `data-inline` and
                  `data-derive` (both score 13)   --> index.st:6:27
  warning[W0201]: Data source 'events' is defined but never used
  warning[W0201]: Data source 'filterCategory' is defined but never used
  0 error(s), 4 warning(s)   (exit 0)

The inline/derive false positive STILL fires on both canonical kind forms.
The range anchoring is also still broken: both E0923s anchor to line 6 (6:1
and 6:27 — the second points past the end of the `$events` directive rather
than at `$filterCategory` on line 7).

Reconciliation: commit 541f625f "E0923 silenced for kind families" fixed the
UNION-LITERAL kind family (@form's six siblings, `$kind:("motion")` unions) but
its commit message explicitly documents the @data BARE-LITERAL kind family as
RESIDUAL, still-live: "the scorer sees captures, not literals, and bare-literal
kind words leave it blind. That family is FUP-154-era work." My repro confirms
that residual is still firing.

CONCLUSION: the acceptance "no E0923 for canonical inline/derive/fetch kind
forms" is NOT met — E0923 still fires on unambiguous `@data inline $x : []` and
`: "all"`. STILL-BROKEN. (Note: it is a warning, so hidden on plain `check`
per #34; visible only under --verbose.)

=== GATE (2026-08-06) ===
check-contract.sh — runnable RED gate today (exit 1). Asserts `check --verbose`
on canonical `@data inline` forms contains ZERO E0923; today both fire, so the
script fails loudly. NOT-OBSERVABLE as .test.st: the contract is compiler
stdout (E0923 is a warning, hidden on plain check). Quoted failure:
  FAIL: E0923 fired on canonical @data inline forms (GH-27 is LIVE).
