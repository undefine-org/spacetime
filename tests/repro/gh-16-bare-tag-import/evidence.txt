GH-16 bare-tag selector after semicolon-less @import — VERDICT: FIXED-WORKS

Commands (fresh binary):
  build repro1.st   (@import "stdlib" NO ';' + canvas.hero first rule)
    -> ✓ Build complete; dist/spacetime.css (48 bytes):
       canvas.hero {
         width: 100%;
         height: 100vh;
       }
  build repro4.st   (same, @import WITH ';') -> same 48-byte rule (identical)
  build repro3.st   (.hero class selector, no ';') -> 42-byte rule, present

  inspect --layer parse repro1.st:
    Scopes: canvas.hero  (css_declarations: 2)   <- the rule survives as a real scope

CONCLUSION: the missing-IDENT newline guard bug is fixed — a bare-tag-led first
rule after a semicolon-less @import now emits identically to the ';' control.

=== GATE (2026-08-06) ===
check-contract.sh — GREEN regression gate (exit 0). Asserts `build repro1.st`
emits the `canvas.hero` rule into spacetime.css after the semicolon-less
@import; today it does, so the fix is locked. Emitted-artifact inspection is
the honest gate — the rule is static width/height, not reactive, and observing
computed style is the layout/CDP rung with no headless stylesheet prop.
Quoted pass:
  PASS: canvas.hero rule reached emitted CSS after the semicolon-less @import.
