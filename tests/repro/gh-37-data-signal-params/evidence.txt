GH-37 bare @data signal params — VERDICT: STILL-SILENT

shape: `@data signal $cursor(x number, y number) to $api { ... }` — bare param
names in the call signature, where param_list only accepts `$binding`/`&element`
(typed form `$x number`). Control: canonical `$add($text string)` in same file.

Command: spacetime check index.st
  ✓ tests/repro/gh-37-data-signal-params/index.st
  ✓ All 1 file(s) passed                    (exit 0, zero diagnostic)

Command: spacetime build index.st -o dist
  ✓ Build complete: /tmp/gh37-dist/spacetime.js (345433 bytes)
  (exit 0)

Emitted JS (grep callParams):
  line 6888:  var callParams = [] || [];              // bare `(x number, y number)`
  line 7414:  var callParams = [{ name: "text", ...}] // canonical `($text string)`

The bare params are silently dropped to an EMPTY callParams (line 6888). The send
body template is still `"x: $x, y: $y"`, so at runtime `new Function('', 'return
({x: $x, y: $y})')` raises a ReferenceError (no `$x`/`$y` params), caught by the
primitive's try/catch fallback, which sends the raw positional args instead —
the wire payload silently changes. check --verbose shows ONLY the unrelated
"data source 'cursor' is defined but never used" (W0201) — nothing about the
invalid bare parameter names.

CONCLUSION: the invalid bare-parameter grammar still compiles clean and emits
`callParams = []` with no diagnostic — exactly the defect in the issue. The
"runtime fallback" is kept defensive; there is no compile-time refusal of the
bare names. STILL-SILENT.
