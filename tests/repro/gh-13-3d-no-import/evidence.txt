GH-13 3D-macros-without-@import — VERDICT: FIXED-LOUD

shape: selector-scope `.hero { @stage(camZ: 5) { @object(...) } }` with NO
`@import "stdlib/3d"`.

Command: spacetime check index.st
  error[E0956]: unknown primitive: stage
  = hint: The primitive was neither a registered `%primitive` nor a macro with an
    `%emit` block. Check the `%binds`/driver that names it.
  ✗ 1 of 1 file(s) had errors   (exit 1)

Command: spacetime build index.st -o dist
  ✗ Compilation failed with 1 error(s):
    error[E0956]: Unknown primitive: stage   (exit 1)

CONCLUSION: the old silent `// Primitive not found: stage` collapse is gone;
the missing-@import 3D macro is now a loud E0956 build failure (BUG-268).

=== GATE (2026-08-06) ===
check-contract.sh — GREEN regression gate (exit 0). Asserts `check` prints
error[E0956] for the missing-@import 3D primitive; today it does, so the fix
is locked. Chose check-contract.sh over a .test.st because the .test.st would
carry the malformed shape and FAIL TO COMPILE — an inverted signal (the test
passes exactly when the diagnostic regresses to silence). The stdout grep is
the clean, non-inverted polarity. Quoted pass:
  PASS: E0956 fired loudly for the missing-@import 3D primitive.
