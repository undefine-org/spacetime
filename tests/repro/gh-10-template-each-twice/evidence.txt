# gh-10 TEMPLATE @each SECOND INVOCATION
COMMAND: spacetime test tests/repro/gh-10-template-each-twice/template-each-twice.test.st --headless
OUTPUT: ✓ template with @each renders rows in BOTH invocations
  @assert window.__aRows === 3 PASSED (first instance)
  @assert window.__bRows === 3 PASSED (SECOND instance)

VERDICT: FIXED-WORKS — second invocation of a template containing @each now re-renders its rows (3 li.t10-row under each host).
