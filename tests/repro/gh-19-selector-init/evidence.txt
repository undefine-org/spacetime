# gh-19 SELECTOR-SCOPE PRIMITIVE INIT
COMMAND: spacetime test tests/repro/gh-19-selector-init/selector-init.test.st --headless (explicit ST.initElement + flush + wait 80ms)
OUTPUT: ✓ selector-scoped reveal splits text after explicit init + flush + wait
  @assert window.__n19 === 2 PASSED (.st-reveal-word count = 2)
COMMAND2: spacetime test tests/repro/gh-19-selector-init/selector-init-auto.test.st --headless (NO manual init; fixture mount + wait 120ms)
OUTPUT2: ✓ selector-scoped reveal auto-inits from fixture mount (no manual initElement)
  @assert window.__n19b === 2 PASSED

VERDICT: FIXED-WORKS — reveal-engine splitText (the primitive's JS side effect) now runs headless via BOTH the explicit init path and the auto MutationObserver path. The issue's 'never fires' is gone.
