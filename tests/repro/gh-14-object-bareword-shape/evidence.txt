GH-14 documented @object bareword-shape silently defaults — VERDICT: STILL-LIVE (re-filed)

shape: the doc-comment call style `@object torusknot(size: 1.2, ...)` — leading
bareword shape token, but object.st's %form declares a flat paren-arg list
`@object(shape: $shape:string = "icosahedron", ...)` with NO leading bareword.
Plus the nested-body form `{ material: glass(...) spin: [...] }` where the
%form reserves `{ $children* }` for child DIRECTIVES.

Defect: `@object torusknot(size: 1.2, ...)` matches NO form, yet is SILENTLY
accepted with all defaults — `geomFor("icosahedron")` emitted from source that
says `torusknot`, zero diagnostics.

Command: check index.st
  ✓ All 1 file(s) passed        (0 errors — silent default)

Tried and REVERTED: extending the pre-ARG_LIST leftover check (form_compiler 2b)
to forms with zero inline elements (so a leading token before the paren args is
E0946). It DID diagnose `@object torusknot(...)`, but it ALSO diagnosed
`@value-change count(...)`, whose leading bareword is a DELIBERATE legacy drop
(the 2026-07-26-on-cutover migration rewrite's `%drops ( _ )`). Reverting to
preserve that documented compat; the diagnosis needs a mechanism that RESPECTS
a form's `%drops`.

check-contract.sh is RED (asserts the bug is live); it goes GREEN when the
%drop-respecting diagnosis lands. Docs half (make the documented spellings
COMPILE) tracked in BUG-319.
