#+TITLE: MCP Builder — Mockup & Interaction Registry
#+CUSTOM_ID: mcp-builder-mockups
#+DATE: 2026-06-20
#+LAYER: mcp

* Purpose
A living registry of EVERY screen + interaction we want the MCP builder to
enable. Two sections: DONE (generated this design arc, with artifact refs) and
TO COME (the screens/interactions we still want to visualize). Update this file
whenever a mockup is generated or a new interaction is imagined. Pairs with the
two design docs:
- @research/mcp-builder-view-and-convergence-2026-06-20.org (topology + convergence)
- @research/mcp-editor-introspection-design-2026-06-20.org (editor + control catalog)

NB on paths: generated mockups live under the session artifact dir
=…/2026-06-20T17-53-05-983Z_1510d7e0efe8848c/main/generate_image/NN.png=. When a
mockup is promoted to a kept asset, copy it into =@research/mocks/builder/= and
update its ref here. Until then the session path is the ref.

* DONE — generated this arc
Session: 1510d7e0efe8848c. Path prefix:
=…/main/generate_image/=. Canonical palette = the calm espresso of 35/39 (NOT the
over-saturated 40/58 hot-orange — a render drift to correct in real CSS).

| # | screen | what it shows | OQ/decision it settled | verdict |
|---+--------+---------------+------------------------+---------|
| 35 | Pinned panels | navigator L · device frame C · inspector R | OQ-B: pinned-on-drawer-kit (CHOSEN) | Ship |
| 34 | Latched drawers | same content as summoned floating drawers | OQ-B alternative (rejected as default) | Ship |
| 39 | Empty-state gallery | "open a page" tiles w/ origin chips | builder entry point | Ship |
| 41 | Live edit | inspector title === hero heading (live) | OQ-A: live preview tier | Ship |
| 40 | Mobile breakpoint | 375px frame reflow; selection persists | W1 device frame; introduced collapsed rows + $brand token (adopted) | Polish (too hot) |
| 52 | Introspection inspector | address line landing.st::.hero, $brand token, grouped semantic widgets, "saved to source · EditAst" | the THESIS render (sidebars from introspection + EditAst) | Ship |
| 58 | 3D object inspector | generated from @object %form; vec3 fusion gizmos; "vary x8" strip | AST-level introspection; PART F Tiers 2/5 | Ship (too hot) |
| 82 | Creation palette | blocks from registry, live-preview, origin-tagged; drag → drop-zone | the ADD verb (FUP-090); 3 fixes logged | Ship; 3 fixes |
| 84 | Two-level inspector (Component mode) | [Instance\|Component] toggle; Component active → editing &card definition; 3 instances glow + tether; amber "affects 3"; editable STRUCTURE; EditAst → components/card.st | PLAN-051 two-level model | Ship; 2 fixes |
| 86 | Two-level inspector (Instance mode) | same toggle, Instance active; ONE card selected; overridden (bold+revert) vs inherited (muted+pill) rows; EditAst → landing.st; "edit component" escape link | PLAN-051 two-level model (counterpart to 84) | Ship; 1 fix |
| 91 | Timeline / motion editor | @load intro keyframes on a scrubbable timeline; opacity/translate-y tracks w/ diamonds; playhead 420/600ms; transport + @clock badge; live cubic-bezier editor | Tier-3 structural (FUP-085); "design ≡ test" via ST._clock | Ship; 2 fixes |
| 95 | Variant / state inspector | typed-union $ws:Connection → variant chips; WHEN Connected shows @state CSS + bound fields ($send fn, $received string); live badge preview | CORRECTED frontier (NOT an FSM — retired PLAN-047); reads pattern_match + union @type | Ship; 1 fix |

* TO COME — screens & interactions still to visualize
Priority-ordered. Check off as generated; add new rows as we imagine more.

** Next up (this session)
- [X] Two-level inspector COMPONENT mode → screen 84 (Ship; 2 fixes). Shows the
  definition-editing half + blast-radius tether.
- [X] Two-level inspector INSTANCE mode → screen 86 (Ship; 1 fix). Override model:
  overridden bold+revert vs inherited muted+pill; EditAst → landing.st.
  FIX (86): address must use the VERIFIED EditAst form "<css> §<macro> #occ"
  (parse_ast_selector), NOT a CSS "> card[1]" child-index (aspirational — server
  does not parse it). RETHINK: "inherited" pill color collides with the teal
  origin chip; give inheritance its own muted treatment.
  ⚠ honesty (both 84 & 86): "affects N instances" needs a DEFINITION→callsites
  index across the project — folded into PLAN-051 as a prerequisite build task.

** Core loop closure
- [ ] Post-drop configure — block dropped → auto-selected → inspector wakes →
  first edit. Finishes the add→configure handoff the palette's dim state promises.
- [ ] Selection across the structure — navigator row ↔ frame ring ↔ inspector,
  all three synced on one $selected (the selection spine, W2).
- [ ] Undo/redo + save status — the edit-history affordance; "saved to source"
  vs in-flight vs rejected (EditAst Ack/Reject states from dev-save).
- [ ] Reject/error state — an EditAst Reject toast + revert (dev-save implements
  it; we haven't visualized the failure path — honesty: show the unhappy path).

** PENDING BATCH — control-frontier mockups (owner-requested, NOT yet generated)
Owner picked these as the next mockup batch (deferred behind the B0-B3 build).
Generate as a focused batch when we return to breadth. All have verified substrate
noted; each must pass the mock-critique gate + get a DONE-table row when made.
- [ ] Slot / drop-target detail — a slot highlighted with its ACCEPTED child type;
  invalid palette blocks dimmed (FUP-090 FIX-1, grammar-guards-creation). Substrate:
  the form's &children* / region type declares the accepted kind.
- [ ] Recursive region editor — &list(&items[]) / &columns(&left,&right): nested
  editable sub-spaces (builtins.st recursive document model; FEAT-097 shipped).
- [ ] @vary variations strip (standalone) — "generate 8", shrink to favorite
  (FUP-086; stdlib/generate + the property-test generator).
- [ ] In-place WYSIWYG marks toolbar — the @editable-mark toolbar generated from
  editable-schema.json; "edit in place" toggling data-st-edit-mode (PART E; FEAT-097).
- [ ] Custom mark control — a project's &citation(&sel,$source) as an inline
  control no other tool has (one-line declaration; FEAT-097 open model).
NB: the Tier-3 rows below overlap this batch — this PENDING BATCH is the
owner-prioritized selection; keep both in sync as screens are generated.

** Tier-3 structural controls (the frontiers; FUP-085)
- [X] Timeline / motion editor → screen 91 (Ship; 2 fixes). @load keyframes,
  scrubbable playhead, transport bound to @time exports, live cubic-bezier.
  FIX (91): (a) tracks must show INDEPENDENT keyframe x-positions (represent
  stagger/staggerFrom, shipped @load params) — mock has them aligned; (b) add an
  "add keyframe" affordance (dbl-click track) — mock is a viewer, not an editor.
  RETHINK: inspector curve edits the SELECTED segment's easing; track glyph only
  indicates custom easing — define the relationship so they don't compete.
  ⚠ honesty (HARD): ST._clock is documented TEST-MODE (RAF harness-driven). A
  design-time scrubber needs the clock seekable OUTSIDE test mode — PREREQUISITE,
  not a UI detail. Promote: "expose ST._clock as a design-time scrub driver"
  (added to FUP-085 Tier-3 timeline note).
- [ ] Variant / STATE inspector (CORRECTED — NOT a transition graph). ⚠ honesty:
  the imperative @state_machine/@transition FSM was RETIRED (PLAN-047/BUG-117 —
  "emitted no event listeners"). Mocking a transition graph = a DELETED feature.
  What IS shipped (stdlib/macros/state-machine.st): @state(when:"loading"){css}
  -> data-st-state CSS renderer; @state(when: $sig is Variant {$binds}){css} ->
  typed-union pattern match; reactive UI = $-signals + reactive class (.cls:$sig)
  / @view $sig. So the editor = a VARIANT inspector: a union-typed signal's
  variants as selectable chips; clicking a variant previews its @state styling on
  the live element (data-st-state) + shows the variant's bound fields. Reads the
  registered pattern_match + the union @type. Unifies with enum/@type work
  (FUP-074). Introspection-true, not a resurrected FSM.
  [X] → screen 95 (Ship; 1 fix). FIX: label "Preview state" (styling via
  data-st-state) NOT "Live preview" — must not imply forcing the real socket
  signal; a signal-override is a SEPARATE explicit toggle (phantom-affordance
  risk). RETHINK: a variant with NO @state block needs an empty "add @state"
  state, not a blank panel. Variant enumeration IS introspection-true (type-sum
  @type variants + pattern_match registry).
- [ ] Slot / drop-target detail — a slot highlighted with its ACCEPTED child
  type; invalid blocks dimmed (FUP-090 FIX-1, grammar-guards-creation).
- [ ] Recursive region editor — &list(&items[]) / &columns(&left,&right): nested
  editable sub-spaces (builtins.st recursive document model).

** Tier-5 generative (FUP-086)
- [ ] @vary variations strip (standalone) — "generate 8", shrink to favorite;
  the design-exploration flow powered by the property-test generator.
- [ ] @sample empty-state fill — an empty collection populated with type-faithful
  generated data (no lorem).

** Convergence / self-host (north-star)
- [ ] Admin-as-a-frame — the CMS mounted INTO the workbench as a guest frame,
  builder sidebars around it (convergence Rung 5).
- [ ] Dev-as-a-drawer — the dev panel as an origin-tagged drawer bundle.
- [ ] The studio editing itself — "Component" toggle pointed at a builder panel;
  the builder's own chrome edited live (the dogfooding endgame; see the self-host
  FUP).

** Inline / WYSIWYG (PART E; FEAT-097 reuse)
- [ ] In-place rich-text edit — the frame's "edit in place ▸" toggling
  data-st-edit-mode; the @editable-mark toolbar generated from the registry.
- [ ] Custom mark control — a project's &citation(&sel,$source) appearing as an
  inline control no other tool has (one-line declaration).

* BUILD CONVERGENCE STATE (2026-06-20)
Owner steer: converge toward an implementable PLAN-051 v1; resolve keystone
unknowns over more mockups. Status:
- KEYSTONE RESOLVED (FUP-089): @macro named-param EditAst WORKS TODAY
  (apply_text_patch + BUG-098 paren-arg handling). The "biggest v1 unknown" is
  gone; only G1 (insert absent param) + G2 (&template invocation addressing)
  remain, both small + specific.
- PLAN-051 now has an authoritative V1 BUILD SEQUENCE (B0-B5): device frame ->
  stage_structure projection -> navigator -> selection+inspector READ -> inspector
  WRITE (keystone) -> two-level toggle. READ path B0-B3 has ZERO unresolved
  unknowns; demoable on its own.
- Mockups DONE this arc cover the whole vision; the TO-COME list below is now
  POST-v1 / FUP-aligned (control frontiers, convergence, unhappy paths). Generate
  these as the matching FUP comes up, OR in a focused mockup batch if the owner
  wants breadth before build.

* Conventions
- Every generated mockup: VIEW it (puppeteer screenshot) before presenting;
  critique per the mock-critique discipline (verdict + ✓/✗/?/⚠ buckets); record
  the verdict in the DONE table.
- A mockup that implies data/behaviour the code lacks → mark its ⚠ honesty note
  here + ensure a plan/FUP carries the prerequisite (never a UI-only promise).
