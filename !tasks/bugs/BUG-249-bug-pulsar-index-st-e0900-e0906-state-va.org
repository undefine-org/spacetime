#+TITLE: BUG: pulsar index.st E0900/E0906 state validation failure
#+STATE: ITEM
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: BUG-249-bug-pulsar-index-st-e0900-e0906-state-va

* Initial Message

continue. use parallel agents when you can

projects/pulsar/index.st fails with two state-validation errors:
1. E0900: "A `` `$navOpen` `` hole cannot be a state declaration"
2. E0906: "No state assignment found in @on body: `$navOpen` <- !$navOpen;"

The offending shape: `@on click { $navOpen <- !$navOpen; }` — a bare event directive whose body is a signal rebinding.

Verified FAILING IDENTICALLY at the pre-cutover worktree (/tmp/pre-w34, commit 025981d5) — predates the SIP-001 @on cutover. The E0900/E0906 pair comes from the state-analysis layer, not the animation directives.

Suspicion: the state declaration for $navOpen is either missing or written in a shape the state analyzer no longer accepts (a `` `$navOpen` `` hole in declaration position). May be stale syntax from an older state wave. Needs a look at pulsar's state declarations — projects/ is a separate repo, the fix lands there or in the state validator if the shape is meant to be legal.
