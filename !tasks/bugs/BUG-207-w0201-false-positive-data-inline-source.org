#+TITLE: W0201 false positive: @data inline source used via text: binding + @handle arms reported "never used"
#+STATE: ITEM
#+SESSION_ID: 1539989eb44441ea
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-22T09-34-26-513Z_1539989eb44441ea.jsonl]]
#+CUSTOM_ID: BUG-207-w0201-false-positive-data-inline-source

* Initial Message

we are going to work on the integration between Spacetime and Elixir. look at the match and the handle macros, and tell me hwo they are translated currently from and to Phoenix LiveView-like constructs, and how they ought to be. I would like the experience to be as seamless as possible: its own `src/` or `frontend` or sth to put the .st files like a proper project (most likely), and a $hosts file that files import (for now, just give them special enough names that importing them is fine).

ok?

* Surfaced by
FEAT-137: elixir/spacetime_lv/frontend/counter.st declares
`@data inline $error string : "" ;` and uses it in BOTH a `text: $error;`
selector binding AND @handle receive arms (`$error <- m;`) — yet
`cargo run -- check` warns `W0201: Data source 'error' is defined but never
used`. $count (used in the same two positions) does NOT warn, so the usage
scan likely misses a position $error happens to occupy (suspect: the
selector-scope `text:` binding or the handle-arm mutation for a source with no
subscriber-like usage).

* Expected
A source referenced by any text:/binding/mutation surface counts as used.

* Reproduce
cargo run -- check elixir/spacetime_lv/frontend/ → W0201 on 'error' (page is
otherwise GREEN).
