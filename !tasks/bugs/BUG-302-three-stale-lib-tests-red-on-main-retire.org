#+TITLE: Three stale --lib tests red on main: retired `~preset` parse pin, deleted `@effect` registry pin, signal timeout emit
#+STATE: ITEM
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: BUG-302-three-stale-lib-tests-red-on-main-retire

* Initial Message

I want you to look at how demos/spacetime-docs uses registries to load macros and their definitions. I would like to do the same kind of thing with forms, and have some sort of globally imported brand attributes so that we can have an automatically generated catalog with a sidebar, etc. with a mix of .st.md files and the auto-generated element showcase from the registry. Explore first to understand what I mean. This would also mean changing the style of the showcases to leverage --form a lot, lot more, which might unearth some bugs with using the video and score and other Spacetime affordacnes within the `@form`, but we can do it

* Symptom

Three `--lib` tests fail on current main (observed 2026-08-05/06 during
PLAN-135). Verified by construction NOT caused by the PLAN-135 changes (none
of the three involves `{`-classification, the only behavioral edit in that
wave):

1. `syntax::cst::parser::tests::a_value_position_call_without_a_trailing_member_stays_an_arg_list`
   pins `@preset easing ~soft-exit: cubic-bezier(...)` parsing cleanly. The
   `~` preset sigil is RETIRED (BUG-263) — the parse now correctly produces
   the loud retirement diagnostic. The test pins dead syntax; rewrite it to
   the current value-position-call surface or delete it.

2. `syntax::events::form_compiler::pdc_diagnostic_tests::test_component_body_known_directive_stays_silent_with_registry`
   asserts `@effect` is a KNOWN directive in a @template body. `@effect` was
   DELETED by the @on cutover arc; the registry no longer knows it, so E0900
   fires. Update the test to a directive that still exists.

3. `compiler::tests::signal_emits_policy_and_lifecycle` asserts
   `var timeoutMs = parseMs(3000)` in the emitted JS for
   `policy queue timeout 3s retry 2`. The sibling signal tests (decode table,
   host registration) pass on the SAME program, so compilation succeeds — the
   policy-timeout threading regressed or was reshaped somewhere in the
   PLAN-133 sexpr-lowering commits (30 src/ commits since 2026-08-04). Needs a
   bisect; start at 8adb5888 (compiled sexpr lowering) / 715c8a02 (derive-match).

* Why these matter

A suite that stays red trains everyone to ignore red — the same normalisation
the silent-acceptance waves fought in the compiler itself.
