#+TITLE: reify_component_body: bare &broken ref silently dropped (no E0900)
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: BUG-064-reify-component-body-bare-broken-ref-sil
#+COMPLETION_HASH: a3b22eabdab977e2

* Initial Message

Ok, start with Phase B. Use a wave manifest with in-chat mid-flight check-ins exlpaining what's going on, what' you've done, and what you're doing next. use main thread for architectural work and reviewer swarms in between waves. add as many gap implementation as needed; you have unlimited time budget. file FEAT/BUG you find mid-flight using the org tool and fix/implement them mid-flight

* Symptom
`<div>Hello</div>
&broken` (a bare `&ident` with no `(args)`). Original parse_component_body:
is_template_ref_line requires parens -> false; is_selector_ref_line -> false; falls to
looks_like_invalid_construct (starts with &) -> E0900. reify_component_body: cb_template_ref
grammar (`"&" balanced(';')`) MATCHES, then parse_template_ref("&broken") returns None (no
parens) -> the template_ref arm produces nothing, NO diagnostic. Parity gap (the keystone
anti-pattern: a malformed construct silently vanishes).

* Found by
form_compiler test test_e0900_bare_ampersand_triggers_diagnostic (post-deletion). Corpus missed
it (no bare-&broken case).

* Fix (structural)
The driver's `template_ref` arm: when parse_template_ref returns None (the matched `&`-run is
not a valid invocation), emit E0900 instead of dropping \u2014 mirrors the original fall-through.
Add `&broken` (and `&counter("x")` valid) to the corpus.

NOTE [2026-06-08]: Fixed: template_ref driver arm emits E0900 when parse_template_ref returns None (malformed bare &ref). Corpus case bug064_bare_broken_ref added with golden showing E0900. lib 2130/0.