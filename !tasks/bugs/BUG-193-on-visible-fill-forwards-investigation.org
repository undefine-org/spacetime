#+TITLE: INVESTIGATION: @on visible one-shot revert-to-resting-state NOT reproduced -- regression guard + findings
#+STATE: ITEM
#+SESSION_ID: 153497c78a510229
#+CUSTOM_ID: BUG-193-on-visible-fill-forwards-investigation

* Source
GitHub issue #6 (undefine-org/spacetime): "one-shot timelines revert to
their resting state after completing; no fill:forwards option".

* Investigation outcome (2026-07-18) -- NOT REPRODUCED
Wrote failing-first probes for @on visible driving opacity/translate on
headless V8 AND real Chromium (--cdp): the final frame APPLIES and persists
after the timeline completes; the element does NOT revert. The `isActive =
progress > 0 && progress < 1` gate in apply-animations.st L473/L500 is an
ownership-arbitration check (which driver owns a property when several
target it), NOT a "drop the value at the ends" check -- the end-frame value
is applied before the gate matters.
Conclusion: the originally reported behavior could not be reproduced at
HEAD. Possibly fixed incidentally since the report, or the report involved
a more specific shape (needs a concrete repro from the reporter).

* Deliverable (honest, not a fabricated red)
tests/bugs/BUG-193-on-visible-fill-forwards-investigation.test.st
- A regression GUARD pinning the CORRECT behavior (one-shot @on visible
  keeps its end state; verified green on --cdp, 2 passes) plus an
  investigation doc header with the reproduction matrix and findings.
- NB: the guard is CDP-required: on --headless it fails NONDETERMINISTICALLY
  because the headless rAF/@wait (250ms cap) timing world can't reliably
  drive the IntersectionObserver + animation frames (documented in-file per
  docs/testing/FIDELITY_LADDER.md).
Run: `cargo run --features cdp -- test <file> --cdp --format json`

* Follow-up
Ask the issue reporter for the exact shape that reverted (property set,
browser, whether a second driver targeted the same property). If a concrete
repro arrives, convert to a true RED pin.
