#+TITLE: Bool capture default-fill: scrub:false reads as macro default true
#+STATE: ITEM
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: BUG-096-bool-capture-default-fill-scrubfalse-rea

* Initial Message

can you check how that looks like in the admin? Can you compare that with stuff that is present in Framer/online/in mockups? I want it to look good please

Found during FEAT-106. A scoped `@scroll reveal(... scrub: false)` captures `scrub` as the macro DEFAULT `true`, not the authored `false`. motion.json (build_motion_json, src/server.rs) reflects the captured value, so it shows scrub=true.

REPRO: projects/cms-testbed/index.st `.masthead { @scroll reveal(start:0, end:1, easing:ease-out, stagger:30, scrub:false){...} }` → /__spacetime/dev/motion.json field scrub.value == "true".

LIKELY CAUSE: bool param capture in the events grammar / form_match falls back to the %form default when the literal is `false` (possibly treats `false` as absent/falsy during capture). Other kinds (Number stagger:30, Ident easing:ease-out) capture correctly.

IMPACT: low — FEAT-106 motion editing works for number/length/duration/ident/select params; only bool toggle params mis-read their initial value (the EDIT still writes correctly via EditAst since that patches source text by key, independent of capture). A toggle would just show the wrong initial position.

FIX: trace bool literal capture in src/syntax/events/grammar + form_match bool handling; ensure `false` literal is captured as Bool(false) not skipped→default. Add a capture unit test. Verify both CST + events parsers (the two-parser rule).
