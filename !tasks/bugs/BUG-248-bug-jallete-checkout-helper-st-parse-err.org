#+TITLE: BUG: jallete checkout-helper.st parse error (expected R_PAREN, found DOLLAR)
#+STATE: ITEM
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: BUG-248-bug-jallete-checkout-helper-st-parse-err

* Initial Message

continue. use parallel agents when you can

projects/jallete/checkout-helper.st fails to parse: "expected R_PAREN, found DOLLAR at offset 479" and the same at offset 1047. This breaks `projects/jallete/index.st` (import resolution failure) and `projects/jallete/tests/overlay-state.test.st`.

Verified identical at the pre-cutover worktree (/tmp/pre-w34, commit 025981d5) — predates the SIP-001 @on cutover (agent-verified classification, agent://18-JalleteCheck).

The DOLLAR-in-parens shape suggests a `$sigil` used where the paren grammar expects a value — likely a reactive binding hole inside a call that the current grammar doesn't accept, or stale syntax from an older wave.

projects/ is a separate git repo (gitignored here) — the fix lands there, but the classification belongs to this repo's tracker since the sweep surfaced it.
