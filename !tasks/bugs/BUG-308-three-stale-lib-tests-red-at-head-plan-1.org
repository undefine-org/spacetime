#+TITLE: Three stale lib tests red at HEAD (PLAN-122/~preset retirement lane, pre-existing)
#+STATE: ITEM
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: BUG-308-three-stale-lib-tests-red-at-head-plan-1

* Found
2026-08-05, during PLAN-135 W4 full-lib verification. Confirmed PRE-EXISTING at HEAD (98a006a7) via a clean worktree without any PLAN-135 changes — all three fail identically there. They belong to the PLAN-122/FEAT-166/FUP-181 lane (the value-checker and `~preset` retirement churn), which is being committed by a parallel session.

* The three
1. `compiler::tests::signal_emits_policy_and_lifecycle` — "timeout 3s must thread as 3000ms"
2. `syntax::cst::parser::tests::a_value_position_call_without_a_trailing_member_stays_an_arg_list` — asserts `@preset easing ~soft-exit: cubic-bezier(...)` still parses; the parser now hard-errors "`~name` preset references are retired: use a `@form easing --name` form or the bare CSS keyword". The test pins RETIRED syntax as valid — stale after the retirement, needs updating to the `@form easing` surface (or deleting).
3. `syntax::events::form_compiler::pdc_diagnostic_tests::test_component_body_known_directive_stays_silent_with_registry`

* Impact
`cargo test --lib` is 3105 passed / 3 failed — a dirty gate for everyone until the owning lane sweeps them.

* Repro
#+begin_src sh
git worktree add /tmp/vb HEAD && cd /tmp/vb
cargo test --lib -- signal_emits_policy_and_lifecycle \
  a_value_position_call_without_a_trailing_member \
  test_component_body_known_directive_stays_silent_with_registry
#+end_src

NOTE [2026-08-06]: 2026-08-05 PLAN-135 W6: the rot is wider. tests/integration_tests could not COMPILE since the W4 commit (FormMatch literal in tests/integration/provenance_tests.rs missed by the doc-field sweep) — masking 15 committed-lane failures at HEAD fcb70f9e (verified in a clean worktree with only the compile fix applied): 7× responsive_nested_directives (E0956 unknown primitive on-cutover#loop-positional), 3× wave1_primitives reveal_*_type (E0946 grammar), 2× bare_ident_string_arg, 1× reactive_properties killed-bind, + the lib snapshot_test_and_fixture drift (__st_noteAssertion emission vs stale snap). Failure sets identical with and without the W6 diff, so none are W6's; the compile fix lands with W6 so the suite is at least runnable again.