#+TITLE: $object-typed signal initializers never reach emitted page JS (silent elision — by design or bug?)
#+STATE: ITEM
#+SESSION_ID: 1534a246b3aa1d15
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-04-13-518Z_1534a246b3aa1d15.jsonl]]
#+CUSTOM_ID: BUG-205-object-typed-signal-initializers-never-r

* Initial Message

continue

* Observation (2026-07-20, during PLAN-079 live pill verification): a page with

  $user object: { name: "Ada" };
  .card { text <- $user.name; }

  served via `spacetime serve` emits NO "Ada" anywhere in the page JS — the object initializer never reaches the emitted runtime, so `ST.resolve(el, 'user')` returns undefined and the binding renders the static fallback text. window.SpacetimeLocal contains no `user` key (scalar `$flag bool: true;` DOES publish).

* Repro: serve a minimal project with the two lines above (pure new syntax, zero migrations involved — confirmed orthogonal to PLAN-079). `curl http://127.0.0.1:PORT/__spacetime/runtime.js?entry=index.st | grep -c Ada` → 0.

* Open question: is `object:` initializer elision BY DESIGN (compile-time-only data? wrong idiom? maybe object signals require @data instead of bare $-decls) or a BUG in the object-signal init/publish path? The scalar path publishes; the object path vanishes silently either way — silent elision is a fidelity-ladder violation even if the idiom is wrong (should warn).

* Next step: trace where `$x object: {...}` initializers go in the emit pipeline (likely src/emit or pipeline data-decls handling); check whether v8/headless suites cover object-signal init at all.
