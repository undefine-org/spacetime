#+TITLE: CMS admin: delete entry has no confirmation (destructive)
#+STATE: DONE
#+SESSION_ID: 14f8685429e4ccf1
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-01T18-22-11-112Z_14f8685429e4ccf1.jsonl]]
#+CUSTOM_ID: BUG-056-cms-admin-delete-entry-has-no-confirmati
#+COMPLETION_HASH: bee0c4e7fa535dbe

* Initial Message

ok let's think deeply deeply. What would a headless CMS for Spacetime look like? like a headless CMS that has all the nice features of a headless CMS and the power of Spacetime combined. Look at the stdlib and the __dev__ to understand some of the pre-existing experiments, and do a research on the state of headless CMSes, before coming back with your THOUGHTS AND REFLECTIONS (no decisions yet)

* Source
PLAN-028 Wave A ground-truth.

* Symptom
The drawer "Delete" button deletes the entry IMMEDIATELY with no confirmation step.
Verified: clicking Delete dropped Services 4→3 entries (and on disk) with no dialog.
A content writer can irreversibly destroy an entry with a single misclick; there is no
undo in V1.

* Fix (Wave C — polish)
Add a lightweight inline confirm (two-step button "Delete" → "Confirm delete", or a
small modal). Keep it calm/editorial per the admin's design language. Optionally pair
with a transient "Undo" toast (the save layer already shows toasts).

* Acceptance
- Delete requires an explicit confirm; accidental single-click does not destroy data.

* Related
- PLAN-028 Wave C, stdlib/__admin__/admin-app.st.

NOTE [2026-06-03]: DONE (Wave C). Two-step inline confirm: 1st click arms (text→"Confirm delete", scarlet --armed class, 3s auto-disarm), 2nd click within 3s deletes. Verified live: arm shows, rows unchanged at 9 until confirm, disarms after 3s. Reviewer: stale-timer safe (openDrawer rebuilds button+closure).