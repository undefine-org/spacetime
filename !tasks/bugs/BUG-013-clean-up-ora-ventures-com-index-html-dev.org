#+TITLE: Clean up ora-venturescom indexhtml dev artifacts and duplicates
#+STATE: DONE
#+SESSION_ID: 14aef9c617fc29ee
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-05T17-30-02-719Z_14aef9c617fc29ee.jsonl]]
#+CUSTOM_ID: BUG-013-clean-up-ora-ventures-com-index-html-dev
#+EFFORT: XS
#+PRIORITY: #B
#+LAYER: site

NOTE [2026-04-05]: Removed localhost:3333 preconnect, duplicate Styles comment, and duplicate FOUC style from index.html. All 30 files pass cargo check.

* Initial Message

Run a full audit of the ora-ventures.com/ website at localhost:9416. A lot of stuff is broken. Please be thorough and list the issues.

* Problem

=ora-ventures.com/index.html= has three cleanup issues left from the recent fixes:

** Issue 1: Hardcoded localhost:3333 preconnect (line 23)
#+begin_example
<link rel="preconnect" href="https://localhost:3333" />
#+end_example
This is a development artifact. In production it causes a failed TLS handshake on every page load. Should be removed entirely — the dev server does not use port 3333.

** Issue 2: Duplicate HTML comment (lines 27 + 29)
#+begin_example
<!-- Styles: generated by SpaceTime from index.st + modules/ -->

<!-- Styles: generated by SpaceTime from index.st + modules/ -->
<!-- (themes.css and ora.css replaced by pure .st modules) -->
#+end_example
The first comment (line 27) is an exact duplicate of line 29. Remove line 27 (the first occurrence). Keep lines 29-30 (the comment + the sub-comment).

** Issue 3: Duplicate FOUC prevention style (line 37)
#+begin_example
<style>:not(:defined){visibility:hidden}</style>
#+end_example
The dev server (=src/server.rs= line ~634) also injects this identical style into =</head>=. Having it in both places means locale-served pages get it twice. Remove it from the HTML — the server is the single source of truth for runtime injection.

* Implementation

** File: =ora-ventures.com/index.html=

Remove these three lines/sections from the =<head>= block:

1. *Delete line 23*: =<link rel="preconnect" href="https://localhost:3333" />=
2. *Delete lines 27-28*: The first =<!-- Styles: generated by SpaceTime from index.st + modules/ -->= and the blank line after it
3. *Delete line 37*: =<style>:not(:defined){visibility:hidden}</style>=

The resulting =<head>= section (lines 15-37 area) should look like:

#+begin_src html
    <link rel="dns-prefetch" href="//fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- Styles: generated by SpaceTime from index.st + modules/ -->
    <!-- (themes.css and ora.css replaced by pure .st modules) -->

    <!-- Favicon -->
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>&#x25CF;</text></svg>"
    />
</head>
#+end_src

* Edge Cases
- The =<link rel="preload" href="/fonts/AspektaVF.woff2" .../>= block (lines 16-22) stays — it is a valid font preload.
- The =<link rel="dns-prefetch" href="//fonts.googleapis.com" />= and =<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />= stay — they are valid production prefetches.
- The =<meta name="color-scheme" content="light dark" />= on line 6 stays.

* Acceptance Criteria
1. =grep -n "localhost:3333" ora-ventures.com/index.html= returns no matches
2. =grep -c "Styles: generated by SpaceTime" ora-ventures.com/index.html= returns =1= (not 2)
3. =grep -c ":not(:defined)" ora-ventures.com/index.html= returns =0=
4. =cargo run -- check ora-ventures.com/= still passes with 0 errors
