#+TITLE: Opening a side drawer dims and shrinks the stage (modal metaphor on a work panel)
#+STATE: DONE
#+SESSION_ID: 1522aa9778a04732
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-04T14-07-53-826Z_1522aa9778a04732.jsonl]]
#+CUSTOM_ID: BUG-152-opening-a-side-drawer-dims-and-shrinks-t
#+COMPLETION_HASH: ad23fdbf76dbbbbd

* Initial Message

i have restarted the session, can you do the visual confirmation now?

User-reported (2026-07-05): "it's a fucking stupid and idiotic decision to increase opacity when we open side bars." (The stage dims/recedes — reads as an opacity change — when a drawer opens.)

** Root cause (source-confirmed)
styles.st:416-423  .mcp-stage-shell[data-drawer="inspector"] .mcp-stage { transform: scale(0.985); filter: brightness(0.7); } (and for nav/structure/origin/comms).
Plus .mcp-scrim (styles.st:758) rgba(12,9,7,0.45) + pointer-events:auto when .is-active ($drawer != "none", env.st:379): a full-inset dark backdrop that dims the canvas AND blocks canvas clicks whenever any drawer is open.
This is a MODAL-dialog affordance (dim the backdrop to focus the dialog) misapplied to a SIDE PANEL that must be used WHILE looking at + editing the canvas. Opening the inspector to edit the hero dims + shrinks + click-blocks the hero.

** Fix (PLAN-065 W_dim — DONE, pending commit)
styles.st: removed the drawer-wide stage transform/filter recede; made the scrim fully transparent + pointer-events:none always (dismissal stays on the close glyph + Escape, so a canvas node-selection click is never stolen). Stage stays full-bright + geometrically stable while any side drawer is open.

** Verify
Open inspector while a guest is selected -> computed .mcp-stage transform=none, filter=none; selection ring stays visible; canvas still clickable for node selection.

Parent: PLAN-065.

NOTE [2026-07-05]: FIXED via W_dim (commit a05d4487) + live-verified. Removed drawer-wide stage transform/filter recede + made scrim inert. Computed .mcp-stage transform=none filter=none with a drawer open. Swarm 0.92.