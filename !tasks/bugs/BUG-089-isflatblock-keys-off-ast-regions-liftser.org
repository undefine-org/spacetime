#+TITLE: isFlatBlock keys off AST .regions, lift+server key off schema (R2 P2)
#+STATE: DONE
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: BUG-089-isflatblock-keys-off-ast-regions-liftser
#+COMPLETION_HASH: 8f927e3e3777a075

* Initial Message

what do FUP-042 and FUP-043 entail (let's start with them). You say that union types are small but are they really? Be synthetical, big picture.

Surfaced by W-R2 reviewer (agent 12-RevR2) on FUP-042 R2.

public/runtime/editable.js isFlatBlock(block) = !block.regions decides project +
size + position paths from the AST .regions key, but liftBlock + server
validate_block decide from the SCHEMA (spec.regions / shape.regions). So project is
not a strict inverse of lift, and blockSize/resolve compute boundaries for a shape
the validator won't agree with, when an AST .regions key disagrees with its schema.

NOT a security break (the untrusted ingestion path liftBlock is schema-keyed and
matches the fail-closed server; project only emits sanitized text/marks). P2 client
data-fidelity for malformed ASTs.

Fix: key the flat-vs-region decision off the schema (E.schema.blocks[type].regions)
in all kernel paths, so project + blockSize + resolve + lift + server all agree.
regionList becomes schema-driven for region blocks (schema region names authoritative;
AST supplies content, missing → empty), AST fallback only when schema unknown.

Parent: PLAN-031 / FUP-042.

NOTE [2026-06-11]: Fixed in a54e28d3. blockRegionSpecs(block) reads E.schema.blocks[type].regions; isFlatBlock + regionList derive from it so project/lift/position-math/server all agree on one block shape. Schema-less fallback trusts AST .regions for pure-kernel tests. 28 editable_model green.