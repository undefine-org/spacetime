#+TITLE: v8 emit-shape assertions drifted from BUG-155 dotted-path emit upgrade (sig→sigHead, sourceName→headName)
#+STATE: DONE
#+SESSION_ID: 1534a246b3aa1d15
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-04-13-518Z_1534a246b3aa1d15.jsonl]]
#+CUSTOM_ID: BUG-199-v8-emit-shape-assertions-drifted-from-bu

* Initial Message

i want us to design a system where the stdlib has 'migrations', i.e. a macro that takes old st syntax and turns it to new st syntax. the macro should include the date so that we can order the migrations by syntax and apply syntax waves. It should be shown in all dev environments next to the current host pill, in the same style as the host pill

* Found
During PLAN-076 W2 verification: 3 v8 runtime tests failed at HEAD (confirmed
via a clean `git worktree` at HEAD — pre-existing, unrelated to PLAN-076):
- feat074_view_emits_reactive_mount
- feat074_effect_emits_subscription_and_body
- bug076_each_dual_channel_dedup_emitted

* Root cause
Test-expectation drift from the BUG-155 dotted-path emit upgrade. The emitters
were changed to key subscriptions off the HEAD signal (`sigHead` for
@view/@effect, `headName` for @each channels, template-literal event names),
but three emit-shape assertions kept the OLD identifiers/forms:
- `'local:' + sig + ':updated'` → emit produces `'local:' + sigHead + ':updated'`
- `local:${sourceName}:updated` / `ST.afterData(sourceName` → emit produces
  `local:${headName}:updated` / `ST.afterData(headName`

Runtime behavior was correct the whole time (the behavior-level tests —
feat074_view_swaps_pane_on_signal_change etc. — passed); only the string
assertions drifted.

* Fix (done this session, PLAN-076 W2)
Updated the three assertions in tests/v8_runtime_test.rs to the BUG-155-era
shapes. `cargo test --test v8_runtime_test`: 437 passed, 0 failed.
