#+TITLE: apply_text_patch eats closing paren+body when editing last paren-list param
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: BUG-098-apply-text-patch-eats-closing-parenbody

* Initial Message

can you check how that looks like in the admin? Can you compare that with stuff that is present in Framer/online/in mockups? I want it to look good please

Found by FEAT-106 reviewer swarm (Opus, P1). Fixed same turn.

apply_text_patch (src/sync/handlers.rs) value-end scanner treated `}` as a depth-0 terminator but `)` and `]` always decremented depth, never terminating. Editing the LAST param of a paren-list directive ran the value past the closing `)` into the body: `@scroll reveal(start:0, stagger:30){ opacity:0->1; }` patched stagger→50 produced `@scroll reveal(start:0, stagger:50;
 }
}` — destroyed the paren list + keyframe body.

WHY MISSED: my FEAT-106 test + browser check only patched NON-last params (comma-terminated). The reviewer flagged the last-param path explicitly.

FIX: `b')' | b']' | b'}' if depth == 0 => terminate; below depth 0 => balance`. RED test test_edit_ast_last_paren_param_preserves_closing_and_body reproduced (stagger:50;
 }), now GREEN. Shared path with brand object-literal patching — full lib 2251/0 no regression. Browser: toggling scrub (last param) writes scrub:true, ) + body intact, compiles.

Commit a9e037c6.
