#+TITLE: editable_bind_is_per_instance test has never compiled on its harness path
#+STATE: TODO
#+PRIORITY: #B

* The bug (agent 73's documented STOP, 2026-08-04)
tests/v8_runtime/per_instance_compiled_tests.rs::
editable_bind_is_per_instance_across_two_compiled_instances has NEVER been
green — two independent breaks:

1. `@editable`'s binds target the `editable-surface` primitive, which lives
   ONLY in the opt-in `stdlib/editable` module (NOT in STDLIB_DIRS,
   src/metasystem/registry.rs). The raw `Compiler::from_ast` path does not
   resolve `@import`; only from_file/resolve_imports does. So the fixture
   dies with E0956 'unknown primitive: editable-surface' and emits empty JS.
   The primitive (eef326be, 2026-06-10) predates the test (fed9297a,
   2026-06-14) — it was born broken.
2. Even on the import-resolving path its 2nd assertion
   `!js.contains("`$f.value`")` false-positives on the editable-surface
   primitive's OWN doc comment (which literally contains `` `$f.value` ``).

* Fix shape
Route the fixture through the import-resolving compile path (or register the
editable module in the harness registry) AND make the assertion target the
emitted bind, not a substring of the whole bundle (the doc-comment class of
false positive — assert on a narrowed scope or a structural property).
