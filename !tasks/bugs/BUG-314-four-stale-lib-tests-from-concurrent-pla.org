#+TITLE: Four stale --lib tests from concurrent PLAN-135 work (not from the issue-backlog waves)
#+STATE: ITEM
#+SESSION_ID: 154d0baaa942d918
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-08-06T12-08-13-733Z_154d0baaa942d918.jsonl]]
#+CUSTOM_ID: BUG-314-four-stale-lib-tests-from-concurrent-pla

* Initial Message

please look at all open issues in the github and compare to main. Cluster them by category, root-cause, impact, and ways to solve. See if any work that's been done on main in the past two weeks addresses them. Then give me practical next steps, and give me an instructional explanation of each issue that an intern would benefit from

#+STATE: ITEM

* Summary

`cargo test --lib` at HEAD: **3110 passed, 4 failed**. All four are stale assertions left by
concurrent PLAN-135 / other-session commits, NOT by the GitHub-issue waves (BUG-309 fcb70f9e,
GH-23 6fb20c2f, GH-26/27/34 040d0411).

* The four

1. `syntax::cst::parser::tests::a_value_position_call_without_a_trailing_member_stays_an_arg_list`
   Asserts `@preset easing ~soft-exit: ...` "must still parse". PLAN-135 RETIRED `~name` preset
   references (`src/syntax/cst/parser.rs:2707`: "`~name` preset references are retired: use a
   `@form easing --name` form or the bare CSS keyword"). The retirement is intentional; the test
   was not updated with it. FIX: update the test to the `@form easing --name` spelling.

2. `syntax::events::form_compiler::pdc_diagnostic_tests::test_component_body_known_directive_stays_silent_with_registry`
   Asserts `@effect` in a @template body emits no E0900. `@effect` was RETIRED onto `@on
   $sig.change` (commit 0000c27d, "15 consumers migrated"), so it is correctly no longer a known
   directive. FIX: update the test to a currently-known directive.

3. `compiler::tests::signal_emits_policy_and_lifecycle` — panics "timeout 3s must thread as 3000ms".
   Duration-unit threading, owned by the PLAN-122/BUG-294 duration work.

4. `compiler::snapshot_tests::snapshot_test_and_fixture` — snapshot needs refreshing after the
   above changes.

* Attribution evidence (not assumed)

- `git status` shows the three files clean at HEAD => the WIP that broke them was committed by
  its own session, not left dirty by mine.
- `git show 040d0411 -- src/syntax/events/form_compiler.rs | grep -iE "E0900|template|known directive"`
  => EMPTY. The only issue-wave change to that file cannot reach the failing assertion.
- The `~soft-exit` retirement string is in HEAD's parser and traces to the PLAN-135 arc.
- A baseline bisect was attempted via a temp worktree but could not build there (the
  `stdlib/text/vendor/pretext` submodule is not checked out in a fresh worktree) — so
  attribution rests on the diff evidence above rather than on a baseline run. Stated plainly
  because it is the weaker form of proof.

* Action

Owned by the PLAN-135 / PLAN-122 authors, not by the issue backlog. Listed here so the
issue-wave commits are not blamed for them and so they are not silently tolerated.
