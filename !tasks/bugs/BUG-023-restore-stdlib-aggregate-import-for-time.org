#+TITLE: Restore stdlib aggregate import for timeline macros before UNKN site work
#+STATE: DONE
#+SESSION_ID: 14cc778b8e323f00
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-28T15-15-29-976Z_14cc778b8e323f00.jsonl]]
#+CUSTOM_ID: BUG-023-restore-stdlib-aggregate-import-for-time
#+LAYER: tooling

* Initial Message

look at ./projects/unkn/*. I want you to create a full Spacetime website, using the latest Spacetime affordances and folder hierarchy, fully manually verified for visual excellence and adherence ot the specs in the PDF. Be comprehensive in determining and describing your understanding of the work.

* Intention & Purpose
Fix the current stdlib aggregate import risk before the UNKN site imports `stdlib`. Current repo evidence: `stdlib/index.st` imports `./macros/scroll-timeline.st`, but `stdlib/macros/` contains `timeline.st` and no `scroll-timeline.st`. The UNKN decision is to import `stdlib` and fix stdlib if compile fails, not bypass stdlib with local hardcoded copies.

* Context & Narrative
The UNKN website will use latest Spacetime affordances: templates, typed data, @each, @on, @scroll/@load timelines, @reveal, @cursor, @magnetic, @media, @font, and tests. Importing individual macro files would avoid the broken aggregate, but the settled design intentionally exercises `@import "stdlib"` so the website represents the current public Spacetime workflow. This task is a small infrastructure preflight so implementation agents do not waste time debugging project code when stdlib resolution is the true issue.

* Scope
In scope:
- Verify whether `@import "stdlib"` fails because of the missing `stdlib/macros/scroll-timeline.st` import.
- Repair the aggregate import path or add a compatibility shim so the public `stdlib` entry point exposes working timeline macros.
- Keep behavior defined by existing `stdlib/macros/timeline.st`; do not create a parallel Rust parser or project-local fallback.

Out of scope:
- Redesign timeline macro semantics.
- Rewrite animation primitives.
- Change UNKN site layout.

* Existing Patterns
- `AGENTS.md`: do not write Rust-based parsers/fallbacks for stdlib constructs; fix stdlib loading.
- `stdlib/index.st`: aggregate macro imports, currently referencing `./macros/scroll-timeline.st`.
- `stdlib/macros/timeline.st`: working definitions for `%macro scroll-timeline`, `%macro load-timeline`, `%macro hover-timeline`, `%macro click-timeline`, `%macro mouse-timeline`, `%macro time-timeline`, `%macro value-change-timeline`.
- `docs/antipatterns.md`: no custom JavaScript outside `%primitive` and `@test`.

* Tests
Workflows to validate first:
1. A minimal `.st` file with `@import "stdlib"` compiles.
2. A selector using `@scroll reveal-name(...) { opacity: 0 -> 1; }` resolves through stdlib.
3. A selector using `@load intro(...) { opacity: 0 -> 1; }` resolves through stdlib.
4. Existing stdlib/tests are not regressed.

Verification commands after implementation:
- `cargo run -- check`
- `cargo test --lib` if the import fix touches Rust embedding or loader code
- Targeted compile of a temporary/manual fixture during implementation; do not commit scratch artifacts.

* Implementation
** ITEM Verify aggregate import failure
:PROPERTIES:
:CUSTOM_ID: BUG-023-restore-stdlib-aggregate-import-for-time::verify-aggregate-import
:END:
- File paths: `stdlib/index.st`, `stdlib/macros/`, minimal implementation scratch only if needed.
- Run the smallest compile/check that proves whether missing `scroll-timeline.st` breaks `@import "stdlib"`.
- Evidence must include the exact diagnostic or confirmation that current embedding masks the filesystem mismatch.

** ITEM Repair stdlib timeline export
:PROPERTIES:
:CUSTOM_ID: BUG-023-restore-stdlib-aggregate-import-for-time::repair-timeline-export
:DEPENDS: BUG-023-restore-stdlib-aggregate-import-for-time::verify-aggregate-import
:END:
- Preferred fix: update `stdlib/index.st` to import `./macros/timeline.st` if that is the canonical file.
- Alternate fix only if compatibility requires it: create a tiny `stdlib/macros/scroll-timeline.st` shim that imports `./timeline.st`.
- Do not duplicate macro definitions.

** ITEM Add or update coverage
:PROPERTIES:
:CUSTOM_ID: BUG-023-restore-stdlib-aggregate-import-for-time::coverage-stdlib-import
:DEPENDS: BUG-023-restore-stdlib-aggregate-import-for-time::repair-timeline-export
:END:
- Add the narrowest coverage proving `@import "stdlib"` exposes `@scroll`/`@load`, only if an existing test does not already cover it.
- Keep the test close to existing stdlib/import tests.

* Edge Cases
- Embedded stdlib registry may include file paths independent of filesystem paths; if so, fix both the source index and embedding list only if needed.
- If `timeline.st` macros compile but behavior is broken, defer behavior bugs to a separate item; this task only restores import resolution.
- Do not delete or rename existing stdlib files without checking import consumers.

* Acceptance Criteria
- `@import "stdlib"` can be used by `projects/unkn/index.st` without missing-file diagnostics.
- Timeline macros remain single-sourced in `stdlib/macros/timeline.st` or an explicitly documented compatibility shim.
- Verification commands above pass or any unrelated failure is documented with exact output and not used to hide this task failure.

NOTE [2026-04-28]: stdlib/index.st now imports ./macros/timeline.st (canonical filename). Resolver maps bare @import \"stdlib\" to stdlib/index.st (or embedded index module). Test test_bare_stdlib_resolves_to_index added. Outdated metasystem test path (scroll-timeline.st) refreshed.