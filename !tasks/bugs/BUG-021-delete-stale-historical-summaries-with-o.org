#+TITLE: Delete stale historical summaries with obsolete paths
#+STATE: DONE
#+SESSION_ID: 14cb4663c8d20fb9
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-27T17-02-15-332Z_14cb4663c8d20fb9.jsonl]]
#+CUSTOM_ID: BUG-021-delete-stale-historical-summaries-with-o
#+LAYER: docs

* Initial Message

Implement these audit recommendations from org://AUD-006-audit-implementation:

## Audit findings

Sorted by effort-to-impact ratio.

---

- **What**: Stale `cargo run -- serve site/` / `check site/` / `build site/` and "Site Directory (`site/` and subdirectories)" ownership in agent context prompts.
- **Where**:
  - `.claude/agents/site-designer.md` — multiple, including `## Tools & Technologies` "`cargo run -- serve site/` for live development server", `## Owns / Site Directory (`site/` and subdirectories)`, `## Notes / Development Workflow` step 1 "Start dev server: `cargo run -- serve site/`", debugging tips `RUST_LOG=trace cargo run -- serve site/`, plus `site/examples/todo-app` example.
  - `.claude/agents/pipeline-engineer.md` — the shell block under `## Notes / Key Debugging Commands` contains `RUST_LOG=spacetime::pipeline=trace cargo run -- serve site/`, `cargo run -- check site/`, `cargo run -- build site/`.
  - `.claude/agents/code-quality-guardian.md` — 1 example mention `site/examples/todo-app`.
- **Why**: `site/` no longer exists in the repo. Subagents loaded under these identities will paste broken commands and propose changes to a non-existent directory. This is precisely the work item FEAT-014::update-skills was meant to cover (the plan explicitly listed "Update only verbatim copy-paste-ready commands"); execution missed `.claude/agents/`.
- **Effort**: trivial (3 files, literal find/replace `site/` → `projects/<name>/`, plus rename "Site Directory" ownership stanza to point at `projects/`).

---

- **What**: `# Or from the Spacetime repo root: cargo run -- serve <cd_target>` hint in `run_init` success output assumes the caller's cwd is the Spacetime repo root.
- **Where**: `src/main.rs::run_init` ~line 1782–1783.
- **Why**: When `cd_target = projects/<name>` the hint is correct only if cwd was already the repo root (so `projects/` was the in-repo gitignored dir). When `cd_target = <name>` (no `projects/` parent), the hint prints `cargo run -- serve <name>`, which only resolves to the right place if the user's cwd happened to be the Spacetime repo root. Misleading for users who installed `spacetime` and ran `init` from a personal scratch dir without `projects/`.
- **Effort**: trivial (gate the "Or from the Spacetime repo root" hint behind the `cd_target.starts_with("projects/")` branch, or drop the hint entirely).

---

- **What**: `resolve_init_target_returns_pathbuf` test is a tautological type assertion duplicating `resolve_init_target_no_projects_dir`.
- **Where**: `src/main.rs` ~lines 3736–3741.
- **Why**: Adds noise without coverage. The function signature already declares `-> PathBuf`; the cargo build proves the type. Dead test.
- **Effort**: trivial (delete).

---

- **What**: No test for `projects/` being a broken symlink (target missing).
- **Where**: `src/main.rs::init_target_tests`.
- **Why**: `std::fs::metadata` follows symlinks; on a broken symlink it returns `Err`, so `unwrap_or(false)` falls back to cwd/name. Behavior is correct but unverified. Adjacent to the existing `with_projects_symlink_to_dir` test, easy to add.
- **Effort**: trivial.

---

- **What**: Tracked compiler bug documentation (`bugs/BUG-000-each-template-rendering-overview.md` … `BUG-003-template-invocation-js-serialization.md`) moved to gitignored `scratch/bugs/`.
- **Where**: `scratch/bugs/` (was root `bugs/`, listed in plan as "non-Spacetime debris").
- **Why**: Reading file contents, these are Spacetime-compiler-specific BUG notes (template-invocation extractor stub, body capture quantifier loop, etc.). They look like the older equivalent of the `!tasks/bugs/` org-mode bug tracker. Demoting them to gitignored `scratch/` removes them from the main repo's history going forward and from anyone cloning the repo. The plan scoping called this a "decision" but the deletion is irreversible after the next prune; consider whether they should instead be archived under `!tasks/` or `docs/legacy-bugs/` before falling out of git visibility.
- **Effort**: small (move tracked-but-still-needed entries into `!tasks/bugs/` legacy area; or `git mv scratch/bugs docs/legacy-bugs` and adjust .gitignore exception).

---

- **What**: `tests/PHASE8_SUMMARY.md` and other historical task summaries still contain absolute `/home/user/code/ora/spacetime/...` paths.
- **Where**: `tests/PHASE8_SUMMARY.md` lines 19, 62, 163, 378; `.serena/memories/STDLIB_JS_EMISSION_TRACE.md:273` references `cargo run -- serve site/`.
- **Why**: Plan deliberately scoped historical notes out ("only forward-facing instructional docs change"), but `tests/PHASE8_SUMMARY.md` is in `tests/`, not under `!tasks/` or `@tasks/`, and might get crawled by future readers as if it were current docs. Borderline — flagged for awareness, not necessarily action.
- **Effort**: trivial (one sed-style substitution) if you choose to action.

* Scope
** In scope
- Delete the two stale historical summary/memory files called out by AUD-006 and confirmed by user clarification:
  - =tests/PHASE8_SUMMARY.md=
  - =.serena/memories/STDLIB_JS_EMISSION_TRACE.md=
- The user explicitly answered =delete them= for the audit's borderline historical-summary recommendation.
- Verify deletion removes stale absolute =/home/user/code/ora/spacetime/...= references and =cargo run -- serve site/= from these files.

** Out of scope
- Do not mass-delete other historical docs or memory files.
- Do not rewrite =!tasks/= or =@tasks/= journals.
- Do not update or regenerate memory artifacts; this task only removes the two specified stale files.
- Do not delete =tests/E2E_TEST_PLAN.md= or =tests/PERFORMANCE.md= unless a separate audit requests it.

* Existing Patterns
- Forward-facing docs were updated in PLAN-015 to use relative paths and =projects/<name>/=.
- =tests/PHASE8_SUMMARY.md= is not under =!tasks/= or =@tasks/= and may be crawled as current test documentation despite being historical.
- =.serena/memories/STDLIB_JS_EMISSION_TRACE.md= contains an obsolete =cargo run -- serve site/= command in a memory artifact.
- User clarification for this plan selected deletion rather than patching these historical summaries.

* Tests
- Baseline scenario A: before deletion, =grep -n '/home/user/code/ora/spacetime\|serve site/' tests/PHASE8_SUMMARY.md .serena/memories/STDLIB_JS_EMISSION_TRACE.md= shows the stale references captured by AUD-006.
- Deletion scenario B: after deletion, =test ! -e tests/PHASE8_SUMMARY.md= passes.
- Deletion scenario C: after deletion, =test ! -e .serena/memories/STDLIB_JS_EMISSION_TRACE.md= passes.
- Search scenario D: =grep -rn '/home/user/code/ora/spacetime\|serve site/' tests/PHASE8_SUMMARY.md .serena/memories/STDLIB_JS_EMISSION_TRACE.md= should fail because files no longer exist; use absence checks as the primary verification.
- Safety scenario E: =find tests -maxdepth 1 -type f -name '*SUMMARY*.md'= should not reveal another stale summary targeted by this plan unless explicitly reviewed and added; do not expand scope automatically.

* Implementation
** ITEM Capture stale-summary baseline (TDD: before deletion)
:PROPERTIES:
:CUSTOM_ID: BUG-021-delete-stale-historical-summaries-with-o::capture-stale-summary-baseline
:END:
- Run the baseline grep from Tests scenario A.
- Record which file references stale absolute paths and which file references =serve site/=.

** ITEM Delete the two stale historical files
:PROPERTIES:
:CUSTOM_ID: BUG-021-delete-stale-historical-summaries-with-o::delete-stale-historical-files
:DEPENDS: BUG-021-delete-stale-historical-summaries-with-o::capture-stale-summary-baseline
:END:
- Delete =tests/PHASE8_SUMMARY.md=.
- Delete =.serena/memories/STDLIB_JS_EMISSION_TRACE.md=.
- Use =git rm= if the files are tracked; if one is untracked, plain deletion is acceptable during implementation.
- Do not delete surrounding directories.

** ITEM Verify stale historical files are gone
:PROPERTIES:
:CUSTOM_ID: BUG-021-delete-stale-historical-summaries-with-o::verify-historical-delete
:DEPENDS: BUG-021-delete-stale-historical-summaries-with-o::delete-stale-historical-files
:END:
- Run =test ! -e tests/PHASE8_SUMMARY.md=.
- Run =test ! -e .serena/memories/STDLIB_JS_EMISSION_TRACE.md=.
- Run =git status --short tests/PHASE8_SUMMARY.md .serena/memories/STDLIB_JS_EMISSION_TRACE.md= and confirm it shows deletions for tracked files or no entries for untracked removed files.

* Edge Cases
- If =.serena/memories/STDLIB_JS_EMISSION_TRACE.md= is untracked or ignored in the implementation session, deletion is still required locally because the user asked to delete it.
- If =tests/PHASE8_SUMMARY.md= has already been removed, treat that part as idempotently complete and verify absence.
- Do not chase every =/home/user/code/ora/spacetime= occurrence across the repo in this item; only the two files named in the clarified scope are targeted.

* Acceptance Criteria
- =tests/PHASE8_SUMMARY.md= does not exist.
- =.serena/memories/STDLIB_JS_EMISSION_TRACE.md= does not exist.
- No other files are deleted by this item.
- Completion report records the user clarification that deletion, not path substitution, was selected.

** Completion [2026-04-27]
*** Verification
- Baseline grep captured stale paths in =tests/PHASE8_SUMMARY.md= (lines 19, 62, 163, 378-381) and =cargo run -- serve site/= reference in =.serena/memories/STDLIB_JS_EMISSION_TRACE.md:273= (see [[file:!tasks/plans/plan-artifacts/PLAN-016-aud-006-implementation-remediation/stale_summaries.txt]]).
- =test ! -e tests/PHASE8_SUMMARY.md= passes.
- =test ! -e .serena/memories/STDLIB_JS_EMISSION_TRACE.md= passes.
- =git status --short= shows both files as ='D'= staged-pending deletions for the upcoming commit.
*** Artifacts
- [[file:!tasks/plans/plan-artifacts/PLAN-016-aud-006-implementation-remediation/stale_summaries.txt]]
*** Deviations
None.