#+TITLE: Flagship showcases: cinematic launch, studio, portfolio + showpiece sections
#+STATE: DONE
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: PLAN-146-flagship-showcases-cinematic-launch-stud

* Initial Message

these are basic pages. I want showcases that really impress. Look at hyperframes' or aeternity's for example. I want them to be highly relevant to websites that graphic designers get paid to make

User verdict on the current tree: "these are basic pages. I want showcases that really impress. Look at hyperframes' or aeternity's. Highly relevant to websites that graphic designers get paid to make."

Direction chosen via Q&A (2026-08-08):
- SHAPE: both — full cinematic flagship PAGES + upgraded showpiece SECTIONS in the gallery.
- SCENARIOS: Brand/product launch (aeternity shape: dark cinematic, 30k-particle morph hero, kinetic type, scroll story), Studio/agency (case-study grid, hover reveals, big numbers, marquee), Designer portfolio (editorial serif, horizontal work gallery, scroll-spy).

Vocabulary is proven, not invented: demos/3d/particle-morph (30k particles morphing on scroll + bloom), @stage/@particles/@scroll-3d/@post, @parallax, @reveal(split: words/chars), @magnetic, @particle-field, @timeline, @smooth-scroll, @scroll-spy.

* Waves
- W1 :: demos/brand-launch/ — flagship: pinned WebGL particle hero + kinetic type + scroll-driven story + spec + CTA. Verify under --cdp.
- W2 :: demos/studio/ — case-study grid with hover reveals, count-up numbers, client marquee rail.
- W3 :: demos/portfolio/ — editorial image-led, oversized serif, horizontal work gallery, scroll-spy.
- W4 :: showpiece @template sections in stdlib/showcases (particle hero, work grid w/ hover, kinetic marquee) registered into the gallery manifest.
- W5 :: gallery surfaces the flagships (live iframes in specimen frames), gates, commit.

* Constraints
- .st-only, zero hand-written JS, zero npm.
- Every visual claim verified in a real browser (--cdp or puppeteer against serve), never source-only.
- Sections stay @template-composable; flagships are demos linked from the gallery.
- AGENTS elegance bar: no new metasystem machinery; assemble existing primitives.

* Outcome (2026-08-08)
- W1 :: demos/brand-launch/ "AURA" — pinned 30k-particle WebGL morph, char-split kinetic headline, spec rail, weight-breathe study, magnetic CTAs. Committed eb037381. Exposed + fixed BUG-354 (@reveal stagger-object NaN delay — reveal was dead everywhere; fixed in stdlib data, gated by tests/unit/primitives/reveal-stagger-progress.test.st, RED-seen under --cdp).
- W2 :: demos/studio/ "NORTH" — light Swiss register, asymmetric 12-col case grid with hover plates, tabular numbers strip, client marquee, services index, dark close. Committed 6afe36bb.
- W3 :: demos/portfolio/ "MARA VOSS" — editorial paper/ink/red, Georgia set deliberately, sticky-stacked project panels (horizontal wheel gallery rejected: needs JS). Filed BUG-355 (@when style-rule bodies compile clean and never apply; yielded signals unreachable from the cascade). Committed d33546cc.
- W4 :: stdlib/showcases/nav/marquee.st + cta/editorial.st registered (15 specimens); gallery copy slots added. Note: particle hero and work grid stayed flagship-only (3D stage + case grid are page-scale compositions, not drop-in sections); marquee + editorial CTA are the composable showpieces.
- W5 :: gallery "The flagships" rail — three live scaled iframes (ports 4342/4343/4344) with per-flagship captions; verified live: 15 specimens, zero stage crops, 3/3 iframes painted, zero page errors. demo_showcase_gallery_test 7/7. Committed 558ca53c.
- Filed en route: BUG-353 (@font relative url() serve/build asymmetry), BUG-354 (fixed), BUG-355 (filed, 3 facets).