**Fixed — closing.**

In-order, out-of-order and fully scrambled named arguments all bind correctly now. `@object(metalness: 0.9, shape: "sphere", detail: 6, size: 1.4, roughness: 0.15)` emits every value from the source, and the `glass-hero` demo you flagged emits `const s = 1.7`, matching what it says.

You were right that this was the important kind of bug. A named-argument syntax with a positional binder underneath is not a small inconsistency — it is the language lying about its own surface. If argument names are decorative, every call site is a trap.

**One honest limitation about the evidence.** 3D values here are observable in the compiler's intermediate representation but not at the logic rung, so the proof is `inspect --layer ir`, not a behavioral assertion in a browser. That's a weaker gate than I'd like, and I'd rather say so than imply otherwise. If you want certainty for your own scene, `cargo run -- inspect --layer ir` and read the constants — they should match your source exactly.
