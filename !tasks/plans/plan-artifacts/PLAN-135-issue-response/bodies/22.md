**Fixed — closing.**

A page using `@use` builds with zero occurrences of `Primitive not found`, and the module's CSS emits normally.

**Same root cause as #13**, which is why one change closed both. The expansion layer had a catch-all branch that wrote a comment instead of failing, so a compile-time-only directive that fell into the primitive path left a note in your bundle rather than an error on your terminal.

**One incidental find while reproducing this**, worth separating from the leak: the literal call in your issue — `@b/badge("NEW")` with no body — now fails with `E0946: Expected body block { ... }`. That's the grammar being strict about a macro that declares it needs a body, and it's unrelated to the comment leak. If you think that shape *should* be legal (a badge with no body is a reasonable thing to want), open a separate issue for it and it'll be looked at as a grammar question rather than a bug.
