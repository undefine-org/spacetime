**Fixed — closing.**

The headless CSS stub no longer dies with a generic `TypeError`. A stylesheet-derived read now **refuses**, naming the rung it needs and how to get it:

```
this assertion needs the `layout` rung — run with --cdp
```

Your repro is now 1 passed, 1 skipped: the inline-style read works headless, the stylesheet read declines honestly instead of crashing.

**Why this mattered more than its symptom.** Spacetime's testing story rests on a *fidelity ladder*. `--headless` runs a fast JavaScript engine and gives you the `logic` rung. `--cdp` drives a real Chromium and gives you `layout`, `timing` and `paint`. The load-bearing rule is that **a backend which cannot provide a rung must refuse, not approximate.**

The refusal is the feature. It's what stops a green suite from certifying something that never actually ran.

A stub returning `{}` broke that contract quietly — worse than crashing, because the crash at least told you something was wrong. Now CSSOM reads refuse the same way timing assertions already did: same pattern, one more rung.

**Being straight with you about the limits here.** The refusal is verified. The *positive* case — that a real stylesheet read returns real values under `--cdp` — is written as a test but has not been run, because there's no Chromium in this environment. It needs to run in CI before I'd claim that half works.
