**Fixed — closing.**

Go-to-definition now resolves for stdlib macros, project macros, variables and templates.

**Both causes you found were about identity**, and they share one root:

1. **Fake paths.** Registry entries carried names like `timeline` instead of a real file URI — so there was nothing to navigate *to*.
2. **Discarded spans.** Definitions were stored without the source span they came from.

**The generalizable bit: an index is only as useful as what it records at *write* time.** You cannot recover a definition's location later if the thing that knew it threw it away. That's also why #31's references land on whole directive blocks rather than the identifier — same root, different symptom. **The span you record is the span you can serve.**

The repair is upstream of both: the editor now serves the compiler's live registry (#28), so definitions carry real paths and real spans, and project macros in `_prelude.st` resolve too — which the embedded snapshot could never do.

Gate: `project_macro_in_prelude_serves_completion_hover_and_definition`. The LSP suite is at 173 passing.
