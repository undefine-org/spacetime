**Fixed — closing.**

The editor now publishes the **compiler's** diagnostics rather than deriving its own approximation. A new document-diagnostics path runs the real check chain, and there's a test asserting parity with what `check` reports.

**You'd already got half of it for free**: the `~preset` machinery was retired in separate work, and the LSP's preset debris went with it, so the false positive on a comment mentioning `~not-a-real-preset` was gone before this wave.

**The larger half is what's now fixed.** LSP diagnostics previously covered parse failures plus a raw text scan, while `check` ran registry semantics, dispatch checks and pipeline analysis. So the editor stayed quiet about an unknown signal, or an unresolved project macro it couldn't even see.

**The target state, now reached: one diagnostic engine, two front-ends.** Anything else drifts again within six months — which is the same lesson as #28 (one registry), #27 (one decision, read not recomputed) and #30 (one place that records spans). Four issues in this backlog, one structural mistake: a second source of truth that nothing forces to agree with the first.
