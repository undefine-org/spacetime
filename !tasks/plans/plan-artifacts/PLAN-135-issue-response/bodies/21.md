**Fixed — closing.**

A `.st`-only page now sets `<title>` and `<html lang>` from source — no authored `index.html` required. And your second observation is addressed too: an `index.html` that silently overrides `index.st`'s own head output is now **diagnosed** rather than quietly winning.

**Your second point was the sharper one**, and I want to be explicit that it's what shaped the fix. A missing feature is an inconvenience. An escape hatch that quietly overrides the primary path converts a missing feature into **data loss** — you write the thing, the tool accepts it, and something else silently wins. The author has no way to discover this except by reading output they had no reason to suspect.

So the override had to become explicit or diagnosed regardless of how the head API itself turned out.

**For tracking:** the broader design (meta tags, remote font registration, the full head surface) lives in the plan `PLAN-035`, which I've retitled to mention SEO and `@head`/`@page` so a search for those terms finds it. There is no item filed under the name `@seo` — if you were looking for one, that's why you didn't find it.
