#+TITLE: PLAN: @entity facet-path build waves (FEAT-142 revised)
#+STATE: DOING
#+SESSION_ID: 151c02da5be0d720
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-29T10-04-08-943Z_151c02da5be0d720.jsonl]]
#+CUSTOM_ID: PLAN-061-plan-entity-facet-path-build-waves-feat
#+PARENT: FEAT-142-spatial-ecs-scene-entities-as-named-sele

* Initial Message

What about a mix of all three, something like @entity vertex &heel { mesh: ... } etc, or @entity vertex(...) &heel {...};?

Look at existing syntax, I want a unified vision overall

* Parent
FEAT-142-spatial-ecs-scene-entities-as-named-sele -- the REVISED CANONICAL SURFACE section
(2026-06-30) is the spec. This PLAN is the TDD execution DAG for building the typed cross-entity
facet-path system. Supersedes PLAN-060's stale W2-W4 (which assumed &self.scrollSpy.isActive was a
working facet path; research task 12 proved it is a flat signal-name string, not a real path).

* Method (how every wave runs)
- TDD: write the failing test FIRST (RED), implement to GREEN, then reviewer swarm before next wave.
- Test tiers: unit (cargo test --lib) for parser/resolver seams; headless .test.st for macro
  expansion; CDP .test.st (--cdp on :3360) for the live reactive behaviour.
- Reviewer swarm (2-3 reviewers) in MAIN THREAD between waves to settle architecture before building on it.
- Commit as you go (projects/everswap-clone has own git; compiler changes in the verse repo).
- cargo check green + relevant tests green is the gate to advance a wave.

* The .st-vs-Rust boundary (from FEAT-142, research-verified)
4 Rust seams + 1 each-scoping + .st reuse of %exports. All GENERAL (zero mountain vocab).

* ==================================================================
* WAVE A -- Entity scope: &name{ @c } becomes a live registry entry
* ==================================================================
DAG: foundation. Blocks B,C,D. Independent of the facet-path parse.
- RED: unit test in src/syntax/cst/ (or parser test) asserting `&name { @peak(...) }` parses to an
  ENTITY SCOPE node (not an element-ref statement, not a dead-letter).
- Rust seam: src/syntax/cst/parser.rs ~2361 (AMPERSAND dispatch) + is_ampersand_scope_block ~2473.
  Route `&ident { ... }` where body holds @directives to an entity-scope path BEFORE the element-ref
  path. Confirm convert_cst_nested_scope (src/parser/mod.rs ~2342) no longer drops it.
- .st: new %primitive entity-scope-impl that registers &name into a world registry (mirror
  element-ref.st __stRefs pattern) and marks nested @directives as its components.
- GREEN gate: `&summit { @peak(at:[0,0,-350], rise:86) }` registers "summit" in the entity registry;
  a headless .test.st asserts the registration. cargo check + cargo test --lib green.
- Accept: entity scope is a live node; components are collected under it.

* ==================================================================
* WAVE B -- Facet-path parse: &e.comp.facet in value position   [blockers: A]
* ==================================================================
- RED: grammar/parser unit test: `to: &evernet.peak.summit` parses as a FACET-PATH value
  (entity=evernet, comp=peak, facet=summit), not `&evernet` + stray dots.
- Rust seam: tree-sitter-spacetime/grammar.js:606 (element_ref = '&' ident only) -> accept trailing
  `.ident`* ; parser value-position handling. Keep &self as the trivial same-entity case.
- .st: a %capture_type facet_ref (or entity_ref) shaped for `&ident(.ident)*`.
- GREEN gate: parser produces a structured facet-path; unit test asserts the 3 segments.
- Accept: &e.c.f parses everywhere a value is accepted; &self.x still works.

* ==================================================================
* WAVE C -- Typed facet resolution + diagnostics                [blockers: A,B]
* ==================================================================
- RED: unit test -- `@anchor(to: &evernet.peak.visible)` where visible:bool but to: wants worldpos
  -> COMPILE ERROR; `to: "evernet"` -> error + fix-it "use &evernet"; `to: &evernet.peak.summit` OK.
- Rust seam: resolver validates facet path against the entity registry + component %exports types.
  New L1 types worldpos, screenpos (general). @anchor's to: param typed :worldpos.
- .st: %exports { $summit: worldpos; $screen: screenpos; $visible: bool } on the peak primitive
  (L2) -- but worldpos/screenpos types are L1/general.
- GREEN gate: 3 unit tests (ok / type-mismatch / string-fixit). cargo test --lib green.
- Accept: typo or type-mismatch in a facet path is a compile error, not a silent float.

* ==================================================================
* WAVE D -- Reactive cross-entity emit (the facet actually drives DOM)  [blockers: A,B,C]
* ==================================================================
- RED: CDP .test.st -- a .copy with @anchor(to: &evernet.peak.summit) tracks evernet's projected
  summit across two scroll positions (reuse anchor.cdp.test.st pattern; gap < 140px).
- Rust seam: emit a signal reference that reads _stWorld.byName[entity].<facet> reactively (per-frame
  via the 3D loop), NOT the &self-local ST.get. Base .summit -> derived .screen/.visible.
- GREEN gate: CDP test green on :3360 (--features cdp). Serve + drive.
- Accept: terrain displaces -> summit -> screen -> DOM moves; reactive, no churn.

* ==================================================================
* WAVE E -- Row-scoped delegate-local &node in @each             [blockers: A,B,C,D]
* ==================================================================
- RED: headless + CDP -- @each($peaks as $p, key:$p.id){ &node{ @peak(...) } .copy{ @anchor(to:
  &node.peak.summit) } } spawns N entities, each .copy tracks ITS row's node; no collision across rows.
- Rust seam: each-with-templates per-iteration ref namespace (stdlib/primitives/template.st +
  src). &node registers per-iteration keyed by $p.id.
- GREEN gate: CDP test with >=3 data rows, each tracked. cargo check green.
- Accept: N-from-data works with delegate-local &node; zero name interpolation.

* ==================================================================
* WAVE F -- everswap-clone migration to the typed surface        [blockers: A-E]
* ==================================================================
- Rewrite index.st: @peak(name:"evernet",...) -> &evernet { @peak(at:[40,0,-130], rise:60) };
  .peak-copy[data-peak="evernet"]{ @anchor(to:"evernet",...) } -> @anchor(to: &evernet.peak.summit,
  when:[0.22,0.46]). Delete data-peak string FKs, at:"x y" strings, when:"a b" strings.
- GREEN gate: existing anchor.cdp.test.st (updated to typed refs) green; serve visually correct;
  screenshots. cargo check + build green.
- Accept: the everswap clone reads in the canonical typed surface end-to-end; the 3 hacks are gone.

* DEFERRED (later waves / follow-ups)
- %default facet: bare &evernet == its %default worldpos facet (QML default property). Wave 1
  requires explicit &e.peak.summit; add %default after D proves the path.
- %provides ambient nesting (old PLAN-060 W4): &summit{ @peak } publishes &self so nested
  @title/@approach default their target to the enclosing entity. Deep compiler; separate FEAT.
- L1 promotion: promote three-anchor/facet types toward stdlib/3d only after a NON-mountain use
  case (globe pin / scatter point) uses the same &entity+facet contract unchanged.

* Definition of done (this PLAN)
- Waves A-F complete; the 3 hacks (stringly FK, name-interp, data-in-strings) DELETED.
- Facet paths are TYPED + compile-checked (typo/type-mismatch = compile error).
- CDP tests green; cargo check/test --lib green; everswap serves visually correct.
- Zero mountain vocabulary in the L1 mechanism (worldpos/screenpos are general spatial types).
- %default + %provides filed as follow-ups, not done here.

NOTE [2026-07-02]: WAVE A empirical grounding (main-thread probe, 2026-06-30):
- CONFIRMED dead-letter: `&evernet { @scroll-spy }` compiles clean (no error/warning) but emits spacetime.js = 0 BYTES -- body silently dropped. Same `@scroll-spy` in `.work { @scroll-spy }` emits 265982 bytes. So the entity body is discarded at AST LOWERING, not rejected.
- CST ALREADY PARSES what we need: parse_element_ref (parser.rs:1898) parses `&name.facet.property` DOTTED PATHS (line 1913 while-DOT loop); parse_element_ref_stmt (parser.rs:1926) already parses an OPTIONAL body `{ }` (line 1953). So `&name{body}` becomes an ELEMENT_REF_STMT-with-body at CST level.
- The drop is at lowering: convert_cst_nested_scope (mod.rs ~2342) returns None for bare element_ref (selector-None), and top-level lowering harvests element_ref_stmts only via collection_ref_scopes_from_body (mod.rs:1162) which `continue`s when selector is empty -- exactly the `&name{}` (no selector) case.
- REVISED Wave A: NOT a parser fork. It is (1) a lowering path that harvests a body-bearing, selector-LESS ELEMENT_REF_STMT (`&name { ... }`) into an entity scope carrying name + component directives, + (2) the .st entity-scope registration primitive. Parser is already sufficient. This also DE-RISKS Wave B: `&e.comp.facet` dotted paths ALREADY parse in parse_element_ref; Wave B is resolution/typing, not grammar.
- Files: src/parser/mod.rs (lowering harvest near 1162 + top-level element_ref_stmts loop ~ the convert path), stdlib/syntax/entity-scope.st (new registration primitive+macro).

NOTE [2026-07-02]: PRE-EXISTING failure (NOT Wave A): tests/import_resolution_test.rs::w1_refuse_to_fake_layout_on_logic_backend expects the headless backend to REFUSE (fail=2) layout assertions (should be_visible / needs layout), but the current backend SKIPS them (skipped=2, passed=1). Verified independent of Wave A: all 3 @test blocks PARSE and are found (logic test runs green); only the layout-assertion refuse-vs-skip semantics differ, which lives in the headless test-runner (src/testing), untouched by Wave A's parse-guard + entity-lowering + class-selector changes. Fidelity-ladder mechanism regression, orthogonal to FEAT-142. File as its own BUG if not already tracked. Wave A's own tests (parser lowering, compile_source emit with 4 assertions incl. interior IntersectionObserver, entity_scope_test, import_resolution entity_scope) are all GREEN; full lib suite 2505 green; integration_tests 181 green.

NOTE [2026-07-02]: revB verdict (reviewer, 0.86, correct, no blocking): flat dotted Element("entity.component.facet") is lossless (segments are dot-free idents/keywords) + consistent with Binding("$data.items"). Two constraints carried into WAVE C:
1. Centralize the facet-path split('.') into ONE typed helper (e.g. a FacetPath{entity, component, facet, tail} parse-from-Element function), do NOT scatter string-splitting across resolve/expand/emit.
2. Wave C must REJECT a dotted Element in a PLAIN-element context (a param that wants a bare element ref like @drag bounds / element-ref-impl), with a diagnostic. Only facet-aware params (@anchor to:) accept a dotted path. evaluate.rs:831 Element(name)->ST.ref(name) is the seam: single-segment stays ST.ref; dotted must route to the facet resolver.
Wave B representation is settled; Wave C builds the typed resolver on it.

NOTE [2026-07-02]: WAVE D/F pre-emptive finding (for the migration agent): everswap @anchor's %form currently declares `to: $to:string`. A facet ref &evernet.peak.summit is an ELEMENT capture, not a string, so `:string` will NOT match/coerce it correctly. FIX: change @anchor's %form to `to: $to:element` (the `:element` capture type matches a &-ref, exactly like @drag's `bounds: $bounds:element?` in stdlib/dnd/macros/drag.st:39). Then the compiler lowers &evernet.peak.summit -> ST.worldFacet("evernet","peak","summit") (a {x,y,z} object) and passes it as the `to` primitive arg. three-anchor's resolveWorld should then use that value directly (it's the world point) instead of the string byName lookup. NB the bare &evernet (no dots) also captures as :element; only DOTTED refs lower to ST.worldFacet, bare stays a plain name — so a bare &evernet to: would need the default-facet path (deferred). Use the explicit &evernet.peak.summit form in the migration.

NOTE [2026-07-03]: WAVE D+F COMPLETE (commit cb8ab1a in everswap-clone git). Typed surface LIVE: 5 peaks as `&name { @peak(at:[x,z], rise,...) }` entity scopes; 5 anchors as `@anchor(to: &name.peak.summit, ...)`. All 3 hacks DELETED (no name: FK, no to:"string", no at:"x y" string -> arrays). @anchor to: changed :string->:element (the anticipated capture-type gap). three-peak publishes components.peak={summit,screen,visible} live functions into window.__stWorld.byName[entity] (entity name derived from marker className). three-anchor resolveWorld uses the lowered ST.worldFacet value each frame. CDP test anchor.cdp.test.st GREEN x3 (asserts 5 entities, every entity publishes components.peak.summit fn, each .peak-copy tracks its peak <140px across 2 scrolls, pointer-events reactive). cargo check 21 files green; build green.

COMPILER GAP (-> WAVE E): entity scopes nested INSIDE @stage's template body are NOT harvested (root_node.element_ref_stmts() is page-level only; entity_scopes_from_body is the dead Body-level helper). Agent workaround: peaks moved to PAGE-SCOPE &name{} (before canvas.world{@stage}). COST: timing fragility -- @peak's three-peak.st does document.querySelector('canvas.world') and BAILS if not found; at page-scope, @peak init can run BEFORE @stage builds the canvas -> no publish. Real Chromium (CDP) tolerates via selector-init deferral (test green), but a headless swiftshader re-navigation showed components:[] empty (raced/ordered differently). WAVE E fixes properly: wire entity_scopes_from_body so `&name{@peak}` works INSIDE @stage where the canvas exists -> peaks declared in-context -> no ordering fragility. This is the planned Wave E (row-scoped &node in @each ALSO needs the same Body-level harvest).

NOTE [2026-07-03]: COMPLETE (2026-07-03). All build waves shipped + reviewer-swarm-hardened + CDP-proven + visually WOW.

WAVES:
- A: `&name { @c }` is a live entity scope -> window.__stWorld.byName + static .st-entity-<name> marker; interior directives emit. Fixed 2 latent @import-swallow bugs (CST + event parser) + primitive-%emit-html gap. revA: deleted ScopeKind::Entity Rust arm (kinds-as-data via the entity-scope-impl match) + dotted-ref guard.
- B: ElementExtractor preserves the dotted facet path (Element("evernet.peak.summit")), mirroring Binding.
- C: parser::entity::{FacetPath, emit_facet_read_js} — the ONE typed facet-path home; dotted Element lowers to ST.worldFacet(entity,component,facet)(+tail) at EVERY capture->JS seam (revC P1: the primitive-arg path @anchor(to:) uses, not just derive; P2: tail preserved, element-param routed). ST.worldFacet runtime resolver.
- D+F: everswap migrated to the typed surface — 5 peaks as &name{@peak(at:[x,z],...)}, 5 anchors as @anchor(to:&name.peak.summit). 3 hacks DELETED (name: FK, to:"string", at:"x y" string). three-peak publishes components.peak={summit,screen,visible} live into the general registry. revD P2: lazy stage-ctx (projection facets were frozen). CDP GREEN (full facet contract).
- E: entity scopes inside @template bodies (nested harvest) + component-body validator accepts them (E0900 fix). Deferred (FUP-101): @stage $children path + row-keyed per-instance identity.
- G: NEW projects/spatial-showcase — the typed spatial-ECS GENERALIZED beyond mountains. A general mountain-free @landmark component; &helmet (glTF DamagedHelmet) + &flux (glass icosahedron) + &prism (metal octahedron) + &crown, each a &name entity anchored by @anchor(to: &name.landmark.pos). Fixed 2 bugs: @landmark %scope selector leak + .pos-vs-.screen double-projection. Pixel-perfect (helmet 683,384=683,384 etc). CDP GREEN.

VERIFICATION: verse lib 2517 green; integration 181 green; everswap CDP green; showcase CDP green. Screenshots: scratch/wg-everswap-evernet.png (Swap.Lend.Borrow on the peak), scratch/wg-showcase-final.png (labels pinned to gltf+procedural objects).

COMMITS: verse e186bfe6..03c5ed38 (6); everswap ..7f7142a; showcase b4f41e2..65444e8.
Follow-ups: FUP-101 (@stage-child + row-keyed identity), FUP-100 (offY responsive). Default-facet (bare &e) still deferred.