#+TITLE: PLAN: W3.6 — signal-subject facets (as $name, done/change/text-change, colon statements, @after+@value-change+@w* retirement)
#+STATE: DONE
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: PLAN-126-plan-w3-6-signal-subject-facets-as-name

* Initial Message

can you explain everything that is still open for my decisions? Explain it with depth and sharpness

Signal-subject facets wave, BEFORE W4 (@score). User-greenlit 2026-07-26.

Design basis: docs/specs/SIP-001-W36-SYNTAX-SHOWCASE.org (approval gate — user approval still pending) + implementation report D24–D29.

THE MECHANISM (D29, implementation-verified): every driver is
listener -> transport -> progress; the wave makes the publication
first-class. `as $name` names the EXISTING %yield/ST.set rail and types the
binding (progress-signal <: signal).

SCOPE:
1. `as $name` typed publication on driver heads (grammar + emission +
   same-file binding analysis)
2. Facet model: `.` = typed facet access, ONE meaning (D26). Registry:
   `text-change` (elem), `change` (signal), `done` (progress-signal,
   pure lowering -> derived signal p >= 1), `running` as planned:
3. Sigil-kind + facet-type checking, one analysis pass mirroring drivers.rs
   (E0948/E0949 extend; E0950 new: wrong facet-type-for-position, unbound
   timeline subject)
4. Arms heads accept facet paths (@on $reveal.done { true => --rise; })
5. D28: body-less @on statements take `: ` before the consequence —
   update drive pseudo-primitive %form, the cutover capsule's 14 rewrite
   rules, the launch article, its gates
6. Migrations into the on-cutover capsule: @after -> @on $x.done arms;
   @value-change -> @on &.text-change (registered two-slot body kind
   `entering-exiting`; value-change-driver re-homes unchanged)
7. DELETIONS: ST.timelines registry, after-driver (drivers.st:1520),
   10 register copies + 8 handle calls, name: params on all driver binds,
   @wscroll/@wload/@wloop + waapi primitives dir
8. Uniform head grammar enforcement: `& <subject?> . <facet> ( <params?> )`
9. D31-ii: `.prev` value-facet on signals (prev-retaining runtime read;
   mount: initial value)
10. D31-iii: arm binding patterns `$old -> $new` + `where <expr>` guards,
    match-time evaluation in the arms runtime (consequence args stay
    compile-time — dynamic args = FUP-156, the SIP seed)

GATES: RED->GREEN per surface; observable-emission assertions (never
inspect output, never check-green alone); article gate incl. node --check;
suites: lib, integration, v8, SIP W1-W3, headless baseline 610/83/38.

SEQUENCED AFTER THIS PLAN: W4 @score (PLAN-125) — consumes named, seekable,
observable timelines. THEN: BUG-247/248/249 (user: after syntax settles).
THEN (post-W4): FUP-153 -> FUP-152 migration wave (user, D24).

DESIGN CLOSED (D28/D30/D31): clause order A; `:` extends to mutations;
bool-statement sugar (compile-time desugar to a true-arm, latch shape);
`.prev` + guards both ship; dynamic consequence args/endpoints deferred to
FUP-156 (SIP seed). Remaining: showcase approval by the user, then waves.

* COMPLETE (2026-07-29)

All six waves landed: W1 grammar (sigil subjects, colon statements,
as-clause, facet heads, guards — all .st), W2 pipeline (publication, facet
checking, E0950), W3 runtime (change-driver, text-change re-home, two-slot
body), W4 arms (guards runtime, bool sugar), W5 migrations+deletions
(@value-change → @on &.text-change 7 sites, @after → @on $x.done, @w*
deleted, ST.timelines deleted incl. probes re-homed to the publication
rail), W6 article extension (Names/Signals/guards sections, every fence
runtime-gated) + 3-reviewer swarm (14 findings incl. a pre-existing W3
publication break — every nameless driver published to "" — all resolved,
gated).

Decisions D24–D33 in docs/specs/SIP-001-W3-IMPLEMENTATION-REPORT.org.
Battery at close: lib 2890 · integration 202 · v8 451 · all SIP gates
(29 w36 + 9 article incl. node --check) · headless 607/74/36 (baseline
610/83/38 — improved).

Filed: FUP-156 (fire-time SIP seed), FUP-157 (mutual capture-type
recursion), FUP-158 (px-threshold scroll). Sequenced next: W4 @score
(PLAN-125), then BUG-247/248/249, then FUP-153→152 (user, D24).
