#+TITLE: __mcp__/__admin__/__dev__ convergence ladder (two-consumer rule, gated rungs)
#+STATE: ITEM
#+SESSION_ID: 1510d7e0efe8848c
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-20T17-53-05-983Z_1510d7e0efe8848c.jsonl]]
#+CUSTOM_ID: PLAN-052-mcp-admin-dev-convergence-ladder-two-con

* Initial Message

launch the workbench and let me drive it for a bit (give me the link) while you explore the various plans that it was building upon (all the way to PLAN-049) and we thoroughly design the multi-frame drag-canvas (W4's strip is the non-canvas bridge), file-scope page mounts (FUP-061), the
 __mcp__/__admin__/__dev__ convergence (north-star, two-consumer rule).

* Origin
North-star from PLAN-049's "out of scope": __mcp__/__admin__/__dev__ convergence
(two-consumer rule). Today THREE parallel chrome systems each introspect code to
build a sidebar: __admin__ (meta-renderer over site.json/types.json),
__dev__ (runtime introspection panels), __mcp__ (bundle contract inspector).
Three palettes, three drawer models, three navigators.

Full design + source-verified duplication inventory:
@research/mcp-builder-view-and-convergence-2026-06-20.org (PART III).

* Locked decision (owner, 2026-06-20)
Approach A — two-consumer LADDER: incremental; hoist ONLY constructs PROVEN
duplicated across >=2 trees; per rung cut BOTH consumers over + DELETE the dup +
pass a harmony grep. Safe, reversible, each rung ships value. (Rejected: big-bang
__sys__ substrate; endgame-first.)

* Precondition already true (Rung 0)
dev-ws / dev-save are ALREADY shared (admin imports them from __dev__). This is
the existing proof the two-consumer rule works; the ladder extends the same move.

* Rungs (ordered by value x 1/blast-radius)
** Rung 1 — design tokens (theme as data)
Hoist palette/type/shape tokens to one module with NAMED THEMES (warm-dark |
editorial-white | dev-dark). Two-consumer: __mcp__ + __admin__. Delete duplicated
hex. Harmony grep: no bespoke palette hex outside the token module in cut trees.

** Rung 2 — drawer / rail / summon macro
Hoist ONE drawer model (env.st's $drawer single-signal + summon-glyph +
reactive .is-open is cleanest) as a macro/kit. Two-consumer: __mcp__ drawers +
__admin__ rail-drawer. (dev-panel shadow-DOM panel folds in on a later rung.)

** Rung 3 — INTROSPECTION-SIDEBAR KIT (keystone; = builder PLAN's sidebars)
Hoist sidebar-tree (array -> indented selectable tree) + sidebar-props
(schema/contract -> labelled widget-typed editable rows), parameterized by a DATA
SOURCE contract. Consumers: __mcp__ builder navigator/inspector (PLAN-051),
__admin__ rail+entry-drawer, __dev__ content-tree+st-properties. Reuses the admin
types.json widget engine. DEPENDS ON the builder PLAN (PLAN-051) authoring the
sidebars first; this rung extracts them. Harmony: one navigator + one props impl,
three consumers.

** Rung 4 — region pipeline as the UNIVERSAL mount (needs FUP-061)
Cut the admin live preview from <iframe src="/"> to a REGION MOUNT (a frame).
With FUP-061 the previewed page is a file-scope guest in a preview region — the
SAME pipeline the workbench stage uses. Closes the PLAN-041 Fork-6 violation
(deletes the second rendering path). Two-consumer: workbench stage + admin
preview share registerBundle/region-mount.

** Rung 5 — endgame: admin + dev MOUNT INTO the workbench
- admin = a guest frame composed onto a workbench region (file-scope page;
  FUP-061 + Rung 4 make it mountable). "Open the CMS" = compose admin into a
  frame, builder sidebars around it.
- dev-panel = an origin-tagged Stdlib DRAWER bundle (the design-language doc's
  "an env/agent can contribute a drawer" extensibility).
- /__spacetime/admin/ + /__spacetime/dev/ become "mount THIS bundle into a
  full-viewport region on a minimal host" — FUP-061's named standalone successor.
One host, one rendering pipeline, three former-apps as origin-tagged bundles.

* Dependencies
Rungs 1-2 independent (anytime). Rung 3 depends on PLAN-051 (builder sidebars).
Rungs 4-5 depend on FUP-061 (file-scope mounts).

* Gates (every rung)
- Pure-Spacetime in chrome trees (AGENTS); JS only in owned %primitive.
- Kit/registry-as-DATA; no parallel subsystem.
- Cut BOTH consumers, DELETE the dup, pass a harmony grep naming the deleted
  construct. Full suite green per rung; each rung its own commit, colocated
  tests+docs.

* Acceptance (PLAN-level)
One token module (themes as data) · one drawer model · one introspection-sidebar
kit with 3 consumers · admin preview on the region pipeline (no iframe) · admin
as a frame + dev as a drawer in the workbench · harmony greps green (no
duplicated palette/drawer/navigator/props impls across the three trees).
