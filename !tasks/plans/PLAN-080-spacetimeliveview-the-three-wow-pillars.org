#+TITLE: Spacetime⇄LiveView: the three wow pillars → collaborative kanban
#+STATE: DONE
#+SESSION_ID: 1539989eb44441ea
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-22T09-34-26-513Z_1539989eb44441ea.jsonl]]
#+CUSTOM_ID: PLAN-080-spacetimeliveview-the-three-wow-pillars
#+COMPLETION_HASH: 68a13f4360288e30

* Initial Message

explain the FUPs and show me your vision for the Spacetime <> LV connection and what would make it *wow*. Exlpain the impact of each bug that you saw

* North star (enshrined 2026-07-22 in elixir/spacetime_lv/AGENTS.md)
**The bridge principle.** One typed state machine: half runs in Erlang, half
runs in the browser, and the compiler proves the seam.

Six principles derived from it (server half/client half · the contract is the
app · diff data never templates · one decode vocabulary · the hook is the only
glue · HEEx is scaffolding) are now the governing rules for all bridge work in
elixir/spacetime_lv/AGENTS.md §Spacetime⇄LiveView bridge principles.

* Proven base (DONE)
- FEAT-136: real LV vertical, st-set seed bridge, phx-update="ignore" subtree.
- FEAT-137: native live transport — send emit over the LV channel, {:reply}
  decoded through receive arms, phx-click retired. frontend/ layout,
  E0928 contract check RED→GREEN, browser-proven loop.

* This plan: the three wow pillars → the kanban demo

** Wave 1 (parallel, independent)
- FEAT-159 — Assign-diff subscribe channel (@data subscribe … from $lv).
  Server reconciler pushes only changed assigns; bridge routes by
  (module, assign); E0928 rule subscribe ⊆ assigns. Closes FUP-122, subsumes
  FUP-129.
- FEAT-160 — The contract is the app. Server DSL (events/assigns/pushes)
  emits the contract (realizes FEAT-139); typed pushes + @data stream decode
  (closes FUP-123); contract rules cover all three server→client kinds.

** Wave 2
- FEAT-161 — Client continuity (DEPENDS: FEAT-159, FEAT-160). Optimistic
  mutation with reconciler rollback · @clock/@timeline animated transitions
  between streamed server states · ephemeral presence channel (cursor trails,
  drag ghosts; Presence relay, latest-wins coalescing in the bridge).

** Wave 3
- FEAT-162 — The kanban wow demo (DEPENDS: FEAT-159, FEAT-160, FEAT-161).
  BoardLive + frontend/board.st + PubSub: optimistic drag with rollback-glide,
  multi-client cursor trails, animated transitions on other users' moves,
  one-shot typed flash toasts. Acceptance includes a two-browser e2e and a
  zero-custom-JS diff audit.

* Out of scope / already tracked elsewhere
- BUG-206 (@on hoisting in nested selector blocks) and BUG-207 (W0201 false
  positive): being handled by another agent. The kanban page MUST use
  top-level selector blocks for @on until BUG-206 lands.
- FUP-121 (compiler-emitted skeleton), FUP-124 (per-host bridge scoping —
  partially absorbed by FEAT-159's bridge routing), FEAT-138 (mix-integrated
  builds / hot reload — improves the dev loop for every wave but blocks none).

* Sequencing rationale
159 and 160 are independent (different surfaces: assign-diff path vs contract
emission/pushes) → parallel. 161 builds on both surfaces. 162 is pure
composition — if it needs anything beyond 159–161, that need is a missing
mechanism and routes back through the elegance bar, not into demo-local hacks.

NOTE [2026-07-22]: DONE 2026-07-22. All three wow pillars shipped and exercised into the live kanban: FEAT-159 (1ac98e80, subscribe channel — counter ticked 21→39 with zero clicks), FEAT-160 (62e10faa + 3378b9ca, server-emitted contract DSL + typed pushes — flash decoded "Count reached 15", zero replays across a forced reconnect), FEAT-161 (f7005283 + 9d5174bb, optimistic rollback browser-proven 0→-1→Failed→0, @flip, local pointer, rAF ephemeral coalescing), FEAT-162 (adb318ba + 746137af + dd6b514c, two-browser kanban: remote labelled cursor, cross-client drag sync with observer toasts, stale-version rejection rollback). The bridge principle is enshrined in elixir/spacetime_lv/AGENTS.md and the demo is its living proof — notably, the two moments an agent hit a missing mechanism it STOPPED instead of hacking (stream registration ordering; ephemeral publish surface), and both became principled fixes. Follow-ups generated by the work: FUP-132 (@flip invisible on @each re-renders), BUG-210 (bare-ident params silent drop), plus the earlier FUP-121..124. The one-sentence wow, delivered: one typed state machine — half in Erlang, half in the browser — and the compiler proves the seam.