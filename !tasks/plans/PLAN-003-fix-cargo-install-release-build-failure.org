#+TITLE: Fix cargo install release build failure
#+STATE: DONE
#+SESSION_ID: 14a6ea821e598033
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-03-30T11-15-30-041Z_14a6ea821e598033.jsonl]]
#+CUSTOM_ID: PLAN-003-fix-cargo-install-release-build-failure


* Initial Message

error[E0432]: unresolved import `form_match::inject_template_provenance`
    --> src/syntax/mod.rs:43:5
     |
  43 |     inject_template_provenance, resolve_bind_value_css_override,
     |     ^^^^^^^^^^^^^^^^^^^^^^^^^^ no `inject_template_provenance` in `syntax::form_match`
     |




during cargo install --path .

* Context
~cargo install --path .~ fails with:

#+begin_example
error[E0432]: unresolved import `form_match::inject_template_provenance`
  --> src/syntax/mod.rs:43:5
#+end_example

Root cause: ~inject_template_provenance~ is ~#[cfg(debug_assertions)]~ only with no release-mode stub. ~cargo install~ compiles in release mode. The fix follows the existing pattern used by sibling function ~inject_provenance_into_matches~ which already has a paired no-op stub.

Single-file fix in ~src/syntax/form_match.rs~. No architectural decisions needed.

* Verification
1. ~cargo build --release~ — must compile cleanly
2. ~cargo test --lib~ — must pass (regression guard)
3. ~cargo install --path .~ — must succeed (original failing command)

* Execution Manifest
1. [[id:BUG-001-missing-release-stub-for-inject-template]] — Add ~#[cfg(not(debug_assertions))]~ no-op stub


** Completion [2026-03-30]

*** Verification
- =cargo build --release= — compiled cleanly (only pre-existing warnings)
- =cargo test --lib= — 1965 passed; 0 failed
- =cargo install --path .= — installed successfully, replacing prior binaries

*** Artifacts
- Modified file: =src/syntax/form_match.rs= — added 12-line release no-op stub after line 1362

*** Deviations
None.
