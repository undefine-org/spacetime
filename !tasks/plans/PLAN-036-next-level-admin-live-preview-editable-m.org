#+TITLE: Next-Level Admin — live preview, editable motion, token widgets, derived tokens
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: PLAN-036-next-level-admin-live-preview-editable-m
#+COMPLETION_HASH: 77754c5646fd0000

* Initial Message

can you check how that looks like in the admin? Can you compare that with stuff that is present in Framer/online/in mockups? I want it to look good please

Four features that take the CMS admin from "edits .st correctly" to "a design tool you'd choose over Framer." Full design: docs/specs/next-level/DESIGN.md (+ 4 mockups 01-04).

THE THESIS: each is a RECOMBINATION of primitives that already exist (verified in repo) — Rust stays a generic engine, grammar+lowering live in .st. New Rust total: 1 DevServerMessage variant + 1 LIVE_RELOAD_SCRIPT edit (①), 1 schema field on motion.json (②). Everything else is stdlib .st + registered @fn.

① LIVE PREVIEW (loop-closer, do first) — split-pane iframe; token edit fans out: optimistic postMessage/WS push → preview recolors no-reload (reuses BUG-091 reactive $brand.scarlet → style.setProperty), release → EditAst persists (suppress_reload_path already set). New: DevServerMessage::TokenPreview + extend LIVE_RELOAD_SCRIPT to call ST.setLocal('brand',…). Admin: split @on input (preview) vs @on change (persist).

② MOTION EDITABLE (cheap, closes last read-only pane) — dev_motion_handler ALREADY emits params:[{key,value}]; add per-motion schema (length/duration/number/select). Pane uses shared form-renderer (&fld-<widget>) not a display card; &ad-motion-card DELETED. Write via EditAst AstDirective (existing directive-prop patch). Easing → &fld-select.

③ TOKEN-AWARE WIDGETS (craft, needs ①) — &fld-color becomes a @portal popover picker (SV-square + hue/alpha); HSV↔hex math = registered pure @fn (hslToHex/hexToHsv). length/duration → &fld-range slider, min/max/step from unit. Picker fill = $-driven gradient (dogfoods BUG-091).

④ DERIVED TOKENS (the system layer, deepest fit) — derived token IS @data derive over $brand: `@data derive $hover : $brand.scarlet | darken(0.08)`. computed-source (Wave A) already does dep-discovery + reactive re-run. Register color-algebra @fn (darken/lighten/alpha/mix) — ONLY new code, stdlib .st. build_brand_json already separates @data inline (literal/editable) vs @data derive (computed/read-only formula chip). New &fld-derived template. Connector lines reuse FEAT-092 declarative SVG + derive's known deps.

SEQUENCING by leverage: ① first (compounds), ② independent/cheap, ③ depends on ①, ④ independent but best with ①. ①∥② parallelizable.

Enablers: FUP-044 (bare-key objects, cosmetic), BUG-084/085 (nested render, workarounds exist — verify in ②/④).

Methodology unchanged: GREEN only when emitted-JS/V8/browser-DOM asserted; structural fixes only; no Rust parsers for stdlib constructs; reviewer swarm between features.

NOTE [2026-06-12]: ALL 4 FEATURES COMPLETE. ① FEAT-105 live preview (split-pane iframe, zero-reload token recolor via postMessage bridge). ② FEAT-106 motion editable (build_motion_json from compiled AST, form-renderer, scoped EditAst; +BUG-097/098 fixes). ④ FEAT-108 derived tokens (color-algebra @fn darken/lighten/alpha/mix, derive pipe, formula chips, self-describing proof). ③ FEAT-107 token widgets (HSV perceptual color picker + drag-to-tune range sliders, all declarative native range). Each browser-verified + reviewer-swarmed (Opus 0.90-0.93), all P1/P2 fixed. The admin is now a Squarespace/Framer-class visual editor over the author's actual .st code. Also produced FEAT-109 deep FUP (implicit value-level type inference — the foundational track that would make ③/④ universal+checked). Bugs filed: BUG-096/097/098/100. Gates throughout: lib 2252/0, integration 172/0, v8 baseline-only (BUG-039). HEAD a8011a90.