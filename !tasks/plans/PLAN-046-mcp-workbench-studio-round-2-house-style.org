#+TITLE: MCP workbench studio round 2 — house style, gallery, select→inspect→edit
#+STATE: DONE
#+SESSION_ID: 150f02eff2cc2dd6
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-19T07-44-15-819Z_150f02eff2cc2dd6.jsonl]]
#+CUSTOM_ID: PLAN-046-mcp-workbench-studio-round-2-house-style
#+COMPLETION_HASH: 7424f5bbbaa865e4

* Initial Message

Test drive the Spacetime MCP workbench and list all the actions and their expected vs. actual output. The workbench should end up feeling like a mix of Spacetime+Squarespace+Figma/Framer in the end, and it doesn't. This is a REVIEW+DESIGN+BRAINSTORM session

* Parent / context
Follows the round-1 live-loop close (FEAT-124 / BUG-111 / FEAT-133, commits 5ffaa959 + 23b36d2b + 4c4061cb). Turns the now-LIVE workbench into a STUDIO toward Spacetime × Squarespace × Figma/Framer.
Design + settled decisions: @research/mcp-workbench-three-dna-brainstorm-2026-06-19.org.
Round-1 review: @research/mcp-workbench-review-2026-06-19.org.

* Settled decisions (locked 2026-06-19, with the human)
- D-Q1 House style = OPT-OUT, zero-specificity typography+color baseline. Split the FEAT-126 layout-neutral conflation: LAYOUT stays neutral (keep `color/font: initial` for layout intent), add a READABLE NEUTRAL typography+color baseline via `:where(.mcp-stage[data-st-region="stage"]) :where(h1,h2,p,...)` (zero specificity → beats the inherited reset, loses to any guest CSS). NOT the chrome identity (no Inter/cream). Baseline is a swappable origin-tagged bundle.
- D-Q2 Canvas = ONE focused stage + in-stage select→inspect→edit. Multi-frame drag-canvas DEFERRED (separate PLAN). Selection via event delegation → `$selected` signal; drag is frame-level, only meaningful in multi-frame, so deferred with it.
- D-Q3 Convergence = BORROW patterns now; one-meta-renderer (__mcp__/__admin__/__dev__) is a written NORTH STAR, not a near-term epic. Extract a shared primitive only on the SECOND real consumer (two-consumer rule). Build compatibly: same region pipeline, same $drawer signal pattern, same contract-fetch shape.

* Prior art to REUSE (verified this session)
- Squarespace DNA = stdlib/__admin__ (fully-declarative CMS meta-renderer, ZERO %emit js, same rail/center/drawer topology): app/app.st shell, app/list.st (contract-driven list + search), app/drawer.st (typed entry editor), app/media.st (@portal media library), app/tools.st (brand/motion/palette).
- @editable + @editable-mark/@editable-block (stdlib/macros/editable.st, stdlib/editable/): inline rich-text, a mark IS a template; editable-surface exposes $doc/$sel/$activeMarks as signals.
- Figma DNA concept = stdlib/__dev__ (dev-selector/dev-inspect/st-properties) — select→inspect→edit PROVEN but %emit js shadow-DOM; borrow CONCEPT, implement declaratively.
- Drag/gesture (SHIPPED, declarative): stdlib/macros/drag.st (@drag axis/bounds/threshold/momentum + dragging states + on-drop), stdlib/primitives/gesture.st, pointer.st, magnetic.st, mobile/macros/gestures/pan.st. (Only needed in the deferred multi-frame PLAN.)
- Persistence: dev-ws bridge (stdlib/__dev__/primitives/dev-save.st, dev-ws.st) — the only %emit js the admin keeps.
- Region pipeline (round-1): stdlib/__mcp__/primitives/bundle.st, env.st stage `@mcp-region @view $stageTpl`, st_mount host=/region= composition, registerBundle hot-swap.

* Execution sequence (each = shipped-primitive + small glue; FEAT per step)
1. M1 House-style baseline — add zero-spec typography+color baseline to stdlib/__mcp__/workbench/styles.st (or a swappable baseline bundle). Acceptance: a bare `@template &main(){ <h1>..<p>.. }` guest renders readable on the dark stage (light warm-neutral text, sensible scale), AND a guest with its own color/font fully overrides it. Live-verify via the stdio-drive + puppeteer harness (see review doc). Keep layout neutral (no centering imposed).
2. M2 Living gallery — replace the 3 inert ghost-pills in the stage empty-state with a real gallery of registered functions / env tabs / kit fragments, each a live mini-region thumbnail; click → st_mount host=inst-1 region=stage. Borrow __admin__/app/list.st (contract-driven list + search). Acceptance: gallery lists ≥ the registered fns; clicking one composes it to the stage (the loop already works).
3. M3' In-stage select→inspect — event delegation on the stage root sets `$selected` (element/region id); reactive `.is-selected` ring; the inspector drawer binds to `$selected` and shows that frame's FEAT-132 contract rows. Pure Spacetime (mirror the $drawer pattern). Acceptance: clicking a composed element highlights it + the inspector reflects its contract.
4. M4 Editable params — render each contract param as a control; editing re-inputs the mounted instance live (per-instance input.json + hot-swap). Reuse __admin__/app/drawer.st rail + @editable for string params. Acceptance: changing a param value updates the live guest without reload.
5. M5 Comms affordances — wire st_await-delivered kit affordances (picker/form/confirm, PLAN-043/044, FEAT-131) into the comms drawer as cards whose actions are the kit-* answers over the signal sink. Acceptance: an agent-sent picker appears in comms; human answer resolves st_await.
6. M6 Inline edit + save — an "edit this bundle" mode flips the composed guest's text into @editable surfaces; edits persist to the function source via the dev-ws bridge. Reuse __admin__ edit-mode (html[data-st-edit-mode]). Acceptance: edit guest text in place → source updated → re-put round-trips. (Last; bridges to convergence.)

* Out of scope (tracked separately)
- Multi-frame drag-canvas (pan/zoom/z-order/occlusion/per-frame position + @drag) → DEFERRED PLAN.
- File-scope/whole-page guest mounts → FUP-061.
- Converging __mcp__/__admin__/__dev__ into one meta-renderer → north-star vision (not yet an epic; revisit when a second consumer makes a shared primitive delete complexity).

* Gates (every step)
Pure-Spacetime / no custom JS in the workbench tree (AGENTS). Live-verify in a real browser via the stdio-drive harness (build binary at ~/.cache/cargo-target/debug/spacetime; drive MCP over stdio; puppeteer the instance page). Tests: lib mcp, v8 region/registerBundle, MCP headless. Baseline to hold: lib 2363/0, integration 172/0, v8 429/9 (pre-existing BUG-039), MCP headless 3/0.

NOTE [2026-06-19]: DONE 2026-06-19 — all 6 moves shipped, each live-verified in a real browser, committed separately. The workbench is now a STUDIO: beautiful defaults, a living gallery, Figma select→inspect→edit, an agent conversation feed, and inline source editing — all on the region pipeline, all pure-Spacetime (no custom page JS).

M1 (054c9b11) house-style baseline — opt-out zero-spec :where() typography+color; newborn guests read on the dark stage, any guest CSS overrides. Required a grammar fix: COLON added to the top-level scope-block dispatch so pseudo-led selectors (:where/:is/:hover) parse (test_pseudo_led_scope_block).
M2 (687472f9) living gallery — empty-state shows clickable function tiles; click composes onto the stage via a new server-audience mcp_compose action. Also fixed the round-1 stale-snapshot bug: the workbench host now serves a LIVE workbench_snapshot per input.json poll; server-filtered gallery_functions.
M3' (d60096a1) in-stage select→inspect — click the stage frame → $selected signal + ember ring + inspector binds the selected guest's contract (stage_mount projection). Pure $drawer-pattern, no JS.
M4 (4f2008cd) editable params — inspector renders an input per param; edit re-inputs the live guest (mcp_param → region_mount_input → bundle input → @view seeds guest root). Extended @mcp-action with an `on` param (change events) + element .value reading.
M5 (0e7e122b) comms feed — the comms drawer shows the agent-audience conversation (comms_events, filtered + newest-first); audience-accented cards.
M6 (c9b5a6ad) inline source edit — "edit source" textarea (text: binding) → mcp_source_edit → update_source recompiles + hot-swaps; a bad edit leaves the guest unchanged. MCP-native (no dev-ws WebSocket, which the MCP server doesn't run).

Gates: lib 2371/0 (incl. 5 mechanically-updated runtime snapshots, b7900270), integration 172/0, v8 429/9 (pre-existing BUG-039 baseline, 0 regressions). New tests: pseudo-led scope, mcp_compose ×2, stage_mount projection, mcp_param, update_source, mcp_source_edit.

Screenshots: scratch/wb-m1-housestyle.png, wb-m2-gallery-composed.png, wb-m3-select-inspect.png, wb-m4-editable-params.png, wb-m5-comms.png, wb-m6-source-edit.png.

Deferred as planned: multi-frame drag-canvas (separate PLAN), file-scope/whole-page guest mounts (FUP-061), the __mcp__/__admin__/__dev__ convergence (north-star). Decision record: DEC-mcp-workbench-studio-round-2-settled-dec.

Notable seams discovered + fixed along the way: CSS grammar didn't parse pseudo-led selectors (M1); the workbench input.json was a frozen snapshot (M2); in-page @each must be selector-scoped not inline HTML-body (M2); textarea content needs `text:` binding, not a backtick hole or `value <-` (M6).