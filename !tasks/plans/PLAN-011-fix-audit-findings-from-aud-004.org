#+TITLE: Fix audit findings from AUD-004
#+STATE: DONE
#+SESSION_ID: 14aef9c617fc29ee
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-05T17-30-02-719Z_14aef9c617fc29ee.jsonl]]
#+CUSTOM_ID: PLAN-011-fix-audit-findings-from-aud-004


* Initial Message

Run a full audit of the ora-ventures.com/ website at localhost:9416. A lot of stuff is broken. Please be thorough and list the issues.

* Context

Audit AUD-004 identified 8 findings across the recent 5-commit batch (locale routing, CSS fixes, dev panel, type schema). This plan addresses findings 1-7 directly and files follow-ups for finding 8.

** Approach
- Findings 1-3 (escaped quotes, double compilation, missing test) are combined into one bug item since they all touch =src/server.rs= in the same function
- Findings 4-6 (localhost preconnect, duplicate comment, duplicate FOUC) are combined into one bug item since they all touch =ora-ventures.com/index.html=
- Finding 7 (V8 degradation) is filed as a FUP tracking item
- Finding 8 (double-IIFE) is filed as a FUP tracking item

** Key Decisions
1. The FOUC =<style>= is removed from HTML rather than from server injection — the server is the canonical injection point, keeping HTML clean
2. The =compile_from_site= call is replaced entirely by =get_or_compile= — no fallback chain, just one compilation per request
3. The locale fallback test replicates the format strings (not the full function) because =try_serve_locale= requires a full site directory and compile infrastructure to test end-to-end

* Verification
- =cargo test --lib -- server::tests= — all 20 tests pass (19 existing + 1 new)
- =cargo run -- check ora-ventures.com/= — 0 errors
- =grep -c 'localhost:3333' ora-ventures.com/index.html= → 0
- =grep -c ':not(:defined)' ora-ventures.com/index.html= → 0
- No =\\\"= inside any =r#"..."#= raw string in the locale fallback branch of =src/server.rs=

* Execution Manifest

** Phase 1: Bug fixes (parallel — no file overlap)
- [[id:BUG-012-fix-locale-fallback-html-injection-escap]] — Fix locale fallback: escaped quotes, double compile, add test (S, #A, server)
- [[id:BUG-013-clean-up-ora-ventures-com-index-html-dev]] — Clean up index.html: dev artifacts and duplicates (XS, #B, site)

** Phase 2: Follow-up tracking (already filed, no implementation needed)
- [[id:FUP-007-v8-thread-degradation-causes-intermitten]] — V8 thread degradation tracking (M, #B)
- [[id:FUP-008-eliminate-double-iife-wrapping-for-globa]] — Double-IIFE wrapping tracking (M, #C)

Total implementation effort: S (both bug fixes are trivial-to-small edits in well-understood code)

** Completion [2026-04-05]

*** Verification
- =cargo test --lib -- server::tests= — 20/20 passed (19 existing + 1 new =test_locale_fallback_inject_no_escaped_quotes=)
- =cargo run -- check ora-ventures.com/= — all 30 files passed, 0 errors
- =grep -n 'localhost:3333' ora-ventures.com/index.html= — 0 matches
- =grep -c 'Styles: generated by SpaceTime' ora-ventures.com/index.html= → 1 (was 2)
- =grep -c ':not(:defined)' ora-ventures.com/index.html= → 0
- Manual spot-check: no =\\\"= inside any =r#"..."#= raw string in the locale fallback branch of =src/server.rs= (only the =format!()= regular string on the =<base href>= line has =\\\"= which is correct)
- =compile_from_site= removed entirely (was dead code after replacement with =get_or_compile=)

*** Artifacts
- =src/server.rs= — single =get_or_compile= call replaces double compilation; escaped quotes fixed in 3 raw string blocks; dead =compile_from_site= function removed; new test added
- =ora-ventures.com/index.html= — localhost preconnect, duplicate comment, and duplicate FOUC style removed

*** Deviations
None.
