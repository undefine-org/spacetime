#+TITLE: Self-documenting Spacetime docs generator
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: PLAN-032-self-documenting-spacetime-docs-generato
#+COMPLETION_HASH: f5c987b9020bfd0a

* Initial Message

can you write docs for them and feature the link to that prominently in AGENTS.md?

first do a markdown version (in .md files) for the content. keep it short and concise, but complete, and focused on the testing infra for now.

then tell me how you'd make a Spacetime-based documentation generator that also acts as a showcase for the features of Spacetime. I want something that really slaps. You can use the latest %capture_type, %primitive, %form, and %macro and whatnot to really really slap (give me your ideas). you can test drive that

The docs site (demos/spacetime-docs, FEAT-081 DONE) becomes a PROJECTION of the metasystem: the docs ARE the feature, generated from the same registry the compiler uses, so they cannot rot. Two tracks from the design brainstorm.

* (1) Registry → live gallery (data plane)
Make the registry a first-class @data source; the page's existing @each + &template renders it.
- [[id:FEAT-083-doc-comment-field-on-registry-summaries]] — capture `///` doc onto registry summaries (shared by LSP/coverage/docs). FOUNDATION.
- [[id:FEAT-084-data-registry-the-self-documenting-galle]] — `@data registry $x [from kind:/module:]`, SSG-unrolled; @each renders it. @reference = filtered-to-one + &template.

* (2) @example dual-render (markup plane)
Show a block BOTH live AND as its own source, from one capture.
- [[id:FEAT-082-userland-emit-html-macros-not-expanded-i]] — file-scope userland %emit html expansion (Piece A) + `.source` escaped block field (Piece B). Then @example is userland .st.

* Discovered + handled this arc
- [[id:BUG-069-measure-lines-live-binding-renders-em-da]] DONE — element-export → file-scope binding bridge (the (3) fix).
- [[id:BUG-070-import-without-terminator-swallows-follo]] #A ITEM — terminator-less @import swallows file-scope markup (blank page). Shares the file-scope collector path with FEAT-082 Piece A.

* Sequencing
BUG-070 (#A, unblocks clean .st-only pages + shares collector w/ 082-A)
  → FEAT-083 (doc field, foundation)
    → FEAT-084 (gallery auto-generates — track (1) lands: fully declarative gallery)
  → FEAT-082 (A+B → @example/@reference — track (2) lands)
Tracks (1) and (2) are independent (data plane vs markup plane) and can run in parallel after their own deps. The W6 introspection surface (Spacetime.forms/iter_macros) is the shared spine for FEAT-083/084.

NOTE [2026-06-09]: All delivered. (1) FEAT-083 (registry doc-comments) + FEAT-084 (@data registry) — docs gallery auto-generates 85 testing directives from the compiler's own registry. (2) FEAT-082 (@example dual-render: live + escaped source, EmitLang::Html + .source field). Bugs fixed: BUG-069 (export bridge), BUG-070 (import-swallows-markup). Filed: FEAT-085 (SSG template-call unroll). Commits: e3f81987, e4081a67, 1a0285de, 3f90b63f, 3a5cbca2, 023c6ce3.