#+TITLE: Pre-public-release secret audit + AGPL/EPL licensing
#+STATE: DONE
#+SESSION_ID: 1581b1cfc9e2fba7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-16T09-35-08-583Z_1581b1cfc9e2fba7.jsonl]]
#+CUSTOM_ID: AUD-012-pre-public-release-secret-audit-agpl-epl

* Scope
Repo made ready to go public: (1) license files added, (2) full secret/env-leak audit of tracked tree AND all 2608 commits of history, (3) push.

* Licensing (committed)
- Root =LICENSE=: AGPL-3.0-or-later (was: README claimed MIT, no LICENSE file existed).
- =stdlib/LICENSE=: EPL-2.0. Rationale noted in README: pages import stdlib; weaker copyleft keeps user pages unencumbered.
- =Cargo.toml=: =license = "AGPL-3.0-or-later"= + =repository= field.
- =tree-sitter-spacetime= (pyproject.toml + package.json): MIT → AGPL-3.0-or-later, for consistency with the repo-wide directive.
- README License section rewritten; vendored third-party (=stdlib/**/vendor/= submodules, =.patches/serde*=) keeps upstream licenses.

* Secret audit (gitleaks 8.24.3)
- =gitleaks git= over FULL history: 2608 commits, ~912 MB, 135,789 raw findings — triaged to ZERO real leaks:
  - ~135.6k generic-api-key = detect-secrets baseline files (=bl-scan.json=) containing SHA1 =hashed_secret= values (hashes, not secrets). History-only, long removed.
  - 101 private-key / 13 github-app-token / 2 github-pat / 1 aws-access-token / 2 jwt → ALL inside a vendored copy of the public n8n repo's test fixtures (history-only, removed in a73c530c). Upstream test data, not ours.
  - 2 gcp-api-key → redacted placeholders =<<$env:S7=...>>=.
  - ig.json (history-only) → analytics UUID, no credential.
- =gitleaks dir= on worktree: 39 findings. Real secrets found ONLY in gitignored paths, verified never committed to this repo (git log -S on each exact key = 0 commits):
  - =commercial/host/.env=: Cloudflare R2 key, CF API tokens, Stripe test keys, webhook secret — =/commercial/= ignored, separate repo.
  - =projects/*/.env=: PUSH_ONLINE_API_KEY (same key in 3 projects) — =/projects/= ignored, separate repo.
  - Rest: build artifacts (=target/=, =target-bug271/=), elixir =deps/= example keys, vendored three.js demo JWT (Cesium ion upstream), false positives.
- No .env/.pem/.key/id_rsa ever committed. Tracked =.envrc= is clean (one line, sources sync script).
- =elixir/spacetime_lv/config/{dev,test}.exs= commit Phoenix =secret_key_base= values — standard Phoenix practice; =runtime.exs= reads =SECRET_KEY_BASE= from env in prod (verified). Acceptable as-is.

* Follow-ups (NOT done, flagged to user)
- TODO =commercial/host= (SEPARATE repo) tracks a =.env= with LIVE Cloudflare + Stripe secrets in ITS git history. If that repo ever goes public: rotate keys + scrub. Out of scope here.
- NOTE =.ancillary/claude-code-spacetime/.claude-plugin/plugin.json= (tracked) hardcodes a stale absolute path =/home/user/code/ora/spacetime-testing/Cargo.toml=. Harmless but machine-specific; left untouched.
- NOTE accidentally-committed junk removed long ago (a73c530c) included Ash resource snapshots (=agentmaker_build_*/.../resource_snapshots/*=) — schema metadata only, no row data/PII. Verified.

* Verification
- gitleaks git exit: 135789 findings, all triaged above; gitleaks dir exit: 39, all triaged.
- =cargo metadata --no-deps=, tomllib, json parse checks: OK.