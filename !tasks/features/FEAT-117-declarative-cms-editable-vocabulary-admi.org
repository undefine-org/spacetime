#+TITLE: Declarative @cms editable vocabulary + admin drawer widgets on the per-instance scope foundation
#+STATE: ITEM
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FEAT-117-declarative-cms-editable-vocabulary-admi

* Initial Message

ok do the full W3-clean. Don't stop until you're fully done. Thanks.

* Goal
With per-instance scope resolution now proven for arbitrary nested constructs in
`@template` bodies (PLAN-039: @editable/@each/@on/class-toggle/content-binding all
resolve `$f`/`$f.value` against the instance via `ST.resolve`), build the CMS
authoring surface on top:
1. **@cms declarative vocabulary** — `editable: inline|off`, `readonly:[...]`,
   per-field `richtext(marks, blocks)`, `number`, `toggle`, `select(options)` —
   a type-contract-driven editing spec compiled to the right `&fld-*` widget.
2. **Admin drawer widgets** — complete the `&fld-*` family in
   stdlib/__admin__/app/drawer.st (richtext done = BUG-103; add number/toggle/
   select/date) using the canonical selector-scope attach form
   (`.ad-input { @editable(...) }`), NOT inline element content (which doesn't
   parse — a documented PLAN-039 learning).
3. **On-page inline widgets** — number/toggle/select inline editors via drawer
   `&fld-*` driven by compiler provenance + the type contract (W3c from the
   editable-everything arc).

* Why now
The hard part is done: the editing constructs mount per-instance and resolve their
binds against the instance scope. BUG-103 (admin richtext) is GREEN. The
foundation is uniform — a new widget is a `@template &fld-X($f)` with
`.ad-input { @editable/@on/... bind:`$f.value` }`, and it Just Works per-instance.

* Constraints
- Spacetime-only: no custom JS; widgets are `.st` templates + stdlib primitives.
- Provenance-driven: the editable surface reads compiler provenance
  (`data-st-editable`, source/path) to know what it edits + how to persist
  (dev-save `{file,path}` hook, FUP-047).
- One sigil one meaning: backtick `` `$f.value` `` is THE hole (arg-value unwrap
  landed earlier this arc); SPACE-form types in param decls.

* Acceptance
- `@cms` spec on a `@type`/`@data` compiles to the correct per-field widgets.
- Admin drawer renders + persists richtext/number/toggle/select per field,
  per-instance, with values resolving to the bound doc.
- On-page inline number/toggle/select widgets mount + persist.
- Tests: Rust v8 per-instance bind tests per widget; live BUG-103-style gate.

* Provenance
Editable-everything arc continuation (W2 declarative vocab, W3c inline widgets).
Sits on PLAN-039 per-instance scope foundation. Demo: projects/editable-blog,
cms-testbed.

NOTE [2026-06-13]: DEFERRED by user (2026-06-13): "defer until everything else is done; I want you to focus on making the meta-compiler clean, straightforward, and world-class, with no contradictions." Do NOT start FEAT-117 until FEAT-115 (delete reify/World B), FEAT-116 (generalize scope identity), BUG-105 (harness), FUP-051 (regression net) are all complete. This is the product/CMS surface — it sits ON the clean meta-compiler, it is not part of cleaning it.