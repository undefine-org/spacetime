#+TITLE: MCP axe legacy surface (propose/choose/picker) — no remnants
#+STATE: DONE
#+CUSTOM_ID: FEAT-131-mcp-axe-legacy-surface-propose-choose-p
#+DEPENDS: PLAN-041-plan-039-unified-live-mcp-environment-or FEAT-126-mcp-region-mount-bundle-transport-regist FEAT-130-mcp-warm-dark-organic-chrome-bundle-stag
#+LAYER: mcp
#+PRIORITY: A
#+COMPLETION_HASH: 1e3094687d348cab

* Parent
[[id:PLAN-041-plan-039-unified-live-mcp-environment-or][PLAN-041]] Wave 5 (the harmony gate).

* Summary
Fork 5: DELETE the bespoke legacy human->host surface now that the unified region pipeline exists. NO remnants by end of PLAN-041. Everything the legacy surface did is expressible as a region-mounted kit function (picker/form/confirm) over the one =/__mcp/signal/{instance}= sink.

* Why
Two parallel human->host signal paths coexist today (/__mcp/choose + /__mcp/signal), plus a bespoke picker page, ChooseForm struct, and option routes. The harmony gate (FEAT-111/123-W6) has been red since the zen slice. The unified region pipeline (FEAT-126) makes the bespoke surface genuinely deletable — picker/form/confirm compose as kit functions in the comms drawer (mock m4).

* Target (DELETE)
- =src/mcp/live.rs= — routes REMOVED: =/__mcp/propose/{id}/=, =/__mcp/propose/{id}/data.json=, =/__mcp/picker.css=, =/__mcp/picker.js=, =/__mcp/choose/{id}= (+ =ChooseForm{usize}= struct at live.rs:919), =/__mcp/option/{id}/{idx}= + =/css= + =/js=.
- =stdlib/__mcp__/picker.st= — DELETED.
- =src/mcp/tools.rs= — =st_propose=, =st_proposal_status=, =st_await_choice= collapsed: =st_propose= becomes a thin wrapper that mounts a picker kit function into a region via st_mount + awaits via st_await (one release shim), then DELETED. =st_elicit= stays (native MCP elicitation, not a page route).

* Change
One human->host signal path: =/__mcp/signal/{instance}=. Picker/form/confirm are region-mounted kit functions ([[id:FEAT-111-mcp-transport-collapse-interfaces-as-liv][FEAT-111]]), not bespoke routes.

* Edge
- Existing callers of st_propose during the shim window -> wrapper works; then removed.
- =st_elicit= (native MCP elicitation) is NOT a page route -> untouched.

* Acceptance (the harmony gate)
- grep =/__mcp/choose= / =/__mcp/propose= / =/__mcp/option= / =/__mcp/picker= / =ChooseForm= / =picker.st= in src + stdlib -> ZERO matches.
- =st_propose= / =st_proposal_status= / =st_await_choice= either removed or thin wrappers over st_mount/st_await.
- One signal sink only; full suite green.
- =cargo test= green.

NOTE [2026-06-18]: W5 committed ba827009. DELETED the entire legacy human->host surface: live.rs routes (propose/picker.css/picker.js/choose/option*), 8 handler fns, ProposalOption/Proposal/PickerCompiled/ChooseForm structs, picker() OnceLock, proposals store field, add_proposal/get_proposal/await_choice; stdlib/__mcp__/picker.st file; tools.rs st_propose/st_proposal_status/st_await_choice (fns+dispatch+schemas); state.rs next_proposal/next_proposal_id; protocol.rs fallback copy. st_elicit retained (native elicitation, not a page route). ONE human->host signal path remains: /__mcp/signal/{instance}. HARMONY GATE GREEN: grep over src+stdlib for __mcp/choose, __mcp/propose, __mcp/option, __mcp/picker, ChooseForm, st_propose, st_proposal_status, st_await_choice, picker.st => ALL ZERO. Gates: lib 2326/0, integration 172/0, v8 428/9 (pre-existing BUG-039), headless 356/251 (0 regressions). NB: the picker/form/confirm kit-function migration (so agents can still offer choices via a region-mounted kit) is future work — the transport (signal sink + region mount) is in place; a kit-function library is not part of PLAN-041.