#+TITLE: UNKN home foundation reset (tokens, assets, talent data, AGENTS.md)
#+STATE: DONE
#+SESSION_ID: 14cedfd0e21b5609
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-30T12-08-01-672Z_14cedfd0e21b5609.jsonl]]
#+CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets
#+EFFORT: 3h
#+LAYER: frontend
#+PRIORITY: #A

* Initial Message

can you tell me how you use the spacetime design skill?

* Intention & Purpose

Restore the foundation that the rest of the home redesign depends on: correct accent color, brand-spec authority, typography ceilings that match the PDF, real photography on disk, talent dataset, and a project AGENTS.md so the brand spec is recoverable without reverse-engineering. This unblocks every section-level item.

* Context & Narrative

The current build assumes a rust-orange accent (=#D43E2B=) and aggressively-clamped display typography (h2 ceiling 44.8px). The Figma reference and PDF both pin the accent at =rgb(255, 0, 32)= = =#FF0020= (true scarlet) and the display at 70px / script at 70px / h3 at 50px. Photography is non-existent on disk: =projects/unkn/assets/images/= is empty while =projects/unkn/_references/images/= and =projects/unkn/_references/Home_Images/= contain the full visual vocabulary. There is no =projects/unkn/AGENTS.md= so any future agent re-derives token decisions from scratch.

This item also creates the talent dataset that the strip section (FEAT-035), the roster section (FEAT-037), and the work section (FEAT-039) all consume.

* Scope

In-scope:
- Author =projects/unkn/AGENTS.md= recording brand spec (palette, typography, signature motions, hard rules).
- Author =projects/unkn/brand-spec.md= with asset paths + token references.
- Fix =projects/unkn/modules/_theme.st=: change =--unkn-accent= from =#D43E2B= to =#FF0020=; raise display/script type ceilings; keep =clamp()= for responsive lower bounds.
- Deploy real images from =_references/= into =projects/unkn/assets/images/= with sensible filenames.
- Author =projects/unkn/data/talent.json= (9 talents: 4 named + 5 placeholders structured for replacement).
- Extend =projects/unkn/data/work.json= with an =image= field on the flagship card.
- Fix footer copy in =projects/unkn/modules/_shared.st=: replace =About Work= with two separate links =About= and =Work=.

Out-of-scope:
- Section-body rewrites (handled by FEAT-035..039).
- Sub-route redesign (handled by FEAT-040 light pass).
- Motion primitives (handled by FEAT-035..037).
- Adding new typography tokens beyond the ceiling fix.

* Existing Patterns

- Token system: =projects/unkn/modules/_theme.st= already declares =--unkn-accent=, =--unkn-text-display=, =--unkn-text-h2=, =--unkn-text-script-lg= as =clamp()= expressions. Update values; do not restructure.
- Data architecture: =projects/unkn/index.st= uses =@type Stat= / =@type Service= / =@type Reason= / =@type WorkCard= and =@data ... { src: "/data/<file>.json"; }=. Add =@type Talent= + =@data talents= following the same pattern.
- Asset references: image relative paths from =assets/images/= reach the dev server at =/assets/images/<file>=. Verify with =cargo run -- serve projects/unkn/= before declaring done.
- Brand-spec structure: see =skill://spacetime-design/references/project-style-protocol.md= for the canonical brand-spec.md template.

* Tests

Compile-time:
- T1: =cargo run -- check projects/unkn/= passes after token + footer + data changes (no parse errors, no type errors on extended Talent data).
- T2: =Spacetime.review()= dim "brand" reports zero unbound hex/rgb literals across all home routes after the change.

Manual visual:
- M1: Boot dev server, open =/=. Every script-font phrase ("Moves Culture", "We build it", "It's about resonance", "We are The (Un)Known", "Let's Build Together", "What We Do", "The (Un)Known Talent") renders in =#FF0020= scarlet, not the previous rust.
- M2: Hero "Where Talent" headline at 1440px viewport renders ≥ 100px font-size (clamp ceiling raised from 152px to 168px desktop).
- M3: Footer "Useful links" lists Home / About / Talent / Work / Expertise / Contact (6 items, "About Work" gone).

Data:
- M4: =/data/talent.json= returns 9 entries with non-null image + name + slug + categories[] + (optional) headline.
- M5: =/data/work.json= returns 1 entry with non-null image path.

* Implementation

** ITEM Author projects/unkn/AGENTS.md
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::author-agents-md
:END:

- File: new =projects/unkn/AGENTS.md=
- Contents (sections):
  1. *Brand summary*: cream paper #F5F0E6 + deep ink #0D0D0D + signature scarlet #FF0020 (NOT #D43E2B); display Oranienbaum, body SF Pro, script Adobe Handwriting Ernie; "Where Talent Moves Culture" headline doctrine.
  2. *Hard rules* (project-specific):
     - All script accents MUST use =var(--unkn-accent)= = scarlet =#FF0020=.
     - All photography MUST live under =projects/unkn/assets/images/=, not in module CSS.
     - Three signature motions (strip-marquee, hero-wipe, roster-slide) are project DNA — don't replace with generic primitives.
     - Footer link list is fixed: Home / About / Talent / Work / Expertise / Contact.
  3. *Section order* (canonical, from PDF): nav → hero → strip → statement → identity → mission → resonance → services → roster → work → why → contact → footer.
  4. *Module map*: which =modules/_*.st= owns which section.
  5. *Data sources*: =data/talent.json=, =data/services.json=, =data/reasons.json=, =data/stats.json=, =data/work.json=.

** ITEM Author projects/unkn/brand-spec.md
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::author-brand-spec
:DEPENDS: FEAT-034-unkn-home-foundation-reset-tokens-assets::author-agents-md
:END:

- File: new =projects/unkn/brand-spec.md=
- Maps every brand decision to its source-of-truth file:
  - palette → =modules/_theme.st= var names + values (table)
  - typography → =modules/_theme.st= type tokens + PDF spec page references
  - asset paths → =assets/images/= filename inventory with semantic role per file (talent portrait / strip thumbnail / brand logo / social icon / divider / ornament)
  - script font usage doctrine → enumerate every script phrase in the home + which token wraps it

** ITEM Fix _theme.st accent + typography ceilings
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::theme-token-fix
:END:

- File: =projects/unkn/modules/_theme.st=
- Changes:
  - =--unkn-accent: #D43E2B= → =--unkn-accent: #FF0020=
  - =--unkn-accent-bright: #E85A2B= → =--unkn-accent-bright: #FF3050= (lift saturation)
  - =--unkn-accent-deep: #A8311E= → =--unkn-accent-deep: #C00018= (deep scarlet for hover/active)
  - =--unkn-text-h2: clamp(1.8rem, 3.6vw, 2.8rem)= → =clamp(2.2rem, 5.4vw, 4.4rem)= (matches 70px PDF ceiling at desktop)
  - =--unkn-text-h3: clamp(1.3rem, 2.2vw, 1.85rem)= → =clamp(1.5rem, 3.6vw, 3.1rem)= (matches 50px PDF ceiling)
  - =--unkn-text-script-lg: clamp(2.4rem, 5vw, 4.5rem)= → =clamp(2.6rem, 6.4vw, 4.4rem)= (matches 70px script PDF ceiling)
  - =--unkn-text-script-md: clamp(1.6rem, 3vw, 2.6rem)= → =clamp(1.8rem, 3.6vw, 3.1rem)= (matches 50px script PDF ceiling)
  - =--unkn-text-display= already at =clamp(3.5rem, 9vw, 9.5rem)= (152px ceiling) — extend to =clamp(3.5rem, 11vw, 10.5rem)= (168px ceiling) for the "Where Talent" hero headline impact.
  - Keep mobile lower bounds (no smaller mobile breakpoint regression).
- Verify: =cargo run -- check projects/unkn/= compiles; load =/= at 1440x900 and screenshot the hero — headline measures ≥ 100px in DOM.

** ITEM Deploy images from _references to assets/images/
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::deploy-images
:END:

- Source dirs: =projects/unkn/_references/images/= (Figma export, 33 files) + =projects/unkn/_references/Home_Images/= (22 portrait files).
- Target dir: =projects/unkn/assets/images/= (currently empty).
- Filename normalization (rename on copy): lowercase, hyphens, no spaces, semantic role:
  - Brand logos: =unkn_logo_full_w-2-45.png= → =brand-logo-wordmark-white.png=; =unkn_icon_w-1-137.png= → =brand-icon-white.png=.
  - Hero/strip thumbnails (rectangle-3xx series, 7 files): =strip-01.png=..=strip-07.png= preserving original order.
  - Roster portraits: 9 entries mapped 1:1 to talent.json in next sub-outline (=talent-01-carolina-mendes.jpg=, etc.).
  - Work flagship: =9k1a9150-2-116.png= → =work-flagship-hero.png=.
  - Eyes close-up: =Close_Up_eyes.png= → =close-up-eyes.png=.
  - Campaign posters: =node-17.png= / =node-18.png= → =poster-the-unknown-01.png= / =poster-the-unknown-02.png=.
  - Footer ornaments: =frame-422/424/425.png= → =ornament-01/02/03.png=.
  - Section divider: =line-12-135.svg= → =divider-rule.svg=.
  - Social icons: =vector-65/69/71/72/73/74.svg= → =social-instagram.svg=, =social-linkedin.svg=, =social-spotify.svg=, =social-tiktok.svg=, =social-x.svg=, =social-youtube.svg= (assign by inspecting SVG path data; document mapping in brand-spec.md).
- Verify: =ls projects/unkn/assets/images/ | wc -l= reports ≥ 35; dev-server resolves =/assets/images/brand-logo-wordmark-white.png= with 200 OK.

** ITEM Author data/talent.json
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::talent-data
:DEPENDS: FEAT-034-unkn-home-foundation-reset-tokens-assets::deploy-images
:END:

- File: new =projects/unkn/data/talent.json=
- Schema (declared in =index.st= per next sub-outline):
  =Talent { id: string; slug: string; name: string; image: string; categories: string[]; headline: string; }=
- Entries (9):
  1. id: =carolina-mendes=, name: =Carolina Mendes=, image: =/assets/images/talent-01-carolina-mendes.jpg=, categories: =["Fashion", "Beauty"]=, headline: =Editorial gravity. Quiet authority.=
  2. id: =rafaella-maia=, name: =Rafaella Maia=, image: =/assets/images/talent-02-rafaella-maia.jpg=, categories: =["Fashion", "Lifestyle"]=, headline: =A face that holds the room.=
  3. id: =gabbi=, name: =Gabbi=, image: =/assets/images/talent-03-gabbi.jpg=, categories: =["Beauty", "Entertainment"]=, headline: =Cultural fluency, on the inside.=
  4. id: =sofi-adams=, name: =Sofi Adams=, image: =/assets/images/talent-04-sofi-adams.jpg=, categories: =["Fashion", "Travel"]=, headline: =Quiet luxury, in motion.=
  5. id: =talent-05=, name: =Talent #5=, image: =/assets/images/talent-05.jpg=, categories: =["Sport", "Lifestyle"]=, headline: =Resonance, not reach.=
  6. id: =talent-06=, name: =Talent #6=, image: =/assets/images/talent-06.jpg=, categories: =["Music", "Entertainment"]=, headline: =Sound that lasts.=
  7. id: =talent-07=, name: =Talent #7=, image: =/assets/images/talent-07.jpg=, categories: =["Travel", "Food"]=, headline: =A passport for taste.=
  8. id: =talent-08=, name: =Talent #8=, image: =/assets/images/talent-08.jpg=, categories: =["Beauty", "Fashion"]=, headline: =New light, every frame.=
  9. id: =talent-09=, name: =Talent #9=, image: =/assets/images/talent-09.jpg=, categories: =["Sport", "Entertainment"]=, headline: =The next move, before it lands.=
- Image-to-name mapping (in brand-spec.md): talent-05 ← pexels-darwin-jair-quispe-paitan, talent-06 ← pexels-lazarus-ziridis, talent-07 ← pexels-matreding, talent-08 ← pexels-ninobur, talent-09 ← pexels-ozan-aldemir.

** ITEM Declare @type Talent and @data talents in index.st
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::declare-talent-type
:DEPENDS: FEAT-034-unkn-home-foundation-reset-tokens-assets::talent-data
:END:

- File: =projects/unkn/index.st=
- After =@type WorkCard { ... }= block, add:
  =@type Talent { id: string; slug: string; name: string; image: string; categories: string[]; headline: string; }=
- After =@data works ...=, add:
  =@data talents: Talent[] { src: "/data/talent.json"; }=
- Verify: =cargo run -- check projects/unkn/= passes; =Spacetime.dataSources= in dev console lists =talents= with 9 entries.

** ITEM Extend data/work.json with image field
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::work-image-field
:DEPENDS: FEAT-034-unkn-home-foundation-reset-tokens-assets::deploy-images
:END:

- File: =projects/unkn/data/work.json=
- Add =image= field on the flagship-placeholder entry: =/assets/images/work-flagship-hero.png=
- Add =@type WorkCard= update in =index.st=: add =image: string;= field.

** ITEM Fix footer "About Work" copy
:PROPERTIES:
:CUSTOM_ID: FEAT-034-unkn-home-foundation-reset-tokens-assets::footer-copy-fix
:END:

- File: =projects/unkn/modules/_shared.st= around line 65 (=&unkn-footer()= template, the "Useful links" =<ul>=).
- Current: 5 items including =<li><a href="/about">About Work</a></li>=.
- Replace with 6 items, exactly: Home / About / Talent / Work / Expertise / Contact.
- Each =<li><a href="/$route">$Label</a></li>= with =$Label= title-cased.

* Edge Cases

- *Image filename collisions*: =RAFAELLA MAIA copy.JPG= and =RAFAELLA MAIA (1) copy.JPG= are different exposures of the same talent. Use the higher-quality one (verify with file size; pick larger) as =talent-02-rafaella-maia.jpg=. Discard the alternate take (or stash under =assets/images/_alts/= for future use).
- *Pexels licensing*: Pexels images are royalty-free for commercial use. Document this in =brand-spec.md= so future audits don't flag.
- *Color regression*: changing =--unkn-accent= will visually shift every script accent. Run =Spacetime.review()= dim "brand" before and after to confirm no other unbound literal sneaks in.
- *Typography mobile regression*: raising the clamp ceiling MUST NOT raise the lower bound. Verify at 360px (smallest breakpoint) the hero headline still fits on 2 lines max.
- *Image deploy idempotency*: if =assets/images/= already has files from a previous run, the deploy script overwrites; document this in the deploy sub-outline.

* Acceptance Criteria

- =cargo run -- check projects/unkn/= exits 0.
- =cargo run -- doctor projects/unkn/ --format json= reports zero compile errors across all home routes (other doctor scores unaffected by this item).
- Open =/= in dev server: every script accent is rendered in =#FF0020= (DOM-inspect at least 5 instances; computed color matches).
- Open =/=: footer "Useful links" shows 6 items in order Home / About / Talent / Work / Expertise / Contact.
- =GET /assets/images/talent-01-carolina-mendes.jpg= returns 200 with non-empty body.
- =GET /data/talent.json= returns 9-entry JSON validating the =Talent[]= type.
- =projects/unkn/AGENTS.md= and =projects/unkn/brand-spec.md= exist and are non-empty.
