#+TITLE: @data + @signal docs + live reader + cross-context demos
#+STATE: ITEM
#+SESSION_ID: 1507711255a45786
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-13T10-37-35-958Z_1507711255a45786.jsonl]]
#+CUSTOM_ID: FEAT-112-data-signal-docs-live-reader-cross-conte

* Initial Message

look at the Spell MCP features. I would like the interfaces to be defined as functions that are loaded into the live environment, so that it is a fully live environment from scratch, instead of just a 'prompt thing'.

1. look at anything that's pending in that thread of work
2. find your own insights w.r.t what I just said
3. come back to me with your realisations

* Parent
[[id:PLAN-038-signal-reactive-output-the-sibling-of-da]] — WAVE 4. Design ∧ docs
is part of the contract (user: OQ3 "write docs … look at demos/spacetime-docs").
Depends on FEAT-110 (core) and ideally FEAT-111 (so the agent demo is real).

* Why
A construct without docs is half-shipped. @signal only earns its place if its
SIBLING relationship to @data is teachable in one read. The docs LEAD with the
symmetry, then show @signal carrying its weight across all four Spacetime contexts.

* Deliverable 1 — docs/language/data-and-signal.md
The canonical pair doc. Structure:
  1. Reactive I/O — the loop  (SOURCE→STATE→VIEW→INTENT→SOURCE; the table pairing
     @data IN ↔ @signal OUT; introducers `: / from` ↔ `to`).
  2. @data recap (link to data-and-rendering.md; don't duplicate — reference).
  3. @signal — one construct, kind-word selects transport (post / emit; future ws/
     device). Fire with `$name(payload)`. Payload schema = a param block.
  4. The reply re-enters as data — `{ optimistic; on-ok; on-err }` + result-signal.
     Show the full mutation loop (read+write share the signal).
  5. Four contexts — web mutation, landing form, mobile command, agent picker —
     each a short real snippet (lift from research/spacetime-signal-S0.org §A-D).
  6. `to <sink>` polymorphism table (URL/mcp/$host/$socket/device).
  7. Harmony notes — no new sigil; `to` mirrors `from`; `:` stays value-only.
NB cross-link from docs/language/data-and-rendering.md and docs/language/README.md.

* Deliverable 2 — a live .st reader (demos/, mirrors demos/spacetime-docs)
A .st-only page that @doc(src: "docs/language/data-and-signal.md") renders the
canonical doc so it CANNOT drift (build-time inline, like spacetime-docs). PLUS a
small LIVE proof section on the page itself: a working @signal post to a mock/echo
endpoint, so the reader demonstrates the construct it documents (alive, not static).
  - dir: demos/spacetime-signal/ (index.st + README.md + screenshot)
  - reuse the spacetime-docs shell pattern (sidebar anchors, @doc sections, @scene
    backdrop optional). Zero authored JS (AGENTS: Spacetime-only).

* Deliverable 3 — one runnable demo per context
Small, `check`-clean pages proving each context (not just prose):
  - demos/signal-web-mutation/   — optimistic add/remove against a mock endpoint
    (the web-app killer case; @data fetch + @signal post w/ on-ok/on-err).
  - demos/signal-landing-form/    — newsletter/contact POST from a static page
    (SSG-friendly; @signal post { on-ok: $done <- true }).
  - demos/signal-mobile-cmd/      — STUB if device sinks (W5) not landed: show the
    intended `@signal emit … to $nav/$store` syntax behind a check-only page or a
    doc-only section; do NOT fake a runtime that doesn't exist (FIDELITY_LADDER:
    refuse to fake). Mark clearly as W5-preview.
  - agent picker             — covered by stdlib/__mcp__/picker.st (FEAT-111); link it.

* Tests / verification
- `cargo run -- check` clean on every demo page (the mobile stub is check-only/doc
  if its sink isn't built — be honest about fidelity).
- the reader renders data-and-signal.md (open + screenshot).
- doc reviewed: the SOURCE↔SINK symmetry is the FIRST thing a reader meets.
- links resolve (README.md + data-and-rendering.md cross-refs).

* Acceptance (W4)
- docs/language/data-and-signal.md exists, leads with the dual, covers 4 contexts.
- a live @doc reader exists in demos/ and renders without drift.
- ≥3 context demos check clean (web, landing real; mobile honest-stub).
- cross-links added; screenshots captured; colocated in the implementing commits.

* Notes
Honesty over coverage: a demo that would require an unbuilt sink is a doc/stub, not
a green-faked runtime. Better a true "W5 preview" than a fake pass.

NOTE [2026-06-13]: Rescoped to §S1 model (2026-06-13): docs/language/data-and-signal.md documents @data signal/stream + @handle + the unified `receive` matcher (NOT @signal). Lead with: @data IN (5 source kinds) ↔ @data signal/stream OUT (kinds 6&7); the one matcher across @match/decode/@handle; @host transport; `final` termination; `policy` races; `as`-ascription typing. Demos use @data signal (web mutation), @data signal to landing endpoint, mobile stub, agent picker (@data stream to $mcp). Depends on FEAT-110 + ideally FEAT-111/114.