#+TITLE: Derived Tokens — @data derive over $brand + color-algebra fns; a token SYSTEM
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FEAT-108-derived-tokens-data-derive-over-brand-co
#+COMPLETION_HASH: 5a6d80ce8d13d0c0

* Initial Message

can you check how that looks like in the admin? Can you compare that with stuff that is present in Framer/online/in mockups? I want it to look good please

PLAN-036 ④ — the system layer, deepest language fit. Tokens gain relationships: ink ← surface·darken, hover ← scarlet·darken. Edit one literal → whole system re-derives. Design: DESIGN.md §④, mockup 04-derived-tokens.png.

ARCHITECTURE (almost entirely already-built):
- @data derive / computed-source (Wave A, BUG-090) already computes a $-value from other signals WITH dep-discovery + reactive re-run.
- pipe + @fn (Wave A): $brand.scarlet | darken(8%) callable fn OR pipe.
- On EditAst scarlet change, computed-source watch re-runs dependents → hover repaints. Reactivity ALREADY built.
- build_brand_json already separates @data inline (literal/editable) from @data derive (computed).

IMPLEMENTATION (ONLY new code = color-algebra @fn in stdlib .st):
1. Derived tokens = @data derive over $brand:
   @data derive $ink   : $brand.surface | darken(0.92);
   @data derive $hover : $brand.scarlet | darken(0.08);
   @data derive $muted : $ink | alpha(0.60);
   No new construct.
2. Register color-algebra @fn: darken/lighten/alpha/mix — pure %emit js, fn-or-pipe.
3. Admin reflects literal vs derived: build_brand_json delivers derives with formula (source expr) + resolved value. Pane: literals = editable pills; deriveds = read-only formula chip (new &fld-derived template) + resolved swatch. Self-describing: add a derive → locked formula row, ZERO admin edits.
4. Connector lines (derived→source): derive dep-discovery already knows the edge; draw with FEAT-092 declarative SVG.

VALIDATION (GREEN = browser DOM):
- RED: add @data derive $hover; pane has no derived section.
- GREEN: edit scarlet → hover resolved swatch re-derives live (NO edit to hover); formula chip read-only; connector links them.
- v8: darken/lighten/alpha/mix correctness (known color → expected hex); derive re-run on dep change.
- self-describing proof: add a new derive → admin renders formula row, zero admin edits.
- Verify BUG-084 (nested-ternary-paren) does not bite color-fn args; workaround if so.
- Reviewer swarm after.

GATE: lib+v8+integration green; browser proof of edit-literal→derived-re-render + self-describing.

NOTE [2026-06-12]: COMPLETE. Design tokens derive from each other: @data derive $hover : $brand.scarlet | darken(0.08). Color algebra darken/lighten/alpha/mix in ST.filters; derived-signal gained pipe desugar + bare-filter resolution; build_brand_json delivers brand.derived=[{name,formula}]; admin &fld-derived chip (swatch+formula+value) via adResolveDerived (evals formula from literal values) + adRefreshDerived (in-place on keystroke). Browser-verified live re-derive (scarlet edit → hover/tintbg track, muted=ink-dep isolated) + SELF-DESCRIBING proof (add @data derive $glow → admin renders 4th chip, ZERO admin edits). Commits: 0bfa867b (filters+derive pipe), <server>, <admin>, aa37f1b7 (review P2 fix). Reviewer Opus 0.92, 1 P2 fixed. Gates lib 2252/0, integration 172/0, v8 395/9 baseline-only. Filed BUG-100 (pipe RHS call w/ $binding arg parse — fn-call workaround). Mockup 04-derived-tokens.png, shipped feat108-shipped.png + self-describing.png.