#+TITLE: W2 CDP substrate: chromiumoxide warm Chromium, layout/timing/paint rungs
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-058-w2-cdp-substrate-chromiumoxide-warm-chro
#+LAYER: infra

* Initial Message

actually, Land the PLAN-026 core now (done/committed); file the testing-macro cluster (@assert swap, @then/@eval nested-match) as a new BUG/PLAN with the diagnosis captured, and STOP — .test.st gating remains blocked
 on that follow-up. Don't register text tests yet.

Put text tests as FEAT within the plan. Be comprehensive

Wave 2 of [[id:PLAN-027-spacetime-testautomation-infra-fidelity]]. Stand up the
real-browser substrate via chromiumoxide (pure-Rust CDP), providing the layout /
timing / paint rungs and the foundation for automation (W7). HIGHEST DIFFICULTY
wave (warm-pool lifecycle + async↔sync test bridge).

* Backend decision (settled)
- ADD =chromiumoxide= to Cargo.toml deps (it is part of the compiler crate — NOT a
  vendored .st; the gingerbill =%vendor= path is for .st-side JS only).
- REMOVE the =playwright= npm-driver crate + the =browser= feature that wraps it.
  Migrate src/browser_test.rs, tests/playwright_st/* to the CDP client.
- A single CDP client serves BOTH testing (this wave) and automation (W7).

* Architecture
- =cdp_session= concept: connect to a Chromium over CDP websocket.
  - =via local=: launch + manage a warm Chromium (chromiumoxide handler) once;
    =Target.createTarget= per test = fresh tab; close tab after. Reuse across the
    whole suite (launch cost paid once, not per test).
  - endpoint is swappable (prepares W7 =via kernel <wsUrl>=, kernel.sh CDP :9222).
- Real APIs unlocked: getComputedStyle, Intl.Segmenter, Canvas, real rAF/layout.
  ⇒ @balance / pretext reflow / visibility become testable for the FIRST time.
- Async↔sync bridge: the test harness is sync (cargo test); CDP is async. Reuse the
  existing tokio block_on pattern from tests/playwright_st/mod.rs; one runtime,
  shared browser via OnceLock.
- Result protocol: keep the page-side =window.__spacetime_test_results= contract
  (already used by the playwright harness) so the test runtime JS is backend-agnostic.

* Warm-pool / isolation
- One browser, N tabs (contexts) for isolation; reset context between tests (ms).
- Design the session API so kernel-images snapshot/restore (W7) slots in without
  changing the test-facing surface.

* Proof obligation (the wave's reason to exist)
- The =@balance= assertion on =.ora-hero__sub= (FEAT-053) — impossible on V8
  (no getComputedStyle/Intl.Segmenter) — RUNS and GATES on the CDP backend:
  a wrong expected balanced-width FAILS; the correct one PASSES.

* Tests
- CDP backend launches, runs a trivial =@then .x { exists }=, returns structured
  results; tab closed after.
- A =needs layout= test (=computed(width)=) passes on CDP, and (from W1) is REFUSED
  on V8 — same test file, two backends, divergent + correct behavior.
- Warm reuse: N tests share one browser process (assert single launch via a counter).

* Risks / unknowns (call out in execution)
- chromiumoxide handler task lifetime + drop ordering under cargo test parallelism.
- Chromium discovery (system vs downloaded); doctor/CI provisioning.
- Flake budget: real timing introduces it — mitigate with virtual clock (W5) for
  timing-sensitive asserts; layout asserts are deterministic.

* Depends on
- [[id:FEAT-057-w1-fidelity-ladder-rung-enum-needs-decla]] (rung gate routes here)

* Parent
[[id:PLAN-027-spacetime-testautomation-infra-fidelity]]

* Chromium provisioning (settled: a + c)
- (a) LOCAL: auto-discover system Chrome/Chromium (chromiumoxide detection + common
  paths); =cargo run -- doctor= reports presence + version, prints install hint if
  absent. No mandatory download on the dev path.
- (c) CI / canonical: kernel-images Docker (chromium-headless) as pinned CI substrate
  — same CDP surface the client speaks, so local<->CI parity is exact. Document the
  run invocation (VENDORING.md-style).
- NOT doing (b) auto-fetch-a-browser by default (avoids heavy implicit download); a
  doctor-driven opt-in fetch may come later if friction warrants.
- Session API treats "local discovered" and "remote CDP url (CI docker / kernel)" as
  the SAME code path (endpoint string), so (a), (c), and W7 =via kernel= share one
  connector.

* COMPLETED (commits 2b603637, 7075f563, +finalize)
CDP substrate shipped. chromiumoxide 0.7 behind `cdp` feature (Cargo dep, not
vendored). src/cdp.rs: warm browser (process-global OnceLock + dedicated Tokio
runtime driving the Handler), per-test page via base64 data: URL goto (set_content
does NOT execute scripts under CDP), direct __spacetime_run_tests invocation,
results from __spacetime_test_results, find_chrome() local discovery, unique
--user-data-dir, 25s launch timeout, shutdown() to not leak Chromium.

Browser harness advertises `paint` rung (ST._rung.setBackend) so layout+ runs.
CLI: `spacetime test --cdp` (dedicated thread to avoid nested-runtime panic).

PROOF (tests/cdp_runner_test.rs): a `be_visible` (needs layout) test REFUSED on V8
PASSES on CDP against real offsetParent; logic test passes; both in one warm
browser (~1.3s = reuse). Fidelity-ladder thesis proven end-to-end.

MIGRATION (hard cutover): removed Playwright crate + `browser` feature.
tests/playwright_st delegates to spacetime::cdp (same run_st_tests sig, cdp-gated).
Deleted src/browser_test.rs::runner (kept parity structs+tests) and
src/cli/doctor.rs::headless_browser (Playwright). --browser flag deprecated ->
--cdp. Losses tracked: [[id:FUP-025-cross-engine-firefox-webkit-test-parity]]
(multi-engine parity + doctor route-screenshots, both pending CDP port).

GRAMMAR FIX (W0 follow-up, repaired the corpus): the capture boundary now only
fires at bracket DEPTH 0, so assertions with NESTED strings
(`@assert (Math.abs(x - f('#f80')) < 1)`) no longer truncate to invalid JS. This
unblocked many test files that previously aborted silently (counted as vacuous
passes). headless audit: 183/45 -> 347/259 (all 606 now actually compile+run; the
259 are real assertions gating + layout-refusals = W8 backlog).

Provisioning (a+c): doctor reports Chrome presence; docs/testing/CDP_BACKEND.md
documents local discovery + kernel-images Docker for CI. Default (no-cdp) build
unchanged. lib 2098 / integration 162 / import 16 / text 8 green.
