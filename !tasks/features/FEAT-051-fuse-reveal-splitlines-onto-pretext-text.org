#+TITLE: Fuse reveal split:lines onto pretext + text module tests + demos
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-051-fuse-reveal-splitlines-onto-pretext-text
#+LAYER: stdlib
#+DEPENDS: FEAT-050-stdlib-text-module-submodule-pretext-pri
#+COMPLETION_HASH: eec2eb54cb98884c

* Initial Message

let's think about how we'll extend the stdlib for Spacetime using this as a philosophical underpinning: https://www.gingerbill.org/article/2025/09/08/package-managers-are-evil/


In the context of integrating pretext to make text layout much simpler in Spacetime, especially in ora-ventures.com's hero section (projects/ora-ventures.com/ has some older syntax which the implementation agent will have to fix).

Gather your thoughts and come up with your own understanding of the nuances of going that way for this project. Remember that the stdlib/ folder is going to go from having `macros/` and `primitives/` to a modular architecture for folders, which is how we are going to make the pretext macros btw.

This is pretext: https://github.com/chenglou/pretext

Also think about stuff we could put in the demos/ folder to showcase the integration with pretext and the latest Spacetime features. Let's not do ora-ventures.com just yet, let's first think about a nice framework for making the stdlib library both lean and "fusion-engine-included", and showcase that with pretext. Consider all angles

Re-drive stdlib reveal split:"lines" on pretext line ranges (kill offsetTop
reflow). text/tests/*.test.st via playwright_st (update harness to serve vendored
bundle if needed). Demos: balanced-headline, fit-to-box, editorial-flow,
reveal-lines-reflow-free, text-engine (flagship). Each README states the exact JS
shipped (lean proof).

Parent: [[id:PLAN-024-modular-stdlib-vendor-git-submodule-pret]]
Depends: FEAT-050.

NOTE [2026-06-02]: W4 landed (commit 912f9925). Fused reveal split:lines onto pretext.layoutWithLines (progressive enhancement: uses globalThis.pretext when present for reflow-free arithmetic line grouping, else offsetTop fallback; core @reveal keeps zero hard dep on opt-in text module). Harness: test_runner resolves @import for non-testing modules + demand-injects vendored IIFE. Verified via tests/text_module_test.rs (5 emit-level tests, genuinely gating — mutation-checked): measure→pretext, demand-inject, lean-law zero-bytes, reveal both-paths, core-only-fallback.

DEFERRED: stdlib/text/tests/*.test.st written but NOT gating — blocked on BUG-051 (pre-existing: .test.st :block body emits as dead string literal so ALL assertions vacuous; reproduces at fdf1dc3b). Tracked by PLAN-026. playwright_st baseline 17pass/13fail identical pre/post my work = zero regressions. lib 2093 + text_module 5 + import_resolution 13 green.

* W5 also landed here (commit b1d02cde)
@balance + @fit layout macros (balance-text/fit-text primitives, pretext binary-search).
4 demos: balanced-headline, fit-to-box, reveal-lines-reflow-free, text-engine (flagship:
text+scene engines on one page, tree-shaking proven balanced-headline webgl=0 vs text-engine webgl=24).
text_module_test 8 (mutation-gated) + lib 2093 + integration 162 green. @flow deferred (not needed by demos).