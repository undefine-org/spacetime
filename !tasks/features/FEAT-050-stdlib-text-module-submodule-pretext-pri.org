#+TITLE: stdlib/text module: submodule pretext, primitives + macros, remove npm
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-050-stdlib-text-module-submodule-pretext-pri
#+LAYER: stdlib
#+DEPENDS: FEAT-048-vendor-meta-construct-shared-bundler-bui FEAT-049-lean-rail-hot-reload-vendor-blobs-demand
#+COMPLETION_HASH: 609f6643d0c5d255

* Initial Message

let's think about how we'll extend the stdlib for Spacetime using this as a philosophical underpinning: https://www.gingerbill.org/article/2025/09/08/package-managers-are-evil/


In the context of integrating pretext to make text layout much simpler in Spacetime, especially in ora-ventures.com's hero section (projects/ora-ventures.com/ has some older syntax which the implementation agent will have to fix).

Gather your thoughts and come up with your own understanding of the nuances of going that way for this project. Remember that the stdlib/ folder is going to go from having `macros/` and `primitives/` to a modular architecture for folders, which is how we are going to make the pretext macros btw.

This is pretext: https://github.com/chenglou/pretext

Also think about stuff we could put in the demos/ folder to showcase the integration with pretext and the latest Spacetime features. Let's not do ora-ventures.com just yet, let's first think about a nice framework for making the stdlib library both lean and "fusion-engine-included", and showcase that with pretext. Consider all angles

git submodule add chenglou/pretext @ pinned SHA at stdlib/text/vendor/pretext.
DELETE package.json devDep @chenglou/pretext + package-lock.json. Build
stdlib/text/: MODULE.st (manifest + %vendor), index.st, primitives
(measure-text, balance-engine, fit-text, flow-text wrapping pretext exports),
macros (@measure, @balance, @fit, @no-widows, @flow). PROVENANCE.md.

Parent: [[id:PLAN-024-modular-stdlib-vendor-git-submodule-pret]]
Depends: FEAT-048, FEAT-049.

NOTE [2026-06-02]: W3 landed (commit e1076acf). git submodule pretext@796b4691 (v0.0.7 MIT); entry-wrapper pattern (bun 1.3 lacks --global-name) re-exports onto globalThis.pretext; vendor.st in import graph; measure-text primitive + @measure macro; MODULE.st + PROVENANCE.md; committed 43KB bundle. npm fully removed.

Fixed 4 PRE-EXISTING bugs that blocked ALL directory-module imports (incl stdlib/mobile): (1) import resolver now supports <module>/index.st + cwd/stdlib fallback; (2) greedy inline-args: bodyless @import without ; swallowed following scope — fixed in BOTH CST and events parsers (added Input.newline_before + Parser.newline_before_current); (3) rematch only assigned to source_file==None scopes, excluding main scopes post-import → added rematch_with_user_macros_in(main_path); (4) merge_ast now tags imported meta_defs with source_file (vendor blob path needs it).

Verified e2e: @measure page injects 43KB pretext IIFE (112KB total); sibling importing-but-not-measuring ships 0 pretext (lean law). lib 2090 + integration 162 green.