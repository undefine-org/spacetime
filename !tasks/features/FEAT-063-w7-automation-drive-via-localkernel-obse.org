#+TITLE: W7 automation: @drive via local|kernel, @observe handles, profiles/replays/live-view
#+STATE: ITEM
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-063-w7-automation-drive-via-localkernel-obse
#+LAYER: infra

* Initial Message

actually, Land the PLAN-026 core now (done/committed); file the testing-macro cluster (@assert swap, @then/@eval nested-match) as a new BUG/PLAN with the diagnosis captured, and STOP — .test.st gating remains blocked
 on that follow-up. Don't register text tests yet.

Put text tests as FEAT within the plan. Be comprehensive

Wave 7 of [[id:PLAN-027-spacetime-testautomation-infra-fidelity]]. The payoff:
Spacetime syntax DRIVES real browsers. The same probe vocabulary
(@when/@then/@wait/@observe) used for tests now drives a LIVE world. Designed-IN
from W1–W6 (same probes, swap world); shipped last.

* North star realized
Test ≡ automation. Only difference: =@mount <fixture>= (synthetic world) vs
=@drive <url>= (live world). Learn @when/@then once → unit tests, E2E, scraping,
agent-driving.

* Surface
#+BEGIN_SRC
@drive "https://news.ycombinator.com" via local {     // or: via kernel <wsUrl>
  @observe .titleline as $stories       // agent-handle for un-authored DOM
  @then $stories { count >= 10 }
  @when input[name=q] input "spacetime"
  @when .submit click
  @wait-for .results
  @capture screenshot "out.png"
}
#+END_SRC
- =@drive <url> via local|kernel { … }= — local warm Chromium (W2) or remote CDP.
  =via kernel <wsUrl>= connects to kernel.sh :9222 endpoint (connectOverCDP model) —
  ZERO syntax delta vs local.
- =@observe <selector> as $handle= — capture stable element handles for DOM you did
  NOT author (complements selectors). The agent-handle pattern.
- Reuse W3 @when (real CDP input: dispatch via Input.dispatchMouseEvent /
  dispatchKeyEvent for OS-level fidelity, not synthetic events) and @then claims.

* kernel.sh reuse (https://github.com/kernel)
- kernel-images: warm snapshot/restore Chromium (<20ms restore) ⇒ ms per-test
  isolation AND resumable automation sessions. Same CDP surface the W2 client speaks.
- profiles: =@drive … profile: "logged-in"= — cookie/auth/localStorage state reuse
  for authenticated automation.
- replays: H.264 session recording — attach a replay to a FAILED test/drive for
  forensics (=--replay-on-fail=).
- neko (WebRTC live view): =@drive … --live= streams an interactive view ⇒ watch /
  take over the automation. The joy factor.

* Scripting tie-in
- Fold into the existing =spacetime script= subsystem (stdlib/scripting/*,
  main.rs run_script_headless): a drive script is a Spacetime program whose world is
  a URL. =spacetime drive <file.st>= as a CLI verb alongside =test=/=script=.

* Tests
- =@drive= against a LOCAL fixture server (reuse W2 axum harness) performs
  observe→assert→interact→wait→capture; verifies real input dispatch changed DOM.
- =via kernel= path unit-tested against a mock CDP endpoint (no live cloud dep in CI);
  document a manual live smoke against a real kernel browser.
- profile reuse: a second drive with a saved profile skips a login step.

* Depends on
- [[id:FEAT-058-w2-cdp-substrate-chromiumoxide-warm-chro]] (CDP client, swappable endpoint)
- [[id:FEAT-059-w3-assertion-grammar-mount-block-claims]] (@when/@then/@observe vocab)

* Parent
[[id:PLAN-027-spacetime-testautomation-infra-fidelity]]

* W7 CORE SHIPPED (commit pending)
The test\u2261automation thesis is PROVEN: @drive "url" { } navigates a live page via
CDP (page.goto + re-inject runtime), and @assert reads the LIVE DOM and GATES
(wrong-DOM expectation fails). Macros @drive/@observe/@capture/@wait-for compile +
register. @capture screenshot fulfilled Rust-side (page.screenshot -> file).
`via kernel`: SPACETIME_CDP_WS env connects to a remote CDP endpoint
(Browser::connect, kernel.sh :9222 model) \u2014 same client, swapped endpoint, zero
@drive surface delta.

KNOWN GAP (BUG-058): bare-block directives (@observe/@then/@capture/@wait-for) do
NOT execute inside a @drive body (re-injection swallows block-scope directives);
@assert works. So @drive automation today gates via @assert; the richer probes
need the BUG-058 re-injection fix.
