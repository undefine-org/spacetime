#+TITLE: Spacetime docs-site demo (self-documenting showcase)
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-081-spacetime-docs-site-demo-self-documentin
#+COMPLETION_HASH: 5c407b252d93a22b

* Initial Message

can you write docs for them and feature the link to that prominently in AGENTS.md?

first do a markdown version (in .md files) for the content. keep it short and concise, but complete, and focused on the testing infra for now.

then tell me how you'd make a Spacetime-based documentation generator that also acts as a showcase for the features of Spacetime. I want something that really slaps. You can use the latest %capture_type, %primitive, %form, and %macro and whatnot to really really slap (give me your ideas). you can test drive that

A `.st`-only doc-site in `demos/spacetime-docs/` that is BUILT FROM the very Spacetime primitives it documents — every showcase is the live primitive running, zero hand-written JS, no `index.html`.

North star (mirrors PLAN-027): the docs ARE the feature. A doc page is a Spacetime program whose WORLD is "the feature set."

W1 (this scaffold): a slapping single-page doc-site using PROVEN primitives —
file-scope `<tag>` literals, `@template`+`@data`+`@each` (generative API cards
from a seeded registry slice), `@balance`/`@reveal`/`@measure` (text engine),
`@scene` glow backdrop, `@on hover`/`@on click` interactions, `@scroll` reveals.
Each section both DESCRIBES and DEMONSTRATES a capability.

Deferred to FEAT (filed separately): userland `@example`/`@reference` macros with
`%emit html` are NOT expanded into the file-scope body today (they render
verbatim) — the dual-render + registry-introspection ideas need that compiler
feature.

NOTE [2026-06-09]: W1 scaffold shipped (commit e290d3fc). .st-only page, test-driven over CDP: hero @balance, live @on-click counter (0->2 verified), 12-card @each/@template gallery from a data slice, @scroll reveals, @scene backdrop. Slapping dark-glass design. Surfaced BUG-069 + FEAT-082 (deferred). Next: feed real registry JSON into the gallery; @example dual-render after FEAT-082.