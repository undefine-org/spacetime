#+TITLE: Spacetime 30s promo film — studio pipeline vs Spacetime-native, with critique
#+STATE: DONE
#+SESSION_ID: 157047c1462c1c38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-02T20-57-35-768Z_157047c1462c1c38.jsonl]]
#+CUSTOM_ID: FEAT-175-spacetime-30s-promo-film-studio-pipeline

* Initial Message

Using whatever tools and techniques you want, make the absolute most impressive, most mind blowing, most visually stunning 30s promo video for Spacetime (this repo). I want something that looks like we paid a studio $5m to produce.

After that, I want you to do the same thing, but with Spacetime, and to compare/critique the two

Two 30-second promo films for Spacetime from ONE storyboard, then a critique.

* Deliverables
1. =scratch/promo-studio/= — the "studio" cut. Any tool allowed: hand-written HTML + JS + WebGL (Three.js), deterministic frame capture through Chrome DevTools Protocol (puppeteer-core + `google-chrome-stable`), ffmpeg encode, procedurally synthesized sound design (numpy → wav), ffmpeg post grade. Output: =scratch/promo-studio/out/spacetime-promo-studio.mp4=.
2. =demos/promo/= — the SAME storyboard authored in Spacetime only (no custom JS, AGENTS.md rule), rendered with =cargo run --features cdp -- render demos/promo/ --out …=, the same audio track muxed afterwards with ffmpeg (audio is outside the render verb today — SIP-001b §8.6). Output: =scratch/promo-st/spacetime-promo-st.mp4=.
3. =demos/promo/CRITIQUE.md= — frame-by-frame comparison (contact sheet), what Spacetime could not express (filed as FUP items), what it did better (determinism, source size, no JS), and the language gaps this surfaced.

* Storyboard (30s · 60fps · 1920×1080)
| t (s) | scene | beat |
|-------+-------+------|
| 0–3   | S1 point of light → "CSS describes space." | focus pull, tracking-in type, sub hit |
| 3–7   | S2 perspective grid materializes, tilts into 3D, a time axis sweeps through → "Spacetime adds time." | grid draw-on, rotate-x, axis sweep |
| 7–12  | S3 code + result split: a .st snippet types in; the described element animates live beside it → "Declare it once." | typed code, live card reveal |
| 12–18 | S4 four drivers (time · scroll · click · playback) drive the SAME composition in four panels, playheads converge to one → "Every clock." | 4-up, playhead bars, convergence |
| 18–23 | S5 filmstrip of frames flies past in perspective, frame counter ticks → "Render it." `spacetime render → film.mp4` | 3D strip, counter, riser |
| 23–27 | S6 `<script>` tag shatters into particles → "No JavaScript." | particle burst, glitch |
| 27–30 | S7 wordmark SPACETIME shimmer, tagline, hold, fade to black | shimmer, bloom |

* Brand
Dark (#06070a) · ink #e6ecf5 · accent amber #ffb454 · secondary cyan #7dd3fc · display font Syne 800 (installed) · secondary Space Grotesk · code JetBrains Mono. Purple gradient / GitHub-dark palette are antipatterns (docs/antipatterns.md §9/§13) — avoided.

* Constraints
- Spacetime cut MUST be 100% .st (no `<script>`); anything missing is a language gap → FUP, not a bypass.
- Both cuts share the storyboard + audio so the comparison is fair.
- Render determinism: all drivers Transport (`&.time`/`&.loop`), per SIP-001b §1.2.

* Test plan
- Studio: =node render.mjs= produces 1800 frames → mp4; ffprobe duration ≈ 30.0s.
- Spacetime: =cargo run -- check demos/promo/= clean; render produces mp4 with ffprobe duration ≈ 30.0s; a `--cdp` behavior gate in =tests/score/promo-film.test.st= asserts scenes move in their windows (BUG-252 rule: behavior, not source strings).

* Outcome (2026-09-03)
Both films rendered: 1800 frames · 60 fps · 1920×1080 · 30.000 s each.
- Studio: =scratch/promo-studio/out/spacetime-promo-studio.mp4= (54 MB, crf 16; 20.5 min wall, GPU). Source preserved at =demos/promo/studio-cut/=.
- Spacetime: =scratch/promo-st/spacetime-promo-st.mp4= (5.1 MB; 14.0 min wall, =--disable-gpu=). Source =demos/promo/index.st=, 513 lines, zero author JS.
- Side-by-side contact sheet: =scratch/compare/side-by-side.jpg=.
- Critique: =demos/promo/CRITIQUE.md= (frame-by-frame verdict; studio wins on light/camera/glyph particles, Spacetime wins on text/layout/editability; overall close).

Surfaced and fixed: BUG-382 (FOUC initial-state CSS flattened nested-scope first stops onto the root — every scene rendered at 15% scale). Gates: unit =test_initial_state_css_keyframes_capture_keeps_nested_scope= + CDP =tests/score/promo-film.test.st= (3/3 green; test 1 seen RED against the pre-fix compiler).

Language gaps filed: FUP-196 (@post 2D), FUP-197 (@camera), FUP-198 (@scatter glyph sampling), FUP-203 (positioned keyframe stops), FUP-204 (@audio + render mux), FUP-205 (render --stills/--from/--to), FUP-206 (@eval `x[1]` parse trap).

Not verified: the promo has no audio-sync gate (hits are hand-placed against the wav; FUP-204 is the real fix). Pre-existing unrelated snapshot drift in =src/compiler.rs= snapshot tests (5) from an uncommitted =public/runtime/st.js= change in the working tree — not touched.