#+TITLE: UNKN responsive sweep + sub-route light pass + doctor gate
#+STATE: DONE
#+SESSION_ID: 14cedfd0e21b5609
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-30T12-08-01-672Z_14cedfd0e21b5609.jsonl]]
#+CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa
#+DEPENDS: FEAT-035-unkn-scrolling-image-strip-hover-pause-r FEAT-036-unkn-hero-scroll-driven-red-wipe-typogra FEAT-037-unkn-roster-vertical-slide-reveal-pair-g FEAT-038-unkn-mid-page-composition-statement-iden FEAT-039-unkn-bottom-page-composition-services-da
#+EFFORT: 3h
#+LAYER: frontend
#+PRIORITY: #A

* Initial Message

can you tell me how you use the spacetime design skill?

* Intention & Purpose

Final delivery gate. Sweep responsive behavior across all 12 home sections at the agreed breakpoints (Mobile ≤720 / Tablet 721–1100 / Desktop ≥1101); apply a *light* pass to sub-routes (=/about=, =/talent=, =/work=, =/expertise=, =/contact=) so the new accent color, typography ceilings, and footer copy carry through; run =cargo run -- doctor= until every route scores ≥6 across all 7 dims. This is the ship gate.

* Context & Narrative

After FEAT-035..039, the home page has the full structure, motion, photography, and typography. This item ensures:
1. Every breakpoint renders cleanly with no horizontal overflow, no clipped typography, no broken motion at touch-only devices.
2. Sub-routes inherit the new theme/typography automatically (because they share =modules/_shared.st= and =_theme.st=) and their footer copy is correct (FEAT-034 already fixed). Light verification only — no redesign.
3. Doctor gate passes on all routes: =/=, =/about=, =/talent=, =/work=, =/expertise=, =/contact=.
4. Accessibility: screen reader announces all key sections; keyboard users can complete the navigation; reduced-motion respected throughout.
5. Reduced-motion verified across the three signature motions (strip, hero wipe, roster slide).

* Scope

In-scope:
- Audit responsive behavior at three viewports per route: 360px (mobile minimum), 720px (mobile/tablet boundary), 1100px (tablet/desktop boundary), 1440px (desktop target).
- Audit reduced-motion at one viewport (1440px) per signature motion.
- Sub-route light pass: load =/about=, =/talent=, =/work=, =/expertise=, =/contact= and verify:
  - Footer copy correct (Home / About / Talent / Work / Expertise / Contact).
  - Accent color =#FF0020= used on script accents.
  - Typography ceilings raised (h2 reaches ~70px at desktop).
  - No console errors.
- Run =cargo run -- doctor projects/unkn/ --format pretty= and resolve every finding ≥4 severity.
- Capture screenshots for the gate proof: 6 routes × 3 viewports = 18 baseline + 3 reduced-motion = 21 screenshots.
- Update =projects/unkn/AGENTS.md= and =projects/unkn/brand-spec.md= with any decisions made during the sweep.
- Add =@media (prefers-reduced-motion: reduce)= overrides where missing (audit each signature motion).

Out-of-scope:
- Performance tuning (image compression, code splitting) — separate FUP if needed.
- Sub-route redesign — this is *light* verification only.
- Cross-browser manual testing beyond Chromium (the headless doctor uses Playwright Chromium) — separate FUP if needed.
- Web-vitals measurement (LCP, CLS, INP) — separate FUP.

* Existing Patterns

- Doctor gate: =cargo run -- doctor <project> --format pretty= for human-readable output, =--format json= for machine-readable. See =skill://spacetime-design= "Pre-delivery doctor".
- Breakpoint convention: =@media("max-width: 720px")= and =@media("max-width: 1100px")= as established in FEAT-035..039 modules.
- Reduced-motion: =@media (prefers-reduced-motion: reduce)= in =patterns/_reduced-motion.st= or inline at end of each motion module.
- Sub-route layout: every sub-route uses =&unkn-route-placeholder()= from =modules/_shared.st= — same template, just different content. They auto-inherit theme tokens.

* Tests

Per-route doctor gates:
- T1: =cargo run -- doctor projects/unkn/ --format json | jq '.routes[] | {path, score, dims}'= shows 6 routes (=/=, =/about=, =/talent=, =/work=, =/expertise=, =/contact=), each with =score >= 6= and zero compile errors.
- T2: =Spacetime.review()= dim "compile" reports zero errors on every route.
- T3: =Spacetime.review()= dim "brand" reports zero unbound hex/rgb literals on every route.
- T4: =Spacetime.review()= dim "a11y" reports zero "missing alt" / "missing label" / "heading-order" findings on every route.

Manual responsive scenarios:
- M1 (=/= 360px): hero copy stack readable; CTAs vertical; strip 1.5 cells visible touch-scrollable; mission 1-col with 4-card stacked stats; roster 1-col pairs; services 1-col; work card aspect-ratio preserved; why 1-col; contact stacks CTAs.
- M2 (=/= 720px): hero copy stack readable; strip 2.5 cells visible auto-scrolling; mission 1-col; roster 2-col; services 2-col cards; work card 16:9; why 1-col; contact CTAs side-by-side with wrap.
- M3 (=/= 1100px): hero full layout; strip 4-5 cells visible; mission 2-col with stats inline-right (or below depending on transition); roster 2-col; services 2-col cards or 3-col; work card 16:9; why 2-col; contact full layout.
- M4 (=/= 1440px): hero full impact; strip 5-6 cells visible; mission 2-col stats inline-right; roster 3-col; services 3-col; work card 16:9; why 2-col; contact full layout.
- M5..M8: at 1440px load each sub-route. Verify nav + hero placeholder + footer render correctly with new tokens; nothing visibly broken.

Manual reduced-motion scenarios:
- M9 (1440px, prefers-reduced-motion: reduce): strip is static scrollable row; hero shows no wipe, typography stays ink; roster pairs show image+name both visible; nav/section reveals do not animate (no opacity/transform transitions trigger).

Console errors:
- M10: every route loads with zero =console.error= and zero =console.warn= flagged by =Spacetime.review()=.

* Implementation

** ITEM Audit + add missing reduced-motion overrides
:PROPERTIES:
:CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::audit-reduced-motion
:END:

- File: =projects/unkn/patterns/_reduced-motion.st=
- Add (or verify exists, from FEAT-035..037 sub-outlines):
  =@media (prefers-reduced-motion: reduce) {=
  =  /* Strip — no autoplay */=
  =  .unkn-strip__track { animation: none !important; flex-wrap: nowrap; overflow-x: auto; }=
  =  .unkn-strip__cell { scroll-snap-align: start; }=
  =  .unkn-strip__category { opacity: 1 !important; background: rgba(245, 240, 230, 0.85); }=
  =  /* Hero wipe — invisible, typography stays ink */=
  =  .unkn-hero__wipe { display: none !important; }=
  =  .unkn-hero__copy { --unkn-hero-fg: var(--unkn-ink) !important; }=
  =  .unkn-hero__headline-script { color: var(--unkn-accent) !important; }=
  =  /* Roster pairs — image + name both visible */=
  =  .unkn-talent-pair__image-slot { transform: none !important; height: 60% !important; }=
  =  .unkn-talent-pair__name, .unkn-talent-pair__headline { opacity: 1 !important; }=
  =  /* All @reveal animations - disable */=
  =  *, *::before, *::after { animation-duration: 0ms !important; transition-duration: 0ms !important; }=
  =}=
- Verify: in DevTools rendering pane → "prefers-reduced-motion: reduce", load =/=, scroll. None of the three signature motions trigger; reveals are instant.

** ITEM Sweep responsive at 360 / 720 / 1100 / 1440
:PROPERTIES:
:CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::sweep-responsive
:DEPENDS: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::audit-reduced-motion
:END:

- Boot dev server: =cargo run -- serve projects/unkn/ --debug=
- For each route in [=/=, =/about=, =/talent=, =/work=, =/expertise=, =/contact=]:
  - Set viewport to 360x640 — visual check; capture screenshot =screenshots/responsive/<route>-360.png=.
  - Set viewport to 720x900 — capture =<route>-720.png=.
  - Set viewport to 1100x800 — capture =<route>-1100.png=.
  - Set viewport to 1440x900 — capture =<route>-1440.png=.
- Per scenario document: any horizontal scrollbar appearing, any text overflowing its container, any image stretched, any CTAs unreachable, any copy clipped.
- Fix any breakage in the appropriate module file. Common fixes likely needed:
  - =overflow-x: hidden= on body to suppress accidental horizontal scrolls (already in =_globals.st=).
  - =flex-wrap: wrap= on flex containers that may collide.
  - =max-width: 100%= + =height: auto= on any image not already covered by =_globals.st=.

** ITEM Sub-route light pass verification
:PROPERTIES:
:CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::subroute-light-pass
:DEPENDS: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::sweep-responsive
:END:

- Load each sub-route at 1440px:
  - =/about=: hero placeholder copy + nav + footer.
  - =/talent=: hero placeholder copy + nav + footer.
  - =/work=: hero placeholder copy + nav + footer.
  - =/expertise=: hero placeholder copy + nav + footer.
  - =/contact=: hero placeholder copy + nav + footer.
- Verify per route:
  1. Footer "Useful links" lists exactly Home / About / Talent / Work / Expertise / Contact (6 items).
  2. Any script accent in route copy renders =#FF0020= scarlet.
  3. Display headlines reach the new clamp ceilings (h2 ~ 70px desktop).
  4. No JS console errors.
  5. =Spacetime.review()= score ≥ 6.
- Apply minimal targeted edits if any sub-route exposes a regression:
  - =modules/_route.st= might need typography token updates if it hardcodes old sizes — audit, fix.

** ITEM Run doctor gate + capture report
:PROPERTIES:
:CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::doctor-gate
:DEPENDS: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::subroute-light-pass
:END:

- Run =cargo run --features browser -- doctor projects/unkn/ --format pretty= (browser feature required for headless route loop).
- If doctor reports score < 6 on any route:
  - Read the dim-by-dim findings.
  - Fix the highest-severity findings first (errors > warnings > info).
  - Re-run doctor until all routes pass.
- Capture =cargo run --features browser -- doctor projects/unkn/ --format json > screenshots/doctor-report.json=.
- Verify: =jq '.routes[] | select(.score < 6) | .path' screenshots/doctor-report.json= returns nothing.

** ITEM Update AGENTS.md + brand-spec.md with sweep findings
:PROPERTIES:
:CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::update-docs
:DEPENDS: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::doctor-gate
:END:

- File: =projects/unkn/AGENTS.md=
- Append section "Responsive Audit Notes" with any non-obvious decisions (e.g. "Strip becomes scroll-snap horizontal at touch", "Hero wipe disabled in reduced-motion", "Roster pairs reveal name on tap on touch").
- File: =projects/unkn/brand-spec.md=
- Append section "Verified Routes + Doctor Gate" with the doctor JSON summary line per route.

** ITEM Final commit + screenshot bundle
:PROPERTIES:
:CUSTOM_ID: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::final-commit
:DEPENDS: FEAT-040-unkn-responsive-sweep-sub-route-light-pa::update-docs
:END:

- Verify =screenshots/responsive/= contains 24 files (6 routes × 4 viewports).
- Verify =screenshots/doctor-report.json= exists and exit code from doctor was 0.
- Verify =screenshots/strip-paused-cell.png=, =screenshots/hero-wipe-100pct.png=, =screenshots/roster-pair-hover.png=, =screenshots/services-dark.png=, =screenshots/work-flagship.png=, =screenshots/eyes-interlude.png=, =screenshots/why-grid.png=, =screenshots/contact-script.png= all present from earlier FEATs.
- Single commit per ship policy: stage all FEAT-034..040 changes; commit with message "PLAN-021: UNKN home — reference-aligned redesign + 3 signature motions + responsive + doctor pass".

* Edge Cases

- *Doctor browser feature not available*: if Playwright not installed, doctor returns benign "browser feature not enabled" skip per skill notes. Install: =bun install --cwd src/cli/playwright-deps= or follow skill instructions. Required for this gate.
- *Score 5 on edge route*: if =/about= route placeholder happens to score 5 (e.g. one =@reveal= timeline matching 0 elements because =_route.st= has no =.unkn-mission__title=), suppress that finding by adjusting selectors in =_reveals.st= to scope to =body:not(.unkn-route)= or by adding stub markers in route placeholder. Choose the *least* invasive option.
- *Image weight regression*: 9 talent images + 1 flagship + 1 eyes + 14 strip thumbnails could push initial bundle above 5MB. =loading="lazy"= on below-fold images mitigates. Document weight in brand-spec.md.
- *Sub-route screenshots may differ in scrolled state*: the doctor headless captures the loaded state; if any sub-route has long copy that scrolls, the screenshot doesn't capture the full page. Acceptable for this audit.
- *Reduced-motion fallback regression for keyboard users*: keyboard users without prefers-reduced-motion still depend on =:focus-visible= triggers (added in roster). Verify Tab navigation through roster pairs reveals names.

* Acceptance Criteria

- =cargo run --features browser -- doctor projects/unkn/ --format json= exits 0; every route score ≥ 6; zero compile errors; zero console errors; zero non-benign skipped routes.
- =screenshots/doctor-report.json= attached; =jq '.routes | length' = 6=, =jq '.routes[].score | min' >= 6=.
- =screenshots/responsive/= contains 24 viewport screenshots.
- All sub-routes load with new accent + typography + footer copy.
- Reduced-motion mode verified manually for all three signature motions.
- =projects/unkn/AGENTS.md= and =projects/unkn/brand-spec.md= updated with audit notes.
- Single commit "PLAN-021: UNKN home — reference-aligned redesign + 3 signature motions + responsive + doctor pass" lands.
