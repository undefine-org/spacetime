#+TITLE: __spacetime_contract__/0 Spark transformer — emit the contract Wave B reads
#+STATE: ITEM
#+SESSION_ID: 1510a559ccf03ced
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-20T14-12-20-403Z_1510a559ccf03ced.jsonl]]
#+CUSTOM_ID: FEAT-139-spacetime-contract-0-spark-transformer-e

* Initial Message

ok i want you to design a prototype a deep integration between Spacetime and elixir, especially Phoenix LiveView and it should be a la Djinn.LiveView (~/code/ora/monorepo).

What could it look like? Show me your DSL design so we can think about it together. Look at the recent Spacetime $host improvements and whatnot, maybe you can extend it to a canonical host for Phoenix LiveView that makes the integration insanely seamless and self-aware (re-uses the power of Elixir macros/Spark and the introspectability of Spacetime to deliver an experience that leverages the best of both).

* Parent: FEAT-136 (Wave C); closes the loop with FEAT-135 (Wave B).

* Goal
On the Elixir side, emit `__spacetime_contract__/0` from the LiveView's declared
events/assigns/pushes — the exact JSON shape Wave B's comparator
(src/analysis/liveview_contract.rs) reads from the `<page>.contract.json` sidecar.
Today that sidecar is hand-written; this generates it from the server module so it
CANNOT drift from the code (the self-aware property, both ends).

* Scope
- A Spacetime.LiveView DSL surface (or Spark extension) declaring events/pushes/
  assigns (re-implementing Djinn.LiveView's sections per the bridge design §3/§10).
- A transformer walking that schema → write priv/spacetime/<page>.contract.json at
  compile (or expose via the FEAT-138 NIF introspect()).
- Wire: a mismatch between the .st page intents and the generated contract is the
  E0928 compile error (already implemented in Wave B).

* Acceptance
- CounterLive declares handle "inc"/"dec" via the DSL; the transformer emits a
  contract.json with events [inc,dec] matching today's hand-written fixture.
- Renaming a server event without updating the .st → E0928 at build.
- No hand-written contract.json needed.
