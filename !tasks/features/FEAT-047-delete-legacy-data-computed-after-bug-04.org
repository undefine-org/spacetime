#+TITLE: delete legacy @data{}/@computed after BUG-042 + deprecation window
#+STATE: DONE
#+SESSION_ID: 14f48ced503111a7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-05-29T18-28-09-664Z_14f48ced503111a7.jsonl]]
#+CUSTOM_ID: FEAT-047-delete-legacy-data-computed-after-bug-04
#+COMPLETION_HASH: 0afda1686d45a64a

* Initial Message

ok start with FEAT-046 Blocker 1 and then continue onto the W3->W6 cluster. Use the native testing tool whenever you can. I'm going for a date, so trust yourself and consider any decision approved. Be rigourous and choose the more structurally sound and long-term option, even if it means adding waves/leaves and surfacing/filing/fixing or implementing FEAT/BUG files. Write a summary in between each sub-wave and whatnot.

* Goal (PLAN-023 W5 tail — deferred from the autonomous arc)
Hard-delete the legacy block-form `@data { src/value/cache/initial }` and `@computed
{ from/where/sort/\u2026 }` macros (stdlib/macros/type-data.st) + the legacy `data-fetch` /
`computed-value` macros (stdlib/macros/data.st), once their replacements fully cover all
capability and external authors have migrated.

* Why deferred (not done in the W5 autonomous arc)
The unified `@data <kind>` surface reached feature PARITY (inline/fetch/query/derive/fold
+ fetch options) and in-repo examples were migrated + the legacy macros marked @deprecated
(commit a244961d). But two reasons block immediate deletion:
1. BUG-042 (inline object-literal arrays): `@data x { value: [{"k":v}] }` cannot yet be
   expressed as `@data inline x : [{\u2026}]` because the directive value parser breaks on
   `{` object literals. cart-demo's product catalog uses this. Deleting legacy now would
   LOSE that capability (violates the refactor contract).
2. Deprecation window for EXTERNAL .st authors of this published stdlib.

* Blockers
- BUG-042 must land first (inline object-literal data).
- Then migrate cart-demo's `@data products { value: [{\u2026}] }` to `@data inline`.

* Scope when unblocked
1. Confirm zero remaining legacy @data{}/@computed call sites in tracked corpus
   (examples/, tests/, demos/, stdlib/).
2. Delete %macro data + %macro computed (type-data.st), %macro data-fetch +
   %macro computed-value (data.st) IF unused.
3. Remove any now-dead analysis branches (analyze_data_sources still handles both Ident
   and Binding name capture \u2014 keep the Binding path, drop legacy-only assumptions).
4. Full reactive regression: cart-demo + reactive demos browser-verified; lib/integration/
   headless green; multi-reviewer wave.

* Acceptance
- Legacy macros gone; one surface (one need = one implementation).
- No capability lost; all demos render + react identically.
- Full gate + live reactive verification green.

NOTE [2026-06-02]: DONE (maximalist). BUG-042 fixed (inline object-literal arrays parse; commit 0ad56a53) + bare-object inline values added (79de13d3) — closed the last capability gaps. Then ruthless cutover (cc6eb3bb):
DELETED legacy macros: type-data.st %macro data + %macro computed; data.st %macro data-fetch + each-block + computed-value (all replaced by migration-note comments; 0 stdlib bindings depended on them).
MIGRATED in-repo corpus: cart-demo (products->@data inline object array), data-binding/styles (fetch/localStorage + fold/query), reactive-list (inline+fold), namespaced-signals (bare-object inline), tests/parser/syntax/macros.st.
PRUNED dead Rust: analysis/mod.rs (||data-fetch/computed-value/each-block filters), cli/inspect.rs, lsp/usage_visitor.rs.
MIGRATED TESTS to unified FormMatch shape preserving intent: parser/tests.rs (7), server.rs (2 sites), usage_visitor.rs, baseline_tests.rs (+snap), compiler.rs snapshot_data_directive (+snap), computed_codegen.rs (rewritten 11), reactive_properties.rs (3, assert surviving SpacetimeLocal + local:<name>:updated contract), v8 data_source_tests.rs (3).
SCOPE NOTE: projects/ is gitignored (separate repo) — out of FEAT-047 scope.
Filed BUG-044 (deprecation diagnostic: removed forms currently silent no-op for external authors, not an error).
GATE: lib 2077, integration 162, headless 441; v8 back to 12 pre-existing (BUG-039), ZERO new regressions. No capability lost (cart-demo reactive arc + object arrays verified compile-equivalent).