#+TITLE: Design multi-file find symbol hit output
#+STATE: DONE
#+SESSION_ID: 150a981da51e09f3
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-15T21-23-37-748Z_150a981da51e09f3.jsonl]]
#+CUSTOM_ID: FEAT-121-design-multi-file-find-symbol-hit-output
#+LAYER: tooling
#+PRIORITY: B
#+COMPLETION_HASH: 177b8ce1410d78bb

* Initial Message

i want the find tool to output symbols also during multi-file hits, and efficiently, and consistently across the variatns.

e.g. first line i

/.../...rs
::Symbol (if several lines hit or several symbols hit within one file, otherwise full codepath on one line)
<lineno>: ...

or something like that. what do you think?

Investigate and implement consistent symbol-aware formatting for find tool multi-file hits.

User intent: `find` output should include enclosing symbols for multi-file line/code hits efficiently and consistently across variants. Desired shape example:

```
/.../...rs
::Symbol (if several lines hit or several symbols hit within one file, otherwise full codepath on one line)
<lineno>: ...
```

Scope:
- Locate find/CodePath formatter implementation.
- Understand existing single-file vs multi-file output variants.
- Design minimal harmonious formatting change.
- Implement symbol annotation without inefficient per-hit reparsing.
- Add/update tests for multi-file symbol grouping and single-hit full codepath behavior if supported by existing snapshots.
- Verify with targeted tests.

Constraints:
- Follow AGENTS.md: no stash/reset/revert/checkout; record work in org; avoid parallel impls; prefer existing grammar/registry affordances.
- Use structural `find`/`edit`; bash only for build/test/git.

NOTE [2026-06-15]: Design response: favor symbol-aware multi-file formatting, with deterministic file→symbol→hit grouping for multi-hit files and compact full codepaths for singleton hits. Key implementation constraint: compute enclosing symbols from the same per-file parse/outline pass and cache an interval index per rendered file; do not reparse per hit.

* Resolution 2026-06-15
Answered design question in chat. Current behavior observed: multi-file `find` line hits print file headers + raw lines, without enclosing symbol context. Recommended symbol-aware multi-file rendering with file→symbol→hit grouping, singleton compact form only when unambiguous, explicit `::<file>` fallback, and efficient per-file symbol interval indexing from the existing parse/outline pass.

No code changed in this Spacetime repo: pi harness `find` formatter source is not present here. Actual implementation deferred to [[file:../follow-ups/FUP-060-implement-symbol-aware-multi-file-find-o.org][FUP-060]].