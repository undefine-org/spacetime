#+TITLE: @data registry — the self-documenting gallery source ((1))
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-084-data-registry-the-self-documenting-galle
#+LAYER: data
#+DEPENDS: FEAT-083-doc-comment-field-on-registry-summaries
#+COMPLETION_HASH: 4915808300dfe19e

* Initial Message

can you write docs for them and feature the link to that prominently in AGENTS.md?

first do a markdown version (in .md files) for the content. keep it short and concise, but complete, and focused on the testing infra for now.

then tell me how you'd make a Spacetime-based documentation generator that also acts as a showcase for the features of Spacetime. I want something that really slaps. You can use the latest %capture_type, %primitive, %form, and %macro and whatnot to really really slap (give me your ideas). you can test drive that

GOAL ((1) of the docs brainstorm): make the compiler's own registry a first-class `@data` source so a `.st` page's EXISTING `@each` + `&template` renders the live standard-library gallery. Add a primitive to stdlib → it appears in the docs on next build, zero page edits. The anti-rot keystone.

SURFACE (new @data mode, mirroring @data inline):
  @data registry $features                       // ALL primitives + macros
  @data registry $text from kind: "text"         // filtered slice
  @data registry $anim from module: "animation"
Each row is the FEAT-083-enriched summary: { name, kind, module, params[], exports[], doc }.

MECHANISM (the Spacetime-y way, verified against existing seams):
- A `%macro registry` (in stdlib/data/) whose `%emit build-js` calls a NEW host
  fn `registry()` returning the introspection JSON. build-js already runs at
  COMPILE time in V8 with sandboxed host fns (src/build_runtime.rs: readJson,
  readFile, getHtml, writeFile) — `registry()` joins them, fed by the SAME
  MetaRegistry/iter_macros surface (DRY with W6 coverage + FEAT-083 doc).
- The macro emits an `@data inline $features : <json>` equivalent, so the result
  SSG-UNROLLS into static HTML (the ssg-list moat, src/html/ssg_unroll.rs):
  crawlers + no-JS see the cards; the runtime hydrates the same DOM.
- The page is UNCHANGED otherwise:
    @data registry $features
    .grid { @each($features as $f) { &feature-card($f); } }

DEPENDENCY: FEAT-083 (doc field) — so cards show real descriptions, not just
names. The `registry()` host fn is the shared introspection surface; build it as
`Spacetime.forms`/`Spacetime.types` so LSP + docs + coverage + W4 generator all
consume it (collapse redundant form-enumeration per the W6 mandate).

@reference COMPOSES THIS (no new mechanism):
  @data registry $b from name: "@balance"
  &api-card($b.0)        // a &template rendering name/params/exports/doc
∴ @reference is just @data registry filtered to one, fed to a template.

ACCEPTANCE:
- demos/spacetime-docs gallery cards generated from @data registry (delete the
  hand-seeded $features array; the 12 cards become N = real registry size).
- `from kind:`/`from module:` filter correctly; an empty filter → all.
- SSG: the cards are in the served HTML BEFORE the runtime script (no-JS view).
- adding a new %primitive to stdlib makes a new card appear with no page edit
  (the anti-rot proof — assert via a fixture primitive).

RISK: registry can be large; default @data registry should be the documentable
subset (has doc OR is public), not every internal primitive. Make the filter the
common path.

NOTE [2026-06-09]: Done (commit 1a0285de). @data registry [from kind X] rewritten to @data inline(registry-json) by inject_registry_data pass; flows through @each SSG-unroll. ~300 cards rendered static; filter yields 85 test directives. 2 regression tests. doc from FEAT-083, kind from stdlib path.