#+TITLE: CMS admin: collection search box (mockup parity)
#+STATE: DONE
#+SESSION_ID: 14f8685429e4ccf1
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-01T18-22-11-112Z_14f8685429e4ccf1.jsonl]]
#+CUSTOM_ID: FEAT-067-cms-admin-collection-search-box-mockup-p
#+COMPLETION_HASH: ec12d7eb55c059fa

* Initial Message

ok let's think deeply deeply. What would a headless CMS for Spacetime look like? like a headless CMS that has all the nice features of a headless CMS and the power of Spacetime combined. Look at the stdlib and the __dev__ to understand some of the pre-existing experiments, and do a research on the state of headless CMSes, before coming back with your THOUGHTS AND REFLECTIONS (no decisions yet)

* Source
PLAN-028 Wave A ground-truth.

* Gap
The validated V1 mockup (docs/specs/cms-mockups/v1-talent-admin.png) shows a search
field in the collection header; the implementation has none (=searchPresent=false=).
For Talents (10) it's minor; for StripRows (28) and future large collections it matters.

* Scope (Wave C)
Add a client-side filter input in the collection header (stdlib/__admin__/admin-app.st)
that filters visible rows by title/subtitle/chips substring. No server round-trip
(purely a view filter over already-loaded data). Calm styling, scarlet focus ring.

* Acceptance
- Typing in the search box narrows the row list live; clearing restores all rows.

* Related
- PLAN-028 Wave C, docs/specs/cms-mockups/v1-talent-admin.png.

NOTE [2026-06-03]: DONE (Wave C). Added #ad-search input in collection header (shown when >4 rows); state.filter + rowMatches() filters by title/subtitle/chips + all string fields; reset on collection switch; caret preserved on re-render. Verified live: StripRows 28→4 for "fashion". Reviewer: correct, filtered indices sound.