#+TITLE: Phase B: component_body self-describing (retire parse_component_body)
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FEAT-079-phase-b-component-body-self-describing-r
#+COMPLETION_HASH: 6fb7cf89cdd3366e

* Initial Message

Ok, start with Phase B. Use a wave manifest with in-chat mid-flight check-ins exlpaining what's going on, what' you've done, and what you're doing next. use main thread for architectural work and reviewer swarms in between waves. add as many gap implementation as needed; you have unlimited time budget. file FEAT/BUG you find mid-flight using the org tool and fix/implement them mid-flight

* Parent
PLAN-030 (Spacetime App Platform). The TRUE full cutover of the metasystem: the ~1100-line Rust
`parse_component_body` (the central self-describing violation) -> stdlib %capture_type grammar +
a generic reifier + existing html path. Closes BUG-060 (@match Rust special-case).

* Spec
[[file:../../@research/spec-phase-b-component-body-self-describing.org]]

* Architecture (decided 2026-06-08, verified against code)
parse_component_body = segment_component_body (GENERIC scanner, KEEP) + 11-branch Rust classifier
(RETIRE) + per-construct Rust parsers parse_state_decl_typed/parse_injection/parse_class_toggle/
parse_on_event_block[_scoped]/parse_self_property_binding/parse_exports_block/
parse_selector_ref_header/parse_template_ref/parse_match_block_via_engine (RETIRE).
=> Phase B: segment_component_body (KEEP) -> per-segment `component_item` Choice grammar (stdlib
component-body.st) -> reify_component_body (generic, dispatches on grammar branch label, PURE
restructure per reifier-honesty rule) + HTML-runs via existing html path.
IR `ComponentBodyDef` + all sub-structs UNCHANGED (bounds blast radius to the PRODUCER; ~9
consumers untouched).

* Waves
- [ ] B3a differential harness (safety net): golden ComponentBodyDef JSON over a corpus of real
  @template bodies; gates every later wave (parity before deletion).
- [ ] B3b stdlib component-body.st leaf grammars (cb_state/cb_inject/cb_toggle/cb_exports/
  cb_selector_ref/cb_block) + component_item Choice + reuse match_block/template_invocation.
- [ ] B3c reify_component_body generic reifier (non-HTML parts); differential-test vs golden.
- [ ] B3d HTML-run interleave (existing html path) + order + provenance (data-st-id).
- [ ] B3e diagnostics parity (E0900/E0920/E0921/E0922/section_transitions).
- [ ] B3f cutover: switch CaptureType::ComponentBody dispatch; DELETE parse_component_body + all
  per-construct parsers + scan_html_tag + parse_match_block_via_engine + @match branch.
- [ ] B3g BUG-060 exit gate: grep finds no construct-name parsing logic in src/.

* Gates (per wave + final)
cargo test --lib / integration / v8 (no new failures beyond pre-existing media cluster);
differential parity green on corpus; headless + a real fleet build byte-identical/audited;
reviewer swarm per wave; HARD EXIT GATE grep clean.

NOTE [2026-06-09]: COMPLETE. parse_component_body (~900 Rust lines) DELETED; template-body grammar lives entirely in stdlib/capture-types/component-body.st + match-block.st. Path: segment_component_body (generic, KEEP) -> component_item/cb_block_item Choice grammar -> reify_component_body generic reifier (branch-label dispatch, value-semantics adapters). IR ComponentBodyDef unchanged. Waves: B3a golden corpus + B3c fleet differential (44 cases + 25 real bodies, byte-identical) licensed deletion; B3f cutover (-843 net lines); B3g exit gate clean (BUG-060 closed). Mid-flight bugs found+fixed via 2 reviewer swarms: BUG-062 (multibyte panic), BUG-063 (4 parity gaps), BUG-064 (bare &ref E0900), BUG-065 (pseudo-class selectors + block content-binding values, incl a latent bug the old parser had). FEAT-080 (block-inner grammar) done. FUP-038 (parse_nested_each) deferred orthogonal. Gates: lib 2134/0, integration 162/0, v8 311/12 (pre-existing media), headless 355/254 (baseline), real fleet build correct. HEAD 11a83e59.