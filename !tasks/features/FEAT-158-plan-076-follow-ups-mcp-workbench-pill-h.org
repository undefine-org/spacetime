#+TITLE: PLAN-076 follow-ups: MCP workbench pill, __host__ module port, LSP code-action, multi-edit rewrites
#+STATE: ITEM
#+SESSION_ID: 1534a246b3aa1d15
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-04-13-518Z_1534a246b3aa1d15.jsonl]]
#+CUSTOM_ID: FEAT-158-plan-076-follow-ups-mcp-workbench-pill-h

* Initial Message

i want us to design a system where the stdlib has 'migrations', i.e. a macro that takes old st syntax and turns it to new st syntax. the macro should include the date so that we can order the migrations by syntax and apply syntax waves. It should be shown in all dev environments next to the current host pill, in the same style as the host pill

* FUP batch for PLAN-076 (filed at plan completion)

1. **Migrations pill in MCP workbench/canvas environments.** PLAN-076's
   dock covers every dev-server page-serve path (incl. locale + .st-only
   synthesized shell). The MCP workbench/canvas environments use a
   different injection substrate (stdlib/__mcp__/env.st) — the pill +
   status/apply routes should surface there too.

2. **Port `__host__` to the module shape.** stdlib/migrations/ is the
   precedent for dev-widget verticals (MODULE.st + __dev__/pill.st +
   tests/). Port stdlib/__host__ to stdlib/host/{MODULE.st,__dev__/pill.st}
   when convenient — no behavior change, pure layout.

3. **LSP code-action from the migration registry.** The same
   %migration entries can drive an LSP quick-fix ("apply migration") on
   W0715/E0910 diagnostics — one more consumer of the same registry, zero
   new machinery.

4. **Multi-edit rewrite templates** (the `%hint`-kind upgrade path).
   Today %rewrite = one span replacement; @show/@input need auxiliary
   rules/two insertions, so they're hint-kind. Extend %rewrite to an
   insertion list (multiple spans + insert-at-anchor forms) and those
   become mechanical too.
