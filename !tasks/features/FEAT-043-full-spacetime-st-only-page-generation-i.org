#+TITLE: Full-Spacetime .st-only page generation (inline HTML to .html)
#+STATE: DONE
#+SESSION_ID: 14f48ced503111a7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-05-29T18-28-09-664Z_14f48ced503111a7.jsonl]]
#+CUSTOM_ID: FEAT-043-full-spacetime-st-only-page-generation-i
#+EFFORT: 1w
#+LAYER: coding-agent
#+COMPLETION_HASH: 768040c59a93050c

* Initial Message

my friend, I want you to do two things that are quite related to each other:
1. look at projects/unkn for the way it structures its files and tell me how you would have structured it
2. look up reactive bindings and how they are done in Spacetime (the $ sigil), as well as full-spacetime work (as in, not using index.html, only index.st and generating the html from it by putting the html inline).

The goal is to build a project that showcases reactive bindings, so that websites can start dealing with userland values instead of just animation-specific values (although one of the strengths of Spacetime is its ability to bring reactive bindings to the animation sphere).

The goal of this is both to make a showcase demo (in demos/) and to use that to find all the BUGs and FEATs and REFACTORs that we need to implement. Do NOT use the 'emacs' way of managing tasks; only the Spell-native way. Think a bit about the type of demo that would make sense to showcase reactive bindings; get inspiration by choosing from awwwards.com too

* Source
User asked about "full-Spacetime" work: no index.html, only index.st, with HTML written inline and the compiler generating the .html.

* Current state (verified)
- `cargo run -- build <dir>` on a pure .st (no sibling .html) emits "Pages: 0" — only spacetime.js + empty spacetime.css. No <!DOCTYPE>/<html>/<head>/<body> is generated.
- src/emit/html.rs is an explicit STUB ("primarily a stub for future template generation features").
- No @document/@page/@head/@title/@meta directives exist.
- `cargo run -- init` always scaffolds BOTH index.html + index.st (src/main.rs ~L1693).
- Today's real pattern (unkn, spacetime-showcase): thin hand-written index.html (head metadata + empty <header>/<main>/<footer> mount points) + ALL markup inline in .st @template blocks, composed via root-level selectors, injected at runtime as DOM factories (window.Spacetime.templates).

* Scope
- Document-level directive(s) to declare page shell from .st: lang, data-theme, <title>, <meta>, charset, viewport, favicon, stylesheet links.
- Complete src/emit/html.rs to emit a full HTML document from .st (doctype + head + body with root-composed templates server-rendered or mount points).
- Decide SSR vs mount: either pre-render @template composition into static HTML at build, or emit mount points + runtime hydration (current model). Prefer pre-render for no-JS-baseline pages.
- Update `init` to optionally scaffold .st-only.

* Out of scope
- Per-route doc generation beyond index (follow-up).

* Acceptance
- A directory with only index.st builds to dist/index.html (Pages: 1) containing a valid document with the declared head metadata and the composed body.
- `cargo run -- init --st-only <name>` scaffolds index.st without index.html.

* Notes
Not required for the reactive-bindings demo (uses the thin-shell pattern). Recorded as separate scope.

NOTE [2026-05-30]: Wave 1 DONE + live-verified (61d2f58d): .st-only projects serve HTTP 200 via synthesize_st_only_shell. Wave 2 (file-scope inline HTML into shell body) SCOPED + deferred to a dedicated session \u2014 from-scratch 4-layer feature (CST grammar arm + AST node + IR lowering + emit/server injection); full scoping in PLAN-023 note. Setting REVIEW (not DONE): Wave 1 ships value standalone, Wave 2 is tracked separately and is non-blocking (full-Spacetime @each works today via @template + thin shell).

NOTE [2026-06-01]: PLAN-023 REWRITTEN (2026-05-30 design session). Wave 2 is no longer "inject inline HTML into shell body" — it is reframed as "< is a first-class Spacetime sigil; valid HTML is valid Spacetime", powered by html5ever + custom TreeSink → ir::HtmlExpr. Static lists SSG-unroll to real HTML (SEO); dynamic lists hydrate. See PLAN-023 North star + Waves 0/1. This FEAT stays REVIEW (Wave 1 .st-only-serves-200 shipped); the remaining work lives in the rewritten PLAN-023.

NOTE [2026-06-01]: DONE via PLAN-023 Wave 1 (2026-05-30). Wave 2 (file-scope inline HTML into served body) is delivered: first-class <tag> literals compile through the html5ever TreeSink to ir::HtmlExpr → CompiledSpacetime.html, injected into the st-only shell <body> ahead of the runtime. LIVE-verified: a pure-.st page (no index.html, no authored .js) serves real <main><h1><ul><li>…</li></ul></main> in view-source (SSG/SEO). Commits d2ba5fd1/0667a224/abffc914. Wave 1 (.st-only serves 200) was already shipped (61d2f58d). Remaining dynamic/SSG-unroll semantics live in PLAN-023 W3/W4.