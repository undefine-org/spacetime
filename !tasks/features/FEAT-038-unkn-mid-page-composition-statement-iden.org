#+TITLE: UNKN mid-page composition (statement / identity / mission / resonance)
#+STATE: DONE
#+SESSION_ID: 14cedfd0e21b5609
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-30T12-08-01-672Z_14cedfd0e21b5609.jsonl]]
#+CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden
#+DEPENDS: FEAT-034-unkn-home-foundation-reset-tokens-assets
#+EFFORT: 4h
#+LAYER: frontend
#+PRIORITY: #A

* Initial Message

can you tell me how you use the spacetime design skill?

* Intention & Purpose

Decompose =modules/_mission.st= (currently 331 lines containing four logically-distinct sections) into four single-responsibility modules: =_statement.st=, =_identity.st=, =_mission.st= (mission + stats inline), =_resonance.st=. Align each to the PDF spec — copy, typography, real photography for resonance — and drop the invented =unkn-portraits= text-rectangle placeholders that aren't in the reference.

* Context & Narrative

The PDF section order between the strip and the services section is:
  statement (italic existential text) → identity ("We are / The (Un)Known. / A global talent agency redefining influence.") → mission ("More Than Talent Management" eyebrow + "Our mission is bold, yet simple." headline + 4-stat row inline-right) → resonance (real portrait + paragraph + "Because today, influence isn't about reach. / It's about resonance.")

The current =_mission.st= mashes all four into one file with four templates (=&unkn-section-statement=, =&unkn-section-identity=, =&unkn-section-mission=, =&unkn-section-portraits=, =&unkn-section-resonance=). The =unkn-portraits= section is invented (two text rectangles "cultural relevance, on the inside" / "resonance, not reach" — not in the PDF). The mission section has a separate =.unkn-mission__stats= populated by =@each= but the layout doesn't match the PDF (which inlines stats *to the right* of mission copy as a 4-col row).

This item splits the modules and aligns each to the PDF, replacing decorative placeholders with real photography deployed in FEAT-034.

* Scope

In-scope:
- New module =projects/unkn/modules/_statement.st= owning =&unkn-section-statement()=.
- New module =projects/unkn/modules/_identity.st= owning =&unkn-section-identity()=.
- New module =projects/unkn/modules/_resonance.st= owning =&unkn-section-resonance()=.
- Rewrite =projects/unkn/modules/_mission.st= to own only =&unkn-section-mission()= (with inline-right stats grid).
- Delete =&unkn-section-portraits()= entirely (out of spec).
- Update =projects/unkn/index.st= imports + page composition to reflect new module structure and remove =&unkn-section-portraits()= call.
- Wire resonance section's portrait to =assets/images/talent-02-rafaella-maia.jpg= (the highest-quality named-talent image) per brand-spec.
- Mission stats use existing =$stats= dataset (4 entries).
- Each section uses correct PDF copy verbatim (script accents wrapped in =<span class="unkn-script">= → renders via =--unkn-accent= = =#FF0020=).

Out-of-scope:
- Hero, strip, services, roster, work, why, contact (handled by other FEATs).
- Restructuring =$stats= dataset (already correct).
- Animation timelines beyond the existing =_motion.st= reveals (which already target =.unkn-statement__text=, =.unkn-identity__title=, =.unkn-mission__title=, =.unkn-resonance__line= — keep these targets, they continue to work).

* Existing Patterns

- Module shape: =@template &unkn-section-*()= + section CSS in same file.
- Spacetime composition: =index.st main { ... }= calls templates in narrative order.
- Reveal targets in =_reveals.st=: keep selectors stable so existing reveals continue working: =.unkn-identity__title= (chars-split), =.unkn-statement__text= (opacity), =.unkn-mission__title= (words-split). Verify after split.
- Stats data: =data/stats.json= — 4 entries, schema =Stat { id, value, label }=. =@each= already populates =.unkn-mission__stats=.

* Tests

Compile-time:
- T1: =cargo run -- check projects/unkn/= passes after split.
- T2: =Spacetime.review()= dim "compile" zero errors on home route.

Visual + content:
- M1: Statement section: centered italic text "we exist to elevate the talent that **makes us feel**, because in the end, it's not automation that moves the world... **it's an emotion.**" — script phrases in =#FF0020=.
- M2: Identity section: "We are" eyebrow + display "The (Un)Known." with red script + caption "A global talent agency redefining influence." (NOTE: PDF shows "The (Un)Known." in red script — verify color application; current code has it in =--unkn-accent= via =unkn-identity__title= class — keep).
- M3: Mission section: "More Than Talent Management." eyebrow (uppercase, small) + display headline "Our mission is **bold,** yet simple." (script "bold,") + paragraph "To monetise identity, transforming **your story**, **your brand** and **your influence** into lasting cultural and commercial impact." + 4 stats inline RIGHT of copy: =+200= talent roster / =+50= Brand partners / =7= Divisions Worldwide / =+30= Global markets.
- M4: Resonance section: large portrait left (Rafaella Maia portrait from =assets/images/talent-02-rafaella-maia.jpg=) + right column with paragraph "We go beyond traditional representation..." + lead "Because today, influence isn't about reach." + script "It's about resonance."
- M5: Removed: =unkn-portraits= text-rectangles ("cultural relevance, on the inside" / "resonance, not reach") — gone from DOM.

Layout:
- M6: Statement is centered, max-width ~32ch.
- M7: Mission grid is 2-col at desktop (copy left, stats right inline), 1-col at ≤1100px (stats below copy).
- M8: Resonance is 2-col at desktop (image left, text right), 1-col at ≤720px.
- M9: Stats row at desktop is 4-col grid; at tablet 2x2; at mobile 4-row stack.

Reveal continuity:
- M10: =.unkn-identity__title= still triggers chars-split reveal.
- M11: =.unkn-mission__title= still triggers words-split reveal.

* Implementation

** ITEM Author modules/_statement.st
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::author-statement
:END:

- File: new =projects/unkn/modules/_statement.st=
- Contents:
  =/// UNKN Statement section — centered italic existential.=
  =@template &unkn-section-statement() {=
  =  <section class="unkn-statement" id="about">=
  =    <p class="unkn-statement__text">we exist to elevate the talent that <span class="unkn-script unkn-script--md">makes us feel</span>, because in the end, it&rsquo;s not automation that moves the world&hellip; <span class="unkn-script unkn-script--md">it&rsquo;s an emotion.</span></p>=
  =  </section>=
  =}=
- CSS (extracted from current =_mission.st= lines 9-33 for =.unkn-statement= + =.unkn-statement__text= + =.unkn-statement__text .unkn-script=):
  - =.unkn-statement { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding: var(--unkn-space-2xl) var(--unkn-gutter); text-align: center; }=
  - =.unkn-statement__text { font-family: var(--unkn-font-display); font-style: italic; font-size: clamp(1.4rem, 2.6vw, 2.2rem); line-height: 1.32; color: var(--unkn-ink); max-width: 32ch; margin-inline: auto; letter-spacing: -0.005em; }=
  - =.unkn-statement__text .unkn-script { font-style: normal; font-family: var(--unkn-font-script); color: var(--unkn-accent); line-height: 0.92; vertical-align: -0.1em; font-size: 1.4em; }=
  - Mobile: =@media("max-width: 720px") { .unkn-statement { padding-block: var(--unkn-space-xl); } }=

** ITEM Author modules/_identity.st
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::author-identity
:END:

- File: new =projects/unkn/modules/_identity.st=
- Contents:
  =/// UNKN Identity section — "We are / The (Un)Known. / A global talent agency redefining influence."=
  =@template &unkn-section-identity() {=
  =  <section class="unkn-identity">=
  =    <div class="unkn-identity__shell">=
  =      <p class="unkn-identity__lead">We are</p>=
  =      <h2 class="unkn-identity__title"><span class="unkn-identity__title-script">The (Un)Known.</span></h2>=
  =      <p class="unkn-identity__caption">A global talent agency redefining influence.</p>=
  =    </div>=
  =  </section>=
  =}=
- Note: PDF shows "The (Un)Known." in red script (Adobe Handwriting Ernie). Wrap entire title in =<span class="unkn-identity__title-script">= so the chars-split reveal in =_reveals.st= can still target =.unkn-identity__title= (parent), while the inner span carries the script font + accent color.
- CSS:
  - =.unkn-identity { width: 100%; padding-block: var(--unkn-space-2xl); background: var(--unkn-paper); }=
  - =.unkn-identity__shell { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding-inline: var(--unkn-gutter); text-align: center; display: grid; gap: var(--unkn-space-xs); }=
  - =.unkn-identity__lead { font-family: var(--unkn-font-body); font-size: var(--unkn-text-lg); color: var(--unkn-ink); }=
  - =.unkn-identity__title { font-family: var(--unkn-font-display); font-size: var(--unkn-text-display); line-height: 0.95; }=
  - =.unkn-identity__title-script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: var(--unkn-text-script-xl); line-height: 0.95; }=
  - =.unkn-identity__caption { font-family: var(--unkn-font-body); font-size: var(--unkn-text-md); color: var(--unkn-ink-muted); text-transform: uppercase; letter-spacing: var(--unkn-tracking-meta); }=

** ITEM Rewrite modules/_mission.st (mission + inline stats)
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::rewrite-mission
:DEPENDS: FEAT-038-unkn-mid-page-composition-statement-iden::author-identity
:END:

- File: =projects/unkn/modules/_mission.st= (rewrite, replace contents)
- New contents:
  =/// UNKN Mission section — eyebrow + headline + monetise-identity copy + 4 inline stats.=
  =@template &unkn-stat-card($s) {=
  =  <div class="unkn-stat" data-stat-id="$s.id">=
  =    <span class="unkn-stat__value">$s.value</span>=
  =    <span class="unkn-stat__label">$s.label</span>=
  =  </div>=
  =}=
  =@template &unkn-section-mission() {=
  =  <section class="unkn-mission">=
  =    <div class="unkn-mission__grid">=
  =      <div class="unkn-mission__copy">=
  =        <p class="unkn-eyebrow">More Than Talent Management.</p>=
  =        <h2 class="unkn-mission__title">Our mission is <span class="unkn-script">bold,</span> yet simple.</h2>=
  =        <p class="unkn-mission__lede">To monetise identity, transforming <strong>your story</strong>, <strong>your brand</strong> and <strong>your influence</strong> into lasting cultural and commercial impact.</p>=
  =      </div>=
  =      <div class="unkn-mission__stats"></div>=
  =    </div>=
  =  </section>=
  =}=
- Note: =@template &unkn-stat-card= already exists in =index.st=; move it here for cohesion. Update =index.st= reference if needed (already calls it via =@each=).
- CSS:
  - =.unkn-mission { width: 100%; padding-block: var(--unkn-space-2xl); background: var(--unkn-paper); }=
  - =.unkn-mission__grid { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding-inline: var(--unkn-gutter); display: grid; grid-template-columns: 1fr 1fr; column-gap: var(--unkn-space-2xl); row-gap: var(--unkn-space-xl); align-items: end; }=
  - =.unkn-mission__copy { display: grid; gap: var(--unkn-space-md); }=
  - =.unkn-eyebrow { font-family: var(--unkn-font-body); font-size: var(--unkn-text-xs); text-transform: uppercase; letter-spacing: var(--unkn-tracking-eyebrow); color: var(--unkn-ink-muted); }= (or move to =_globals.st= if shared by services + roster — verify)
  - =.unkn-mission__title { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h2); line-height: 1.05; max-width: 12ch; }=
  - =.unkn-mission__title .unkn-script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: 1.05em; line-height: 0.95; }=
  - =.unkn-mission__lede { font-family: var(--unkn-font-body); font-size: var(--unkn-text-lg); line-height: 1.55; color: var(--unkn-ink-muted); max-width: 38ch; }=
  - =.unkn-mission__lede strong { color: var(--unkn-ink); font-weight: 600; }=
  - =.unkn-mission__stats { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--unkn-space-md); }=
  - =.unkn-stat { display: grid; gap: var(--unkn-space-xs); padding: var(--unkn-space-md); border: 1px solid var(--unkn-rule-soft); }=
  - =.unkn-stat__value { font-family: var(--unkn-font-display); font-size: var(--unkn-text-stat); line-height: 0.95; color: var(--unkn-ink); }=
  - =.unkn-stat__label { font-family: var(--unkn-font-body); font-size: var(--unkn-text-xs); text-transform: uppercase; letter-spacing: var(--unkn-tracking-meta); color: var(--unkn-ink-muted); }=
- Mobile breakpoints:
  - =@media("max-width: 1100px") { .unkn-mission__grid { grid-template-columns: 1fr; } }=
  - =@media("max-width: 720px") { .unkn-mission__stats { grid-template-columns: 1fr 1fr; } }=

** ITEM Author modules/_resonance.st
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::author-resonance
:DEPENDS: FEAT-038-unkn-mid-page-composition-statement-iden::rewrite-mission
:END:

- File: new =projects/unkn/modules/_resonance.st=
- Contents:
  =/// UNKN Resonance section — portrait left + paragraph + "Because today, influence isn't about reach. / It's about resonance."=
  =@template &unkn-section-resonance() {=
  =  <section class="unkn-resonance">=
  =    <div class="unkn-resonance__shell">=
  =      <figure class="unkn-resonance__portrait">=
  =        <img src="/assets/images/talent-02-rafaella-maia.jpg" alt="Resonant talent portrait" loading="lazy">=
  =      </figure>=
  =      <div class="unkn-resonance__copy">=
  =        <p class="unkn-resonance__lede">We go beyond traditional representation. We build cultural relevance, global visibility and meaningful partnerships. Through a tailored, hands-on approach, we connect talent with the world&rsquo;s most influential brands, platforms and moments.</p>=
  =        <p class="unkn-resonance__line">Because today, influence isn&rsquo;t about reach.</p>=
  =        <p class="unkn-resonance__script">It&rsquo;s About Resonance.</p>=
  =      </div>=
  =    </div>=
  =  </section>=
  =}=
- CSS:
  - =.unkn-resonance { width: 100%; padding-block: var(--unkn-space-2xl); background: var(--unkn-paper); }=
  - =.unkn-resonance__shell { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding-inline: var(--unkn-gutter); display: grid; grid-template-columns: 1fr 1fr; column-gap: var(--unkn-space-2xl); align-items: center; }=
  - =.unkn-resonance__portrait { aspect-ratio: 4 / 5; overflow: hidden; }=
  - =.unkn-resonance__portrait img { width: 100%; height: 100%; object-fit: cover; }=
  - =.unkn-resonance__copy { display: grid; gap: var(--unkn-space-md); }=
  - =.unkn-resonance__lede { font-family: var(--unkn-font-body); font-size: var(--unkn-text-lg); line-height: 1.55; color: var(--unkn-ink-muted); max-width: 50ch; }=
  - =.unkn-resonance__line { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h3); line-height: 1.05; color: var(--unkn-ink); }=
  - =.unkn-resonance__script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: var(--unkn-text-script-lg); line-height: 0.92; }=
- Mobile:
  - =@media("max-width: 720px") { .unkn-resonance__shell { grid-template-columns: 1fr; row-gap: var(--unkn-space-lg); } }=

** ITEM Update index.st imports + composition
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::index-update
:DEPENDS: FEAT-038-unkn-mid-page-composition-statement-iden::author-resonance
:END:

- File: =projects/unkn/index.st=
- Replace =@import "./modules/_mission.st"= block with:
  =@import "./modules/_statement.st"=
  =@import "./modules/_identity.st"=
  =@import "./modules/_mission.st"=
  =@import "./modules/_resonance.st"=
- In =main { ... }= body, ensure order: hero → strip → statement → identity → mission → resonance → services → roster → work → why → contact.
- Remove =&unkn-section-portraits()= call entirely (template deleted).
- Verify =@template &unkn-stat-card= is defined in =_mission.st= (moved here from =index.st=) and the root-level =@each($stats as $s) { &unkn-stat-card($s); }= populating =.unkn-mission__stats= still works — no changes needed on the =@each= call.

** ITEM Delete _mission.st orphans (statement / identity / portraits / resonance)
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::cleanup-old-mission
:DEPENDS: FEAT-038-unkn-mid-page-composition-statement-iden::index-update
:END:

- The old =modules/_mission.st= (331 lines) is being replaced. After the new =_mission.st= is in place (rewrite-mission sub-outline), confirm:
  - =&unkn-section-statement= moved to =_statement.st= ✓
  - =&unkn-section-identity= moved to =_identity.st= ✓
  - =&unkn-section-mission= rewritten with inline stats ✓
  - =&unkn-section-resonance= moved to =_resonance.st= ✓
  - =&unkn-section-portraits= deleted ✓
- Grep =projects/unkn/= for any sub-route still importing or calling the old templates. Update if needed.

** ITEM Manual verify
:PROPERTIES:
:CUSTOM_ID: FEAT-038-unkn-mid-page-composition-statement-iden::manual-verify
:DEPENDS: FEAT-038-unkn-mid-page-composition-statement-iden::cleanup-old-mission
:END:

- =cargo run -- doctor projects/unkn/ --format pretty=. =/= scores ≥ 6 across 7 dims.
- Manual scenarios M1-M11. Capture =screenshots/mid-statement.png=, =screenshots/mid-identity.png=, =screenshots/mid-mission.png=, =screenshots/mid-resonance.png=.

* Edge Cases

- *Reveal target drift*: =_reveals.st= and =_motion.st= reference =.unkn-statement__text=, =.unkn-identity__title=, =.unkn-mission__title=, =.unkn-resonance__line=. After split, these selectors must still match exactly the same elements. Verify.
- *=.unkn-eyebrow= shared usage*: this class is used by mission, services, roster. Move definition to =_globals.st= to avoid duplication. Verify with grep.
- *Stats grid alignment to copy bottom*: PDF shows stats inline-right, baseline-aligned to the bottom of mission copy. =align-items: end= on =.unkn-mission__grid= achieves this.
- *Resonance portrait file size*: =talent-02-rafaella-maia.jpg= will be ~1.3MB. =loading="lazy"= helps below-fold.
- *script italic vs script-font*: ".unkn-script" class already used in statement. Keep this generic class (defined in =_globals.st=) and apply it consistently across all script accents. Audit existing markup to standardize.

* Acceptance Criteria

- =cargo run -- check projects/unkn/= exits 0.
- =cargo run -- doctor projects/unkn/ --format json= for =/= scores ≥ 6.
- 4 modules =_statement.st=, =_identity.st=, =_mission.st=, =_resonance.st= exist with single-section responsibility each.
- =&unkn-section-portraits()= no longer exists in any template file.
- =/= renders sections in correct order: hero → strip → statement → identity → mission → resonance → ...
- Mission section has stats inline-right (desktop) or stacked-below (≤1100px), 4-card grid using =$stats= dataset.
- Resonance section displays =/assets/images/talent-02-rafaella-maia.jpg= as a portrait.
- Existing reveals on =.unkn-identity__title= and =.unkn-mission__title= still trigger.
- Screenshots attached.
