#+TITLE: @mount file-reference + @self (mount other modules / this file's scopes)
#+STATE: ITEM
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-066-mount-file-reference-self-mount-other-mo
#+LAYER: testing

* Initial Message

actually, Land the PLAN-026 core now (done/committed); file the testing-macro cluster (@assert swap, @then/@eval nested-match) as a new BUG/PLAN with the diagnosis captured, and STOP — .test.st gating remains blocked
 on that follow-up. Don't register text tests yet.

Put text tests as FEAT within the plan. Be comprehensive

Extend =@mount= (PLAN-027 W3, FEAT-059) beyond inline blocks.

* Shipped (FEAT-059)
=@mount { …real .st… }= \u2014 inline; compiles real markup + nested primitives.

* This FEAT
1. =@mount "path.st"= \u2014 mount another module by reference (reuse @import resolution,
   compile its scopes into the test world).
2. =@mount @self= / =@mount @self <selector>= \u2014 mount THIS test file's own
   non-test scopes (co-located component + test). =@self= is the light isolated
   reference attempt; general namespacing -> [[id:FUP-024-namespace-module-reference-system-supers]].
   Whatever spelling actually works wins (K1).
3. Reusable fixtures = ordinary .st modules mounted by reference.

* Why split
The inline @mount (the common case) works now. File/self references need import-
resolution wiring into the test-body compile path \u2014 a separate, larger change.

* Parent
[[id:PLAN-027-spacetime-testautomation-infra-fidelity]]
