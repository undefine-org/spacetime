#+TITLE: ora-ventures hero: migrate to text module (@balance + reveal-lines) + fix older syntax
#+STATE: DONE
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-053-ora-ventures-hero-migrate-to-text-module
#+COMPLETION_HASH: fab452b07514cb7e

* Initial Message

let's think about how we'll extend the stdlib for Spacetime using this as a philosophical underpinning: https://www.gingerbill.org/article/2025/09/08/package-managers-are-evil/


In the context of integrating pretext to make text layout much simpler in Spacetime, especially in ora-ventures.com's hero section (projects/ora-ventures.com/ has some older syntax which the implementation agent will have to fix).

Gather your thoughts and come up with your own understanding of the nuances of going that way for this project. Remember that the stdlib/ folder is going to go from having `macros/` and `primitives/` to a modular architecture for folders, which is how we are going to make the pretext macros btw.

This is pretext: https://github.com/chenglou/pretext

Also think about stuff we could put in the demos/ folder to showcase the integration with pretext and the latest Spacetime features. Let's not do ora-ventures.com just yet, let's first think about a nice framework for making the stdlib library both lean and "fusion-engine-included", and showcase that with pretext. Consider all angles

The original motivation for PLAN-024. Now unblocked: the text module
(@measure/@balance/@reveal-lines) shipped. Migrate the ora-ventures hero onto it
and fix the older animation syntax in the hero block.

* Current state (projects/ora-ventures.com/)
- Hero MARKUP: index.html lines ~46-65 (.ora-hero__title with two .ora-hero__line
  spans + a .ora-hero__flap split-flap span; .ora-hero__sub; .ora-cta).
- Hero CSS: modules/_hero.st (178 lines, styling only).
- Hero BEHAVIOR: inline in index.st (~lines 260-340) using the OLDER syntax:
  - =@on visible line-1(600ms, immediate: true, easing: ~ease-out-expo) { ... }=
  - =@after line-1 flap-reveal(...) { ... }= / =@after flap-reveal line-2(...)= /
    =@after line-2 sub-reveal= / =@after sub-reveal cta-bounce= (named-stage chain)
  - =@cycle(source: heroTerms, interval, field: "text")= + =@value-change { :entering / :exiting }=
  - =@reveal(split: "chars", ...)= on each .ora-hero__line
- Data: =@type HeroTerm= + =@data fetch $heroTerms : "/data/hero-terms.{locale}.json"=.
- Does NOT yet =@import "stdlib/text"=.

* Goal
1. Add =@import "stdlib/text"= to the hero's module/index.
2. =.ora-hero__title= -> =@balance= so the multi-line headline stays evenly ragged
   and resize-stable (replaces manual two-span line splitting where the design
   allows; keep the split-flap term as its own element).
3. Re-point the kinetic reveal to line granularity where appropriate:
   =@reveal(split: "lines")= now uses pretext line grouping (reflow-free) instead
   of per-char offsetTop work. Keep per-char only where the design demands it.
4. Audit + fix the older animation syntax in the inline hero block against current
   stdlib forms (verify =@on visible name(...)=, =@after <stage> name(...)=,
   =@cycle=, =@value-change= still match registered macro forms; update any that
   drifted). Use =cargo run -- check= + =inspect --layer expansion= to confirm
   each directive matches.
5. Keep AGENTS rule: NO custom JS — only Spacetime macros/primitives.

* Acceptance
- =cargo run -- serve projects/ora-ventures.com/= renders the hero with balanced
  headline + reflow-free line reveal; split-flap term cycle intact.
- Bundle ships pretext ONLY because the hero uses it (lean rail); confirm via grep
  on the built spacetime.js.
- No older-syntax directives left unmatched (=inspect --layer expansion= clean).
- Playwright/visual check of the hero entrance sequence (deferred gating note:
  .test.st assertions blocked on BUG-051 / PLAN-026 — use a Rust emit check or
  manual browser pass meanwhile).

* Dependencies
- Text module: [[id:FEAT-050-stdlib-text-module-submodule-pretext-pri]] (DONE),
  [[id:FEAT-051-fuse-reveal-splitlines-onto-pretext-text]] (DONE).
- Real .test.st gating for the hero is blocked on [[id:BUG-051-test-st-assertions-never-execute-block-b]] / PLAN-026.
- AGENTS.md (projects/): website behavior must be pure Spacetime.

NOTE [2026-06-02]: Hero migrated + verified live in Chromium. Changes (projects/ora-ventures.com/ — gitignored separate repo, edits on disk): (1) added @import "stdlib/text"; (2) @balance on .ora-hero__sub (the genuine wrapping paragraph at max-width:440px — pretext computes balanced width 286/440). NOT on the title: it's hand-authored into 3 intentional editorial rows + a cycling flap, so @balance/reveal-lines would fight the design — kept its char-reveals. (3) FIXED the real breakage: @data fetch $heroTerms had no trailing ; so the URL capture swallowed following comments -> fetched /%22...%22//... -> 404 -> empty heroTerms -> @cycle/@value-change never fired. Added ; -> fetch 200, flap cycles through all terms, value-change :entering/:exiting animates (browser-verified). Root parser bug filed as BUG-052. Older syntax (@on visible/@after/@cycle/@value-change) verified NOT drifted — all match current forms (inspect --layer expansion clean). Lean: pretext injected only because @balance fires. check: 30/30; lib 2093 green.