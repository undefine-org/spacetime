#+TITLE: Gate stdlib/text module .test.st in playwright harness
#+STATE: ITEM
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-055-gate-stdlib-text-module-test-st-in-playw
#+LAYER: testing

* Initial Message

let's think about how we'll extend the stdlib for Spacetime using this as a philosophical underpinning: https://www.gingerbill.org/article/2025/09/08/package-managers-are-evil/


In the context of integrating pretext to make text layout much simpler in Spacetime, especially in ora-ventures.com's hero section (projects/ora-ventures.com/ has some older syntax which the implementation agent will have to fix).

Gather your thoughts and come up with your own understanding of the nuances of going that way for this project. Remember that the stdlib/ folder is going to go from having `macros/` and `primitives/` to a modular architecture for folders, which is how we are going to make the pretext macros btw.

This is pretext: https://github.com/chenglou/pretext

Also think about stuff we could put in the demos/ folder to showcase the integration with pretext and the latest Spacetime features. Let's not do ora-ventures.com just yet, let's first think about a nice framework for making the stdlib library both lean and "fusion-engine-included", and showcase that with pretext. Consider all angles

Register the text module's real-DOM tests in the playwright harness, now that
=.test.st= assertions gate.

* Depends on
- [[id:FEAT-054-make-test-st-assertions-gate-fix-testing]] (assertions must gate first)

* Context
=stdlib/text/tests/measure.test.st= and =reveal-lines.test.st= already exist
(written in PLAN-024 W4) but are NOT registered in the harness \u2014 they would be
false-green until assertions gate. They carry a header note pointing at BUG-051 /
PLAN-026. The text module is meanwhile verified at the emit level by
=tests/text_module_test.rs= (8 mutation-proven tests).

* Scope
1. Add =unit_text_measure= + =unit_text_reveal_lines= back to
   =tests/playwright_st_test.rs= (the commented-out block references them).
2. Confirm they GATE: mutate an assertion, see it fail; revert.
   - NB: pretext needs Canvas + Intl.Segmenter (real Chromium) \u2014 these belong in
     playwright_st, not the headless V8 path.
3. Remove the BUG-051/PLAN-026 header note from the two .test.st files.
4. Decide whether to KEEP =tests/text_module_test.rs= (emit-level) as a fast
   complementary check or fold it in \u2014 recommend KEEP (emit-level is faster + tests
   the lean-rail/demand-injection which DOM tests don't cover).

* Acceptance
- Both text .test.st run + gate under =cargo test --test playwright_st_test=.
- A deliberately-wrong assertion in them FAILS.
- text_module_test.rs still green (or consciously removed with rationale).

* Parent
[[id:PLAN-026-fix-block-body-capture-so-test-st-assert]]

NOTE [2026-06-03]: SUPERSEDED by PLAN-027 W8 (FEAT-064): text .test.st registered into the CDP runner, meaningful only after W0 (gating) + W2 (real layout). Do not execute standalone.