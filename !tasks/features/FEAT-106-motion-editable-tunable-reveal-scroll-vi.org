#+TITLE: Motion Editable — tunable @reveal/@scroll via form-renderer + EditAst AstDirective
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FEAT-106-motion-editable-tunable-reveal-scroll-vi
#+COMPLETION_HASH: 30577b4cf0fd436c

* Initial Message

can you check how that looks like in the admin? Can you compare that with stuff that is present in Framer/online/in mockups? I want it to look good please

PLAN-036 ② — closes the LAST read-only pane. @reveal/@scroll directives become tunable slider cards editing the actual .st source. Design: DESIGN.md §②, mockup 02-motion-editable.png.

ARCHITECTURE (Wave C already delivers the data):
- dev_motion_handler (src/server.rs:3483) ALREADY emits params:[{key,value}] — the tunable numbers extracted by param_re.
- Shared form-renderer (ST.adFormFields → &fld-<widget>) turns schema × values into slider/number fields.
- EditAst AstDirective branch (__content absent → apply_text_patch) ALREADY patches directive props by selector.

NEW RUST (minimal):
1. dev_motion_handler: add per-motion `schema` (key → widget: length for *px/distance, duration for *ms, number for ratios, select for easing enum). Pure delivery shaping, mirrors annotate_widgets. No new machinery.

ADMIN (.st only):
2. Motion pane uses form-renderer: @each($motions as $m){ <fieldset> @each($m.fields as $f){ &$f.tpl($f); } }. &ad-motion-card DELETED (last AP2 card gone).
3. Write via EditAst AstDirective: param edit addresses <file> §<directive> .<key> (existing directive-prop patch path). Source @reveal(duration: 600) literal updates.
4. Easing → &fld-select with CSS easing enum (exists).
5. (with ①) repaint via TokenPreview-style push; else file-watcher reload.

VALIDATION (GREEN = browser DOM + source assertion):
- RED: motion pane shows display card (current); no sliders.
- GREEN: pane renders sliders per param; drag DURATION → @reveal(... duration: N) number in source .st changes; siblings byte-exact (read file).
- live page animation runs at new duration (browser).
- lib: motion.json schema shape test (extend test_templates_and_motion_deliver_sig_and_label). EditAst AstDirective motion-param patch test.
- Verify BUG-085 (nested @each in template body) does NOT bite the nested-param render; workaround if so.
- Reviewer swarm after.

GATE: lib + v8 + integration green; browser proof of slider→source→animation.

NOTE [2026-06-12]: COMPLETE. Motion pane = editable form-renderer over real @scroll/@reveal/@load directives; closes the LAST read-only pane (AP2 fully gone). build_motion_json reads compiled-AST scope FormMatches (killed the regex scrape of fictional syntax); param widget from CapturedValue kind; edit via scoped EditAst. Found+fixed 3 bugs: BUG-097 (scope resolved first selector match not directive-containing block), BUG-098 (apply_text_patch ate )+body on last paren-param — caught by reviewer P1), BUG-096 (bool capture default-fill, deferred). P2 import-file routing fixed. Browser-verified: stagger(number)+easing(text)+scrub(toggle,last-param) all write .st byte-exact, compiles. Commits: ed322009 (server), <admin>, a9e037c6 (review fixes). Reviewer Opus 0.91. Gates: lib 2251/0, integration 172/0, v8 391/9 baseline-only. Mockup 02-motion-editable.png, shipped feat106-shipped.png.