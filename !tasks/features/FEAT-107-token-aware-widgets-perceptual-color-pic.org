#+TITLE: Token-Aware Widgets — perceptual color picker + drag-to-tune scalar sliders
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FEAT-107-token-aware-widgets-perceptual-color-pic
#+COMPLETION_HASH: 2b5fb711a788191b

* Initial Message

can you check how that looks like in the admin? Can you compare that with stuff that is present in Framer/online/in mockups? I want it to look good please

PLAN-036 ③ — the craft layer. Raw hex field → perceptual HSL picker; typed scalar → drag slider. Design: DESIGN.md §③, mockup 03-color-picker.png. DEPENDS ON ① (live perceptual picking is the payoff).

ARCHITECTURE (fits existing widget dispatch — no engine change):
- Widgets dispatch by &fld-<widget> templates (drawer.st). Richer color widget = richer template.
- length/duration already carry format (src/type_system.rs) the widget engine maps.

IMPLEMENTATION (.st + pure @fn only):
1. &fld-color → @portal-relocated popover picker (SAME portal as drawer + media library): SV-square + hue + alpha sliders. Output writes the same data-path input → existing @on input → preview/persist. Picker chrome = declarative markup + reactive bindings.
2. HSV↔hex math = registered pure @fn (hslToHex, hexToHsv) — same class as currency/basename. NO imperative widget object.
3. length/duration → &fld-range: native <input type=range> bound to data-path; min/max/step inferred from unit (px 0-64, ms 0-2000). Value readout = reactive text <- $f.value hole.
4. SV-square fill = $-driven CSS gradient (dogfoods BUG-091).

VALIDATION (GREEN = browser DOM):
- open SCARLET → SV handle drag → hex readout updates live → (with ①) preview hero recolors continuously → release persists to .st.
- range slider: drag 8px → readout + (①) preview radius change; persist.
- v8: hslToHex/hexToHsv pure-fn round-trip (hex→hsv→hex identity within rounding).
- @portal popover open/close delegation (browser).
- Reviewer swarm after.

SCOPE: real surface area (picker geometry). Land ①+② first. GATE: lib+v8+integration green; browser proof of perceptual pick→live→persist.

NOTE [2026-06-12]: COMPLETE. Brand token fields gained design-tool controls: length/duration → drag-to-tune range sliders (radius/reveal); color → expandable perceptual HSV picker (Hue/Sat/Val/Alpha range sliders, gradient tracks). FULLY DECLARATIVE — native type=range, no pointer-drag (the @drag macro exists but HSV sliders gave the same feel with zero custom interaction code). ST.adColorToHsv/adHsvToHex (reuse FEAT-108 parser); adColorPickerSync seeds, adColorSlider recomposes→hex text→existing persist+preview; adRangeInput recomposes num+unit→hidden twin→same path. annotate_widgets length→range-length, duration→range-duration. Browser-verified: radius 8→20→'20px' to .st; picker seed #FF0020→H352, Hue→210→#0080ff persists + derived re-resolves. Reviewer Opus 0.90: math/routing/safety correct, 2 P2 fixed (float-precision cache so alpha-only edit doesn't drift RGB — browser-verified rgba(255,0,32,0.5) exact; saturation gradient uses current value). Commits: <range>, <picker>, <review fix>. Gates lib 2252/0, integration 172/0, v8 397/9 baseline-only. Mockup 03-color-picker.png, shipped feat107-shipped.png + feat107-range.png.