#+TITLE: score-launch as the @score showcase catalog (effects + transitions, HyperFrames-class)
#+STATE: DONE
#+SESSION_ID: 154a974abe45a002
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-08-04T14-22-49-081Z_154a974abe45a002.jsonl]]
#+CUSTOM_ID: FEAT-171-score-launch-as-the-score-showcase-catal

* Initial Message

can you tell me which examples and demos use `@score`? I want to test drive the video creation. And create a catalog that lets us see the new opportunities provided to us by this new feature

* Goal
Turn =demos/score-launch/= from a minimal thesis proof into the *wow* catalog of what =@score= + the W5 render verb enable — effects and transitions at or beyond the HyperFrames registry class (transitions-*, captions, vfx), with zero custom JS (AGENTS rule: Spacetime only).

* Inventory (what exists today)
- Only =demos/score-launch/index.st.md= uses =@score= in demos; examples/ has none.
- Vocabulary (verified in src + stdlib):
  - scores: =@score &.loop(Ns) / &.time(Ns) / &.clip=, placements =&x at 0s for 3s rate 2=, relative =for 50%=, sequencing =->= (compile-time; no runtime repr), =@form score --frag= splicing, nested =&.clip= inheriting parent window.
  - motion keyframes: transforms (translate-x/y/z, scale[-x/y], rotate[-x/y], skew[-x/y]), filters (blur, brightness, contrast, grayscale, saturate, sepia, hue-rotate, invert), opacity, ANY numeric-with-unit property (leading number interpolates), COLORS (rgb/oklch via ST.parseColor), CUSTOM PROPERTIES (=--r: 0 -> 75= set inline per frame → drives clip-path/gradient via var()).
  - control props: easing (incl. stdlib =@form easing --ease-out-expo= presets + =--ease-out-elastic= spring), =range: a to b=, =stagger= / =staggerFrom=.
  - multi-stop keyframes =0 -> 8px -> -6px -> 0= (glitch/jitter possible).
- Render: =cargo run --features cdp -- render demos/score-launch/ --out film.mp4= — duration = longest declared =@score= total (merged AST, imports resolved); captures viewport at page top ⟹ renderable film section must sit at page top, viewport-sized.

* Design
Literate page stays the thesis ("a website and a video differ only in which signal drives the timeline") but becomes visual-first:
1. HERO FILM at page top, 100vh stage, =@score &.time(12s)= — kinetic-type title sequence with scene transitions (blur-dissolve, iris wipe via custom-prop clip-path, glitch flash, scale punch + shimmer sweep). This IS the renderable film.
2. CATALOG specimen sections (each an executable fence): kinetic type / transitions deck / color & light / depth (rotate-x/y) / rate lane / two poles.
3. Render how-to + honesty boundary (update: W5 render SHIPPED; README stale).
- Keep existing gate contracts intact: =.reel .card.title/.body/.cta= (9s loop) and =.rates .dot.*= (4s loop) — tests/score/score-launch.test.st mounts them verbatim.

* Gates
- Extend tests/score/score-launch.test.st with new sections' verbatim mounts (hero film clip moves; iris clip-path var moves; color interpolates).
- Run: =cargo run --features headless -- test tests/score/ --cdp= (CDP = real Chromium; timing claims refuse on headless by design).
- Compile: =cargo run -- check= on the demo; render smoke: produce an mp4 in scratch/ and ffprobe it.
- Update demos/score-launch/README.md (render shipped — "What it does NOT prove" section is stale).

* Constraints
- No custom JS anywhere. Raw CSS (keyframes, clip-path, gradients) is fine — it is not JS.
- No purple-gradient-hero / Inter-headline / rounded-card-left-border tics (docs/antipatterns.md 9/11/14).
- Comments =//@todo= etc. for anything found-but-not-fixed.

* Outcome (2026-08-04) — DONE
Page rewritten as renderable film + effect catalog (§0 film, §1 kinetic type, §2 transitions, §3 color/light, §4 depth, §5 rate, §6 two poles, §7 honesty). Zero custom JS.

** Render smoke: =cargo run --features cdp -- render demos/score-launch/ --out scratch/score-launch.mp4= → 720 frames, 12s @ 60fps, 1920x1080 h264, scene content verified in extracted frames.
** Gates: tests/score/score-launch.test.st (reel+rate, 5 tests), tests/score/score-launch-film.test.st (film/cover/color/stagger, 6 tests, NEW — trace-sampling pattern), two-pole unchanged. Suite: 28/28 on --cdp.
** Compiler bugs found BY the showcase (the point of building it): BUG-275 (inline-body clip consumer watched synthetic name), BUG-276 (Selector variant dropped → nested scopes flatten; + stagger time units), BUG-277 (score loop palindromed on pingpong default). All fixed + gated. BUG-278 (E0952 multiline CSS value with $ref) filed OPEN with the workaround in place.
** Regression: render_test 8/8; pre-existing lib/integration failures (8 + 15) verified unrelated (stale snapshots, ~preset retirement, E0927→E0956 drift, stdlib %form grammar) — none touch drivers/score/expand emission paths changed here.
** Docs: README rewritten (render shipped; old "W5 not yet" claims removed); page §7 honesty updated to match the new gates.
** Follow-up (2026-08-04, render verification): two more defects found by looking at frames
1. Document order: the film must be the FIRST element in the document because
   `spacetime render` captures the viewport from the top. The stage skeleton
   now lives in a top-of-file ```st hidden fence (above the # H1); the §0
   fence keeps the executing+displayed program. Verified by frame extraction:
   frames 60/240/420/660 show the four scenes, no prose.
2. Reactive `background:` shorthand reset — `background: linear-gradient(... $brand.ink ...)`
   lands via setProperty (BUG-091 contract) and the shorthand resets static
   longhands (background-clip: text → border-box) → white slab instead of
   glyph-clipped shimmer. Worked around with background-image longhands in the
   demo; filed BUG-279 for the compiler-level fix. NB: `$`-valued declarations
   are NOT dropped — they are intentionally reactive setProperty calls in
   spacetime.js; spacetime.css legitimately omits them.
Gates after both fixes: 28/28 on --cdp (143s); film re-rendered, 720 frames.
