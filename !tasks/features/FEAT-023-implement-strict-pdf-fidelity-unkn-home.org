#+TITLE: Implement strict PDF-fidelity UNKN home page sections
#+STATE: DONE
#+SESSION_ID: 14cc778b8e323f00
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-28T15-15-29-976Z_14cc778b8e323f00.jsonl]]
#+CUSTOM_ID: FEAT-023-implement-strict-pdf-fidelity-unkn-home
#+DEPENDS: BUG-023-restore-stdlib-aggregate-import-for-time FEAT-022-build-unkn-project-foundation-with-norma
#+LAYER: website

* Initial Message

look at ./projects/unkn/*. I want you to create a full Spacetime website, using the latest Spacetime affordances and folder hierarchy, fully manually verified for visual excellence and adherence ot the specs in the PDF. Be comprehensive in determining and describing your understanding of the work.

* Intention & Purpose
Implement the full UNKN home page from `projects/unkn/UNKN_Desktop_Home.pdf` with very strict fidelity to the PDF's section order, typography hierarchy, whitespace rhythm, black/white editorial restraint, and copy. Creative latitude is limited here: layout and style should stay as close to the PDF as possible; motion and interaction are handled separately.

* Context & Narrative
The PDF is the primary product specification. Text extracted from the PDF establishes this desktop home sequence: navigation; hero; existential mission statement; `We are The (Un)Known`; agency positioning; `More Than Talent Management`; mission; four stats; resonance statement; `What We Do`; three service columns; `Our Roster`; talent positioning copy; `Featured Work`; flagship campaign card; `Why (Un)Known`; six reasons; contact CTA; useful links/social/footer/legal. User decision: build home plus placeholder routes, but home must be comprehensive and visually excellent. Missing imagery may be generated only as abstract monochrome typographic/editorial compositions, no faces and no invented real campaigns.

* Scope
In scope:
- Create or update `projects/unkn/modules/_hero.st`, `_home.st` or section-specific modules (`_mission.st`, `_services.st`, `_roster.st`, `_work.st`, `_why.st`, `_contact.st`), depending on executor's split.
- Create `projects/unkn/data/stats.json`, `services.json`, `reasons.json`, and `work.json` for repeatable arrays.
- Define `@type` contracts in `index.st` or `data/_types.st` for `Stat`, `Service`, `Reason`, and `WorkCard`.
- Use `@data` and `@each` for stats/services/reasons/work-card arrays, while hard-coding hero and mission copy where exact line breaks/typographic handling demand direct control.
- Add abstract monochrome artwork only where necessary to replace absent visual assets.

Out of scope:
- Placeholder route pages.
- Motion implementation beyond static-ready classes/hosts; the motion feature attaches interactions.
- Real talent biographies, real campaign images, real social URLs, or a contact backend.

* Existing Patterns
- `docs/DATA_BINDING_GUIDE.md`: `@type`, `@data`, `@each($items as $item, key: $item.id) { &template($item); }`.
- `stdlib/macros/each.st`: do not nest `@each` inside `@template` bodies; loops belong in parent selector scopes.
- `projects/adovasio/index.st`: editorial portfolio with `@type Project`, `@data projects`, and reusable templates.
- `stdlib/showcases/hero/editorial.st`: editorial hero baseline; use as inspiration, not copy-paste, because PDF fidelity dominates.
- `AGENTS.md`: every image must have descriptive `alt`.

* Tests
Enumerated user workflows and visual requirements:
1. A desktop visitor sees the exact PDF section order without unexpected sections or missing content.
2. Hero communicates `Where Talent Moves Culture.` with `Global Talent Agency - Est. 2025`, paragraph copy, `Find Out More`, and `View talent` CTAs.
3. User can scroll from hero to mission and understand the agency as `The (Un)Known`.
4. User can read mission/stat proof points: `+200 talent roster`, `+50 Brand partners`, `7 Divisions Worldwide`, `+30 Global markets`.
5. User can understand three services: Talent Representation, Brand Partnerships, Creative Strategy.
6. User can see roster and featured work placeholders without mistaking them for real people/campaigns.
7. User can read the six Why (Un)Known reasons with numbering 01-06.
8. User reaches contact CTA and footer links.
9. Mobile visitor gets same content stacked with readable line lengths and no horizontal overflow.

Verification:
- `cargo run -- check`
- `cargo run -- inspect --layer source`
- `cargo run -- inspect --layer emit`
- Desktop screenshot at 1440x1200 or taller stitched/full-page if available.
- Mobile screenshot at 390x844.
- Section-by-section PDF comparison checklist in implementation notes.

* Implementation
** ITEM Extract PDF content map
:PROPERTIES:
:CUSTOM_ID: FEAT-023-implement-strict-pdf-fidelity-unkn-home::extract-pdf-content-map
:END:
- File: `projects/unkn/UNKN_Desktop_Home.pdf`.
- Produce an implementation-local checklist mapping every PDF text block to a target selector/template/data source.
- Preserve exact words except where ASCII-safe punctuation is required in `.st`; use HTML entities for typographic punctuation.

** ITEM Define typed repeatable data
:PROPERTIES:
:CUSTOM_ID: FEAT-023-implement-strict-pdf-fidelity-unkn-home::define-home-data-types
:DEPENDS: FEAT-023-implement-strict-pdf-fidelity-unkn-home::extract-pdf-content-map
:END:
- Files: `projects/unkn/index.st`, `projects/unkn/data/stats.json`, `services.json`, `reasons.json`, `work.json`.
- Types: `Stat { id, value, label }`, `Service { id, number, title, description }`, `Reason { id, number, title, description }`, `WorkCard { id, meta, title, category, description?, image?, imageAlt? }`.
- Keep JSON values truthful to PDF. Do not invent claim-heavy copy.

** ITEM Build static home templates
:PROPERTIES:
:CUSTOM_ID: FEAT-023-implement-strict-pdf-fidelity-unkn-home::build-home-templates
:DEPENDS: FEAT-023-implement-strict-pdf-fidelity-unkn-home::define-home-data-types
:END:
- Files: `index.st` plus home modules.
- Templates: `&unkn-stat-card`, `&unkn-service-card`, `&unkn-reason-row`, `&unkn-work-card`, `&unkn-section-kicker`.
- Parent host selectors perform `@each` invocations; templates remain loop-free to respect parser limits.

** ITEM Style desktop to PDF fidelity
:PROPERTIES:
:CUSTOM_ID: FEAT-023-implement-strict-pdf-fidelity-unkn-home::style-desktop-pdf-fidelity
:DEPENDS: FEAT-023-implement-strict-pdf-fidelity-unkn-home::build-home-templates
:END:
- Use large serif display type, uppercase sans nav/metadata, generous white space, crisp black rules, minimal grayscale panels.
- Match PDF section cadence: broad hero, centered/offset mission statements, grid stats, three-column services, roster/work editorial splits, six-item why grid, dark/contact footer if PDF indicates contrast.
- Avoid decorative color.

** ITEM Adapt mobile and tablet
:PROPERTIES:
:CUSTOM_ID: FEAT-023-implement-strict-pdf-fidelity-unkn-home::adapt-responsive-home
:DEPENDS: FEAT-023-implement-strict-pdf-fidelity-unkn-home::style-desktop-pdf-fidelity
:END:
- Use `@media` from stdlib or valid CSS media blocks.
- Stack grids, preserve hierarchy, ensure nav remains usable, no horizontal overflow, CTAs remain tappable.

* Edge Cases
- PDF has smart apostrophes, multiplication signs, and dashes; `.st` must remain parser-safe.
- No real talent/campaign imagery exists. Abstract placeholders must be clearly art-directed and alt text should describe them as abstract compositions, not factual photos.
- If AI image generation is used, generated files belong under `assets/images/` with descriptive names and checked visual contrast.
- Large white logo assets are invisible on white; use black wordmark in light sections, white wordmark only on dark contact/footer.

* Acceptance Criteria
- Every extracted PDF text block is present once in the home page or explicitly documented as intentionally omitted with rationale.
- Desktop visual structure closely matches the PDF: section order, typography hierarchy, whitespace, black/white palette, and editorial rhythm.
- Mobile preserves all content and interaction targets without layout breakage.
- Repeatable content uses `@type`/`@data`/`@each` where settled: stats, services, reasons, work card.
- No fake claims, fake social links, fake form submission, or real-person placeholder imagery are introduced.

NOTE [2026-04-28]: Home rebuilt section-by-section to match UNKN_Desktop_Home.pdf: stacked the(un)known lockup, hero with red script Moves Culture, existential statement, talent grid (abstract tiles), We are The (Un)Known red script, mission + stats inline, paired ink/cream portraits, resonance with red script line, dark services band with three abstract image cards, roster checkerboard with they-set-them script, featured work card with overlay + we build it script, eyes strip, why grid, contact CTA with Let's Build Together red script. Mobile @media in every section module.