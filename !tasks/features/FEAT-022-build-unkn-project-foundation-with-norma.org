#+TITLE: Build UNKN project foundation with normalized assets and reusable templates
#+STATE: DONE
#+SESSION_ID: 14cc778b8e323f00
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-28T15-15-29-976Z_14cc778b8e323f00.jsonl]]
#+CUSTOM_ID: FEAT-022-build-unkn-project-foundation-with-norma
#+DEPENDS: BUG-023-restore-stdlib-aggregate-import-for-time
#+LAYER: website

* Initial Message

look at ./projects/unkn/*. I want you to create a full Spacetime website, using the latest Spacetime affordances and folder hierarchy, fully manually verified for visual excellence and adherence ot the specs in the PDF. Be comprehensive in determining and describing your understanding of the work.

* Intention & Purpose
Create the `projects/unkn` Spacetime project foundation that all page work depends on: normalized assets, semantic HTML shells, theme tokens, global styles, reusable `&... {}` templates, and the import structure for modules and patterns. The foundation must reflect the PDF brand before any animation polish: black and white editorial identity, supplied logos, supplied fonts, strict naming, and no custom JavaScript outside sanctioned Spacetime primitives.

* Context & Narrative
The source folder currently contains only `UNKN_Desktop_Home.pdf`, `Fonts/`, and `Logos/`. There are no existing `.st` files or project AGENTS. The PDF text defines a global talent agency homepage for The (Un)Known, established 2025, with navigation About, Talent, Work, Expertise, Contact; hero line "Where Talent Moves Culture."; positioning copy; stats; services; roster; featured work; reasons to choose the agency; contact CTA; and footer links. Decisions already settled: use `The (Un)Known` for agency references, `(un)known`/logo for the brand mark, `(un)kn` only in footer copyright; normalize source assets into `assets/fonts/` and `assets/logos/`; keep all image alt text descriptive; use `href="#contact"` for CTAs; use clean dev routes for placeholder pages.

* Scope
In scope:
- Create or update `projects/unkn/index.html`, `projects/unkn/index.st`, `projects/unkn/modules/_theme.st`, `projects/unkn/modules/_globals.st`, `projects/unkn/modules/_nav.st`, `projects/unkn/modules/_footer.st`, `projects/unkn/modules/_layout.st`, and `projects/unkn/modules/_routes.st` if route shell styles need separation.
- Normalize fonts into `projects/unkn/assets/fonts/`: `oranienbaum-regular.ttf`, `sf-pro-text-regular.otf`, `sf-pro-text-bold.otf`, `adobe-handwriting-ernie.ttf`.
- Normalize logos into `projects/unkn/assets/logos/`: black and white full wordmark and icon PNGs with clean names.
- Add `@font` declarations or valid `@font-face` emission using stdlib CSS at-rule support.
- Define reusable templates: `&unkn-shell`, `&unkn-nav`, `&unkn-footer`, `&unkn-route-placeholder`, `&unkn-cta-link`, and small template primitives for section labels/stat cards if appropriate.

Out of scope:
- Full home section styling beyond structural containers; handled by the home fidelity feature.
- Motion/interaction behavior beyond safe focus/hover baseline; handled by motion feature.
- Filling complete route content; handled by route placeholder feature and follow-up.

* Existing Patterns
- `projects/dataiku/index.st`: import order: modules first, patterns after, @preset definitions, @type/@data, then templates.
- `projects/dataiku/modules/_theme.st`: `[data-theme="..."]` selector with CSS custom properties for palette/type/spacing.
- `projects/dataiku/modules/_globals.st`: reset, base elements, utility classes.
- `stdlib/macros/template.st`: `@template &name($params) { ... }` and inline `&name(args);` invocation.
- `stdlib/macros/css-at-rules.st`: `@font("Family") { src: ...; font-display: swap; }`.
- `AGENTS.md`: no custom JS for Spacetime websites; every `<image>`/`img` needs descriptive alt.

* Tests
Workflows to validate first:
1. A blank home shell loads with theme tokens and fonts applied.
2. The logo image is visible on light and dark backgrounds with correct alt text.
3. Navigation links resolve to home anchors or clean route URLs per later features.
4. Shared footer renders exactly once on home and placeholder pages.
5. No parser-forbidden constructs appear in `.st`: no HTML comments, no emoji/Unicode glyphs, no nested `@each` inside `@template` bodies.

Verification:
- `cargo run -- check`
- `cargo run -- inspect --layer source` and `cargo run -- inspect --layer emit` for `projects/unkn/index.st` if available through CLI syntax; otherwise use the documented inspect layer command and capture output.
- Browser smoke at `/` with dev server after implementation.

* Implementation
** ITEM Define asset inventory and normalized names
:PROPERTIES:
:CUSTOM_ID: FEAT-022-build-unkn-project-foundation-with-norma::define-asset-inventory
:END:
- Source files: `projects/unkn/Fonts/*`, `projects/unkn/Logos/*`.
- Destination: `projects/unkn/assets/fonts/`, `projects/unkn/assets/logos/`.
- Preserve originals. Normalize destination names only.
- Record which black/white logo is used for light/dark sections and alt text: full wordmark alt `The (Un)Known wordmark`; icon alt `The (Un)Known monogram`.

** ITEM Create HTML shells
:PROPERTIES:
:CUSTOM_ID: FEAT-022-build-unkn-project-foundation-with-norma::create-html-shells
:DEPENDS: FEAT-022-build-unkn-project-foundation-with-norma::define-asset-inventory
:END:
- Files: `projects/unkn/index.html` plus route shell files created by the route task.
- Home `index.html` owns semantic landmarks only: `<header>`, `<main>`, `<footer>` and section host elements with stable classes/IDs for Spacetime injection.
- Do not place large content in HTML when it belongs in templates; keep content source in `.st` templates/data for reviewable Spacetime architecture.

** ITEM Create theme and global modules
:PROPERTIES:
:CUSTOM_ID: FEAT-022-build-unkn-project-foundation-with-norma::create-theme-globals
:DEPENDS: FEAT-022-build-unkn-project-foundation-with-norma::create-html-shells
:END:
- Files: `_theme.st`, `_globals.st`.
- Use black/white/off-white tokens only unless PDF extraction proves a gray value. Any hardcoded colors outside tokens must be promoted before acceptance.
- Fonts: Oranienbaum for display/editorial serif, SF Pro Text for navigation/body, Adobe Handwriting only as an accent if PDF visual review supports it.

** ITEM Create shared templates
:PROPERTIES:
:CUSTOM_ID: FEAT-022-build-unkn-project-foundation-with-norma::create-shared-templates
:DEPENDS: FEAT-022-build-unkn-project-foundation-with-norma::create-theme-globals
:END:
- File: `projects/unkn/index.st` or `modules/_templates.st` if executor chooses to split.
- Define `&unkn-nav`, `&unkn-footer`, `&unkn-shell`, `&unkn-route-placeholder` using the latest `&... {}` template syntax.
- Include accessibility attributes: `aria-label` for nav, visible focus states, descriptive image alt, skip link if visually acceptable.

* Edge Cases
- Font filenames contain spaces and `copy`; destination names must avoid spaces to reduce quoting/path fragility.
- White logo on white background appears blank in current asset preview; only use white logo on black/dark sections.
- If `@font` macro fails, fall back to standards-compliant `@font-face` in `.st` only after documenting the compiler diagnostic.
- Keep all `.st` text ASCII-safe; use HTML entities (`&rsquo;`, `&mdash;`, `&times;`, `&copy;`) where punctuation must match PDF.

* Acceptance Criteria
- `projects/unkn` has a coherent modern hierarchy: `assets/`, `modules/`, `patterns/`, `data/`, `tests/`, route dirs, root `index.html`, root `index.st`.
- Shared nav/footer/template code is defined once and reused by home and placeholders.
- Supplied brand assets and fonts are normalized and referenced from `/assets/...` paths.
- The site shell compiles with no missing import, asset, or HTML validation diagnostics attributable to this task.

NOTE [2026-04-28]: projects/unkn/ foundation: assets/{fonts,logos}/ normalized, index.html shell, modules/{_theme _fonts _globals _nav _footer _shared _route}.st, shared @template &unkn-nav, &unkn-footer, &unkn-route-placeholder. Cream paper palette + signature red Adobe-Handwriting-Ernie script accent.