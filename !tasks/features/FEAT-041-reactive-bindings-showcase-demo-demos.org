#+TITLE: Reactive bindings showcase demo (demos/)
#+STATE: DONE
#+SESSION_ID: 14f48ced503111a7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-05-29T18-28-09-664Z_14f48ced503111a7.jsonl]]
#+CUSTOM_ID: FEAT-041-reactive-bindings-showcase-demo-demos
#+EFFORT: 1d
#+LAYER: coding-agent
#+COMPLETION_HASH: 403355fa23bd3bb6

* Initial Message

my friend, I want you to do two things that are quite related to each other:
1. look at projects/unkn for the way it structures its files and tell me how you would have structured it
2. look up reactive bindings and how they are done in Spacetime (the $ sigil), as well as full-spacetime work (as in, not using index.html, only index.st and generating the html from it by putting the html inline).

The goal is to build a project that showcases reactive bindings, so that websites can start dealing with userland values instead of just animation-specific values (although one of the strengths of Spacetime is its ability to bring reactive bindings to the animation sphere).

The goal of this is both to make a showcase demo (in demos/) and to use that to find all the BUGs and FEATs and REFACTORs that we need to implement. Do NOT use the 'emacs' way of managing tasks; only the Spell-native way. Think a bit about the type of demo that would make sense to showcase reactive bindings; get inspiration by choosing from awwwards.com too

* Source
Goal: build a demo showcasing reactive bindings so websites deal with userland values, not just animation-specific values. Proving ground for the reactive harmonization (BUG-026).

* Concept
Interactive configurator surfacing userland state: live counter/steppers, build-a-list (add/remove/edit via expression mutations), computed order summary (@computed from $state), two-way filter/search (@input + @show/@if), localStorage persistence + cross-tab sync, and a reactive number feeding @value-change (reactive -> animation bridge). Hybrid thin-shell pattern (index.html mount points + inline @template markup in index.st). No custom JS.

* Status
- Compiler harmonization (BUG-026) DONE + committed (b778b8be); reactive forms now work end-to-end.
- Verified live in-browser: counter increments, derived `text: $count * 2` updates, --st-count CSS var mirrors state. Reactive list (add/remove via $items.concat/.filter, $items.length binding) compiles clean and serves.
- Remaining: author the polished demo under demos/, pick an awwwards-grade interaction, verify all primitives in browser with screenshots.

* Dependencies
- BUG-026 (done). Nice-to-have: BUG-027 (inline data) for self-contained data; can use /data/*.json meanwhile.

* Acceptance
- demos/<name>/ compiles via `cargo run -- check` and serves via `cargo run -- serve`.
- Browser-verified: mutation updates DOM + computed + CSS var; reload + cross-tab persistence; reduced-motion respected; reactive number animates per-digit.
- No JS outside %primitive/@test (AGENTS hard rule).

NOTE [2026-05-30]: DONE (commit 4f47e2a3). Built demos/reactive-configurator/ — "Orbit Team Plan Configurator". Showcases reactive $ signals for USERLAND state with zero custom JS. Verified live in-browser: @data src:inline ($seats, $annual), @on click {$x <- expr} mutations, and file-scope reactive bindings over signals incl. arithmetic ($seats*12), ternary ($annual?"Annual":"Monthly"), and a MULTI-signal expression (.grand-total = $annual ? $seats*12*12*0.8 : $seats*12) that recomputes when either signal changes. Test results: init 3 seats/$36; +2->5 seats/$60 monthly; annual toggle->576, "/year"; dec clamps at 1 (115.2). Thin-shell hybrid (index.html markup + mount points; index.st owns all state/bindings/styling). Files: index.st, index.html, README.md, screenshot.png. Used the build to surface BUG-035 (inline @template renders nothing — only external <template data-component> work), which together with BUG-028 + FEAT-043 forms the cluster blocking a no-external-files full-Spacetime @each list path. NOTE: the cart-demo (examples/) remains the @each+@computed+drawer showcase; this demo focuses on scalar/boolean userland signals + reactive expressions, which is the cleaner pedagogical surface.