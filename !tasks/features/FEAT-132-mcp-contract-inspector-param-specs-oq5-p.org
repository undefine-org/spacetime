#+TITLE: MCP contract inspector (param specs, OQ5 partial)
#+STATE: DONE
#+CUSTOM_ID: FEAT-132-mcp-contract-inspector-param-specs-oq5-p
#+DEPENDS: PLAN-041-plan-039-unified-live-mcp-environment-or FEAT-125-mcp-bundle-extraction-compile-to-bundle FEAT-128-mcp-origin-field-macrorecord-fnrecord-o
#+LAYER: mcp
#+PRIORITY: B
#+COMPLETION_HASH: 309f56d5341ddf80

* Parent
[[id:PLAN-041-plan-039-unified-live-mcp-environment-or][PLAN-041]] Wave 4 (surface).

* Summary
The inspector drawer renders the mounted bundle's contract. v1 contract = the bundle's param specs (name/kind/optional/default), derived at extraction (FEAT-125) — NO need for FEAT-113 signal-interface introspection yet. Leans on existing compile-time validation (E0402 template refs, E0403 slots, E0401 types per docs/COMPILE_TIME_ANALYSIS.md) — the contract is partially validated before the bundle ships.

* Why
C2 (agent-comms) and the inspector drawer (mock m2/m6) need to show what a mounted function accepts. Full signal-interface introspection (FEAT-113) is the long-term contract; for v1, the bundle's param specs ARE a usable contract and require no new introspection rail. Widening later doesn't change transport (contract is a field on Bundle).

* Target
- =stdlib/__mcp__/workbench/= — inspector drawer (cards/templates): renders the mounted region's bundle contract (param rows + type chips + origin chip + bundle size, per mock m2).
- Bundle carries contract = param specs (FEAT-125 already includes this).

* Change
The contract field on Bundle (FEAT-125) is the extension point. v1 minimal = params. Widening to signal/hook vocabulary ([[id:FEAT-113-reference-st-render-data-signal-response][FEAT-113]]) later doesn't change transport.

* Edge
- Bundle with no params -> "no parameters" state.
- Bundle not mounted -> inspector shows empty/placeholder (mock m1).
- Open-detail from error card (m6) -> inspector shows the failing bundle + the error excerpt.

* Acceptance
- Inspector renders the mounted region's params with correct types (mock m2 parity).
- Error state "Open detail" surfaces the failing bundle in the inspector.
- =cargo test= + headless green.

NOTE [2026-06-18]: W4 committed 416194a0. Inspector drawer renders the bundle contract = template param specs (FEAT-125 extraction, no FEAT-113 introspection needed). put_function derives contract_params (name/kind/optional/type/default per template) via contract_params_json; flatten_contract_params folds all functions' params into ONE top-level snapshot array tagged with owning function + origin; @mcp-input maps it to $mcpContractParams; inspector renders a flat @each of &mcp-param-card. FLAT by necessity — a nested @each inside a template BODY emits literally (only the selector/slot form supports nesting). Verified live via puppeteer: 3 param rows render with kind chips (binding=sage/element=amber), types, owner + origin chips. Tests: contract_params_tests (flatten incl. element kind; empty contract). Gates green (see FEAT-130).