#+TITLE: spacetime-design SKILL.md core doctrine refresh (Status, hard rules, junior-designer Position 4-Q + checkpoints)
#+STATE: DONE
#+SESSION_ID: 14cc890346343e2b
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-28T16-31-49-016Z_14cc890346343e2b.jsonl]]
#+CUSTOM_ID: FEAT-027-spacetime-design-skill-md-core-doctrine
#+DEPENDS: AUD-007-spacetime-design-skill-obsolete-content
#+EFFORT: 3h
#+LAYER: coding-agent
#+PRIORITY: #A

* Initial Message

what makes huashu design so good? https://github.com/alchaincyf/huashu-design and is the spacetime-design applying that?

* Intention & Purpose

Refresh =.claude/skills/spacetime-design/SKILL.md= and the supporting =junior-designer-workflow.md= reference so the skill's *spine* — Status, hard rules, the Junior workflow, the references list, the file anchors — reflects current shipped state (PLAN-018 doctor + Review tab) AND imports the huashu doctrine pieces that survive the typed-DSL domain shift: domain embodiment, mandatory STOP-AND-WAIT checkpoints, and the Position 4-questions ritual that forces context before system-design.

* Context & Narrative

PLAN-017 shipped the umbrella skill (FEAT-021). PLAN-018 shipped doctor's =check= phase + dev-panel Review tab (FEAT-016, FEAT-020, BUG-022 closed). Today, =SKILL.md='s Status, Hard Rule #4, Pre-delivery doctor block, and references list still hedge with phrasing like "(when FEAT-020 lands)" and "Until FEAT-020 ships, run this manually" — phrasing untrue the moment the agent reads the file in 2026-04-28.

Separately, the huashu-design analysis (this session) showed spacetime-design covers ~35% of huashu doctrine. The two pieces this FEAT lands are the cheapest, highest-leverage doctrine borrows:

1. *Position 4-questions* (narrative-role · viewer-distance · temperature · capacity-sketch) — forces context before system-design. In Spacetime: which =@page= role does this section play, what device class is the canvas, what brand temperature is appropriate, can the design fit the route's payload budget. Inserted as junior-workflow step before showcase pick.

2. *🛑 STOP-AND-WAIT checkpoints* — explicit halt points where agent must show progress and wait for user confirmation. Lifted directly from huashu's "talk = act" anti-pattern. Two checkpoints: after compile/serve, before review pass.

Both pieces live in =SKILL.md= at the doctrine level AND in =references/junior-designer-workflow.md= at the procedural level.

This FEAT also DELETES migration banners on =runtime-debugging.md= and =compiler-debugging.md= ("Migrated from the legacy `debug-spacetime` skill...") — migration is settled.

* Scope

In-scope:
- =.claude/skills/spacetime-design/SKILL.md= edits per [[id:AUD-007-spacetime-design-skill-obsolete-content]] §SKILL.md (134 lines):
  - Status section (lines 10-26): rewrite per audit table.
  - Hard rules (line 33): rewrite Hard Rule #4 (drop "(When the doctor CLI ships)" caveat).
  - Junior-designer workflow code-fence (lines 38-49): insert Position 4-questions step + 🛑 checkpoint markers.
  - Pre-delivery doctor block (lines 80-91): drop "(Until FEAT-020 ships)" caveat.
  - References list (lines 116-125): delete "Replaces former `debug-spacetime`/`debug-compiler` skill" prose.
  - File anchors (lines 127-134): update FEAT-020 status to DONE.
- =.claude/skills/spacetime-design/references/junior-designer-workflow.md= edits per audit:
  - The loop block (lines 13-25): insert Position 4-questions + 🛑 checkpoint markers.
  - Definition of done (lines 79-86): drop "(when FEAT-020 lands)" caveat.
- =.claude/skills/spacetime-design/references/runtime-debugging.md= line 3-5: delete migration banner. Line 184: dedupe table-header artifact.
- =.claude/skills/spacetime-design/references/compiler-debugging.md= line 3-5: delete migration banner. Line 138: update BUG-022 caveat → "(until [[id:FUP-017]] structural lexer fix lands)".

Out-of-scope:
- Project-style protocol Step 0 — owned by FEAT-028.
- Fallback-advisor 5-school taxonomy — owned by FEAT-029.
- Showcase tree changes — owned by FEAT-030.
- Visual anti-slop section — owned by FEAT-031.
- Review-rubric aesthetic dims + runtime walkers — owned by FEAT-032.
- Doctor headless route loop — owned by FEAT-033.
- This FEAT does NOT introduce new dimensions to =Spacetime.review()= or change CLI behaviour.

* Existing Patterns

- Skill umbrella shape (=SKILL.md= header → procedures → references): established by FEAT-021 (PLAN-017 close-out).
- Skill validate harness: =@tasks/AGENTS.md= line 14-17. Validate via =emacsclient -e "(spacetime--prd-validate-all-cli 'json)"=. Note: skill files NOT in =@tasks/=, so validation is sanity check only.
- =references/junior-designer-workflow.md='s loop format (numbered code fence) lifts from FEAT-021's authoring style.
- huashu mechanical pattern: rule = directive + concrete failure + cost. Apply when authoring Position 4-Q rationale text.

* Tests

T1 — *Skill content integrity*:
- T1.1: =grep -c "(When FEAT-020 ships" .claude/skills/spacetime-design/SKILL.md= = 0.
- T1.2: =grep -c "Until FEAT-020 ships" .claude/skills/spacetime-design/SKILL.md= = 0.
- T1.3: =grep -c "Replaces former" .claude/skills/spacetime-design/SKILL.md .claude/skills/spacetime-design/references/runtime-debugging.md .claude/skills/spacetime-design/references/compiler-debugging.md= = 0.
- T1.4: =grep -c "Position 4-questions" .claude/skills/spacetime-design/SKILL.md .claude/skills/spacetime-design/references/junior-designer-workflow.md= ≥ 2.
- T1.5: =grep -c "🛑" .claude/skills/spacetime-design/references/junior-designer-workflow.md= ≥ 2.

T2 — *Cross-reference integrity*:
- T2.1: =grep -n "FUP-017\|FUP-016\|FEAT-033\|FEAT-020" .claude/skills/spacetime-design/SKILL.md= shows expected anchors.
- T2.2: All =[[id:...]]= anchors in modified files resolve (=org get= each).

T3 — *Validate baseline preserved*: 0 net new errors versus pre-change snapshot.

Manual:
- M1: Open fresh agent session, ask "build me a hero section for a new project". Confirm agent encounters Position 4-questions before showcase pick AND honors at least one 🛑 checkpoint.

* Implementation

** ITEM Edit SKILL.md status + hard rules + procedures (satisfies T1.1, T1.2, T1.4, T2.1)
:PROPERTIES:
:CUSTOM_ID: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-skill-md
:END:

File: =.claude/skills/spacetime-design/SKILL.md=

Concrete edits (verbatim, line numbers per current file as of 2026-04-28):

1. *Line 15* (Status — doctor entry): keep "(partial)" until FEAT-033 lands; reword pointer to =[[id:FEAT-033-cargo-run-doctor-headless-route-loop-wit]]=. Existing FUP-016 anchor stays inside FEAT-033's body.

2. *Line 16* (Status — showcase count): keep "7 of 24" today; FEAT-030 rewrites this when it ships. FEAT-027 makes NO change to this line.

3. *Line 20* (Planned — headless route loop): change anchor from =[[id:FUP-016-...]]= to =[[id:FEAT-033-cargo-run-doctor-headless-route-loop-wit]]=; add note "absorbed FUP-016 into FEAT-033".

4. *Line 24* (Planned — regex-literal lexer): rewrite to "Structural regex-literal-aware lexer fix tracked by [[id:FUP-017-bug-022-structural-fix-regex-literal-awa]]". Drop BUG-022 anchor (DONE).

5. *Line 26* (Hard Rule #4 framing): drop "becomes automatic once FUP-016 lands"; replace with "is enforced by `cargo run -- doctor`'s check phase + the Review tab in devtools, with the headless route loop landing in [[id:FEAT-033-cargo-run-doctor-headless-route-loop-wit]]".

6. *Line 33* (Hard Rule #4): replace =4. **Every delivered website MUST pass `cargo run -- doctor <project>`.** No exceptions. (When the doctor CLI ships per FEAT-020. Until then: hand-run the 5-dim review per [references/review-rubric.md](references/review-rubric.md).)= with =4. **Every delivered website MUST pass `cargo run -- doctor <project>`.** No exceptions.= (the doctor caveat is gone — doctor ships).

7. *Lines 38-49* (Junior-designer workflow code fence): replace 5-step block with 7-step block:

   #+begin_src
   0. Read project's AGENTS.md (if present) and existing index.st.
   1. Run project-style protocol; freeze tokens before authoring.
   2. *Position 4-questions* (answer ALL before picking a showcase):
      - narrative-role: which @page role does this section play (hero / data / closer)?
      - viewer-distance: 10cm phone / 1m laptop / 10m presentation?
      - temperature: quiet / professional / loud / warm / playful?
      - capacity-sketch: does the layout fit the route's payload budget at expected viewport sizes?
   3. Sketch HTML structure with placeholder copy and stub data via @data.
   4. Pick a showcase from stdlib/showcases/ that matches narrative-role + temperature.
   5. Compile early (cargo run -- check), serve early (cargo run -- serve).
      🛑 STOP-AND-WAIT — show user the compiled placeholder before filling in.
   6. Iterate visually. Refine copy and tokens last.
   7. Run review/doctor; fix findings; repeat until score >= 8.
      🛑 STOP-AND-WAIT — present the review report; do not declare "ready" until user signs off.
   #+end_src

8. *Line 90* (Pre-delivery doctor caveat): delete "(Until FEAT-020 ships, run this manually...)"; replace with "Pass requires score ≥ 6 on every route's 5-dim review (extends to 7 dims after [[id:FEAT-032-spacetime-review-aesthetic-dims-hierarch]])."

9. *Lines 118-119* (References list — Replaces former): delete "Replaces former `debug-spacetime` skill" / "Replaces former `debug-compiler` skill" prose.

10. *Line 132* (File anchors — Doctor CLI): change "(FEAT-020, planned)" → "(FEAT-020 DONE; headless loop in [[id:FEAT-033-cargo-run-doctor-headless-route-loop-wit]])".

** ITEM Edit junior-designer-workflow.md (satisfies T1.4, T1.5)
:PROPERTIES:
:CUSTOM_ID: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-junior-workflow
:DEPENDS: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-skill-md
:END:

File: =.claude/skills/spacetime-design/references/junior-designer-workflow.md=

1. *Lines 13-25* (=The loop= code fence): replace 8-step list with 11-step list mirroring SKILL.md's 7-step block plus the file's existing depth (compile early / serve early / iterate / review). Insert step 1.5 ("Position 4-questions") between current step 1 and step 2. Insert 🛑 STOP-AND-WAIT marker after step 6 (serve early) and after step 8 (review/doctor).

2. *Line 84* (=Definition of done=): drop "(when FEAT-020 lands)". Reformat: "- =cargo run -- doctor projects/<name>/= exits 0 with score ≥ 6 across all routes."

** ITEM Drop migration banners + dedupe table-header in runtime-debugging.md (satisfies T1.3)
:PROPERTIES:
:CUSTOM_ID: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-runtime-debugging
:DEPENDS: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-skill-md
:END:

File: =.claude/skills/spacetime-design/references/runtime-debugging.md=

1. *Lines 3-5* (migration banner): delete entire =Migrated from the legacy `debug-spacetime` skill...= blockquote.
2. *Line 184* (duplicate =| File | Purpose |= header): delete duplicate row.

** ITEM Drop migration banner + update BUG-022 caveat in compiler-debugging.md (satisfies T1.3, T2.1)
:PROPERTIES:
:CUSTOM_ID: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-compiler-debugging
:DEPENDS: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-skill-md
:END:

File: =.claude/skills/spacetime-design/references/compiler-debugging.md=

1. *Lines 3-5* (migration banner): delete entire blockquote.
2. *Line 138* (BUG-022 workaround caveat): replace "(until BUG-022 is fixed)" with "(until [[id:FUP-017-bug-022-structural-fix-regex-literal-awa]] structural lexer fix lands; loud diagnostic is what BUG-022 closed with)".

** ITEM Run T1, T2, T3 verification + capture artifact (satisfies all)
:PROPERTIES:
:CUSTOM_ID: FEAT-027-spacetime-design-skill-md-core-doctrine::verify
:DEPENDS: FEAT-027-spacetime-design-skill-md-core-doctrine::edit-junior-workflow FEAT-027-spacetime-design-skill-md-core-doctrine::edit-runtime-debugging FEAT-027-spacetime-design-skill-md-core-doctrine::edit-compiler-debugging
:END:

Steps:
1. Run T1.1..T1.5 grep harness; capture output to =!tasks/plans/plan-artifacts/PLAN-020-doctrine-import/feat-027-grep.txt=.
2. Run T2.1 grep + T2.2 anchor resolution (=org get= for each id mentioned).
3. Run T3 validate baseline; assert no NEW errors versus pre-change snapshot.
4. M1: open fresh agent session per FEAT-021 M1 pattern; transcript at =@research/spacetime-design-doctrine/feat-027.md=.

* Edge Cases

- *Migration banners may be referenced from other skills*: =grep -rn "debug-spacetime\|debug-compiler" .claude/skills/= must return only runtime-debugging.md and compiler-debugging.md mentions BEFORE the edit, and zero hits in =SKILL.md= AFTER the edit. (SKILL.md description line keeps the legacy mention since that's the public skill description; only body's "Replaces former" prose is removed.)
- *Position 4-questions step numbering*: SKILL.md code fence (7 steps) intentionally tighter than references file (11 steps with @data stub, hard rule reminders). Both must remain semantically consistent.
- *Anchor resolution failures during T2.2*: if =FEAT-033= or =FUP-017= ids don't resolve at write time, verify step records the gap. Those ids are created in this plan, so they should resolve once PROJ-003 finishes Phase-Setup.
- *🛑 character rendering*: U+1F6D1 is Unicode emoji. Skill files are markdown (not =.st=); permitted. Confirm AGENTS.md "no Unicode/emoji" rule applies only to =.st= files (it does — see AGENTS.md line 35).
- *Doctor caveat removal asymmetry*: SKILL.md drops FEAT-020 caveat entirely; review-rubric.md keeps single-line attribution but reformats. (review-rubric.md edit is OWNED by FEAT-032, not this FEAT — only flagged here for context.)

* Acceptance Criteria

1. T1.1, T1.2, T1.3, T1.4, T1.5 all pass.
2. T2.1, T2.2 all pass — every =[[id:...]]= in modified files resolves.
3. T3 — pre-change vs post-change validate diff = 0 net new errors / 0 net new warnings.
4. M1 transcript shows agent visibly performing Position 4-questions and honoring at least one 🛑 checkpoint.
5. =git diff .claude/skills/spacetime-design/= shows expected line-level changes per audit table; nothing else.

NOTE [2026-04-28]: SKILL.md doctrine refresh + Position 4-Q + STOP-AND-WAIT + reference banners removed + FUP-017 caveat. Gates T1.1-T1.5 + T2.1-T2.2 pass.