#+TITLE: CMS admin: Pages nav section is inert (no page view)
#+STATE: DONE
#+SESSION_ID: 14f8685429e4ccf1
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-01T18-22-11-112Z_14f8685429e4ccf1.jsonl]]
#+CUSTOM_ID: FEAT-068-cms-admin-pages-nav-section-is-inert-no
#+COMPLETION_HASH: 5ea85b50ffbbeae5

* Initial Message

ok let's think deeply deeply. What would a headless CMS for Spacetime look like? like a headless CMS that has all the nice features of a headless CMS and the power of Spacetime combined. Look at the stdlib and the __dev__ to understand some of the pre-existing experiments, and do a research on the state of headless CMSes, before coming back with your THOUGHTS AND REFLECTIONS (no decisions yet)

* Source
PLAN-028 Wave A ground-truth.

* Gap
The left rail renders a "PAGES" section with "Home" (from site.json .pages), but clicking
it is a no-op — the center keeps showing the previously-active collection. Pages have no
view/behavior in V1. This is consistent with V1 scope (entries, not pages — page
composition is a Stage-3 / Framer-killer concern), but the inert nav item is misleading.

* Options
(a) Make "Home" open a read-only page summary (route + which @data/@each it renders) —
    a useful preview stub.
(b) Visually mark Pages items as non-interactive in V1 (muted, cursor:default) so they
    don't imply broken behavior.
RECOMMENDATION: (b) for V1 (cheap, honest); (a) becomes the seed of the Stage-3 page
builder (PLAN-029 Wave 8).

* Acceptance
- Either the Pages item does something useful, or it is clearly non-interactive and not
  misleading.

* Related
- PLAN-028 Wave C; PLAN-029 Wave 8 (typed page composition); src/server.rs dev_site_handler.

NOTE [2026-06-03]: DONE (Wave C) via option (b): Pages rail items now clearly non-interactive — "soon" badge, cursor:default, hover suppressed, title tooltip "Page editing arrives with the visual builder". Honest about V1 scope; the real page view is PLAN-029 Wave 8. Verified live: soon badge present.