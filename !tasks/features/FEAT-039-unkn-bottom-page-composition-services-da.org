#+TITLE: UNKN bottom-page composition (services dark band / work flagship / why grid / contact / eyes interlude)
#+STATE: DONE
#+SESSION_ID: 14cedfd0e21b5609
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-30T12-08-01-672Z_14cedfd0e21b5609.jsonl]]
#+CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da
#+DEPENDS: FEAT-034-unkn-home-foundation-reset-tokens-assets
#+EFFORT: 4h
#+LAYER: frontend
#+PRIORITY: #A

* Initial Message

can you tell me how you use the spacetime design skill?

* Intention & Purpose

Align the bottom half of the home page (services, roster→work transition, work flagship, eyes close-up interlude, why-6-reasons grid, contact CTAs) with the PDF reference. Wire the real flagship work image, fix the services dark band, and replace the decorative eyes strip with the actual close-up photograph deployed in FEAT-034.

* Context & Narrative

Current state vs PDF for each bottom section:
- *Services*: PDF shows services on a *dark band* (cream paper interrupted by a black section) with red script "What We Do" + display "Three ways we build lasting influence." + Learn More CTA + 3 service cards (numbered 01/02/03, white type). Current build has =.unkn-services= in =_services.st= but on cream background, not dark. Cards exist but spacing/typography needs auditing.
- *Roster→Work transition*: PDF shows the closing line "Our talents don't follow trends. they set them." (handled by FEAT-037 inside =_roster.st=) immediately followed by the dark featured-work card with red script "Campaigns That Moved Culture".
- *Work*: PDF shows a single flagship card with a real photograph (=9k1a9150-2-116.png= = =work-flagship-hero.png=), eyebrow "Featured Work", display headline "Campaigns That Moved Culture" with red script, View All Work CTA, then a dark card with meta "Channel · 2024" + display "Your Flagship Campaign Here" + category chip "Fashion × Lifestyle" + "View Case Study" link, then closing display+script line "We don't follow culture. We build it." Current build matches the structure but has no real photograph (image field doesn't exist in =work.json=).
- *Eyes interlude*: PDF shows a wide letterbox close-up of eyes (=Close_Up_eyes.png= = =close-up-eyes.png=) between the work and why sections — current build has =&unkn-section-eyes()= as a decorative div with no image.
- *Why*: PDF shows 6 reasons in a 2-col x 3-row grid with red number + title (e.g. "01 DEEPLY PERSONAL") + paragraph. Current build has =.unkn-why__list= populated by =@each= over =$reasons=, but the layout uses single-column flow, not the 2-col grid the PDF shows.
- *Contact*: PDF shows eyebrow "Ready to build something that matters?" + red script "Let's Build Together" (NOT display headline as current code has it) + paragraph + 2 CTAs.
- *Footer*: copy fix from FEAT-034 already in place. Position-relative to bottom (already correct).

* Scope

In-scope:
- Rewrite =projects/unkn/modules/_services.st= for the dark band layout + 3 service cards.
- Rewrite =projects/unkn/modules/_work.st= to wire =$work[0].image= field + correct dark-card layout + closing display+script line.
- Rewrite =projects/unkn/modules/_why.st= to use 2-col x 3-row grid layout with 6 reasons from =$reasons=.
- Rewrite =projects/unkn/modules/_contact.st= to render "Let's Build Together" as red script (Adobe Handwriting Ernie), not display.
- Move =&unkn-section-eyes()= template from =index.st= (currently inline) into a new module =projects/unkn/modules/_eyes.st= or replace inline with simple =<figure>= referencing =/assets/images/close-up-eyes.png=.
- Update =projects/unkn/index.st=: imports + page composition for the bottom half.
- Confirm =data/services.json= has 3 entries (currently has 6 — VERIFY: PDF shows only 3 service cards). If 6, prune to 3 (Talent Representation / Brand Partnerships / Creative Strategy) and document in brand-spec.md.

Out-of-scope:
- Hero, strip, statement, identity, mission, resonance, roster (handled by FEAT-035..038).
- Multiple flagship work cards (single placeholder only — future FUP can extend =work.json=).
- New service cards beyond the 3 PDF entries.

* Existing Patterns

- Section dark band: =_globals.st= already has =.unkn-section--dark= class with =background: var(--unkn-bg-dark); color: var(--unkn-paper-on-dark);=. Use as the services section base class.
- =@each= root-level: same pattern as elsewhere.
- =@template &unkn-service-card($s)= already in =index.st= — keep, possibly move to =_services.st= for cohesion.
- =@template &unkn-reason-row($r)= already in =index.st= — keep, possibly move to =_why.st=.

* Tests

Compile-time:
- T1: =cargo run -- check projects/unkn/= passes.

Visual + content:
- M1: Services section: dark background; red script "What We Do" + display "Three ways we build lasting influence." + Learn More CTA + 3 cards numbered 01/02/03 with white text + service name + description.
- M2: Service cards data: exactly 3 (Talent Representation / Brand Partnerships / Creative Strategy). =/data/services.json= has 3 entries.
- M3: Work section: cream background; eyebrow "Featured Work" + display headline + red script "Campaigns That Moved Culture" + View All Work CTA in top-right.
- M4: Work card: dark card with =/assets/images/work-flagship-hero.png= as background image (50% darken via overlay), meta "Channel · 2024" eyebrow, display "Your Flagship" + red script "Campaign Here", category chip "Fashion × Lifestyle", "View Case Study" link.
- M5: Closing line below work card: display "We don't follow culture." + script "We build it." (red).
- M6: Eyes interlude: full-width letterbox image =/assets/images/close-up-eyes.png= between work and why.
- M7: Why section: 6 reasons in 2-col grid (3 rows): each reason has red number + title (e.g. "01 DEEPLY PERSONAL" inline-flex) + paragraph below.
- M8: Contact section: eyebrow "Ready to build something that matters?" + red script "Let's Build Together" (NOT display) + paragraph + 2 CTAs (Partner With Us / Join The (Un)Known).
- M9: Footer: 6 useful-link items (per FEAT-034 fix).

Layout:
- M10: Services 3 cards: 3-col at desktop, 2-col at 721-1100, 1-col at ≤720.
- M11: Why 6 reasons: 2-col at desktop, 1-col at ≤1100.
- M12: Eyes letterbox: aspect-ratio 21:9, width 100%, no margin, edge-to-edge.

* Implementation

** ITEM Audit + prune data/services.json
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::audit-services-data
:END:

- File: =projects/unkn/data/services.json=
- Verify: PDF shows 3 service cards (01 Talent Representation / 02 Brand Partnerships / 03 Creative Strategy). Confirm =services.json= entries match. If file has more than 3, prune to:
  - id: =talent-representation=, number: =01=, title: =Talent Representation=, description: =Full-service representation with strategy, negotiation and partnership management at every stage.=
  - id: =brand-partnerships=, number: =02=, title: =Brand Partnerships=, description: =Connecting talent with globally aligned brands for campaigns that create cultural moments.=
  - id: =creative-strategy=, number: =03=, title: =Creative Strategy=, description: =Content direction, social strategy and personal brand identity built for long-term relevance.=
- Document any pruned entries in brand-spec.md as "future expansion".

** ITEM Rewrite modules/_services.st (dark band)
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-services
:DEPENDS: FEAT-039-unkn-bottom-page-composition-services-da::audit-services-data
:END:

- File: =projects/unkn/modules/_services.st=
- Template (move =&unkn-service-card= here from =index.st=):
  =@template &unkn-service-card($s) {=
  =  <article class="unkn-service" data-service-id="$s.id" data-cursor>=
  =    <span class="unkn-service__number">$s.number</span>=
  =    <h3 class="unkn-service__title">$s.title</h3>=
  =    <p class="unkn-service__desc">$s.description</p>=
  =  </article>=
  =}=
  =@template &unkn-section-services() {=
  =  <section class="unkn-services unkn-section--dark" id="expertise">=
  =    <div class="unkn-services__shell">=
  =      <header class="unkn-services__head">=
  =        <p class="unkn-services__script">What We Do</p>=
  =        <h2 class="unkn-services__title">Three ways we build lasting influence.</h2>=
  =        <p class="unkn-services__sub">From personal brand development to international positioning, every service is designed to compound your cultural and commercial impact over time.</p>=
  =        <a class="unkn-cta unkn-cta--inverse" href="/expertise">Learn More</a>=
  =      </header>=
  =      <div class="unkn-services__grid"></div>=
  =    </div>=
  =  </section>=
  =}=
- CSS:
  - =.unkn-services { width: 100%; padding-block: var(--unkn-space-2xl); }= (background inherited from =.unkn-section--dark=)
  - =.unkn-services__shell { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding-inline: var(--unkn-gutter); display: grid; grid-template-columns: 1fr 2fr; column-gap: var(--unkn-space-2xl); row-gap: var(--unkn-space-xl); align-items: start; }=
  - =.unkn-services__head { display: grid; gap: var(--unkn-space-md); }=
  - =.unkn-services__script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: var(--unkn-text-script-md); line-height: 0.92; }=
  - =.unkn-services__title { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h2); line-height: 1.05; color: var(--unkn-paper-on-dark); }=
  - =.unkn-services__sub { font-family: var(--unkn-font-body); font-size: var(--unkn-text-md); line-height: 1.55; color: var(--unkn-paper-on-dark-muted); max-width: 50ch; }=
  - =.unkn-services__grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--unkn-space-lg); }=
  - =.unkn-service { display: grid; gap: var(--unkn-space-sm); padding: var(--unkn-space-md) 0; border-top: 1px solid var(--unkn-rule-on-dark); }=
  - =.unkn-service__number { font-family: var(--unkn-font-display); color: var(--unkn-accent); font-size: var(--unkn-text-h3); }=
  - =.unkn-service__title { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h3); color: var(--unkn-paper-on-dark); line-height: 1.1; }=
  - =.unkn-service__desc { font-family: var(--unkn-font-body); font-size: var(--unkn-text-md); line-height: 1.55; color: var(--unkn-paper-on-dark-muted); }=
  - =.unkn-cta--inverse { color: var(--unkn-paper-on-dark); border: 1px solid var(--unkn-paper-on-dark); padding: 0.75rem 1.5rem; transition: background var(--unkn-dur-mid) var(--unkn-ease), color var(--unkn-dur-mid) var(--unkn-ease); }=
  - =.unkn-cta--inverse:hover { background: var(--unkn-paper-on-dark); color: var(--unkn-bg-dark); }=
- Mobile breakpoints:
  - =@media("max-width: 1100px") { .unkn-services__shell { grid-template-columns: 1fr; } .unkn-services__grid { grid-template-columns: repeat(2, 1fr); } }=
  - =@media("max-width: 720px") { .unkn-services__grid { grid-template-columns: 1fr; } }=

** ITEM Rewrite modules/_work.st (real flagship image)
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-work
:DEPENDS: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-services
:END:

- File: =projects/unkn/modules/_work.st=
- Template (consume =$works[0]=):
  =@template &unkn-section-work() {=
  =  <section class="unkn-work" id="work">=
  =    <div class="unkn-work__shell">=
  =      <header class="unkn-work__head">=
  =        <p class="unkn-eyebrow">Featured Work</p>=
  =        <h3 class="unkn-work__title-line">Campaigns <span class="unkn-work__title-script">That Moved Culture</span></h3>=
  =        <a class="unkn-cta" href="/work">View All Work</a>=
  =      </header>=
  =      <div class="unkn-work__cards"></div>=
  =      <p class="unkn-work__sign-off">We don&rsquo;t follow culture. <span class="unkn-work__sign-off-script">We build it.</span></p>=
  =    </div>=
  =  </section>=
  =}=
  =@template &unkn-work-card($w) {=
  =  <article class="unkn-work__card" aria-label="$w.title">=
  =    <div class="unkn-work__card-image" style="background-image: url('$w.image');"></div>=
  =    <div class="unkn-work__card-inner">=
  =      <p class="unkn-work__meta">$w.meta</p>=
  =      <h4 class="unkn-work__name">Your Flagship <span class="unkn-work__name-script">Campaign Here</span></h4>=
  =      <div class="unkn-work__foot">=
  =        <span class="unkn-work__category">$w.category</span>=
  =        <a class="unkn-work__study-link" href="/work">View Case Study &rarr;</a>=
  =      </div>=
  =    </div>=
  =  </article>=
  =}=
- Decision: keep title hardcoded "Your Flagship Campaign Here" since the data has only 1 placeholder; when real flagship copy lands, swap to =$w.title= with script-detection logic. Document in brand-spec.md.
- *Better*: actually use =$w.title= directly; the data placeholder already holds "Your Flagship Campaign Here". Use it.
- CSS:
  - =.unkn-work { width: 100%; background: var(--unkn-paper); padding-block: var(--unkn-space-2xl); }=
  - =.unkn-work__shell { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding-inline: var(--unkn-gutter); display: grid; gap: var(--unkn-space-xl); }=
  - =.unkn-work__head { display: flex; align-items: end; justify-content: space-between; gap: var(--unkn-space-md); flex-wrap: wrap; }=
  - =.unkn-work__title-line { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h2); line-height: 1.05; max-width: 14ch; }=
  - =.unkn-work__title-script { display: block; font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: var(--unkn-text-script-md); line-height: 0.92; margin-top: -0.05em; }=
  - =.unkn-work__cards { display: grid; gap: var(--unkn-space-md); }=
  - =.unkn-work__card { position: relative; aspect-ratio: 16 / 9; overflow: hidden; background: var(--unkn-bg-dark); color: var(--unkn-paper-on-dark); }=
  - =.unkn-work__card-image { position: absolute; inset: 0; background-size: cover; background-position: center; transform: scale(1.02); transition: transform var(--unkn-dur-slow) var(--unkn-ease-out-expo); }=
  - =.unkn-work__card-image::after { content: ""; position: absolute; inset: 0; background: linear-gradient(180deg, rgba(13, 13, 13, 0.2) 0%, rgba(13, 13, 13, 0.8) 100%); }=
  - =.unkn-work__card:hover .unkn-work__card-image { transform: scale(1.06); }=
  - =.unkn-work__card-inner { position: absolute; inset: 0; z-index: 2; display: flex; flex-direction: column; justify-content: flex-end; padding: var(--unkn-space-md); gap: var(--unkn-space-sm); }=
  - =.unkn-work__meta { font-family: var(--unkn-font-body); font-size: var(--unkn-text-xs); text-transform: uppercase; letter-spacing: var(--unkn-tracking-meta); color: var(--unkn-paper-on-dark-muted); }=
  - =.unkn-work__name { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h2); line-height: 1.05; }=
  - =.unkn-work__name-script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: 1em; line-height: 0.92; display: inline-block; margin-left: 0.2em; }=
  - =.unkn-work__foot { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: var(--unkn-space-sm); }=
  - =.unkn-work__category { font-family: var(--unkn-font-body); font-size: var(--unkn-text-sm); color: var(--unkn-paper-on-dark-muted); }=
  - =.unkn-work__study-link { font-family: var(--unkn-font-body); font-size: var(--unkn-text-sm); color: var(--unkn-paper-on-dark); border-bottom: 1px solid var(--unkn-paper-on-dark); }=
  - =.unkn-work__sign-off { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h2); text-align: center; line-height: 1.1; }=
  - =.unkn-work__sign-off-script { display: block; font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: 1em; line-height: 0.95; margin-top: 0.05em; }=

** ITEM Author modules/_eyes.st (close-up image)
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::author-eyes
:END:

- File: new =projects/unkn/modules/_eyes.st=
- Contents:
  =/// UNKN Eyes interlude — full-width letterbox close-up between work and why.=
  =@template &unkn-section-eyes() {=
  =  <figure class="unkn-eyes" aria-hidden="true">=
  =    <img src="/assets/images/close-up-eyes.png" alt="" loading="lazy">=
  =  </figure>=
  =}=
- CSS:
  - =.unkn-eyes { width: 100%; aspect-ratio: 21 / 9; overflow: hidden; background: var(--unkn-bg-dark); }=
  - =.unkn-eyes img { width: 100%; height: 100%; object-fit: cover; object-position: center; filter: grayscale(100%) contrast(1.1); }=
- Mobile: =@media("max-width: 720px") { .unkn-eyes { aspect-ratio: 16 / 9; } }=

** ITEM Rewrite modules/_why.st (2-col x 3-row grid)
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-why
:DEPENDS: FEAT-039-unkn-bottom-page-composition-services-da::author-eyes
:END:

- File: =projects/unkn/modules/_why.st=
- Template (move =&unkn-reason-row= here from =index.st=):
  =@template &unkn-reason-row($r) {=
  =  <li class="unkn-reason" data-reason-id="$r.id">=
  =    <p class="unkn-reason__head"><span class="unkn-reason__number">$r.number</span> <span class="unkn-reason__title">$r.title</span></p>=
  =    <p class="unkn-reason__desc">$r.description</p>=
  =  </li>=
  =}=
  =@template &unkn-section-why() {=
  =  <section class="unkn-why">=
  =    <div class="unkn-why__shell">=
  =      <header class="unkn-why__head">=
  =        <h3 class="unkn-why__title">Why <span class="unkn-why__title-script">(Un)Known</span></h3>=
  =        <p class="unkn-why__intro">We offer a strategic, globally connected, and deeply personal approach to every partnership, focusing on creating meaning as well as visibility.</p>=
  =      </header>=
  =      <ul class="unkn-why__list"></ul>=
  =    </div>=
  =  </section>=
  =}=
- CSS:
  - =.unkn-why { width: 100%; background: var(--unkn-paper); padding-block: var(--unkn-space-2xl); }=
  - =.unkn-why__shell { width: 100%; max-width: var(--unkn-content-max); margin-inline: auto; padding-inline: var(--unkn-gutter); display: grid; gap: var(--unkn-space-xl); }=
  - =.unkn-why__head { display: grid; gap: var(--unkn-space-sm); }=
  - =.unkn-why__title { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h2); }=
  - =.unkn-why__title-script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: 1em; line-height: 0.92; }=
  - =.unkn-why__intro { font-family: var(--unkn-font-body); font-size: var(--unkn-text-md); line-height: 1.55; max-width: 60ch; color: var(--unkn-ink-muted); }=
  - =.unkn-why__list { list-style: none; display: grid; grid-template-columns: 1fr 1fr; column-gap: var(--unkn-space-2xl); row-gap: var(--unkn-space-xl); padding: 0; margin: 0; }=
  - =.unkn-reason { display: grid; gap: var(--unkn-space-sm); padding-top: var(--unkn-space-md); border-top: 1px solid var(--unkn-rule-soft); }=
  - =.unkn-reason__head { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h3); display: flex; gap: 0.6em; align-items: baseline; line-height: 1.1; }=
  - =.unkn-reason__number { color: var(--unkn-accent); }=
  - =.unkn-reason__title { color: var(--unkn-ink); text-transform: uppercase; letter-spacing: 0.04em; }=
  - =.unkn-reason__desc { font-family: var(--unkn-font-body); font-size: var(--unkn-text-md); line-height: 1.55; color: var(--unkn-ink-muted); max-width: 45ch; }=
- Mobile: =@media("max-width: 1100px") { .unkn-why__list { grid-template-columns: 1fr; } }=

** ITEM Rewrite modules/_contact.st (script "Let's Build Together")
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-contact
:DEPENDS: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-why
:END:

- File: =projects/unkn/modules/_contact.st=
- Template:
  =@template &unkn-section-contact() {=
  =  <section class="unkn-contact" id="contact">=
  =    <div class="unkn-contact__shell">=
  =      <p class="unkn-contact__eyebrow">Ready to build something that matters?</p>=
  =      <h2 class="unkn-contact__title"><span class="unkn-contact__title-script">Let&rsquo;s Build Together</span></h2>=
  =      <p class="unkn-contact__copy">Whether you&rsquo;re a brand looking to partner with culture, or talent ready to build a legacy &mdash; we want to hear from you.</p>=
  =      <div class="unkn-contact__ctas">=
  =        <a class="unkn-cta unkn-cta--solid" href="/contact">Partner With Us</a>=
  =        <a class="unkn-cta" href="/talent">Join The (Un)Known</a>=
  =      </div>=
  =    </div>=
  =  </section>=
  =}=
- Note: PDF shows "Let's Build Together" rendered in red script (Adobe Handwriting Ernie). The =h2= is purely structural for hierarchy; visual is fully script.
- CSS:
  - =.unkn-contact { width: 100%; background: var(--unkn-paper); padding-block: var(--unkn-space-2xl); text-align: center; }=
  - =.unkn-contact__shell { width: 100%; max-width: 880px; margin-inline: auto; padding-inline: var(--unkn-gutter); display: grid; gap: var(--unkn-space-md); place-items: center; }=
  - =.unkn-contact__eyebrow { font-family: var(--unkn-font-display); font-size: var(--unkn-text-h3); color: var(--unkn-ink); font-style: italic; }=
  - =.unkn-contact__title { font-family: var(--unkn-font-display); font-weight: 400; }=
  - =.unkn-contact__title-script { font-family: var(--unkn-font-script); color: var(--unkn-accent); font-size: var(--unkn-text-script-xl); line-height: 0.95; }=
  - =.unkn-contact__copy { font-family: var(--unkn-font-body); font-size: var(--unkn-text-md); line-height: 1.55; max-width: 50ch; color: var(--unkn-ink-muted); }=
  - =.unkn-contact__ctas { display: flex; gap: var(--unkn-space-sm); flex-wrap: wrap; justify-content: center; margin-top: var(--unkn-space-md); }=

** ITEM Update index.st imports + composition
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::index-update
:DEPENDS: FEAT-039-unkn-bottom-page-composition-services-da::rewrite-contact
:END:

- File: =projects/unkn/index.st=
- Add import: =@import "./modules/_eyes.st"=
- Move =@template &unkn-service-card= and =@template &unkn-reason-row= and =@template &unkn-work-card= out of =index.st= (they're now in their respective modules — verify and remove from index.st).
- =main { ... }= body, final order:
  =&unkn-section-hero();=
  =&unkn-section-strip();=
  =&unkn-section-statement();=
  =&unkn-section-identity();=
  =&unkn-section-mission();=
  =&unkn-section-resonance();=
  =&unkn-section-services();=
  =&unkn-section-roster();=
  =&unkn-section-work();=
  =&unkn-section-eyes();=
  =&unkn-section-why();=
  =&unkn-section-contact();=
- Add a root-level =@each= for work cards in the work section grid:
  =.unkn-work__cards { @each($works as $w, key: $w.id) { &unkn-work-card($w); } }=

** ITEM Manual verify
:PROPERTIES:
:CUSTOM_ID: FEAT-039-unkn-bottom-page-composition-services-da::manual-verify
:DEPENDS: FEAT-039-unkn-bottom-page-composition-services-da::index-update
:END:

- =cargo run -- doctor projects/unkn/ --format pretty=. =/= scores ≥ 6 across 7 dims.
- Manual scenarios M1-M12. Capture =screenshots/services-dark.png=, =screenshots/work-flagship.png=, =screenshots/eyes-interlude.png=, =screenshots/why-grid.png=, =screenshots/contact-script.png=.

* Edge Cases

- *Service card count*: if =services.json= had >3 entries before pruning, the grid =repeat(3, 1fr)= would still render them but would over-flow. Pruning to exactly 3 is mandatory.
- *=$w.title= containing "Campaign Here"*: the script-mark is hardcoded around "Campaign Here" in the template. When a real flagship is added later, =$w.title= must contain a special character that the template recognizes — OR add a =$w.titleScript: string= field for the script portion. *Decision*: extend =@type WorkCard= with =titleMain: string= and =titleScript: string= now, populate from =data/work.json= (split "Your Flagship" / "Campaign Here"), and use both in the template. Update FEAT-034's =work.json= edit to include this split.
- *Eyes image grayscale filter*: =filter: grayscale(100%)= is GPU-accelerated. No performance concern.
- *Why grid even-row alignment*: when reasons have varying paragraph lengths, rows may have ragged baselines. =align-items: start= ensures top-alignment. Acceptable.
- *Contact "Let's Build Together" line break*: at narrow viewports the script may wrap awkwardly. =max-width: 14ch= on =.unkn-contact__title-script= forces controlled wrapping or kept on one line. Audit at 360px.

* Acceptance Criteria

- =cargo run -- check projects/unkn/= exits 0.
- =cargo run -- doctor projects/unkn/ --format json= for =/= scores ≥ 6.
- Services section renders on dark background with 3 cards, red script "What We Do".
- Work card displays =/assets/images/work-flagship-hero.png= as background with overlay.
- Eyes interlude renders =/assets/images/close-up-eyes.png= edge-to-edge between work and why.
- Why section renders 6 reasons in 2-col grid (1-col mobile).
- Contact "Let's Build Together" rendered in red Adobe Handwriting Ernie script (NOT display).
- Page composition order: hero / strip / statement / identity / mission / resonance / services / roster / work / eyes / why / contact.
- Screenshots attached.
