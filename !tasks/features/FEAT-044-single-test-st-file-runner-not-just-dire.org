#+TITLE: single .test.st file runner (not just directory)
#+STATE: ITEM
#+SESSION_ID: 14f48ced503111a7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-05-29T18-28-09-664Z_14f48ced503111a7.jsonl]]
#+CUSTOM_ID: FEAT-044-single-test-st-file-runner-not-just-dire

* Initial Message

Ok do me a full summary of the spec. Now we're PLANNING, and once the plan is valid, we ORCHESTRATE. Get yourself ready and pumped

* Motivation
`cargo run --features headless -- test <dir> --headless` discovers *.test.st recursively, but
there is NO way to run a SINGLE test file in isolation. During PLAN-023 W3/W4 (dynamic HTML
hydration) this is constant friction: iterating on one reactive test forces a full-suite run
(500-800ms V8+LinkedOM baseline × all files). Confirmed via test-system recon (agent
2-MapTestSt): run_test()/discover_tests() walk directories; a file path likely works only if
it ends in .test.st but is not first-class.

* Proposal
Accept a single .test.st FILE path (already `paths: Vec<PathBuf>` in the Test subcommand,
src/main.rs:228). Ensure discover_tests handles a direct file arg (not only a dir), and
`--filter` composes with it. Add a convenience: `test <file.test.st> --headless` runs just
that file. Optionally a `--filter`-by-test-name within the file (already exists as --filter).

* Acceptance
- `cargo run --features headless -- test tests/integration/counter.test.st --headless` runs
  ONLY that file's tests.
- directory arg behavior unchanged.
- TDD: a test_runner unit test asserting discover_tests(file) == [file] for a .test.st path.

* Source map (from recon)
- src/main.rs:228-242 (Test subcommand), src/main.rs:2200-2300 (run_test), 1800-1900
  (run_headless_tests), src/test_runner.rs:140-230 (discover_tests / DiscoverOptions).
