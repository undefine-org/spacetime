#+TITLE: CMS admin: empty/titleless entries render as "Entry N" (singles + blanks)
#+STATE: DONE
#+SESSION_ID: 14f8685429e4ccf1
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-01T18-22-11-112Z_14f8685429e4ccf1.jsonl]]
#+CUSTOM_ID: FEAT-069-cms-admin-empty-titleless-entries-render
#+COMPLETION_HASH: abc161511700a75a

* Initial Message

ok let's think deeply deeply. What would a headless CMS for Spacetime look like? like a headless CMS that has all the nice features of a headless CMS and the power of Spacetime combined. Look at the stdlib and the __dev__ to understand some of the pre-existing experiments, and do a research on the state of headless CMSes, before coming back with your THOUGHTS AND REFLECTIONS (no decisions yet)

* Source
PLAN-028 Wave A ground-truth.

* Observations (two related cases)
1. TITLELESS TYPES: StripRow has no title-role field (display.title=null — it's
   image+category data), so rows render "Entry 1/2/3". Acceptable fallback, but a better
   default would be the thumbnail + the chips/category value as a secondary label.
2. BLANK ENTRIES: data/talent.json contains a trailing ALL-EMPTY entry
   ({"id":"","name":"",...}) — a leftover from a prior "add entry" that was never filled.
   The admin renders it as "Entry 10" with an empty thumbnail. NOTE: this is pre-existing
   test debris in the fixture, NOT an admin bug — but it exposes that newly-added blank
   entries look orphaned until edited.

* Scope (Wave C)
- For titleless types: fall back to (thumbnail + first non-empty string field) instead of
  a bare "Entry N".
- For freshly-added blank entries: auto-open the drawer on add (so the writer immediately
  fills it) and/or label as "Untitled <Type>" rather than "Entry N".
- SEPARATELY: clean the stray empty entry from projects/unkn/data/talent.json (fixture
  hygiene) — 10→9 entries. (Confirm with user before mutating their project data.)

* Acceptance
- Titleless rows show a meaningful label; adding an entry opens its editor; no orphan
  "Entry N" rows for blank adds.

* Related
- PLAN-028 Wave C, stdlib/__admin__/admin-app.st; projects/unkn/data/talent.json (debris).

NOTE [2026-06-03]: DONE (Wave C). fallbackLabel(): titleless types now labeled by chips/first-non-empty-string-field (skipping id/slug) else "Untitled N", not "Entry N". Verified: StripRows show "Beauty"/"Entertainment"/"Fashion". The stray blank talent entry was test debris from my session — unkn data restored via git (9 talents, no blank).