#+TITLE: Add clean UNKN placeholder routes using shared Spacetime templates
#+STATE: DONE
#+SESSION_ID: 14cc778b8e323f00
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-28T15-15-29-976Z_14cc778b8e323f00.jsonl]]
#+CUSTOM_ID: FEAT-025-add-clean-unkn-placeholder-routes-using
#+DEPENDS: FEAT-022-build-unkn-project-foundation-with-norma
#+LAYER: website

* Initial Message

look at ./projects/unkn/*. I want you to create a full Spacetime website, using the latest Spacetime affordances and folder hierarchy, fully manually verified for visual excellence and adherence ot the specs in the PDF. Be comprehensive in determining and describing your understanding of the work.

* Intention & Purpose
Create lightweight but polished placeholder routes for Talent, Work, Expertise, About, and Contact using shared Spacetime templates and clean dev routes. These pages should prove the site architecture and navigation are real while clearly deferring full route content to a follow-up.

* Context & Narrative
The user explicitly requested home page plus lightweight placeholder routes using reusable `&... {}` syntax, with FUPs for filling routes if needed. The selected route model is clean dev routes: `projects/unkn/talent/index.html` + `index.st` and equivalents, served as `/talent`, `/work`, `/expertise`, `/about`, `/contact`. Current export code primarily handles root `index.html` and `pages/**/*.html`, so a follow-up will track export-route parity if needed. Placeholder pages must not invent detailed talent rosters, work case studies, bios, social links, or contact submission behavior.

* Scope
In scope:
- Create directories and route shell files after approval: `projects/unkn/talent/index.html`, `talent/index.st`, `work/index.html`, `work/index.st`, `expertise/index.html`, `expertise/index.st`, `about/index.html`, `about/index.st`, `contact/index.html`, `contact/index.st`.
- Reuse root/shared templates for nav/footer/shell rather than duplicating markup.
- Each route gets: shared nav, route title, short PDF-derived intro, abstract monochrome composition or typographic block if needed, CTA back to `/#contact` or `/`, and a clear note that detailed content is forthcoming only if visually appropriate.
- Nav links point to clean route URLs for top-level nav and home CTAs remain anchors where settled.

Out of scope:
- Full route content expansion.
- Backend contact forms or mailto placeholders.
- Static export tooling changes beyond documenting the limitation/FUP.

* Existing Patterns
- `src/server.rs` catch-all route handling: directory index routes serve `site_dir/<route>/index.html` and inject matching `index.st` if present.
- `src/server.rs::derive_st_path`: `foo.html -> foo.st`, `index.html -> index.st`.
- `src/export.rs`: exports root `index.html`, `pages/`, and supporting `data/assets/locales`; clean root dirs may need future export parity.
- `stdlib/macros/template.st`: shared `&unkn-route-placeholder($route)` template can reduce duplication.
- `projects/dataiku/index.st`: shared templates and module imports.

* Tests
User workflows:
1. Visitor clicks About in nav and reaches `/about` with shared brand/nav/footer.
2. Visitor clicks Talent and sees a polished placeholder that does not invent real talent.
3. Visitor clicks Work and sees PDF-derived work positioning plus no fake campaign claims beyond the PDF's flagship placeholder.
4. Visitor clicks Expertise and sees the three service pillars from the home spec summarized.
5. Visitor clicks Contact and sees the PDF CTA language with no fake form submission.
6. Visitor can navigate back home from every placeholder.
7. Browser refresh on each route works in dev server.
8. Each route has page-specific title/metadata if HTML shell supports it.

Verification:
- `cargo run -- check`
- Visit `/talent`, `/work`, `/expertise`, `/about`, `/contact` in dev server.
- `cargo run -- inspect --layer source` or route-specific entry inspection where supported.
- Capture at least one desktop screenshot of a route and one mobile screenshot if time permits; home screenshots are required elsewhere.

* Implementation
** ITEM Define route content brief
:PROPERTIES:
:CUSTOM_ID: FEAT-025-add-clean-unkn-placeholder-routes-using::define-route-brief
:END:
- Write a route map in implementation notes or data: route path, title, intro copy, CTA label, source PDF phrase.
- Copy must be PDF-derived and conservative. No invented services beyond the three pillars and six reasons.

** ITEM Build route shell files
:PROPERTIES:
:CUSTOM_ID: FEAT-025-add-clean-unkn-placeholder-routes-using::build-route-shells
:DEPENDS: FEAT-025-add-clean-unkn-placeholder-routes-using::define-route-brief
:END:
- Files: route `index.html` and `index.st` entries.
- Each route `.st` should import shared root modules/templates through relative imports, not duplicate whole style systems.
- If relative imports from nested dirs are fragile, create a shared `modules/_shared.st` or use correct `../` paths and verify.

** ITEM Implement shared placeholder template
:PROPERTIES:
:CUSTOM_ID: FEAT-025-add-clean-unkn-placeholder-routes-using::implement-placeholder-template
:DEPENDS: FEAT-025-add-clean-unkn-placeholder-routes-using::build-route-shells
:END:
- Template: `&unkn-route-placeholder($label, $title, $intro, $ctaHref, $ctaLabel)` or an object-param equivalent.
- Use root nav/footer templates.
- Ensure route pages can have motion hooks from the motion feature without adding route-specific custom JS.

** ITEM Verify clean routes and nav state
:PROPERTIES:
:CUSTOM_ID: FEAT-025-add-clean-unkn-placeholder-routes-using::verify-clean-routes
:DEPENDS: FEAT-025-add-clean-unkn-placeholder-routes-using::implement-placeholder-template
:END:
- Confirm `/about`, `/talent`, `/work`, `/expertise`, `/contact` all load directly and via nav.
- Confirm 404 behavior remains normal for unknown routes.
- Record export limitation and link to FUP if static build does not include clean route directories.

* Edge Cases
- Root home CTA `View talent` may point to `/talent`; `Find Out More` can point to `#about` or `/about` depending final home navigation. Preserve settled `href="#contact"` for contact CTAs.
- Nested route `.st` paths must not accidentally import `stdlib` twice in a way that creates duplicate timelines; verify diagnostics.
- Route pages should not include fake social links; footer can show text `Follow our socials` without inactive fake URLs unless implementation uses `#` safely.
- If static export misses route dirs, do not silently declare export complete; create/update the FUP.

* Acceptance Criteria
- Five clean dev routes exist and render as polished placeholders.
- Shared templates are reused; nav/footer are not copy-pasted five times.
- Route content is conservative, PDF-derived, and honest about being placeholders.
- Direct refresh and nav clicks work for every route in dev server.
- A follow-up item exists for full route content and export-route parity if needed.

NOTE [2026-04-28]: Five clean dev routes: talent/ work/ expertise/ about/ contact/. Each pair index.html + index.st imports modules/_shared.st and invokes &unkn-route-placeholder($label, $title, $intro, $ctaHref, $ctaLabel, $note). All return HTTP 200 with shared chrome.