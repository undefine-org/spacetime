#+TITLE: Make .test.st assertions gate (fix testing-macro cluster + audit)
#+STATE: ITEM
#+SESSION_ID: 14f94e45c6c3f247
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-02T11-06-49-499Z_14f94e45c6c3f247.jsonl]]
#+CUSTOM_ID: FEAT-054-make-test-st-assertions-gate-fix-testing
#+LAYER: testing

* Initial Message

let's think about how we'll extend the stdlib for Spacetime using this as a philosophical underpinning: https://www.gingerbill.org/article/2025/09/08/package-managers-are-evil/


In the context of integrating pretext to make text layout much simpler in Spacetime, especially in ora-ventures.com's hero section (projects/ora-ventures.com/ has some older syntax which the implementation agent will have to fix).

Gather your thoughts and come up with your own understanding of the nuances of going that way for this project. Remember that the stdlib/ folder is going to go from having `macros/` and `primitives/` to a modular architecture for folders, which is how we are going to make the pretext macros btw.

This is pretext: https://github.com/chenglou/pretext

Also think about stuff we could put in the demos/ folder to showcase the integration with pretext and the latest Spacetime features. Let's not do ora-ventures.com just yet, let's first think about a nice framework for making the stdlib library both lean and "fusion-engine-included", and showcase that with pretext. Consider all angles

Make =.test.st= assertions actually gate. The PLAN-026 W1 core fix (block bodies
compile) landed; this fixes the testing macros so a deliberately-false test FAILS,
then audits the existing suite.

* Depends on
- [[id:BUG-053-testing-macros-then-eval-wait-mock-unmat]] (the root defects)
- PLAN-026 W1 core (DONE, commit 97723e7c)

* Scope
1. Fix BUG-053 F2/F3: @then/@eval/@wait/@mock must MATCH (form-matcher /
   prefix-index audit, or load stdlib/testing into the parse-time registry).
2. Fix BUG-053 F1: @assert =(expr) "msg"= captures condition=(expr), message="msg".
3. Negative-gate proof (the acceptance):
   - =@assert (1 === 2) "x"= -> test FAILS
   - =@then .does-not-exist should exist= -> test FAILS
   - a known-good assertion still PASSES; a 42->999 mutation now FAILS
4. AUDIT every existing =.test.st= (=cargo run --features headless -- test tests/
   --headless= + =cargo test --test playwright_st_test=): with assertions now live,
   triage each newly-failing test \u2014 genuine product bug (fix or file) vs bad test
   (correct the test). Capture the triage in this item. The playwright_st baseline
   was 17 pass / 13 fail (pre-existing) \u2014 re-baseline after the fix.

* Acceptance
- Negative gate passes (false assertions FAIL).
- Full headless + playwright suites re-baselined; every failure triaged with a
  disposition (real bug filed/fixed, or test corrected).
- No assertion type silently no-ops.

* Parent
[[id:PLAN-026-fix-block-body-capture-so-test-st-assert]]

NOTE [2026-06-03]: SUPERSEDED by PLAN-027: gating fix → W0 (FEAT-056); corpus audit → W8 (FEAT-064). Do not execute standalone.