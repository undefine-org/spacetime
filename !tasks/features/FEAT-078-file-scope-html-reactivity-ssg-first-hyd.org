#+TITLE: File-scope HTML reactivity (SSG-first hydration)
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FEAT-078-file-scope-html-reactivity-ssg-first-hyd
#+COMPLETION_HASH: 282cb1b7e3c6fba6

* Initial Message

hey what would it look like if the admin app were done multi-file? what reactive primitives and modular programming principles (and other missing constructs that you find) do you need to make this an amazing DX for developing this kind of app?

* Parent
[[id:PLAN-030-spacetime-app-platform-declarative-schem]] / O2 convergence. The file-scope half
of the unified reactive HTML model (templates = FEAT-077, done).

* Spec
FULL spec: [[file:../../@research/spec-file-scope-html-reactivity.org]] (compaction-proof,
written from scratch, verified against code).

* One-line
Make file-scope `$signal` HTML interpolation REACTIVE while preserving the SSG contract: served
HTML = real static initial values (first-paint/SEO/no-JS), JS HYDRATES holes IN PLACE (not
build-from-empty). Global scope (SpacetimeLocal). Today: emit_hole emits `<!--st-hole-->` comment
placeholders (SSG-only, inert) \u2014 the PLAN-023 W3/W4 reactive-emit gap.

* Waves (see spec): F0 spike (marker+manifest shape) -> F1 hydration emit -> F2 runtime
hydrateHoles -> F3 attr/multi-hole/filters -> F4 cutover (delete comment-placeholder path) ->
F5 unify with template builder (Build vs Hydrate mode, one lowering).

* Gates: SSG invariant (real initial values, no <!--st-hole-->, correct with JS off) + reactive
update in headless + reviewer swarm per wave.

NOTE [2026-06-09]: DONE (F0-F4). src/emit/html_hydrate.rs::emit_hydrated lowers file-scope HtmlExpr -> static-initial HTML (text hole -> span data-st-hole=N marker; attr hole -> real attr + data-st-attr-name=N) + Global-scope hydration JS (apply/render/local:<dep>:updated). Initials from build_scope_tree.global_states. emit_html_blocks returns (html,js), threads monotonic hole-id across blocks. F4: emit_hole no longer emits comment placeholder. Reviewer swarm -> BUG-067 (4 fixed: mixed-attr JS injection, cross-block id collision, content-attr misroute, span-in-RCDATA) + latent build_scope_tree number/bool->null fix. V8-verified DOM behavior. Gates: lib 2140/0, integration 162/0, v8 313/12 (pre-existing media), headless 355/254 baseline. HEAD 53e204e0. Later: widen static-initial evaluator beyond bare $x; SSG export path (compiled.html dev-server only today).