#+TITLE: Block-inner self-describing grammar (cb_block nested items)
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FEAT-080-block-inner-self-describing-grammar-cb-b
#+COMPLETION_HASH: 0d67258723879b56

* Initial Message

Ok, start with Phase B. Use a wave manifest with in-chat mid-flight check-ins exlpaining what's going on, what' you've done, and what you're doing next. use main thread for architectural work and reviewer swarms in between waves. add as many gap implementation as needed; you have unlimited time budget. file FEAT/BUG you find mid-flight using the org tool and fix/implement them mid-flight

* Parent
FEAT-079 Phase B. Reviewer swarm (agents 15/16) P2 architectural: reify_block re-segments the
`.sel { ... }` block inner + string-classifies each sub-construct in Rust (parse_class_toggle,
parse_on_event_block_scoped, parse_self_property_binding, make_content_binding). This leaves
construct classification in Rust, contrary to the self-describing mandate, and BLOCKS the B3g
exit-gate grep (which forbids parse_class_toggle/parse_on_event/etc.).

* Fix
Model the block inner as a nested grammar in component-body.st: a `cb_block_item` Choice over
scoped constructs (@on / class toggle / self-prop / content binding), and cb_block captures
`( $items:cb_block_item )*` instead of an opaque `balanced('}')` blob. reify_block then dispatches
on grammar branch labels (value-semantics), mirroring the top-level component_item pattern \u2014 the
scoped @on diagnostic (E0906) flows through naturally.

NB content-binding `prop: $expr` is the tricky arm (a bare `ident: $expr` with NO leading
sigil). Needs a leaf like `$prop:ident ":" "$" $val:balanced(';')` distinct from cb_toggle
(which starts with `.`).

* Gate
Differential (corpus + fleet) stays byte-identical; the scoped reify_* helpers consume grammar
records only. Enables deletion of parse_class_toggle/parse_self_property_binding/parse_on_event*
string-structural logic at B3f (keeping only value-semantics action/dep helpers).

NOTE [2026-06-08]: Added cb_block_item Choice (cb_root_on/cb_toggle/cb_self_prop/cb_content_bind) + cb_content_bind leaf to component-body.st. reify_block now classifies block-inner segments via the grammar (classify_named_item) + dispatches on branch labels, NOT Rust string-prefix checks. Generalized component_item_extractor->named_choice_extractor(name) + classify_component_item->classify_named_item (shared top-level + block-inner). Corpus 43/43 + fleet 25/25 byte-identical, lib 2156/0. Block classification no longer in Rust; the string parse_* helpers remain reachable only via parse_component_body (die at B3f).