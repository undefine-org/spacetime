#+TITLE: Paste gate + nesting + advanced marks (editable wave 3)
#+STATE: DONE
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FEAT-101-paste-gate-nesting-advanced-marks-editab
#+COMPLETION_HASH: f37b02efa7bfab66

* Initial Message

PLAN-031 -> read it, show me the boldest version of implementing @editable, and how you'd integrate it to the current admin

Parent: [[id:PLAN-031-editable-structured-rich-text-input-mult]] — Wave 3. Depends: FEAT-098, FEAT-099, FEAT-100.

* Target
- stdlib/editable/primitives/editable-surface.st (paste handler)
- src/sync (server-side paste lift endpoint OR client lift + server validate)
- stdlib/editable/blocks/list.st (ul>li nesting), columns.st (multi-&hole)
- stdlib/editable/tests/paste.test.st, nesting.test.st, live-marks.test.st

* Change
- PASTE GATE: clipboard HTML → html5ever (FEAT-095 rails) → tree → unify against
  INVERTIBLE mark/block bodies (the lens backward direction): <a href>…</a>
  matches &link's body → {type:"link",attrs:{href},content}. Non-matching nodes
  unwrap (children kept); residue text-only. Non-invertible marks: explicit
  match clause (design in-feature) or excluded.
- NESTING: list (ul>li), quote>p; mark combinations render order = registration
  order. Multi-hole blocks (&columns(&left,&right)): every &hole = nested
  editable region; position map extended (this is the deferred hard part).
- ADVANCED show-pieces (each ≤1 file, prove the model):
  - &mention(source: $x) partial application — context vs content demo + test
  - live-data mark (&price w/ catalog binding) — AST stable, projection reactive
  - animated mark (:entering on &highlight) — @clock/@timeline test (ANIMATION.md)

* Acceptance
- @test paste corpus (incl. Word/GDocs-ish soup, script/onerror payloads):
  only registered nodes survive; @property fuzz arbitrary HTML strings → lifted
  doc always validates against schema (headless for lift purity; CDP for real
  clipboard event)
- nesting round-trip headless; &columns каждый hole edits independently (CDP)
- @timeline claims on &highlight :entering (CDP, timing rung)

NOTE [2026-06-11]: DONE 2026-06-10 (core: paste gate + showcases; full nesting deferred).
- public/runtime/editable.js: E.liftHtml(html, document) = the lens BACKWARD
  direction. Parses pasted HTML, builds tag→mark/block reverse map from the schema
  (invertible entries only), keeps schema-known nodes, unwraps unknown tags (text
  kept), DROPS rawtext content (script/style/iframe/svg/\u2026 \u2014 not even as text).
  E.insertDoc(doc, pos, other) merges a lifted doc at the cursor (first block
  inline, rest appended).
- stdlib/editable/primitives/editable-surface.st: paste handler routes
  insertFromPaste with text/html through liftHtml \u2192 insertDoc; text/plain fallback.
- stdlib/editable/builtins.st: &pre code block + show-pieces &highlight($color
  string) (typed-attr mark, zero engine change) + &kbd (structural mark).
Tests:
- 4 lift tests (paragraphs+marks, link href, unknown-tag drop, rawtext drop) +
  FUZZ (50 seeds of HTML soup incl. script/iframe/js: payloads → lifted doc ALWAYS
  schema-valid \u2014 the paste-gate guarantee).
- 2 insertDoc tests (single-block inline join, multi-block append).
- highlight_showcase_mark_with_typed_color (custom mark \u2192 invertible schema w/ attr).
Lib 2202 green; 20 v8 editable-model tests green. 5 runtime snapshots accepted
(editable.js growth, additive).
DEFERRED (honest scope cut): full block-in-block list nesting (ul>li) + multi-region
&columns block need a position-map extension (the deferred hard part from round 3).
Inline mark nesting + single-region blocks work. Tracked for a follow-up wave.