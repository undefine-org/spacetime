#+TITLE: spacetime-design Spell skill (umbrella, replaces debug-spacetime + debug-compiler)
#+STATE: DONE
#+SESSION_ID: 14cb55e291e3888a
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-27T18-09-57-319Z_14cb55e291e3888a.jsonl]]
#+CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re
#+DEPENDS: FEAT-015-spacetime-review-runtime-audit-api FEAT-016-dev-panel-review-tab-5-dim-score-actions FEAT-017-dev-panel-brand-tab-css-var-inventory-li FEAT-018-stdlib-showcases-tree-curated-patterns-i FEAT-019-cargo-run-showcase-cli-gallery-variant-g FEAT-020-cargo-run-doctor-cli-one-shot-project-au
#+EFFORT: 5h
#+LAYER: coding-agent
#+PRIORITY: #A

* Initial Message

what can you learn from this https://github.com/alchaincyf/huashu-design

to make a nice spell skill that helps with Spacetime? think about the skill + the features and improvements to the debugger that you can land

* Scope

In-scope:
- New skill at =.claude/skills/spacetime-design/SKILL.md= replacing both =debug-spacetime= and =debug-compiler= (delete those two SKILL.md files; relocate their content into =.claude/skills/spacetime-design/references/=).
- Skill structure (mirrors huashu-design):
  - =SKILL.md= — short, opinionated, procedural. Includes hard rules and decision branches; every long topic links into =references/=.
  - =references/runtime-debugging.md= — distilled from former =debug-spacetime/SKILL.md= (Spacetime.diagnose, inspectTimeline, inspectElement, forcePlay, console patterns).
  - =references/compiler-debugging.md= — distilled from former =debug-compiler/SKILL.md= (inspect layers, RUST_LOG modules, parse/expansion/registry/IR/emit).
  - =references/anti-patterns.md= — Spacetime-specific slop list (no inline handlers, no `<script>` outside `%primitive`, no hex without var, no Rust fallbacks for stdlib, no copy-pasted data outside `@data`, plus existing =docs/antipatterns.md= content).
  - =references/showcases.md= — catalog mirroring =stdlib/showcases/CATALOG.md=, mapped "I need X" → file path + macro name.
  - =references/review-rubric.md= — explains the 5 dimensions, scoring formula, when to defer a finding.
  - =references/project-style-protocol.md= — the brand-asset analog: 5-step procedure (read project AGENTS.md → read existing index.st → grep CSS vars + literals → write/update tokens.st → reference vars only).
  - =references/junior-designer-workflow.md= — assumptions+placeholders, compile-and-show-early, iterate through doctor.
  - =references/fallback-advisor.md= — when ambiguous: `cargo run -- showcase generate <scenario>` → 3 differentiated variants → user picks.
- =SKILL.md= header includes the standard Spell skill frontmatter:
  - `name: spacetime-design`
  - `description:` covering compiler + runtime debugging + design workflow + verification (single sentence per Spell convention).
- Skill includes hard procedure for "before delivering ANY website work": run =cargo run -- doctor <project> --format pretty=, ensure all routes pass.

Out-of-scope:
- Auto-installation script.
- Translating any existing SKILL.md content into Chinese (huashu-design ships bilingual; Spacetime stays English).
- Adding skill-internal scripts (assume existing CLI commands cover the workflow).

* Existing Patterns

- Skill file shape: =.claude/skills/debug-spacetime/SKILL.md=, =.claude/skills/debug-compiler/SKILL.md=, =.claude/skills/frontend-design/SKILL.md= already in repo. Mirror frontmatter.
- Reference doc style: huashu-design's =references/animation-pitfalls.md= and =references/design-styles.md= (terse, table-heavy, links back into SKILL.md).
- Anti-pattern format: existing =docs/antipatterns.md= (very thin today — expand it).
- The skill MUST refer to repo files via project-relative paths (e.g. `stdlib/showcases/`, `src/cli/doctor.rs`) not absolute.

* Tests

Validation:
- T1: `emacsclient -e '(spacetime--prd-validate-all-cli :format json)'` passes after the item lands (catches link-format issues; the skill itself is not in @tasks/, but this is a post-merge sanity check anyway).
- T2: All file paths referenced in SKILL.md and references/*.md resolve (a small grep harness verifying every `=path=` refers to an existing file).
- T3: =.claude/skills/debug-spacetime/= and =.claude/skills/debug-compiler/= directories are gone after the migration (the umbrella replaces them).

Manual:
- M1: Open a fresh agent session, ask "debug why my @load timeline isn't firing in projects/dataiku/" and confirm the agent loads spacetime-design SKILL.md, then references/runtime-debugging.md.
- M2: Ask "design a hero section for projects/foo/, I want minimal" and confirm the agent invokes `cargo run -- showcase generate hero --styles minimal --out projects/foo/scratch/`, then runs doctor.

* Implementation

** ITEM Skeleton SKILL.md with frontmatter
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::skeleton
:END:
- File: new =.claude/skills/spacetime-design/SKILL.md=.
- Sections: Purpose · Hard Rules · Procedures (Junior Designer / Project Style / Fallback Advisor / Pre-Delivery Doctor) · 5-Dim Review · References Index.
- Each long subsection points into =references/=.

** ITEM Migrate runtime-debugging reference
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::ref-runtime
:DEPENDS: FEAT-021-spacetime-design-spell-skill-umbrella-re::skeleton
:END:
- File: =.claude/skills/spacetime-design/references/runtime-debugging.md=.
- Source content: existing =.claude/skills/debug-spacetime/SKILL.md=. Extend with `Spacetime.review()` doc.

** ITEM Migrate compiler-debugging reference
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::ref-compiler
:DEPENDS: FEAT-021-spacetime-design-spell-skill-umbrella-re::skeleton
:END:
- File: =.claude/skills/spacetime-design/references/compiler-debugging.md=.
- Source content: existing =.claude/skills/debug-compiler/SKILL.md=.

** ITEM Author project-style-protocol reference
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::ref-style-protocol
:DEPENDS: FEAT-021-spacetime-design-spell-skill-umbrella-re::skeleton
:END:
- File: =.claude/skills/spacetime-design/references/project-style-protocol.md=.
- 5-step procedure explicit. Concrete example using =projects/undefine.org/AGENTS.md= ("Indigo: #3B3A9E").

** ITEM Author junior-designer + fallback-advisor + review-rubric + anti-patterns + showcases references
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::ref-batch
:DEPENDS: FEAT-021-spacetime-design-spell-skill-umbrella-re::ref-style-protocol
:END:
- Files: 5 references files in =.claude/skills/spacetime-design/references/=.
- =anti-patterns.md= MUST list at least: inline event handlers, `<script>` outside `%primitive`, hex without var, Rust fallbacks for stdlib (per CLAUDE.md decision rule), hardcoded data not via `@data`, JSON.stringify of large objects in `%emit js` builds.
- =fallback-advisor.md= calls `showcase generate <scenario>` then `doctor` once user picks.

** ITEM Delete old skills
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::delete-old
:DEPENDS: FEAT-021-spacetime-design-spell-skill-umbrella-re::ref-batch
:END:
- Delete =.claude/skills/debug-spacetime/= and =.claude/skills/debug-compiler/=.
- Update =CLAUDE.md= (or workspace-level skill index if any) to point to the new umbrella.
- Run T2 grep harness, fix dangling refs.

** ITEM Manual session validation
:PROPERTIES:
:CUSTOM_ID: FEAT-021-spacetime-design-spell-skill-umbrella-re::manual-verify
:DEPENDS: FEAT-021-spacetime-design-spell-skill-umbrella-re::delete-old
:END:
- Run M1, M2 in a fresh agent session. Capture transcripts under =@research/spacetime-design-skill/=.

* Edge Cases

- Skill content drifts from CLI: every reference doc that mentions `cargo run -- doctor`/`showcase` MUST link to the source file (`src/cli/doctor.rs` etc.) so future code changes have a back-pointer.
- The fallback-advisor procedure prescribes parallel variant generation; ensure it ONLY runs `showcase generate` (no LLM-creative generation in the skill itself — variants come from the curated stdlib/showcases catalog).
- Anti-pattern doc MUST cross-reference =docs/antipatterns.md=; both copies stay in sync (canonical source = repo doc, skill version is a digest with examples).

* Acceptance Criteria

- =.claude/skills/spacetime-design/= exists with `SKILL.md` + 7 references files.
- =.claude/skills/debug-spacetime/= and =.claude/skills/debug-compiler/= gone.
- T1, T2, T3 pass.
- M1 transcript shows agent successfully referencing the umbrella skill instead of the deprecated ones.
- M2 transcript shows the fallback-advisor flow producing 3 variants and concluding with a doctor pass.

NOTE [2026-04-27]: Closed by PLAN-017. Shipped .claude/skills/spacetime-design/ with SKILL.md (purpose + hard rules + procedures + 5-dim review + references index) and 8 reference files (runtime-debugging migrated from debug-spacetime, compiler-debugging migrated from debug-compiler, anti-patterns, showcases, review-rubric, project-style-protocol, junior-designer-workflow, fallback-advisor). Deleted .claude/skills/debug-spacetime/ and .claude/skills/debug-compiler/. Updated dangling refs.