#+TITLE: Update docs and CLI messages for projects/ convention
#+STATE: DONE
#+SESSION_ID: 14cb41bb54d20fb7
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-27T16-41-54-259Z_14cb41bb54d20fb7.jsonl]]
#+CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project
#+DEPENDS: PROJ-001-reorganize-websites-into-projects-privat PROJ-002-relocate-compiler-test-fixtures-and-remo FEAT-013-auto-detect-projects-in-spacetime-init
#+EFFORT: 45m
#+LAYER: tooling
#+PRIORITY: #B

* Initial Message

I want you to make it very easy to make websites with spacetime.

this means you reorganise the various websites into a projects/ folder that is gitignored, and you move all the websites inside it. i'll move the demo ones back at some point. initialise a new git repository there to keep track of our private websites.

on top of that, the projects/ folder has a .gitignore of **/dist which is where the built website is put.

make sure the experience of the product and the instructions on how to use it match this

* Scope
** In-scope
- Update top-level =README.md= so all path examples and the "Real-World Examples" section use =projects/<name>/= conventions; absolute =/home/user/code/ora/spacetime/...= links re-routed to the in-repo =docs/= and =examples/=.
- Update top-level =AGENTS.md= and =CLAUDE.md= so commands like =cargo run -- serve site/= become =cargo run -- serve projects/<name>/=, and add a "Project layout" section explaining =projects/= (gitignored private repo), =scratch/= (gitignored debris), =tests/fixtures/= (compiler test fixtures), =site/= (removed).
- Update CLI help text and runtime messages that mention =site/=:
  - =src/main.rs= long-help comment for =--perf-trace= ("Then run: cargo run --features perf-trace -- serve site/" → ="serve projects/<name>/=").
  - =src/main.rs= main warning message similarly.
  - All eprintln strings in =run_live_test_runner= already updated by sibling fixture item.
- Update =docs/DEV_SERVER_README.md=: replace the =cargo run -- serve site/= example with =cargo run -- serve projects/<name>/=.
- Update =docs/live-repl.md= file-structure references: =site/repl/= → =projects/repl/= (or note as future-tense placeholder; keep consistent with the new convention).
- Update =DEV_SERVER_README.md= and =TESTING_GUIDE.md=, =TESTING_QUICK_START.md= at repo root if they contain =site/= path examples (audit and edit).
- Update the =run_init= "Next steps" wording to reflect =projects/= when applicable (handled in sibling FEAT, but verify wording consistency here).
- Add a brief "Project layout" section to =README.md= explaining the three top-level dirs the user interacts with (=projects/=, =stdlib/=, =docs/=) plus =tests/fixtures/= for contributors.

** Out of scope
- Rewriting historical task notes under =!tasks/= and =@tasks/=. Those are journals; only forward-facing instructional docs change.
- Modifying =stdlib/= doc comments that mention =/fonts/...= URLs (these are illustrative URL strings, not paths into the repo).
- Updating skill files under =.claude/skills/=, =skills/=, or external editor configs unless they contain literal incorrect commands a user would copy.
- New documentation files; only edits to existing files.

* Existing Patterns
- =README.md= uses fenced =bash= and =css= code blocks with concrete commands; preserve that voice.
- =AGENTS.md= uses minimal bullet form; preserve.
- The init success output already follows ="Next steps:" → "  cd <dir>" → "  spacetime serve ."=. Keep that idiom.
- Use relative paths in docs (=projects/<name>/=) rather than absolute machine paths.

* Tests
- =grep -n 'serve site/' README.md AGENTS.md CLAUDE.md docs/= prints zero matches after the edit (apart from historical notes deliberately preserved, which should be none in these forward-facing files).
- =grep -n '/home/user/code/ora/spacetime' README.md= prints zero matches.
- =grep -rn 'site/' docs/ DEV_SERVER_README.md TESTING_*.md= reviewed — any remaining references are intentional (e.g., tracker for a future feature) and called out in commit message.
- =cargo run -- init smoke-test= success output references =projects/= path and matches the new docs prose (manual verification).
- =cargo run -- serve --help= help text and =serve --perf-trace= long-help mention =projects/<name>/= rather than =site/=.

* Implementation
** ITEM Audit forward-facing docs for site/ usage
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::audit-docs
:END:
- Run =grep -rn 'site/\|serve site\|build site' README.md AGENTS.md CLAUDE.md docs/ DEV_SERVER_README.md TESTING_GUIDE.md TESTING_QUICK_START.md SPEC.md=.
- Build a TODO list of file:line for each forward-facing reference to update; ignore historical task references.

** ITEM Update README.md
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::update-readme
:DEPENDS: FEAT-014-update-docs-and-cli-messages-for-project::audit-docs
:END:
- Add a top-of-file or near-top "Project layout" section: explains =projects/= (gitignored, your private sites), =stdlib/=, =docs/=, =tests/fixtures/= (for compiler contributors), =examples/= (Spacetime DSL examples).
- Replace absolute =/home/user/code/ora/spacetime/...= links in the Documentation section with relative =./docs/= paths.
- Replace =cargo install spacetime-cli= and ad-hoc paths so all the worked examples use =projects/<name>/= form.
- The "Real-World Examples" subsections (zeystudios, ikarchitecte, jallete) — keep as illustrative names but route any paths to =projects/<name>/= form; or rewrite the subsection lead-in to clarify these are user sites in their own private repo.
- Update "Project Structure" near the bottom to include =projects/= and =tests/fixtures/=, drop =site/=.

** ITEM Update AGENTS.md and CLAUDE.md
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::update-agents
:DEPENDS: FEAT-014-update-docs-and-cli-messages-for-project::audit-docs
:END:
- In =AGENTS.md=, replace =cargo run -- serve site/= (Debugging and Commands sections, lines 14, 33) with =cargo run -- serve projects/<name>/=.
- Add a "Project layout" subsection clarifying:
  - =projects/= — gitignored, your private Spacetime websites; build outputs to =projects/<name>/dist/= (gitignored within the projects repo).
  - =scratch/= — gitignored, miscellaneous non-Spacetime content.
  - =tests/fixtures/= — compiler integration test fixtures (do not edit casually).
  - =stdlib/=, =docs/=, =examples/=, =site/= removed.
- Repeat in =CLAUDE.md= if it has equivalent content.

** ITEM Update CLI help and runtime warning strings
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::update-cli-strings
:DEPENDS: FEAT-014-update-docs-and-cli-messages-for-project::audit-docs
:END:
- =src/main.rs= line ~142 (perf-trace doc-comment): change ="cargo run --features perf-trace -- serve site/ --perf-trace"= to ="cargo run --features perf-trace -- serve projects/<name>/ --perf-trace"=.
- =src/main.rs= line ~332 (warning eprintln): same substitution.
- =cargo run -- --help=, =cargo run -- serve --help=, =cargo run -- build --help=, =cargo run -- init --help= each manually verified to contain no stale =site/= example.

** ITEM Update docs/DEV_SERVER_README.md and other root *.md
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::update-other-md
:DEPENDS: FEAT-014-update-docs-and-cli-messages-for-project::audit-docs
:END:
- =docs/DEV_SERVER_README.md= line 8: ="cargo run -- serve site/ --debug"= → ="cargo run -- serve projects/<name>/ --debug"=.
- =docs/live-repl.md= file-structure mentions of =site/repl/=: replace with =projects/repl/= or annotate as future placeholder consistent with new convention.
- Skim =DEV_SERVER_README.md=, =TESTING_GUIDE.md=, =TESTING_QUICK_START.md=, =LIVE_TEST_RUNNER.md= at repo root for =site/= references; update or note that these may be slated for archival cleanup.

** ITEM Update memory/skill references that show up in agent context
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::update-skills
:DEPENDS: FEAT-014-update-docs-and-cli-messages-for-project::audit-docs
:END:
- Search =skill://coding=, =skill://debug-spacetime=, =skill://debug-compiler= shipped under =.claude/= or =skills/= for literal =site/= example commands. Update only verbatim copy-paste-ready commands; conceptual mentions of "the site dir" stay.

** ITEM Reflow README "Project Structure" block
:PROPERTIES:
:CUSTOM_ID: FEAT-014-update-docs-and-cli-messages-for-project::readme-structure
:DEPENDS: FEAT-014-update-docs-and-cli-messages-for-project::update-readme
:END:
- Replace the ascii-art block under "Project Structure" so it lists =projects/= (gitignored), =stdlib/=, =docs/=, =examples/=, =public/runtime/=, =src/=, =tests/=, =tests/fixtures/=. Drop =site/=.

* Edge Cases
- Some docs reference =site/dataiku= as the working example. Replace with =projects/dataiku= consistently — but =dataiku= is now a private user site in =projects/=, so a public reader may not be able to reproduce. Where that matters, switch the example to a name like =projects/<your-site>/= or use =projects/playground/= which is intended to ship.
- Historical bug reports and audit notes under =!tasks/= retain old paths: that's intentional (journals are immutable). Make sure not to rewrite them.
- =init= command output wording is owned by the sibling FEAT-init item; this docs item only verifies the wording matches the prose in =README.md='s Quick Start.
- Skill files have indirect references via descriptions; if a skill description mentions ="serve site/"=, update it or note as an out-of-band edit. Keep this item's scope to repo-tracked text.

* Acceptance Criteria
- =grep -n 'serve site/\|build site/' README.md AGENTS.md CLAUDE.md docs/= prints nothing.
- =grep -n '/home/user/code/ora/spacetime' README.md= prints nothing.
- =cargo run -- init demo-site= success output and the README Quick Start instructions agree on the =cd= path (manual diff).
- =cargo run -- serve --help= and the perf-trace warning text both reference =projects/<name>/=, not =site/=.
- A new contributor can clone the main repo, read =README.md= top-to-bottom, and follow the Quick Start to a working =projects/demo/= site without consulting any other doc (manual walkthrough check).
