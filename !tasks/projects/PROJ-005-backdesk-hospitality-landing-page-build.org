#+TITLE: Backdesk Hospitality landing page — build
#+STATE: REVIEW
#+SESSION_ID: 15052f2e47ce81ee
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-11T16-32-45-343Z_15052f2e47ce81ee.jsonl]]
#+CUSTOM_ID: PROJ-005-backdesk-hospitality-landing-page-build

* Initial Message

my dude, I want you to make a modular project in projects/backdesk-hospitality that follows the rough COPY of ~/code/ora/highlead/clients/backdesk-hospitality (JUST THE COPY), and then you make a FUCKING AMAZING mobile-ready landing page for our campaign, in French, that leverages the latest Spacetime features and has a really cool admin interface-ready code organisation and is super well modularised (not the _*.st bullshit you had before, I want you to use this as a testing ground for Spacetime as a fully fledged language and whatnot).

You need to go through design inspirations. Please write a set of design specs in projects/backdesk-hospitality/design/ that cites specific WOW award-winning showcases from awwwards.com and other sources, that you have carefully studied for both the visual and the lead experience and the convertion potential. You'll include puppeteer screenshots in your research and explain all the facets that you intend on using from them for the backdesk hospitality website, and your reasoning and taste for why you went in a certain way. Then you'll show me how you will turn this in to a consistent experience with a clear CTA and quick form (we'll worry about hoking the form to a live server later, right now I want copy + style + experience)

* Goal
Mobile-first, motion-led, French-first campaign landing page for Backdesk
Hospitality (WhatsApp AI concierge for hotels), at projects/backdesk-hospitality/.
Modular Spacetime (no _*.st partials): @template components fed by admin-editable
@data. Copy + style + experience this pass; form-to-server later.

* Delivered
- Source copy → source/ ; optimized assets → assets/.
- Design dossier → design/ (README + 01-showcases + 02-design-system + 03-experience + 04-motion) citing live studies Linear/Sierra.ai/igloo.inc/Cloudbeds with Puppeteer screenshots in design/refs/ and take/refuse reasoning.
- 18 .st modules + 12 admin-editable @data JSON collections.
- Full page: hero (self-typing WhatsApp thread + floating console over riad photo), interactive I→V parcours (click-switch pills), 03:47 night-booking SCROLLYTELLING (booking completes as you scroll via scroll-scrubbed CSS var), 3 product views + live mono telemetry, native-RTL FR/AR/DR/EN cards (darija moat), integrations + trust tiles, avant/après, native <details> FAQ, 4-field demo form w/ inline success, footer, mobile sticky CTA.
- Verified: all 18 compile, served, Puppeteer screenshots desktop + true-mobile(402px) of every section incl RTL + form success + mobile sticky CTA.

* Bugs filed (full repro/root-cause)
- BUG-086 @on visible/@scroll appliers never run (anims=string[]). Workaround: publish --st-<name> var, consume in CSS.
- BUG-087 CSS emitter drops @media. Workaround: intrinsic responsive (auto-fit/minmax/clamp + fluid show/hide).
- BUG-088 @scroll-spy sub-state not attaching. Workaround: static nav/CTA.
- PLAN-035 .st-only shell can't set head → kept minimal index.html.

* Toolchain caveat
Live cargo build broken by UNRELATED uncommitted edits in src/syntax/events/extractors/{simple,custom}.rs (dup consume_type_term/TyperefExtractor → E0119/E0428). Built/served via prebuilt target/debug/spacetime (pre-breakage). Did NOT revert (git rule). Re-verify bugs on fresh build.

* Next
- Wire demo form to live server (inline success only now).
- Re-verify the 3 bugs on a fixed compiler; if fixed, simplify motion + restore @media + scroll-spy reveals.
