#+TITLE: Backdesk landing v2 — fix motion engine + rebuild scrolly
#+STATE: REVIEW
#+SESSION_ID: 15052f2e47ce81ee
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-11T16-32-45-343Z_15052f2e47ce81ee.jsonl]]
#+CUSTOM_ID: PROJ-006-backdesk-landing-v2-fix-motion-engine-re

* Initial Message

my dude, what you did is stain on shit. Please rewrite the copy, and make a NEW project whose frontend is based on award-winning scrolly experience that are mobile-first

* Goal
User rejected v1 backdesk-hospitality landing and asked to rewrite the copy + build
a NEW project with a genuinely award-winning, mobile-first scrolly frontend. v1 had
faked scrollytelling because the Spacetime motion engine was broken.

* Decision: fix the compiler motion bugs FIRST, then build backdesk-v2.
(The prior session's "cargo build broken" claim was FALSE — tree was clean, built fine.)

* Compiler fixes (committed to main verse repo)
- BUG-086 (8548b21f): flat @on visible/@scroll keyframes serialized to a string
  array → anims.properties undefined → applyStyle never ran. Routed Keyframes
  through build_keyframes_js_object + FOUC arm. Verified: opacity interpolates on scroll.
- BUG-087 (8548b21f): top-level @media dropped by cst_to_stfile. Added
  StFile.raw_css_blocks, normalize @media("q")→@media (q), propagate through both
  import mergers, append after base CSS. Verified: 390px breakpoint applies.
- BUG-088: @when &self.<ns>.<sig> guard never implemented. Built stdlib when-guard
  primitive+macro (ST.watch+ST.runMutations) + 3 form-matcher fixes (multi-token
  literal eat_text, match_sink directive-inline-ref deferral, BodyBrace boundary +
  ExprExtractor trivia fix). Verified: scroll A↔B flips $navTheme, nav bg follows.
- BUG-094 filed (:root doesn't parse as scope; workaround html {}).
- Regression tests added; 264 parser + 172 integration + unit tests green. The 5
  snapshot + 9 v8 media_dedup failures are PRE-EXISTING (editor drift + BUG-039).

* Rebuild — projects/backdesk-v2/ (own git repo, init'd this session)
16 .st files (theme/globals/data/whatsapp + 11 section modules) + 8 @data JSON.
Key learnings: @template needs & prefix to register; @data fetch needs trailing ;;
scroll-scrub must bind the tall track not the sticky pin.
11-section spine: nav · hero (monolith reveal + self-typing thread + KPI console) ·
parcours I→V (click-switch) · NIGHT 03:47 (scroll-scrub booking) · produit · langues
(darija moat, native RTL) · integrations · avant/après · faq · demo (form+success) ·
footer. All copy rewritten to editorial French; design/README.md dossier.

* Verified browser desktop+mobile: all @each render; hero/parcours/night motion;
scroll-spy nav theming; demo success toggle; native RTL Arabic; @media mobile collapse.

* Next: wire demo form to live server; revisit BUG-094.
