#+TITLE: Expand UNKN placeholder routes and close static export parity gap
#+STATE: ITEM
#+SESSION_ID: 14cc778b8e323f00
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-28T15-15-29-976Z_14cc778b8e323f00.jsonl]]
#+CUSTOM_ID: FUP-018-expand-unkn-placeholder-routes-and-close
#+DEPENDS: FEAT-025-add-clean-unkn-placeholder-routes-using FEAT-026-manually-verify-unkn-visual-excellence-a
#+LAYER: website

* Initial Message

look at ./projects/unkn/*. I want you to create a full Spacetime website, using the latest Spacetime affordances and folder hierarchy, fully manually verified for visual excellence and adherence ot the specs in the PDF. Be comprehensive in determining and describing your understanding of the work.

* Intention & Purpose
Track deliberate post-launch work for UNKN routes. The implementation plan will ship polished placeholders for Talent, Work, Expertise, About, and Contact; this follow-up covers replacing placeholders with full content and ensuring clean routes are exported statically if the current export command misses root route directories.

* Context & Narrative
The user chose clean dev routes (`talent/index.html`, `work/index.html`, etc.) and accepted a follow-up if export-route parity is needed. The current export implementation processes root `index.html`, `pages/**/*.html`, and supporting `data/assets/locales`; it may not copy/process root clean route directories. The user also explicitly said FUPs are acceptable for filling the routes. This item prevents placeholder content or export limitations from becoming hidden debt.

* Scope
In scope:
- Replace route placeholders with full Talent, Work, Expertise, About, and Contact content when real content/assets/specs are available.
- Add real talent roster data, real work/campaign case studies, real contact/social/legal links only from authoritative sources.
- Decide whether to fix static export to process clean route directories or to adopt a documented pages convention.
- Add route-specific visual and content acceptance tests.

Out of scope:
- The initial home launch.
- Inventing fake talent/case study data.
- Backend contact submission unless a future spec requests it.

* Existing Patterns
- Initial UNKN route shell from `FEAT-025-add-clean-unkn-placeholder-routes-using`.
- `src/export.rs`: current site export behavior.
- `src/server.rs`: current dev-server clean directory route behavior.
- `docs/DATA_BINDING_GUIDE.md`: future route data can use `@type`/`@data`/`@each`.

* Tests
Future workflows:
1. Talent route lists real talent with names/images/categories from approved data.
2. Work route lists real campaigns/case studies from approved data.
3. Expertise route expands service pillars into detailed offerings without contradicting home positioning.
4. About route tells agency story with approved copy.
5. Contact route has real contact methods or approved backend behavior.
6. Static export includes all clean routes and assets.
7. Direct refresh works in dev and exported output.

* Implementation
** ITEM Gather approved route content
:PROPERTIES:
:CUSTOM_ID: FUP-018-expand-unkn-placeholder-routes-and-close::gather-approved-route-content
:END:
- Collect authoritative copy, images, social links, legal links, contact destinations.
- Reject unapproved AI/fake content for real route pages.

** ITEM Expand route data and templates
:PROPERTIES:
:CUSTOM_ID: FUP-018-expand-unkn-placeholder-routes-and-close::expand-route-data-templates
:DEPENDS: FUP-018-expand-unkn-placeholder-routes-and-close::gather-approved-route-content
:END:
- Add typed JSON data and route templates using `@type`, `@data`, `@each`.
- Preserve shared nav/footer/motion system.

** ITEM Resolve static export parity
:PROPERTIES:
:CUSTOM_ID: FUP-018-expand-unkn-placeholder-routes-and-close::resolve-export-parity
:DEPENDS: FUP-018-expand-unkn-placeholder-routes-and-close::expand-route-data-templates
:END:
- Verify whether `cargo run -- build projects/unkn/` exports clean route dirs.
- If not, fix export behavior or document an accepted convention with tests.

* Edge Cases
- Real people require correct rights/alt text; do not reuse placeholder art.
- Social/legal links must not point to `#` in production.
- Export fix may affect other projects; add regression coverage.

* Acceptance Criteria
- Placeholder copy is replaced with approved real content.
- Static export serves the same route set as dev server.
- Route pages pass the same visual, a11y, and Spacetime review gates as the home page.

NOTE [2026-04-28]: Deferred follow-up. Placeholder routes shipped; full route content + static-export parity for clean route directories remains scoped here.