#+TITLE: Delete @exempt_sources after verse BUG-380/381 fix lands
#+STATE: DONE
#+SESSION_ID: 1569ef14bcd1663a
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-08-28T22-39-17-235Z_1569ef14bcd1663a.jsonl]]
#+CUSTOM_ID: FUP-195-delete-exempt-sources-after-verse-bug-38
#+COMPLETION_HASH: fc4de0c1b1edd052

* Initial Message

look up the `@exempt_sources` in interface, and tell me how we would fix this structurally, either here or directly in spacetime (~/code/ora/verse)

* Context

`Interface.Verse.@exempt_sources` (apps/interface/lib/interface/verse.ex:260) is a
name-keyed filter that downgrades `spacetime check --deny-warnings` failures to
passes when every diagnostic is a W0201 naming one of 8 hand-verified sources
(draft status send partial error refresh vars_count new-session). It exists
because verse's `analyze_read_usages` cannot see two read-positions:

- `@on` motion-body entries (fired signal-call targets + args, mutation
  targets/exprs) — verse BUG-380
- `@view`/`@match` subjects — verse BUG-381

Both fixed upstream in ~/code/ora/verse. The module's own docstring states the
exit condition: "If verse's usage analysis learns about @on/@view, this filter
deletes and nothing else changes."

* Work

- Delete `@exempt_sources`, `exempt?/1`, and the `:only_exempt_warnings` arm of
  `classify/1`; `classify` collapses to `{:problems, lines} | :unrecognised`.
- Rewrite the W0201 section of `check/1`'s doc and `classify/1`'s doc.
- Update test/verse_policy_test.exs: exemption cases become classification
  cases (a W0201 for ANY source is now a problem line; only crash/usage output
  is unrecognised).
- Acceptance: `mix test` in apps/interface green; a seeded machine page passes
  rung 3 with NO exemptions, and a page with a genuinely unused source still
  fails.

* Dependency

Requires the verse-side BUG-380/BUG-381 fix to be built into the release
binary that `Interface.Verse.binary/0` resolves (CARGO_TARGET_DIR release
build), or rung 3 will start refusing real pages.

NOTE [2026-08-28]: DONE in the same wave as the verse fix: @exempt_sources, exempt?/1 and the :only_exempt_warnings arm deleted from Interface.Verse; verse_policy_test.exs flipped (any W0201 is a refusal — RED confirmed against the pre-cutover module, then green); new apps/spell/test/verse_check_test.exs proves the seeded page passes rung 3 with zero exemptions and that a genuinely unused source still refuses. Verse BUG-380/381 are DONE with the analysis fix in the release binary.