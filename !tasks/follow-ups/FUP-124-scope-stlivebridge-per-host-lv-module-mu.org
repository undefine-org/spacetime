#+TITLE: Scope __stLiveBridge per host/LV module (multi-LV pages cross-wire today)
#+STATE: ITEM
#+SESSION_ID: 1539989eb44441ea
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-22T09-34-26-513Z_1539989eb44441ea.jsonl]]
#+CUSTOM_ID: FUP-124-scope-stlivebridge-per-host-lv-module-mu

* Initial Message

we are going to work on the integration between Spacetime and Elixir. look at the match and the handle macros, and tell me hwo they are translated currently from and to Phoenix LiveView-like constructs, and how they ought to be. I would like the experience to be as seamless as possible: its own `src/` or `frontend` or sth to put the .st files like a proper project (most likely), and a $hosts file that files import (for now, just give them special enough names that importing them is fine).

ok?

* Parent: FEAT-137.

window.__stLiveBridge is a single page-global: on a page with several
Spacetime-hosted LiveViews, the LAST-mounted SpacetimeBridge hook wins and
every `live` host's signals route through that one LV's channel.

* Fix
Key the dispatcher per host — e.g. __stLiveBridge[module] with the hook
registering under its LV's module (data-attr on the host element), and
signal-call's doLiveRequest resolving resolveHost().module. Region/instance
scoping should ride the FEAT-127 region rail rather than a parallel mechanism.
