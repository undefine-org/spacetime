#+TITLE: @flip does not visibly animate on @each re-renders in a live page (node identity vs FLIP persistence investigation)
#+STATE: ITEM
#+SESSION_ID: 1539989eb44441ea
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-22T09-34-26-513Z_1539989eb44441ea.jsonl]]
#+CUSTOM_ID: FUP-132-flip-does-not-visibly-animate-on-each-re

* Initial Message

explain the FUPs and show me your vision for the Spacetime <> LV connection and what would make it *wow*. Exlpain the impact of each bug that you saw

* Surfaced by
FEAT-162 two-browser e2e. @flip's @clock headless test (FEAT-161) passes, and
the DRAG path visibly animates (12 intermediate transform frames sampled during
a card drag, FEAT-161's card-follows-cursor machinery).

* Symptom
`@flip $items(duration: 260ms)` on `.card` (elixir/spacetime_lv/frontend/board.st):
when $items changes — optimistic local projection OR a remote assign diff via
subscribe — the moved card is at its FINAL position with `transform: none` at
the first 33ms sample after the write. No intermediate FLIP frames, ever, in a
real browser.

* Hypothesis
@each (key:) may RECREATE the row node on every $items write rather than
reconciling keyed nodes — FLIP needs a persistent node to measure First/Last
against. If @each replaces, @flip silently no-ops. Alternatively @flip only
engages on the subscribe path it was tested against and local optimistic
writes bypass it.

* Why it matters
The wow-demo promise is "other users' moves GLIDE on your screen". The sync +
toast + cursor all work; the glide does not visibly happen.

* Investigate
1. Does keyed @each preserve node identity across source-array writes?
2. Does @flip hook the signal write before/after the @each re-render, and does
   it see a persistent node?
3. RED→GREEN: a @clock test that writes a keyed @each source and asserts
   intermediate transform frames on the moved row.

NOTE [2026-07-26]: ACCEPTED 2026-07-24 (user review of spacetime_lv). Scoped under PLAN-116, Wave 1. Core question: does keyed @each preserve node identity across $items writes? If it recreates rows, @flip has no persistent node to measure First/Last → silent no-op. RED→GREEN @clock test on a keyed @each source write asserting intermediate transform frames.