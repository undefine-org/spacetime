#+TITLE: Compiled per-instance regression tests: @on isolation, nested @each, @editable bind
#+STATE: DOING
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FUP-051-compiled-per-instance-regression-tests-o

* Initial Message

ok do the full W3-clean. Don't stop until you're fully done. Thanks.

* Goal
Close the compiled-level test gaps the review (agent://22-ReviewTestCoverage)
found in the PLAN-039 unified scope path. The runtime PRIMITIVES are well covered
(tests/v8_runtime/scope_resolution_tests.rs, 19 tests incl the watcher-leak
regression), but the COMPILED `@template`→register→invoke×N→interaction path is
under-tested end to end.

* Tests to add (Rust v8_runtime, deterministic + CI-runnable)
1. **@on per-instance isolation (compiled)** — compile `@template &counter(){ $count
   number:0; .inc{ @on click { $count <- $count+1 } } }`, invoke twice into the
   DOM, click instance A's button, assert `ST.get(instA,'count')===1 &&
   ST.get(instB,'count')===0` and class-toggle visibility tracks per instance.
   (Today only the hand-built `run_mutations_is_per_instance` + the live browser
   check cover this; no compiled regression.)
2. **Nested @each per-instance (compiled)** — register a template whose body is
   `@each($items as $i){ &row($i) }` with `$items` a template PARAM; invoke two
   instances with different arrays; assert each renders N rows from ITS OWN param
   (the `ST.watchScoped(containerEl, sourceName)` instance channel). Would also
   have caught the `&list($rows)` `[object Object]` stringify regression.
3. **@editable bind per-instance (compiled)** — mount `@editable(bind:`$f.value`)`
   inside a `@template` body across two instances; assert each editor reads/writes
   its own `$f` instance signal (closes the `resolvePath`-in-context gap the
   per-primitive unit test leaves open).

* Already added this arc (do not duplicate)
- reactive_binding_init_is_idempotent_no_watcher_leak (bounded watcher growth)
- test_template_body_state_does_not_page_leak + same-name page coexistence
- test_class_toggle / test_content_binding lower_to_scoped_binding (emit shape)
- 18 scope_resolution_tests (resolve/resolveOwner/resolvePath/watchScoped + scoped
  runMutations).

* Acceptance
Three compiled per-instance tests added + green; they fail if the unified scope
path regresses (selector leak, global shadow, watcher leak, wrong-instance write).

* Provenance
PLAN-039 test-coverage follow-up. Reviewer: agent://22-ReviewTestCoverage
findings P2 (×3). Prefer Rust v8 over .test.st until BUG-105 (@mount E0402) lands.
