#+TITLE: FUP: @anchor offY responsive + retire stale scene.cdp.test.st text3d asserts
#+STATE: ITEM
#+SESSION_ID: 151c02da5be0d720
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-29T10-04-08-943Z_151c02da5be0d720.jsonl]]
#+CUSTOM_ID: FUP-100-fup-anchor-offy-responsive-retire-stale

* Initial Message

my dude, the everswap clone thing is not leveraging the spacetime scope resolution and language extensibility as much as it could.

only the 'EverSwap' font is visually on top of the first peak, but the rest of the copy still feels like a 2d scroll thing. what would the API that lets us naturally put text on top of peaks look like? like all text, and it feels natural and lets the rest of the code reads naturally?

* Parent
FEAT-142 spatial-ECS. Non-blocking polish surfaced during W3.

* Items
1. RESPONSIVE SEATING: projects/everswap-clone/index.st @anchor offY values (everblade 150, evernet 490,
   everyone 460, relayers 440, summit 380) are absolute px tuned for ~1365x768. On very different viewports
   the copy may sit too high/low. Options: (a) express offY as a vh fraction in the primitive (offY as % of
   ctx.size().height), (b) anchor at a viewport-relative band. Keep it vocabulary-agnostic (general @anchor).
2. STALE TEST: projects/everswap-clone/tests/scene.cdp.test.st asserts @text3d wordmark + exactly 3
   @flightpath keyframes — both predate FEAT-142 (there are now 7 @viewpoints; hero is @worldtext not
   @text3d). Update or retire those asserts.
3. (optional) Promote three-anchor/anchor from projects/everswap-clone/lib toward stdlib/3d once a SECOND
   3D site reuses them. Do NOT pre-abstract before a real second consumer.

* Acceptance
- offY seating holds on two viewport sizes (e.g. 1440x900 and 1280x720).
- scene.cdp.test.st passes against the current scene (no text3d/3-keyframe asserts).
