#+TITLE: stdlib/generate shared base + @vary/@sample (design-variation from the test generator)
#+STATE: ITEM
#+SESSION_ID: 1510d7e0efe8848c
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-20T17-53-05-983Z_1510d7e0efe8848c.jsonl]]
#+CUSTOM_ID: FUP-086-stdlib-generate-shared-base-vary-sample

* Initial Message

launch the workbench and let me drive it for a bit (give me the link) while you explore the various plans that it was building upon (all the way to PLAN-049) and we thoroughly design the multi-frame drag-canvas (W4's strip is the non-canvas bridge), file-scope page mounts (FUP-061), the
 __mcp__/__admin__/__dev__ convergence (north-star, two-consumer rule).

* Parent / design
Full design: @research/mcp-editor-introspection-design-2026-06-20.org PART D +
PART F Tier 5. Owner decision: EXTEND, don't repoint — stdlib/generate is the
shared base NOW; stdlib/testing becomes a CONSUMER; the builder's design-variation
is a SECOND consumer.

* Source-verified
public/runtime/generative.js: window.__stGen (spec->value, seeded), __stProperty
(samples+predicate+shrink), __stFuzz (adversarial+crash+budget), __stParseBinding
("name: spec" -> {name,spec}). Spec grammar: number | number A..B | int | int
A..B | bool | string | string ~ corpus | scalar[]. Consumed TODAY only by
stdlib/testing/generative.st (@property/@fuzz).

* Part 1 — the refactor (re-home generation)
Move the spec->value engine to a stdlib/generate primitive:
  gen(spec, seed) -> value
  samples(spec, n, seed) -> value[]
  shrink(spec, value, pred) -> value
  parseSpec("name: spec") -> {name, spec}
stdlib/testing imports it; @property/@fuzz behavior UNCHANGED (a re-home, not a
rewrite). This corrects the mis-ownership (generation isn't test-specific). One-
time refactor of the test runtime; full test suite must stay green.

* Part 2 — the builder consumers (@vary / @sample)
@vary(node, n)   -> n generated value-sets for the node's TYPED params -> a
                    variations strip (mockup 58); preview via the ephemeral tier
                    (region_mount_input), commit a pick via EditAst.
@vary(@object,8) -> the 3D "vary x8".
@sample($coll,n) -> fill an empty collection with n generated @type rows (no
                    lorem; type-faithful: string~unicode, number A..B, scalar[]).
@sample(&card)   -> one generated template instance for layout preview.
The spec for a node's params is DERIVED from the form's CaptureTypes (Color->color
spec, Number A..B->range, Union->pick-one, String->corpus) — @vary needs NO author
annotation; it reads the same introspection the editor does. @editor MAY refine a
vary spec (accent: vary(brand-palette)) — a sub-follow-up.

* Constraints
- Spacetime-only in the builder surfaces; generator JS stays in the owned
  primitive (vendored-behind-a-primitive rail).
- Previews never write source; only a committed pick emits EditAst.
- "Generate N, shrink to the favorite" reuses the SAME shrinker as @property.

* Acceptance
- stdlib/generate exists; testing/generative imports it; @property/@fuzz suite
  green (no behavior change).
- @vary(node, 8) shows 8 live-preview variations in the builder; picking one
  commits via EditAst + persists to source.
- @sample fills an empty @type collection with type-faithful data.
- Tests: generator unit parity (pre/post re-home), a live builder @vary gate.
