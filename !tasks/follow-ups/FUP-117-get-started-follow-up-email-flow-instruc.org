#+TITLE: Get Started follow-up email flow — instructions, sequencing, CRM handoff (out of PLAN-071 site scope)
#+STATE: ITEM
#+SESSION_ID: 152939d2e5b25e66
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-09T16-24-34-454Z_152939d2e5b25e66.jsonl]]
#+CUSTOM_ID: FUP-117-get-started-follow-up-email-flow-instruc

* Initial Message

can dom-refs (`&my-template(..., ...) { ... }`) contain all of ST? As in, html, CSS, ST syntax through macros, etc. and then it gets its scope solved to be within the template? Or does it have some special case?

The goal is to empower the 'element-ref as function' way of creating Spacetime affordances the most blessed way to build encapsulation and whatnot. I would like to do that for Ora Ventures, whose website we want to rewrite completely to be attractive to Chief AI Officers at big groups in emerging markets, as well as capital-rich SMBs that want to be more dynamic and competitve. That website should be sober, have crisp but minimal copy, and showcase Spacetime features in a way that is unique and helps us convert more (so think of advanced interactions and motions in terms of high-convertion)

Come back with your plan, sources, inspirations, for the copy
Come back with your analysis for the &... thing

* Context
PLAN-071 (ora-ventures.com rewrite) stakeholder decision: the Get Started
form's CTA is "Get Started" with a simple qualifying form (name, work
email, company, role, market, process, timeframe). Per that decision, "the
follow-up email will have all the instructions" \u2014 explicitly deferred as a
FUP rather than built into this site rewrite.

* What this covers
- The transactional/sequenced EMAIL sent after a form submission (today the
  form only does a client-side state toggle to a "Received." success
  message \u2014 there is NO backend transport, NO email delivery at all).
- What instructions that email should contain (the plan's copy promises "A
  senior operator will reply within two business days" \u2014 decide whether
  the automated email ALSO carries next-step instructions, or is purely a
  confirmation, with the senior operator's manual reply carrying
  instructions).
- CRM/inbox handoff: where does a submission actually land? (Currently:
  nowhere \u2014 the form has no submit endpoint at all.)

* Acceptance (for whoever picks this up)
1. Decide + implement an actual form transport (server endpoint, email
   service, or CRM webhook \u2014 stakeholder input needed on which).
2. Define the confirmation email content + sequencing (immediate receipt
   confirmation vs. a drip sequence).
3. Wire the site's form (projects/ora-ventures.com/index.st +
   for-ai-officers/index.st, =.ora-contact-form__submit= handler) to the
   real transport once decided, replacing the current pure-client-state
   success toggle.
4. Note: BUG-171 (=@on submit.prevent= unimplemented) is a SEPARATE,
   already-filed compiler bug that also needs fixing before a real
   =<form method="POST">= submission flow can rely on the documented
   =.prevent= modifier; this FUP's transport work should route around it
   the same way PLAN-071 did (type="button" + @on click) until BUG-171
   lands, or fix BUG-171 first if the transport needs true form semantics.

* Priority
Deferred by explicit stakeholder decision (PLAN-071). Not blocking site
launch \u2014 the current client-side "Received." state is a legitimate
placeholder UX for a v1 (no dead ends, sets correct expectations "two
business days").
