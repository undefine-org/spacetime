#+TITLE: FUP: mutual recursion between %capture_types silently breaks matching
#+STATE: ITEM
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: FUP-157-fup-mutual-recursion-between-capture-typ

* Initial Message

can you explain everything that is still open for my decisions? Explain it with depth and sharpness

Found in PLAN-126 W3: a %capture_type that references another which references it BACK (mutual recursion — on_text_slot { … $body:on_motion_body … } and on_motion_body including $slot:on_text_slot) silently BREAKS the type: matches produce nothing, directives fall through to W0714, and no diagnostic names the cause. Self-recursion (on_motion_body referencing itself) works via the `visiting` cycle guard, which compiles the recursive position as Expr; mutual recursion appears to degrade the whole type the same way — losing the OUTER structure too.

Cost found by: the two-slot body (`:entering { … }`) silently not matching until the slot body was changed to `motion_line*` (non-recursive).

Expected: either mutual recursion works through the same visiting guard (compiling the recursive position as Expr at ONE level, not both), or the registry rejects it loudly at load with a "mutually recursive capture type" diagnostic. Current behavior is the worst cell: silent.

Acceptance: a RED→GREEN pair — a fixture with mutually recursive capture types either matches (if supported) or fails at registry load with a named diagnostic.
