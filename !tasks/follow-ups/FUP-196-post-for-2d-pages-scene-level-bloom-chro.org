#+TITLE: @post for 2D pages — scene-level bloom / chromatic aberration / vignette as registry data
#+STATE: ITEM
#+SESSION_ID: 157047c1462c1c38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-02T20-57-35-768Z_157047c1462c1c38.jsonl]]
#+CUSTOM_ID: FUP-196-post-for-2d-pages-scene-level-bloom-chro

* Initial Message

Using whatever tools and techniques you want, make the absolute most impressive, most mind blowing, most visually stunning 30s promo video for Spacetime (this repo). I want something that looks like we paid a studio $5m to produce.

After that, I want you to do the same thing, but with Spacetime, and to compare/critique the two

Surfaced by FEAT-175 (promo film). The studio cut's entire 'lens' look is UnrealBloomPass + a 20-line post shader; the Spacetime cut has per-element `filter:` only. SIP-001b family E already names `@post` for stdlib/3d. Proposal: a 2D `@post` on a stage element that composes `backdrop-filter`/`mix-blend-mode` layers from a registry (bloom = blurred screen-blend clone of the stage, aberration = two hue-shifted clones offset by a custom property). Variants as stdlib data, no Rust arm. Elegance bar: it must delete the hand-rolled `.tagr/.tagb` RGB-split clones in demos/promo/index.st S6.

Acceptance: demos/promo S6 glitch expressed as `@post --rgb-split(amount: --glitch)` with a `--cdp` gate that the clones exist and offset.

NOTE [2026-09-03]: Subsumed by PLAN-150 (film surface). Design + syntax: docs/language/film.st.md. Keep this item for tracking; implement per the plan's wave.