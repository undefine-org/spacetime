#+TITLE: FEAT-118 FUP-055 residual: ImportScope validation (unbound alias / only-hiding)
#+STATE: ITEM
#+SESSION_ID: 1508cbf54b12d2b0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-14T11-53-10-188Z_1508cbf54b12d2b0.jsonl]]
#+CUSTOM_ID: FUP-057-feat-118-fup-055-residual-importscope-va

* Initial Message

look at ~/code/ora/verse and how it generates tree-sitter from the meta-compiler language.

your goal is to look at how pi-* crates manage dynamic language profiles. Consider runtime/kdl/some other addition (also consider something specific to Spacetime. Weigh in all the options and show me your reasoning).

Please understand the nuances of the work here. You are also free to propose improvements to the tree sitter generator and to the Spacetime LSP to make it world-class, as it hasn't been touched in a while and could benefit from some love. Since I am the creator of both Spell and Spacetime, I would LOVE to make the two seamlessly work with each other (also look at the Spacetime MCP btw).

Deep architecture dive first, then come back with your insights, revelations, challenges (to me and to you), questions, and overall state of things

Parent: FUP-055. The two high-value pieces of FUP-055 are DONE + verified: qualified dispatch (@scene/camera resolves to scene/camera FQN; commit 0c18c72a) and cross-namespace ambiguity (E0924; commit e9b99326 + detector verified-passing in pipeline/mod.rs). This is the residual VALIDATION layer.

WHAT: build a per-file ImportScope{open[ns], aliased{alias->ns}, only{}, hidden{}} from file.imports (ImportAst.{namespace,alias,only,hiding} \u2014 all captured already) and validate each match:
- @alias/x errors if `alias` not bound by any @use...as in the file;
- @ns/x errors if `ns` not @use'd;
- only(a,b) / hiding(a,b) filter which imported names are visible (referencing a hidden/non-only name -> error).

WHY LOWER PRIORITY (C): qualified ADDRESSING already works (FUP-055 core) and collisions already warn (E0924). This adds typo/visibility ERRORS \u2014 correctness polish, not capability.

WHERE: must emit Diagnostics like the other check_* passes in src/pipeline/mod.rs, AND needs file.imports which CompileContext currently lacks (add `imports: Vec<ImportAst>` to CompileContext, or thread via with_imports()). 

BLOCKER/RISK: this work is concentrated in pipeline/mod.rs which is HOT (other agent's FEAT-119 W3 ScopeBlock/World-A migration churns it, build thrashes). Best done when that contention clears. NOTE: the check_namespace_ambiguity fn + test for E0924 ALSO currently sit uncommitted in pipeline/mod.rs (riding the other agent's next commit of that file) \u2014 confirm they landed before adding more there.
