#+TITLE: Auto-diff push_view + document text:$sig binding shape for host-supplied DOM
#+STATE: ITEM
#+SESSION_ID: 1510a559ccf03ced
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-20T14-12-20-403Z_1510a559ccf03ced.jsonl]]
#+CUSTOM_ID: FUP-095-auto-diff-push-view-document-textsig-bin

* Initial Message

ok i want you to design a prototype a deep integration between Spacetime and elixir, especially Phoenix LiveView and it should be a la Djinn.LiveView (~/code/ora/monorepo).

What could it look like? Show me your DSL design so we can think about it together. Look at the recent Spacetime $host improvements and whatnot, maybe you can extend it to a canonical host for Phoenix LiveView that makes the integration insanely seamless and self-aware (re-uses the power of Elixir macros/Spark and the introspectability of Spacetime to deliver an experience that leverages the best of both).

* Parent: FEAT-136 (Wave C). Two small ergonomics follow-ups.

1. Auto-diff push_view: today Spacetime.LiveView.push_view/2 pushes named assigns
   explicitly. Add a change-tracking layer (compare prev/next assigns, push only
   the delta automatically on handle_event/handle_info) so a LiveView author never
   hand-lists what changed — the §8 "server diffs DATA" story, automatic.

2. Document the scalar-binding shape: a Spacetime view binding into a
   HOST-SUPPLIED (LiveView-rendered) DOM element must use the `text: $sig` property
   surface, NOT a `$hole` backtick inside a pre-existing element (the latter is for
   markup the .st itself emits / SSG-unrolls). Discovered building Wave C:
   `> .value { backtick-count }` rendered empty against a hand-written <span>, while
   `> .value { text: $count; }` wired correctly. Add to docs/language + the bridge
   design doc so the next author doesn't hit it.

* Acceptance
- push_view(socket) with no args pushes all changed assigns since last render.
- A docs section states the text:$sig vs backtick-hole rule for host-supplied DOM.
