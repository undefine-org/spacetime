#+TITLE: Unify hole syntax: deprecate bare holes in component bodies (backtick is the one form)
#+STATE: DONE
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FUP-041-unify-hole-syntax-deprecate-bare-holes-i
#+COMPLETION_HASH: 37d7037fb3e104f4

* Initial Message

due you're being very thick. HOLES ARE WRITTEN WITH BACKTICKS YOU FUCKING IDIOT
THERE'S A WHOLE TEST INFRA BUILT ON CHROMEOXIDE


How the f are you so thick.

Parent: [[id:PLAN-031-editable-structured-rich-text-input-mult]]. Created from FEAT-096 decision.

EMPIRICAL FINDING (2026-06-10, verified by build):
Two HTML-hole mechanisms coexist in the language:
1. PAGE MARKUP / @each inline → BACKTICK holes `` `$x` `` via CST HTML_HOLE
   (src/syntax/cst/parser.rs:1813; usage stdlib/primitives/data/each-inline.st:10).
2. @template / component BODIES → BARE holes `$x`, `&param` via
   normalize_bare_holes (src/emit/html_reactive.rs:55; usage
   examples/data-binding-demo.st:55).
Proof: a component body `<li>bare $it and tick `$it`</li>` builds to a reactive
text node for BARE `$it` and emits the BACKTICKS AS LITERAL TEXT
(createTextNode("... and tick `")). So inside a component body, backtick is NOT
a hole delimiter today.

FEAT-096 makes this ADDITIVE: normalize_bare_holes ALSO honors backtick holes
(`` `$x` ``, `` `...&sel` ``) in component bodies; bare holes keep working. Two
forms coexist.

THIS FUP = the cleanup: make backtick the SINGLE hole form everywhere.
- migrate all bare-hole component bodies (stdlib/**, examples/**, projects/**,
  tests/fixtures/**) to backticks via a codemod (stdlib/scripting or a Rust
  one-shot over §template bodies)
- deprecate bare-hole recognition in normalize_bare_holes (warn → remove)
- update docs/language/data-and-rendering.md (§2 already says backtick is THE
  hole; the bare form is undocumented legacy) + @template doc comments
  (stdlib/macros/template.st show bare $title/&content — stale)
- AGENTS.md harmony rule: one need (HTML interpolation) → one impl (backtick)
Do AFTER editable waves land (backtick-in-body proven in production by FEAT-096/097).
Low urgency; high harmony payoff.

NOTE [2026-06-11]: DONE 2026-06-10 (6abb90eb). User chose hard-remove (clean cutover). Backtick is the
ONE hole form everywhere. Engine: deleted bare $/& scan branches from
normalize_bare_holes + dead scan_bare_var/scan_amp_param; dropped element_params from
component_html_to_exprs (backtick &name is lexical). Codemod (scratch/fup041/holemod.py,
signature-aware, HTML-run-only) migrated 16 stdlib + 5 examples + test-runner + 75
projects/ files. Render-equivalence PROVEN: DOM builders byte-identical pre/post (same
hole sentinel); only provenance string + data-st-id hash differ. Migrated compiler.rs +
v8 FEAT-077 fixtures; 3 snapshots re-accepted. Docs (template.st, data-and-rendering.md)
teach backtick as THE form. Lib 2217 green. Hole sugar = future additive layer.

NOTE [2026-06-11]: REVIEW FIX (5a9d11e0): reviewer caught codemod missed web/index.st + demos/reactive-list
+ test fixtures (original globs too narrow). Migrated full tracked template-bearing corpus;
ZERO bare holes remain. examples/debug_*.rs + 3 lsp diagnostics failures = pre-existing
concurrent FEAT-088/089 regressions (ScopeBlock/dispatch), not FUP-041.