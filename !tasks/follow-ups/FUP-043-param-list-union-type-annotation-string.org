#+TITLE: param_list union-type annotation (string-literal-led: "S" | "M" | "L")
#+STATE: DONE
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FUP-043-param-list-union-type-annotation-string
#+COMPLETION_HASH: b2d21f0247db9d57

* Initial Message

FEAT-103 + FUP-040/041/042: Please give me your synthetical overview; what are the tradeoffs, the things to think about, the syntax overall? Show me all the challenges and whatnot, I want a deep and wide view to really get this design right

Parent: [[id:PLAN-031-editable-structured-rich-text-input-mult]]. Surfaced by the
W-A2 reviewer (agent 8-RevFn040) during FUP-040.

param_list's `$ptype:typeref?` now captures `Type`, `Type[]`, `Type?` (FUP-040
upgrade from `ident?`), but NOT a UNION type that begins with a string literal:
`$size "S" | "M" | "L"`. The TyperefExtractor requires the first token to be IDENT,
so a string-literal-led union matches nothing; the type is silently dropped (the
param NAME still captures, so the generated JS function works \u2014 params are positional
bare names \u2014 but the declared type is lost from the schema/registration).

Impact: LOW. The only corpus occurrence (projects/zeystudios priceFor) is inside a
/* */ comment block. No live code declares a union param type. Functionality is
preserved (name + body work); only the type annotation metadata is lost.

Fix when prioritized: extend TyperefExtractor (or add a `union_typeref` terminal) to
capture a `|`-separated run of string-literals / idents as a single type expression,
balanced up to the next top-level `,`/`)`. Then param_list union types round-trip.
Add a golden: `$size "S" | "M" | "L"` -> type_ref Some("\"S\" | \"M\" | \"L\"").

NOTE [2026-06-11]: Fixed in c8ef92cd. TyperefExtractor extended to term ( | term )* — leading term may be STRING (enables string-led unions), union fold skips WHITESPACE trivia, trailing pipe left for surrounding pattern. TypeRef preserves source spelling; field_text already surfaced it so no reifier change. 3 extractor unit tests + 12th param_list golden. Lib 2220 green. Framed + committed as the type-system half of Cluster A's grammar unification (parity with %primitive's ParamType::Union).