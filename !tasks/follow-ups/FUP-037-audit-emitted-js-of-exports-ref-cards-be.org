#+TITLE: Audit emitted JS of @exports / &ref / &cards[] before building on them
#+STATE: ITEM
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FUP-037-audit-emitted-js-of-exports-ref-cards-be

* Initial Message

hey what would it look like if the admin app were done multi-file? what reactive primitives and modular programming principles (and other missing constructs that you find) do you need to make this an amazing DX for developing this kind of app?

* Parent
[[id:PLAN-030-spacetime-app-platform-declarative-schem]] — hygiene gate, do EARLY (during
Wave 1) before FEAT-071 relies on these.

* Why
On 2026-06-06 we proved that =cargo run -- check= passing means NOTHING for template
constructs — unrecognized syntax is swallowed into the html field and silently no-ops.
The component-model features that "parse and seem to work" (=@exports=, named refs
=&n &c()=, cross-ref =&n.$x=, collection refs =&cards[]= + aggregates =&cards.sum(.$x)=)
were only verified by check, NOT by reading emitted JS. We must AUDIT each one's emitted
JS + headless behavior before FEAT-071/073/076 build on them, or we risk constructing on
silent no-ops.

* Scope — for EACH construct, produce a fixture, BUILD it, read dist/spacetime.js, and
assert the lowering is real (or file a BUG):
1. =@exports { $x, $y: mut }= — does it emit access metadata? Is mut enforced? Does a
   non-exported read error (E0917) AND a write to a read-only export error?
2. =&n &c()= named ref — does it store the instance under the name + allow =&n.$x= to
   resolve to ST.get(thatEl, 'x')?
3. =&n.$x <- v= write — only allowed for =: mut= exports; emits ST.set(thatEl,'x',v)?
4. =&cards[]= collection ref in @each — builds the array of instances?
5. =&cards.sum(.$x)= / =.each(...)= / =.length= / =.some(...)= — emit reactive derived
   values that update when any member changes?

* Deliverable
- A short report (append to this item) per construct: WORKS / PARTIAL / NO-OP, with the
  emitted-JS evidence.
- For each PARTIAL/NO-OP: file a BUG (bugs/) linked here and to PLAN-030, and either fix
  it inline (small) or block the dependent FEAT on it.

* Acceptance criteria
- [ ] all five constructs audited with emitted-JS evidence recorded here.
- [ ] every no-op/partial has a linked BUG (or an inline fix commit referenced).
- [ ] FEAT-071 proceeds only on constructs proven real (or with their BUGs resolved).
