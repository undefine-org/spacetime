#+TITLE: Unify type-annotation surfaces ($x type vs name: type vs $x:type)
#+STATE: DONE
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FUP-040-unify-type-annotation-surfaces-x-type-vs
#+COMPLETION_HASH: 06eec4da9cbbc7ca

* Initial Message

PLAN-031 -> read it, show me the boldest version of implementing @editable, and how you'd integrate it to the current admin

Parent: [[id:PLAN-031-editable-structured-rich-text-input-mult]] (round-3 design).

Three type-annotation surfaces coexist:
1. signal decls / template params (after FEAT-096): `$count number: 0`, `$href url` — SPACE separator
2. @fn / @compute params (stdlib/capture-types/params.st): `price: number` — COLON separator
3. %form meta-layer captures: `$name:ident` — COLON, meta-only (correct, stays)

User direction 2026-06-10: author-level convention is the SPACE form (`$title string`),
matching signal declarations. @fn's `name: type` diverges. Decide: migrate @fn/@compute
to `$price number` (breaking; needs migration script via stdlib/scripting) or document
the split (params-without-$ keep colon). Low priority; do after editable waves land
so the new grammar (FEAT-096) is in evidence.

NOTE [2026-06-11]: DONE 2026-06-10 (ce19ec6d). User chose breaking migration. @fn adopts param_list
(space-form $name type); colon-form `name: type` retired. Return type keeps `: T`.
Deleted params.st (was never embedded; builtin handled it). Codemod over 4 .st files
incl projects/zeystudios. fn-registry + compute-fn primitives → param_list (to_js
identical). Removed obsolete params golden. Lib 2217 green. The %form meta-capture
`$name:ident` colon stays (different layer, correct).

NOTE [2026-06-11]: REVIEW FIX (ce504d83): reviewer caught param_list type captured via ident? (one
token), dropping Type[]/Type?/union. Upgraded to typeref? + field_text reads TypeRef.
Union-type string-literal-led gap → FUP-043 (low impact, commented-out only occurrence).