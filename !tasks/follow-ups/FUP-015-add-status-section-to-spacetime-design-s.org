#+TITLE: Add Status section to spacetime-design SKILL.md (executable today vs planned)
#+STATE: DONE
#+SESSION_ID: 14cb5ac02323888c
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-04-27T18-31-12-780Z_14cb5ac02323888c.jsonl]]
#+CUSTOM_ID: FUP-015-add-status-section-to-spacetime-design-s
#+EFFORT: 30m
#+LAYER: coding-agent
#+PRIORITY: #B

Round-2 honesty pass on the SKILL.md text shipped in PLAN-017.

* Scope

In-scope:
- Add a top-of-file =## Status= section (immediately after the frontmatter, before =## Hard rules=) to =.claude/skills/spacetime-design/SKILL.md=.
- Section lists what is executable today vs planned, with concrete commands and links to the FEAT/FUP items tracking each "planned" entry.
- After PLAN-018 lands FEAT-020 doctor + FEAT-016 review tab, update the Status section to reflect the new reality (move them from "planned" to "today").

Out-of-scope:
- Re-writing the rest of SKILL.md.
- Updating the references files (those already contain inline parenthetical notes like "(when FEAT-020 lands)" — leave them).

* Existing Patterns

- Frontmatter shape: =---
name: spacetime-design
description: ...
---=. The Status section goes immediately under the H1 (=# Spacetime Design=) and BEFORE =## Hard rules=.
- Convention: org links in =[[id:FEAT-XXX]]= form, file links in =[[file:src/cli/doctor.rs]]= form.

* Tests

Manual:
- M1: =cat .claude/skills/spacetime-design/SKILL.md | head -50= shows the new Status section.
- M2: Every "planned" entry in the Status section links to a real org item (FEAT-/FUP-) that still has =STATE: ITEM= or =STATE: BLOCKED=.
- M3: Every "today" entry has a runnable command that succeeds (where applicable).

* Implementation

** ITEM Add initial Status section (before doctor + review tab land)
:PROPERTIES:
:CUSTOM_ID: FUP-015-add-status-section-to-spacetime-design-s::initial-status
:END:
- File: =.claude/skills/spacetime-design/SKILL.md=.
- Insert after =# Spacetime Design= H1, before =## Hard rules=:

#+begin_src markdown
## Status

What works today:
- `Spacetime.review()` runtime API — open devtools in a `cargo run -- serve <project> --debug` session, run `Spacetime.review()` to get a 5-dim audit. See [references/review-rubric.md](references/review-rubric.md).
- Showcase tree at `stdlib/showcases/` — 7 of 24 patterns shipped (full hero × 3 styles + minimal nav/cta/footer/features-grid). See [references/showcases.md](references/showcases.md).
- This skill's 8 reference files — procedural, browseable.

Planned (not yet runnable):
- `cargo run -- doctor <project>` — the gate referenced in Hard Rule #4. Tracked by [[id:FEAT-020-cargo-run-doctor-cli-one-shot-project-au]] and PLAN-018.
- Dev-panel "Review" tab — visual surface for `Spacetime.review()`. Tracked by [[id:FEAT-016-dev-panel-review-tab-5-dim-score-actions]].
- Dev-panel "Brand" tab — inventory + promote-to-token flow. Tracked by [[id:FEAT-017-dev-panel-brand-tab-css-var-inventory-li]].
- `cargo run -- showcase` CLI — catalog + variant generation. Tracked by [[id:FEAT-019-cargo-run-showcase-cli-gallery-variant-g]].
- Remaining 17 showcase patterns. Tracked by [[id:FUP-013-feat-018-remaining-17-showcases-8-scenar]].

Until doctor ships, Hard Rule #4 ("every delivered website MUST pass `cargo run -- doctor`") is enforced manually: run `cargo run -- check`, then `Spacetime.review()` in console for each route.
#+end_src

** ITEM Update Status after FEAT-020 lands (PLAN-018 wave 4+)
:PROPERTIES:
:CUSTOM_ID: FUP-015-add-status-section-to-spacetime-design-s::post-doctor
:DEPENDS: FUP-015-add-status-section-to-spacetime-design-s::initial-status FEAT-020-cargo-run-doctor-cli-one-shot-project-au::manual-smoke
:END:
- After FEAT-020 doctor merges (its =manual-smoke= sub-outline is the final gate), edit the Status section to:
  1. Move =cargo run -- doctor= from "Planned" to "Today" with a one-line example invocation.
  2. Remove the manual-workaround paragraph (the last paragraph of the section).

** ITEM Update Status after FEAT-016 lands
:PROPERTIES:
:CUSTOM_ID: FUP-015-add-status-section-to-spacetime-design-s::post-review-tab
:DEPENDS: FUP-015-add-status-section-to-spacetime-design-s::post-doctor FEAT-016-dev-panel-review-tab-5-dim-score-actions::manual-verify
:END:
- After FEAT-016 review tab merges (its =manual-verify= sub-outline is the final gate), edit Status to move "Dev-panel Review tab" from "Planned" to "Today".

* Edge Cases

- If FEAT-020 ships partial (e.g., pretty mode but no JSON mode): note partial status explicitly in the Status section, not "Today".
- If a referenced org item changes ID, keep the link valid via =org get= verification before saving.

* Acceptance Criteria

- Status section appears in =SKILL.md= after wave-1 of PLAN-018.
- Every Planned bullet links to a real org item.
- After doctor + review tab land, Status section reflects them as "Today" entries.
- =grep "MUST pass.*doctor" .claude/skills/spacetime-design/SKILL.md= still finds Hard Rule #4 (we are NOT removing the rule — only adding context that the gate is currently manual).

NOTE [2026-04-27]: PLAN-018: Status section added to .claude/skills/spacetime-design/SKILL.md. Initial wave (initial-status) + post-doctor and post-review-tab updates landed in the same edit because both FEATs shipped in the same plan. Status now lists Spacetime.review(), Review tab, and partial doctor (with FUP-016 pointer for the route loop) under "What works today", and BUG-022 / Brand tab / showcase CLI / 17 patterns under "Planned".