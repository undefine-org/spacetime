#+TITLE: Visual page builder: drag palette → canvas, emit .st section composition
#+STATE: ITEM
#+SESSION_ID: 14f8685429e4ccf1
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-01T18-22-11-112Z_14f8685429e4ccf1.jsonl]]
#+CUSTOM_ID: FUP-036-visual-page-builder-drag-palette-canvas

* Initial Message

ok let's think deeply deeply. What would a headless CMS for Spacetime look like? like a headless CMS that has all the nice features of a headless CMS and the power of Spacetime combined. Look at the stdlib and the __dev__ to understand some of the pre-existing experiments, and do a research on the state of headless CMSes, before coming back with your THOUGHTS AND REFLECTIONS (no decisions yet)

* Source
PLAN-029 Stage-3 Wave-8. The TYPED PALETTE is done (/dev/templates.json +
admin Components panel: 43 unkn templates with binding/slot signatures, draggable
cards). The remaining Framer-killer piece is the visual CANVAS that composes them.

* What exists (W8)
- templates.json: every @template's typed signature (data needs $x, slots &y, optional).
- Components palette UI: draggable cards. unkn pages already compose as a section
  list in .st (`&unkn-section-hero(); &unkn-section-roster(); ...` in index.st:143-155).

* Scope (when picked up)
1. CANVAS: render the current page (compile-time-hydrated, FUP-035) with each
   section invocation selectable; show a floating toolbar (drag/duplicate/delete)
   + a binding label per section (mockup stage3-page-builder.png).
2. DRAG-TO-PLACE: drop a palette card between sections → insert a `&template()`
   invocation into the page .st via EditAst (the .st write path is solved, 3C).
3. VALID-BY-CONSTRUCTION: only allow a placement whose binding params are
   satisfiable in scope (a $service binding needs a Service entry / @each); only
   accept a child in a &slot if it's a valid element. Use templates.json's typed
   params to gate drops (the differentiator Framer can't reach \u2014 no type system
   under its canvas).
4. REORDER: drag sections to reorder the invocation list in .st.
5. INSPECTOR: selected section → DATA/PARAMS/SLOTS/MOTION/STYLE panels (motion =
   FUP for W9; style = @theme tokens from W7).

* Acceptance
- A designer drags a component onto the page, binds its data, reorders sections,
  and the admin emits clean .st invocations indistinguishable from hand-authored;
  invalid placements are prevented (typed composition).

* Related
- docs/specs/spacetime-cms-stage2-3.md §"VEIN 1"; mockup stage3-page-builder.png
- FUP-035 (compile-time hydration of template-bodied @each \u2014 needed for canvas fidelity)
- src/sync/handlers.rs handle_edit_ast (.st write path); /dev/templates.json
- projects/unkn/index.st:143-155 (section-list composition unkn already uses)
