#+TITLE: Workbench information-architecture reconception spike (docked panels vs overlay drawers)
#+STATE: ITEM
#+SESSION_ID: 1522aa9778a04732
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-04T14-07-53-826Z_1522aa9778a04732.jsonl]]
#+CUSTOM_ID: FUP-109-workbench-information-architecture-recon

* Initial Message

i have restarted the session, can you do the visual confirmation now?

PLAN-065 fixes the workbench editing MODEL (state ownership: server/client/canvas planes) under the CURRENT overlay-drawer shell. It deliberately does NOT re-question the information architecture — that is this spike.

** Why deferred (and why PLAN-065 makes it cheap)
The user wants the interface "rethought from scratch" eventually, but the acute failures (edit-disappears, stage-dims, silent-clicks) are LIFECYCLE/OWNERSHIP faults, not layout faults. PLAN-065 is built so that the three-plane state model (client-owned edit sessions, committed-only canvas, explicit action-transaction state) is LAYOUT-INDEPENDENT: the inspector binds to $activeEditorFields regardless of whether it renders in an overlay drawer, a docked side rail that reserves layout width, or a floating palette. So this reconception should be MOSTLY COSMETIC — swap the shell chrome + CSS, reuse the same signals/actions/edit lifecycle.

** Spike questions
- Overlay drawers (current) vs DOCKED panels that reserve layout width (Webflow/Framer): a docked inspector never occludes the canvas; the canvas artboard shrinks to fit rather than being covered. Tradeoff: less canvas width vs no occlusion.
- Left = structure/layers tree ALWAYS visible (like Framer/Figma) vs summon-on-demand.
- Right = inspector ALWAYS visible when something is selected vs drawer.
- Top bar = device toggles + breakpoints + zoom.
- Whether the "stage" metaphor (one dominant glowing surface) survives or becomes a bounded artboard within a persistent chrome frame.

** Acceptance
A prototype (or a written IA decision + mock) that a from-scratch reviewer signs off, PLUS confirmation that migrating PLAN-065's edit lifecycle into it touched only chrome/CSS (no signal/action/lifecycle rewrite). If the migration turns out NON-cosmetic, that is a finding: PLAN-065 leaked layout assumptions into the state model — file the leak.

Parent: PLAN-065.
