#+TITLE: FUP: Wave E follow-ups — @stage-child + row-keyed entity identity
#+STATE: ITEM
#+SESSION_ID: 151c02da5be0d720
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-29T10-04-08-943Z_151c02da5be0d720.jsonl]]
#+CUSTOM_ID: FUP-101-fup-wave-e-follow-ups-stage-child-row-ke

* Initial Message

What about a mix of all three, something like @entity vertex &heel { mesh: ... } etc, or @entity vertex(...) &heel {...};?

Look at existing syntax, I want a unified vision overall

* Parent
PLAN-061 (FEAT-142). Wave E landed the @template-body nested entity harvest; these two deeper cases are deferred here.

* Deferred 1: entity scopes directly inside @stage {} ($children capture)
- @stage uses `$children*` (stdlib/3d/macros/stage.st), a DIFFERENT capture path than @template's `$body:component_body`. The Wave E harvest wired into the component_body path (src/parser/mod.rs ~661) does NOT cover @stage's children.
- IMPACT: `&evernet { @peak(...) }` directly inside `@stage {}` does not register (build emits 0 st-entity markers). everswap works around with PAGE-SCOPE peaks (before canvas.world{@stage}), CDP-green + deterministically ordered + P2-fixed (lazy _stThree). Elegance, not a correctness blocker.
- FIX: generalize the nested entity harvest to the $children/%template capture path. Start: stdlib/3d/macros/stage.st %template{$children} + children-segment classification in src/syntax/events/.

* Deferred 2: row-keyed per-instance entity identity in @each
- A `&node { @c }` inside a @template delegate gets a FIXED `.st-entity-node` selector + registers window.__stWorld.byName['node']. Under `@each($rows as $r){ &spawn($r) }` with N rows, all N share ONE entity id -> collision.
- IMPACT: true data-driven scatter (10k points each its own entity) not yet working; only SINGULAR nested entity per template def.
- FIX: key registration by row key __stWorld.byName['node:'+$r.id]; marker gets per-instance attr. @anchor(to: &node.peak.summit) resolves THIS row's entity via delegate row context. Needs per-instance ref namespace in each-with-templates (stdlib/primitives/template.st). This is the QML-Repeater delegate-id model (row-scoped &ref keyed by data, NOT name interpolation).

* Acceptance (when done)
- &name{@peak} inside @stage{} registers.
- @each over N rows produces N DISTINCT keyed entities; each anchored label tracks its own.
- CDP test with >=3 rows tracked independently.
