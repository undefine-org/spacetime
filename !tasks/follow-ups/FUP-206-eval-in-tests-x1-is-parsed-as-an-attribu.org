#+TITLE: `@eval` in tests: `x[1]` is parsed as an attribute selector — loud but misleading
#+STATE: ITEM
#+SESSION_ID: 157047c1462c1c38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-02T20-57-35-768Z_157047c1462c1c38.jsonl]]
#+CUSTOM_ID: FUP-206-eval-in-tests-x1-is-parsed-as-an-attribu

* Initial Message

Using whatever tools and techniques you want, make the absolute most impressive, most mind blowing, most visually stunning 30s promo video for Spacetime (this repo). I want something that looks like we paid a studio $5m to produce.

After that, I want you to do the same thing, but with Spacetime, and to compare/critique the two

Surfaced by FEAT-175 writing tests/score/promo-film.test.st. `var pa = f(i[1]);` inside a multi-line `@eval (…)` fails the WHOLE file with `expected '{' after selector` pointing at the `[`. `.item(1)` works. Either (a) `@eval`'s body should be captured as a balanced raw run (no selector scan inside the parens — FUP-193 "foreign-code islands need a lexer mode" is the same root), or (b) the diagnostic should say "inside @eval, `[` starts an attribute selector; use .item(n)/.at(n)".

Related: a `$` in a class name (`.pr$` — my typo) reports the same generic "expected '{' after selector"; it should name the sigil.

Acceptance: a test containing `i[1]` in `@eval` compiles, OR the error names the cause and the workaround.
