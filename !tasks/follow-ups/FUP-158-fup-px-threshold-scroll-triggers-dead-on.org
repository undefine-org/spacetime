#+TITLE: FUP: px-threshold scroll triggers (dead `@on scroll x(threshold: Npx)` shape)
#+STATE: ITEM
#+SESSION_ID: 1534a2eae1903af0
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-18T13-07-01-638Z_1534a2eae1903af0.jsonl]]
#+CUSTOM_ID: FUP-158-fup-px-threshold-scroll-triggers-dead-on

* Initial Message

can you explain everything that is still open for my decisions? Explain it with depth and sharpness

The retired on-event dispatcher had hover/click/focus/visible/intersect arms but NO scroll arm — yet `@on scroll x(threshold: 80px) { … }` appeared in production (ora-ventures.com/for-ai-officers), silently DEAD the whole time: a fixed nav's getBoundingClientRect never moves, so even a working px-threshold scroll event was the only firing mechanism.

W5 deleted the dead call. The LEGITIMATE need it gestures at: "when the page scrolls past N px, apply a state" — the ratio-based scroll driver (element position scopes) cannot express an absolute px threshold, and a FIXED element can't be its own subject.

Possible shapes: a `threshold: 80px` param on the scroll driver (px mode, viewport-relative), or a `&.page-scroll` driver whose progress is document scroll px (not element ratios). Design needed; record real use cases when they appear.

Also note: the deleted site's intent (solid nav background past 80px) is today usually done with scroll-driven animations or a sentinel + `&.visible` on a ghost element — the sentinel pattern is expressible NOW: an 80px-tall ghost at page top with `@on &ghost.visible …` inverted.
