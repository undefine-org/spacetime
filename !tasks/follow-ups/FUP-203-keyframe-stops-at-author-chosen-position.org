#+TITLE: Keyframe stops at author-chosen positions (`a -> b @ 0.25 -> c`) or a `hold` stop
#+STATE: ITEM
#+SESSION_ID: 157047c1462c1c38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-02T20-57-35-768Z_157047c1462c1c38.jsonl]]
#+CUSTOM_ID: FUP-203-keyframe-stops-at-author-chosen-position

* Initial Message

Using whatever tools and techniques you want, make the absolute most impressive, most mind blowing, most visually stunning 30s promo video for Spacetime (this repo). I want something that looks like we paid a studio $5m to produce.

After that, I want you to do the same thing, but with Spacetime, and to compare/critique the two

Surfaced by FEAT-175 (promo film). `a -> b -> c` stops are evenly spaced (stdlib/capture-types/keyframes.st; reify assigns `at`). A flash at 2.2s inside a 30s clip needed a 60-stop chain (rejected in review) or six extra hit clips plus a 0.12s pre-offset so the peak stop lands on the sound (see `--flash` in demos/promo/index.st).

Proposal (spec first, pick one): (a) `value @ position` suffix on any stop, reified into the runtime's existing `at` field; (b) a `hold(N)` stop that repeats the previous value N times. (a) is the smaller grammar change and maps 1:1 onto `at`. Must keep `->` = "a step between values" (AGENTS harmony rule) — no second arrow.

Acceptance: `--flash` in demos/promo becomes ONE clip spanning the film with six positioned stops; `tests/unit/primitives/scroll-multi-keyframe.test.st` gains a positioned-stop case that fails on the even-spacing code.

NOTE [2026-09-03]: Subsumed by PLAN-150 W1 (film surface). Design + syntax: docs/language/film.st.md §2 — `at` (not `@`) positions a stop; a form-ref after a stop names the step's easing.