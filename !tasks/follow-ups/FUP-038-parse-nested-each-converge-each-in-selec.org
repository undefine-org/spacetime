#+TITLE: parse_nested_each: converge @each-in-selector-ref to stdlib grammar
#+STATE: DONE
#+SESSION_ID: 14fc108ed3772fa8
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-04T14-32-37-965Z_14fc108ed3772fa8.jsonl]]
#+CUSTOM_ID: FUP-038-parse-nested-each-converge-each-in-selec
#+COMPLETION_HASH: 73e3986ebaf2760a

* Initial Message

Ok, start with Phase B. Use a wave manifest with in-chat mid-flight check-ins exlpaining what's going on, what' you've done, and what you're doing next. use main thread for architectural work and reviewer swarms in between waves. add as many gap implementation as needed; you have unlimited time budget. file FEAT/BUG you find mid-flight using the org tool and fix/implement them mid-flight

* Parent
FEAT-080 / FEAT-079 Phase B. Post-cutover reviewer (agent 18) P2: the LAST residual
construct-classifier. parse_nested_each (form_compiler.rs ~3010) string-parses @each(...) inside
`&cards[] .cards { @each($s as $i) { &t($args); } }` selector-ref blocks: finds "@each(", splits
paren content, splits " as ", parses key:, per-line scans &template(args). Duplicates the stdlib
`%macro each` grammar (stdlib/macros/each.st).

* Why deferred
Orthogonal selector-ref SUB-feature (NestedEachDef), not part of the parse_component_body
classifier Phase B retired. Core mandate (retire parse_component_body + per-construct classifiers
+ @match special case) is COMPLETE; this is the one remaining @each-specific parser.

* Fix
Express @each-in-selector-ref as a stdlib %capture_type (cb_nested_each reusing the each macro's
binding grammar) + thin reifier filling NestedEachDef {source,item,key,invocations} from
already-split fields. Differential vs current parse_nested_each over selector_ref_collection_each
corpus + fleet (unkn/admin use it). Gate: byte-identical NestedEachDef, then delete
parse_nested_each.

NOTE [2026-06-09]: DONE. cb_nested_each stdlib grammar (reuses each macro binding header + template_invocation+) + reify_nested_each replace parse_nested_each (deleted). Required BUG-066 fix (template_invocation not wired in %capture_type engine). Differential byte-identical before deletion; selector_ref_collection_each golden + real nested-@each fleet build confirm end-to-end. PHASE B FULLY PURE: zero template-body construct parsers in Rust. lib 2135/0.