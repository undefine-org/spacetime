#+TITLE: @scatter — sample an element's rendered glyphs into per-child custom properties
#+STATE: ITEM
#+SESSION_ID: 157047c1462c1c38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-02T20-57-35-768Z_157047c1462c1c38.jsonl]]
#+CUSTOM_ID: FUP-198-scatter-sample-an-elements-rendered-glyp

* Initial Message

Using whatever tools and techniques you want, make the absolute most impressive, most mind blowing, most visually stunning 30s promo video for Spacetime (this repo). I want something that looks like we paid a studio $5m to produce.

After that, I want you to do the same thing, but with Spacetime, and to compare/critique the two

Surfaced by FEAT-175. S6 wanted `<script>` to shatter INTO ITS OWN SHAPE; CSS trig over sibling-index() gives a generic burst. stdlib/text (pretext) measures but does not emit positions. Proposal: `@scatter(from: &tag, count: 400)` stamping `--x/--y` on N children from the alpha mask of a canvas-rasterized copy of &tag (one primitive, vendored nothing, uses the existing text module's canvas). Positions are data; motion stays in `@on &.clip` bodies.

Acceptance: demos/promo S6 burst uses `@scatter`; contact-sheet frame at 23.3s shows the word shape.

NOTE [2026-09-03]: Subsumed by PLAN-150 (film surface). Design + syntax: docs/language/film.st.md. Keep this item for tracking; implement per the plan's wave.