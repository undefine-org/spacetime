#+TITLE: @signal ws + device sink variants (realtime + mobile)
#+STATE: ITEM
#+SESSION_ID: 1507711255a45786
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-13T10-37-35-958Z_1507711255a45786.jsonl]]
#+CUSTOM_ID: FUP-049-signal-ws-device-sink-variants-realtime

* Initial Message

look at the Spell MCP features. I would like the interfaces to be defined as functions that are loaded into the live environment, so that it is a fully live environment from scratch, instead of just a 'prompt thing'.

1. look at anything that's pending in that thread of work
2. find your own insights w.r.t what I just said
3. come back to me with your realisations

* Parent
[[id:PLAN-038-signal-reactive-output-the-sibling-of-da]] — WAVE 5 (deferred).
Unblocked by FEAT-110 (@signal core macro family + st-signal primitive).

* Why deferred
v1 ships post + emit(mcp/host) — enough to prove the dual and collapse the MCP.
ws + device sinks are the SAME macro family (added %form variants over the same
st-signal primitive), so they extend rather than redesign. Deferring avoids
over-building before the core shape is validated in real use.

* Scope (when picked up)
- @signal ws $n {P}? to $socket { … }   — send over an @websocket-opened channel
  (reuse stdlib/macros/websocket.st `send`). Proves realtime sink.
- @signal emit $n {P}? to $nav     { … } — push/replace a screen (stdlib/mobile/
  macros/navigation/stack.st). 
- @signal emit $n {P}? to $store   { … } — persist (mobile storage adapter).
- @signal emit $n {P}? to $haptic / $share — device effects (stdlib/mobile/primitives).
Each = a new %form variant + a sink-target route in st-signal; NO core change.

* Acceptance
- each variant compiles + fires through its transport; one .st test per sink.
- the four-context polymorphism table in data-and-signal.md becomes fully runnable
  (the mobile demo stub from FEAT-112 promotes to a real runtime).
- harmony: still ONE @signal family, ONE st-signal primitive — variants only.

* Trigger to promote
Pick up once FEAT-110/111 are DONE and a real mobile or realtime page needs to send.
