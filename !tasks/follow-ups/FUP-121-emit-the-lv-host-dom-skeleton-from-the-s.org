#+TITLE: Emit the LV host DOM skeleton from the Spacetime compiler (stop hand-mirroring in render_host)
#+STATE: ITEM
#+SESSION_ID: 1539989eb44441ea
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-07-22T09-34-26-513Z_1539989eb44441ea.jsonl]]
#+CUSTOM_ID: FUP-121-emit-the-lv-host-dom-skeleton-from-the-s

* Initial Message

we are going to work on the integration between Spacetime and Elixir. look at the match and the handle macros, and tell me hwo they are translated currently from and to Phoenix LiveView-like constructs, and how they ought to be. I would like the experience to be as seamless as possible: its own `src/` or `frontend` or sth to put the .st files like a proper project (most likely), and a $hosts file that files import (for now, just give them special enough names that importing them is fine).

ok?

* Parent: FEAT-137 (native live transport).

Spacetime.LiveView.render_host/1 hand-mirrors the DOM skeleton that
frontend/counter.st binds onto by selector (`.counter > .value`, the buttons,
…). The two can drift silently.

* Fix
The Spacetime compiler already knows the page's static structure; emit a
skeleton HTML artefact alongside spacetime.js/css at build time (or expose it
via the bundle), and have render_host/1 serve THAT instead of a hand-written
~H template. One source of truth: the page.

NOTE [2026-07-26]: ACCEPTED 2026-07-24 (user review of spacetime_lv). Scoped under PLAN-116, Wave 1. Fix: compiler emits skeleton.html beside spacetime.{js,css}; render_host/1 injects it via Phoenix.HTML.raw; kill the `if @__root__ == ".board"` per-page branch. Must be emitted by FEAT-138's batch/watch build too.