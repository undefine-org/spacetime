#+TITLE: Unify @cms (display) + FEAT-117 (edit) under one @editor(Type), no regressions
#+STATE: ITEM
#+SESSION_ID: 1510d7e0efe8848c
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-20T17-53-05-983Z_1510d7e0efe8848c.jsonl]]
#+CUSTOM_ID: FUP-087-unify-cms-display-feat-117-edit-under-on

* Initial Message

launch the workbench and let me drive it for a bit (give me the link) while you explore the various plans that it was building upon (all the way to PLAN-049) and we thoroughly design the multi-frame drag-canvas (W4's strip is the non-canvas bridge), file-scope page mounts (FUP-061), the
 __mcp__/__admin__/__dev__ convergence (north-star, two-consumer rule).

* Parent / design
@research/mcp-editor-introspection-design-2026-06-20.org PART E + PART C
(@editor(Type) target). Owner: "make sure the implemented ones are ported over
with no regressions."

* Current state (source-verified)
- @cms(Type){ title:f; subtitle:f; thumbnail:f; chips:f } is the ONLY implemented
  analog (stdlib/__admin__/cms-hint.st). DISPLAY hints only; read server-side via
  matches.filter(macro_name == "cms") + merged with convention inference into
  /__spacetime/dev/types.json (src/server.rs ~3159/3404). The admin rail/list/
  drawer render from types.json.
- FEAT-117 (@cms editable vocabulary: editable:inline; richtext(marks); number;
  toggle; select(options)) is DEFERRED until the meta-compiler is clean
  (FEAT-115/116, BUG-105, FUP-051 gate). It is the EDIT face.

* The unification
One @editor(Type) carries BOTH faces:
  @editor(Talent) {
    tree  { title: name; subtitle: headline; thumbnail: image; chips: categories }
    panel { name: text; headline: text; image: url; bio: richtext(["bold","link"]) }
  }
- tree{} = the @cms DISPLAY role (admin rail rows + builder navigator).
- panel{} = the FEAT-117 EDIT role (admin drawer widgets + builder inspector).

* No-regression plan
1. Keep @cms working while @editor(Type) lands (parse both; @editor(Type){tree{}}
   produces the SAME display object @cms does -> types.json unchanged shape).
2. Port the admin's types.json consumer to read @editor-or-@cms (union), so the
   admin renders identically from either.
3. When FEAT-117 un-defers, its widgets land AS @editor(Type){panel{}} (NOT a
   parallel macro), reusing the &fld-* drawer widget family it specifies.
4. Then retire @cms -> alias to @editor(Type){tree{}} (one-meaning harmony) OR
   keep @cms as sugar; decide at port time. Delete the duplicate display path.

* Gate / ordering
HONOR FEAT-117's deferral: do NOT pull the edit-vocabulary forward until the
meta-compiler cleanup gate clears. The DISPLAY port (steps 1-2) can proceed
earlier (it only refactors a shipped surface). The EDIT unification (step 3) waits
on FEAT-117.

* Acceptance
- The admin CMS renders + persists identically before/after (no regression):
  collection rails, entry drawers, richtext.
- @editor(Talent){tree{...}} yields the same types.json display object as
  @cms(Talent){...}.
- A single @editor(Type) drives BOTH the admin CMS and the builder
  navigator/inspector for that type.
- Tests: types.json projection parity (@cms vs @editor); admin drawer
  persistence gate; convergence harmony grep (one display-hint path).
