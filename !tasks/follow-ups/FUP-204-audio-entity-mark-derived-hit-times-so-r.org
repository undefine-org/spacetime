#+TITLE: @audio entity + mark-derived hit times so `render` muxes sound from the score
#+STATE: ITEM
#+SESSION_ID: 157047c1462c1c38
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-undefine-spacetime/2026-09-02T20-57-35-768Z_157047c1462c1c38.jsonl]]
#+CUSTOM_ID: FUP-204-audio-entity-mark-derived-hit-times-so-r

* Initial Message

Using whatever tools and techniques you want, make the absolute most impressive, most mind blowing, most visually stunning 30s promo video for Spacetime (this repo). I want something that looks like we paid a studio $5m to produce.

After that, I want you to do the same thing, but with Spacetime, and to compare/critique the two

Surfaced by FEAT-175. The promo's six hits are locked to a wav by hand-placed clip times; `render` knows nothing about audio (SIP-001b §8.6 still open). Today the film is finished with a manual `ffmpeg -i video.mp4 -i promo.wav …` step.

Proposal: `@audio(src: …) as &bed;` as a score entity; `mark &hit1 at 2.2s;` becomes both a `during`/`after` anchor AND a marker exported to WebVTT/ffmpeg; `render --audio` muxes `&bed` at its `at` offset through the existing ffmpeg pipe (src/render.rs::spawn_ffmpeg). Live playback: an `<audio>` element driven by the same transport — read BUG-306/BUG-310/BUG-311 (playback-driver blockers) first. Pick ONE model (WebAudio live vs ffmpeg render) per SIP-001b §8.6 before implementing, with a conformance test that live and rendered timing agree.

Acceptance: `render demos/promo/` emits an mp4 WITH the track, no manual ffmpeg; the live page plays the wav in sync under `&.time`.

NOTE [2026-09-03]: Subsumed by PLAN-150 W7. Design: docs/language/film.st.md §8 — score is truth; browser <audio> and render mux both conform to marks.