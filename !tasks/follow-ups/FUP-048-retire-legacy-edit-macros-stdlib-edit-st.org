#+TITLE: Retire legacy @edit macros (stdlib/edit.st, macros/edit.st, primitives/edit.st)
#+STATE: DONE
#+SESSION_ID: 1503ca05d05c0c62
#+TRANSCRIPT_PATH: [[file:/home/user/.spell/agent/sessions/-code-ora-verse/2026-06-10T14-32-18-497Z_1503ca05d05c0c62.jsonl]]
#+CUSTOM_ID: FUP-048-retire-legacy-edit-macros-stdlib-edit-st
#+COMPLETION_HASH: b9d624b73f8edeba

* Initial Message

(b) -> what would that achieve?
(c) -> is an FUP filed?
(d) -> yeah what about integration points with stdlib/__admin__? I'd LOVE so automatic content editing on the website directly. This is a DESIGN investigation, let's think about what's most Spacetimey. At the same time, you may think about showing me how you'd use this to edit blogs and whatnot.

Also, think about how you could set up a default editable flow and leverage Spacetime's introspection to automatically augment the admin side with editable-everything, optionally improvable with something akin to the @cms block

* Context
The user (FUP-047 turn) flagged: "be careful, there are some super old macros that should
be removed; only extend based on what you recently did + the @cms approach (progressive
improvement)." Identified during the editable-everything work.

* The dead macros
Three old contenteditable-based inline-edit constructs predate the structured @editable/
@richtext surface and the dev-editable provenance path:
- stdlib/edit.st           (66 lines) — `@edit(persist:"page.html", trigger:dblclick, …)`
- stdlib/macros/edit.st     (76 lines) — `@edit(trigger, persist, validate)` state-machine pattern
- stdlib/primitives/edit.st (127 lines) — contentEditable enable/persist primitives

They compose a @state_machine + raw contenteditable + a hand-rolled persist URL — a
PARALLEL implementation of what the editable stack now does properly (AST-true surface,
provenance, EditJson/EditAst rail, gate-mode). Keeping them violates the harmony rule
(one need, one implementation).

* Usage audit (done this turn)
- projects/jallete/index.st: only a COMMENT mentions "@edit directive" — no real `@edit(` call.
- No live `@edit(` invocation anywhere in stdlib/ projects/ examples/ (only the defs +
  tests/fixtures/test-runner/edit-demo.* + tests/unit/macros/edit.test.st exercise them).
∴ retiring them breaks no live project.

* Plan
1. Confirm zero live `@edit(` users (re-grep at retirement time; projects/ is a separate repo).
2. Remove the three files + their @import from stdlib/index.st (line ~90, state-machine.st
   comment also references @edit).
3. Remove/migrate tests/unit/macros/edit.test.st + the test-runner edit-demo fixtures.
4. If any real need remains (inline plain-text edit with persist), it is already covered by
   dev-editable (provenance click-to-edit) + @richtext (structured) — point any migrator there.

* Why a follow-up, not inline
Out of FUP-047's scope (persistence), and a deletion wants its own focused diff + a fresh
zero-usage re-check (concurrent sessions touch the tree). Filed per the user's caution so
it is not forgotten.

NOTE [2026-06-12]: DONE (commit 1b742fc1). Removed the 3 legacy @edit files (stdlib/edit.st orphaned, macros/edit.st, primitives/edit.st), their 2 EmbeddedFile entries (stdlib_embedded.rs), their 2 include_str! entries (lsp/form_registry.rs), the @edit self-test + 3 test-runner fixtures, and the stale @edit mention in stdlib/index.st. Audit confirmed zero live users; the 3 primitives had 0 external refs. Verified: build clean, @edit now compiles to 0 bytes (no stale registration), @state_machine intact, @editable/@richtext/@edit-toggle unaffected. lib 2254, integration 172 green. Migration path: dev-editable (provenance click-to-edit) + @richtext (structured) supersede @edit.