[env]
# V8 (rustyscript) isolates are not safe for concurrent creation/destruction
# across threads. Serial execution prevents SIGSEGV in test binaries.
RUST_TEST_THREADS = "1"

# Increase minimum stack size for deep parser recursion
# The chumsky parser uses a lot of stack space due to combinator design
RUST_MIN_STACK = "8388608"
