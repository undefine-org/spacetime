{
  "//": "Resolves the bare `three` / `three/addons/*` specifiers in three-entry.ts to the pinned submodule, with NO npm/node_modules. bun discovers this file by walking up from the entry, so the bundle resolves whatever cwd the bundler runs from (build.rs runs at the repo root). Keep paths in sync with the %vendor declaration.",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "three": [
        "./three/build/three.module.js"
      ],
      "three/addons/*": [
        "./three/examples/jsm/*"
      ]
    }
  }
}
